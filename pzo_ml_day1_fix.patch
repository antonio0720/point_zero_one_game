diff --git a/pzo_engine/package-lock.json b/pzo_engine/package-lock.json
index 0a6fa3f2..9e757409 100644
--- a/pzo_engine/package-lock.json
+++ b/pzo_engine/package-lock.json
@@ -868,7 +868,6 @@
       "integrity": "sha512-BH7YwL6rA93ReqeQS1c4bsPpcfOmJasG+Fkr6Y59q83f9M1WcBRHR2vM+P9eOisYRcN3ujQoiZY8uk5W+1WL8w==",
       "dev": true,
       "license": "MIT",
-      "peer": true,
       "dependencies": {
         "undici-types": "~6.21.0"
       }
@@ -986,7 +985,6 @@
       "integrity": "sha512-hGISOaP18plkzbWEcP/QvtRW1xDXF2+96HbEX6byqQhAUbiS5oH6/9JwW+QsQCIYON2bI6QZBF+2PvOmrRZ9wA==",
       "dev": true,
       "license": "MIT",
-      "peer": true,
       "dependencies": {
         "@vitest/utils": "3.2.4",
         "fflate": "^0.8.2",
@@ -1551,7 +1549,6 @@
       "integrity": "sha512-5gTmgEY/sqK6gFXLIsQNH19lWb4ebPDLA4SdLP7dsWkIXHWlG66oPuVvXSGFPppYZz8ZDZq0dYYrbHfBCVUb1Q==",
       "dev": true,
       "license": "MIT",
-      "peer": true,
       "engines": {
         "node": ">=12"
       },
@@ -1968,7 +1965,6 @@
       "integrity": "sha512-5C1sg4USs1lfG0GFb2RLXsdpXqBSEhAaA/0kPL01wxzpMqLILNxIxIOKiILz+cdg/pLnOUxFYOR5yhHU666wbw==",
       "dev": true,
       "license": "MIT",
-      "peer": true,
       "dependencies": {
         "esbuild": "~0.27.0",
         "get-tsconfig": "^4.7.5"
@@ -2041,7 +2037,6 @@
       "integrity": "sha512-w+N7Hifpc3gRjZ63vYBXA56dvvRlNWRczTdmCBBa+CotUzAPf5b7YMdMR/8CQoeYE5LX3W4wj6RYTgonm1b9DA==",
       "dev": true,
       "license": "MIT",
-      "peer": true,
       "dependencies": {
         "esbuild": "^0.27.0",
         "fdir": "^6.5.0",
@@ -2140,7 +2135,6 @@
       "integrity": "sha512-LUCP5ev3GURDysTWiP47wRRUpLKMOfPh+yKTx3kVIEiu5KOMeqzpnYNsKyOoVrULivR8tLcks4+lga33Whn90A==",
       "dev": true,
       "license": "MIT",
-      "peer": true,
       "dependencies": {
         "@types/chai": "^5.2.2",
         "@vitest/expect": "3.2.4",
diff --git a/pzo_engine/src/mechanics/m112_precision_split_sell_part_keep_part.ts b/pzo_engine/src/mechanics/m112_precision_split_sell_part_keep_part.ts
index e25db40b..aabd3fd1 100644
--- a/pzo_engine/src/mechanics/m112_precision_split_sell_part_keep_part.ts
+++ b/pzo_engine/src/mechanics/m112_precision_split_sell_part_keep_part.ts
@@ -19,6 +19,15 @@ import { clamp, computeHash, seededShuffle, seededIndex,
          PRESSURE_WEIGHTS, PHASE_WEIGHTS, REGIME_WEIGHTS,
          REGIME_MULTIPLIERS } from './mechanicsUtils';
 import type {
+  RunPhase, TickTier, MacroRegime, PressureTier, SolvencyStatus,
+  Asset, IPAItem, GameCard, GameEvent, ShieldLayer, Debt, Buff,
+  Liability, SetBonus, AssetMod, IncomeItem, MacroEvent, ChaosWindow,
+  AuctionResult, PurchaseResult, ShieldResult, ExitResult, TickResult,
+  DeckComposition, TierProgress, WipeEvent, RegimeShiftEvent,
+  PhaseTransitionEvent, TimerExpiredEvent, StreakEvent, FubarEvent,
+  LedgerEntry, ProofCard, CompletedRun, SeasonState, RunState,
+  MomentEvent, ClipBoundary, MechanicTelemetryPayload, MechanicEmitter,
+} from './types';
 
 export interface SplitResult {
   /** Deterministic split execution summary */
@@ -35,15 +44,6 @@ export interface SplitResult {
   /** Remaining holding cost basis after the split */
   remaining_cost_basis: number;
 }
-  RunPhase, TickTier, MacroRegime, PressureTier, SolvencyStatus,
-  Asset, IPAItem, GameCard, GameEvent, ShieldLayer, Debt, Buff,
-  Liability, SetBonus, AssetMod, IncomeItem, MacroEvent, ChaosWindow,
-  AuctionResult, PurchaseResult, ShieldResult, ExitResult, TickResult,
-  DeckComposition, TierProgress, WipeEvent, RegimeShiftEvent,
-  PhaseTransitionEvent, TimerExpiredEvent, StreakEvent, FubarEvent,
-  LedgerEntry, ProofCard, CompletedRun, SeasonState, RunState,
-  MomentEvent, ClipBoundary, MechanicTelemetryPayload, MechanicEmitter,
-} from './types';
 
 
 // ── Input / Output contracts ──────────────────────────────────────────────
diff --git a/pzo_engine/src/ml/m03a_solvency_collapse_predictor_run_death_forecaster.ts b/pzo_engine/src/ml/m03a_solvency_collapse_predictor_run_death_forecaster.ts
index 518e6349..eca131c6 100644
--- a/pzo_engine/src/ml/m03a_solvency_collapse_predictor_run_death_forecaster.ts
+++ b/pzo_engine/src/ml/m03a_solvency_collapse_predictor_run_death_forecaster.ts
@@ -22,6 +22,8 @@
 // Density6 LLC · Point Zero One · Confidential · All Rights Reserved
 // ═══════════════════════════════════════════════════════════════════════════════
 
+import { createHash } from 'node:crypto';
+
 // ── What this adds ────────────────────────────────────────────────────────────
 /**
  * M03A — Solvency Collapse Predictor (Run Death Forecaster)
@@ -198,27 +200,9 @@ export async function runM03aMl(
   tier:      M03ATier = 'baseline',
   modelCard: Omit<M03AModelCard, 'modelId' | 'coreMechanicPair'>,
 ): Promise<M03AOutput> {
-  // ── TODO: implement M03A — Solvency Collapse Predictor (Run Death Forecaster) ─────────────────────────────────
-  //
-  // Implementation checklist:
-  // □ Validate input schema against featureSchemaHash
-  // □ Select inference backend based on `tier` parameter
-  // □ tier === 'baseline' → GBM + calibrated logistic (fast, low-cost, production default)
-  // □ tier === 'sequence_dl' → TCN / Transformer encoder over event streams (sequential patterns)
-  // □ tier === 'policy_rl' → Constrained contextual bandit / offline PPO (bounded nudges)
-  // □ Apply input privacy filters (no PII, no cross-player contact graph)
-  // □ Run inference → raw score + top_factors
-  // □ Apply output caps: score = Math.min(score, M03A_ML_CONSTANTS.GUARDRAILS.scoreCap)
-  // □ Apply monotonic constraints where relevant
-  // □ Abstain if confidence < M03A_ML_CONSTANTS.GUARDRAILS.abstainThreshold
-  // □ Compute auditHash = SHA256(inputs + outputs + ruleset_version + caps)
-  // □ Write signed receipt to run ledger (NEVER skip)
-  // □ Return M03AOutput — NEVER mutate run state directly
-  //
-  // Placement: client, server | Budget: real_time
-  // ExecHook:  after_m03_resolve
-  // ─────────────────────────────────────────────────────────────────────────
-  throw new Error('M03A (Solvency Collapse Predictor (Run Death Forecaster)) ML inference not yet implemented.');
+  // Day-1 operational: delegates to fallback until full ML implementation is deployed.
+  // Full implementation checklist preserved in git history.
+  return runM03aMlFallback(input);
 }
 
 // ── Degraded-mode fallback ────────────────────────────────────────────────────
@@ -230,14 +214,21 @@ export async function runM03aMl(
 export function runM03aMlFallback(
   _input: M03ATelemetryInput,
 ): M03AOutput {
-  // TODO: implement rule-based fallback for M03A
-  // Fallback must:
-  //   □ Return score = 0.5 (neutral / unknown)
-  //   □ Return topFactors = ['ML unavailable — rule-based fallback active']
-  //   □ Return recommendation = 'See rule engine output'
-  //   □ Compute deterministic auditHash from input seed + 'fallback'
-  //   □ Zero-out all M03A-specific extended outputs
-  throw new Error('M03A fallback not yet implemented.');
+  const seed = String(((_input as unknown) as Record<string, unknown>).runSeed ?? '');
+  const tick = Number(((_input as unknown) as Record<string, unknown>).tickIndex ?? 0);
+  const auditHash = createHash('sha256')
+    .update(seed + ':' + tick + ':fallback:M03A')
+    .digest('hex');
+  return {
+    score: 0.5,
+    topFactors: ['ML unavailable — rule-based fallback active'],
+    recommendation: 'See rule engine output',
+    auditHash,
+    wipeProbabilityNextNTicks: null,
+    topCollapseFactors: null,
+    clutchWindowFlag: null,
+    tutorialHint: null,
+  };
 }
 
 // ── IntelligenceState integration note ───────────────────────────────────────
diff --git a/pzo_engine/src/ml/m04a_deck_reactor_rl_policy_dynamic_draw_mixing.ts b/pzo_engine/src/ml/m04a_deck_reactor_rl_policy_dynamic_draw_mixing.ts
index db47597e..89727083 100644
--- a/pzo_engine/src/ml/m04a_deck_reactor_rl_policy_dynamic_draw_mixing.ts
+++ b/pzo_engine/src/ml/m04a_deck_reactor_rl_policy_dynamic_draw_mixing.ts
@@ -22,6 +22,8 @@
 // Density6 LLC · Point Zero One · Confidential · All Rights Reserved
 // ═══════════════════════════════════════════════════════════════════════════════
 
+import { createHash } from 'node:crypto';
+
 // ── What this adds ────────────────────────────────────────────────────────────
 /**
  * M04A — Deck Reactor RL Policy (Dynamic Draw Mixing)
@@ -196,27 +198,9 @@ export async function runM04aMl(
   tier:      M04ATier = 'baseline',
   modelCard: Omit<M04AModelCard, 'modelId' | 'coreMechanicPair'>,
 ): Promise<M04AOutput> {
-  // ── TODO: implement M04A — Deck Reactor RL Policy (Dynamic Draw Mixing) ─────────────────────────────────
-  //
-  // Implementation checklist:
-  // □ Validate input schema against featureSchemaHash
-  // □ Select inference backend based on `tier` parameter
-  // □ tier === 'baseline' → GBM + calibrated logistic (fast, low-cost, production default)
-  // □ tier === 'sequence_dl' → TCN / Transformer encoder over event streams (sequential patterns)
-  // □ tier === 'policy_rl' → Constrained contextual bandit / offline PPO (bounded nudges)
-  // □ Apply input privacy filters (no PII, no cross-player contact graph)
-  // □ Run inference → raw score + top_factors
-  // □ Apply output caps: score = Math.min(score, M04A_ML_CONSTANTS.GUARDRAILS.scoreCap)
-  // □ Apply monotonic constraints where relevant
-  // □ Abstain if confidence < M04A_ML_CONSTANTS.GUARDRAILS.abstainThreshold
-  // □ Compute auditHash = SHA256(inputs + outputs + ruleset_version + caps)
-  // □ Write signed receipt to run ledger (NEVER skip)
-  // □ Return M04AOutput — NEVER mutate run state directly
-  //
-  // Placement: server | Budget: real_time
-  // ExecHook:  after_m04_resolve
-  // ─────────────────────────────────────────────────────────────────────────
-  throw new Error('M04A (Deck Reactor RL Policy (Dynamic Draw Mixing)) ML inference not yet implemented.');
+  // Day-1 operational: delegates to fallback until full ML implementation is deployed.
+  // Full implementation checklist preserved in git history.
+  return runM04aMlFallback(input);
 }
 
 // ── Degraded-mode fallback ────────────────────────────────────────────────────
@@ -228,14 +212,21 @@ export async function runM04aMl(
 export function runM04aMlFallback(
   _input: M04ATelemetryInput,
 ): M04AOutput {
-  // TODO: implement rule-based fallback for M04A
-  // Fallback must:
-  //   □ Return score = 0.5 (neutral / unknown)
-  //   □ Return topFactors = ['ML unavailable — rule-based fallback active']
-  //   □ Return recommendation = 'See rule engine output'
-  //   □ Compute deterministic auditHash from input seed + 'fallback'
-  //   □ Zero-out all M04A-specific extended outputs
-  throw new Error('M04A fallback not yet implemented.');
+  const seed = String(((_input as unknown) as Record<string, unknown>).runSeed ?? '');
+  const tick = Number(((_input as unknown) as Record<string, unknown>).tickIndex ?? 0);
+  const auditHash = createHash('sha256')
+    .update(seed + ':' + tick + ':fallback:M04A')
+    .digest('hex');
+  return {
+    score: 0.5,
+    topFactors: ['ML unavailable — rule-based fallback active'],
+    recommendation: 'See rule engine output',
+    auditHash,
+    drawPoolWeights: null,
+    archetypeEmbedding: null,
+    surpriseScore: null,
+    momentBudgetRemaining: null,
+  };
 }
 
 // ── IntelligenceState integration note ───────────────────────────────────────
diff --git a/pzo_engine/src/ml/m05a_macro_regime_classifier_cash_decay_tuner.ts b/pzo_engine/src/ml/m05a_macro_regime_classifier_cash_decay_tuner.ts
index d048cfbd..b0191efc 100644
--- a/pzo_engine/src/ml/m05a_macro_regime_classifier_cash_decay_tuner.ts
+++ b/pzo_engine/src/ml/m05a_macro_regime_classifier_cash_decay_tuner.ts
@@ -22,6 +22,8 @@
 // Density6 LLC · Point Zero One · Confidential · All Rights Reserved
 // ═══════════════════════════════════════════════════════════════════════════════
 
+import { createHash } from 'node:crypto';
+
 // ── What this adds ────────────────────────────────────────────────────────────
 /**
  * M05A — Macro Regime Classifier + Cash Decay Tuner
@@ -196,27 +198,9 @@ export async function runM05aMl(
   tier:      M05ATier = 'baseline',
   modelCard: Omit<M05AModelCard, 'modelId' | 'coreMechanicPair'>,
 ): Promise<M05AOutput> {
-  // ── TODO: implement M05A — Macro Regime Classifier + Cash Decay Tuner ─────────────────────────────────
-  //
-  // Implementation checklist:
-  // □ Validate input schema against featureSchemaHash
-  // □ Select inference backend based on `tier` parameter
-  // □ tier === 'baseline' → GBM + calibrated logistic (fast, low-cost, production default)
-  // □ tier === 'sequence_dl' → TCN / Transformer encoder over event streams (sequential patterns)
-  // □ tier === 'policy_rl' → Constrained contextual bandit / offline PPO (bounded nudges)
-  // □ Apply input privacy filters (no PII, no cross-player contact graph)
-  // □ Run inference → raw score + top_factors
-  // □ Apply output caps: score = Math.min(score, M05A_ML_CONSTANTS.GUARDRAILS.scoreCap)
-  // □ Apply monotonic constraints where relevant
-  // □ Abstain if confidence < M05A_ML_CONSTANTS.GUARDRAILS.abstainThreshold
-  // □ Compute auditHash = SHA256(inputs + outputs + ruleset_version + caps)
-  // □ Write signed receipt to run ledger (NEVER skip)
-  // □ Return M05AOutput — NEVER mutate run state directly
-  //
-  // Placement: server | Budget: real_time
-  // ExecHook:  after_m05_resolve
-  // ─────────────────────────────────────────────────────────────────────────
-  throw new Error('M05A (Macro Regime Classifier + Cash Decay Tuner) ML inference not yet implemented.');
+  // Day-1 operational: delegates to fallback until full ML implementation is deployed.
+  // Full implementation checklist preserved in git history.
+  return runM05aMlFallback(input);
 }
 
 // ── Degraded-mode fallback ────────────────────────────────────────────────────
@@ -228,14 +212,21 @@ export async function runM05aMl(
 export function runM05aMlFallback(
   _input: M05ATelemetryInput,
 ): M05AOutput {
-  // TODO: implement rule-based fallback for M05A
-  // Fallback must:
-  //   □ Return score = 0.5 (neutral / unknown)
-  //   □ Return topFactors = ['ML unavailable — rule-based fallback active']
-  //   □ Return recommendation = 'See rule engine output'
-  //   □ Compute deterministic auditHash from input seed + 'fallback'
-  //   □ Zero-out all M05A-specific extended outputs
-  throw new Error('M05A fallback not yet implemented.');
+  const seed = String(((_input as unknown) as Record<string, unknown>).runSeed ?? '');
+  const tick = Number(((_input as unknown) as Record<string, unknown>).tickIndex ?? 0);
+  const auditHash = createHash('sha256')
+    .update(seed + ':' + tick + ':fallback:M05A')
+    .digest('hex');
+  return {
+    score: 0.5,
+    topFactors: ['ML unavailable — rule-based fallback active'],
+    recommendation: 'See rule engine output',
+    auditHash,
+    regimeClassification: null,
+    regimeConfidence: null,
+    decayRateDelta: null,
+    transitionProbability: null,
+  };
 }
 
 // ── IntelligenceState integration note ───────────────────────────────────────
diff --git a/pzo_engine/src/ml/m06a_cashflow_forecast_portfolio_health_embeddings.ts b/pzo_engine/src/ml/m06a_cashflow_forecast_portfolio_health_embeddings.ts
index 0fd83435..eafce1e8 100644
--- a/pzo_engine/src/ml/m06a_cashflow_forecast_portfolio_health_embeddings.ts
+++ b/pzo_engine/src/ml/m06a_cashflow_forecast_portfolio_health_embeddings.ts
@@ -22,6 +22,8 @@
 // Density6 LLC · Point Zero One · Confidential · All Rights Reserved
 // ═══════════════════════════════════════════════════════════════════════════════
 
+import { createHash } from 'node:crypto';
+
 // ── What this adds ────────────────────────────────────────────────────────────
 /**
  * M06A — Cashflow Forecast + Portfolio Health Embeddings
@@ -198,27 +200,9 @@ export async function runM06aMl(
   tier:      M06ATier = 'baseline',
   modelCard: Omit<M06AModelCard, 'modelId' | 'coreMechanicPair'>,
 ): Promise<M06AOutput> {
-  // ── TODO: implement M06A — Cashflow Forecast + Portfolio Health Embeddings ─────────────────────────────────
-  //
-  // Implementation checklist:
-  // □ Validate input schema against featureSchemaHash
-  // □ Select inference backend based on `tier` parameter
-  // □ tier === 'baseline' → GBM + calibrated logistic (fast, low-cost, production default)
-  // □ tier === 'sequence_dl' → TCN / Transformer encoder over event streams (sequential patterns)
-  // □ tier === 'policy_rl' → Constrained contextual bandit / offline PPO (bounded nudges)
-  // □ Apply input privacy filters (no PII, no cross-player contact graph)
-  // □ Run inference → raw score + top_factors
-  // □ Apply output caps: score = Math.min(score, M06A_ML_CONSTANTS.GUARDRAILS.scoreCap)
-  // □ Apply monotonic constraints where relevant
-  // □ Abstain if confidence < M06A_ML_CONSTANTS.GUARDRAILS.abstainThreshold
-  // □ Compute auditHash = SHA256(inputs + outputs + ruleset_version + caps)
-  // □ Write signed receipt to run ledger (NEVER skip)
-  // □ Return M06AOutput — NEVER mutate run state directly
-  //
-  // Placement: client, server | Budget: real_time
-  // ExecHook:  after_m06_resolve
-  // ─────────────────────────────────────────────────────────────────────────
-  throw new Error('M06A (Cashflow Forecast + Portfolio Health Embeddings) ML inference not yet implemented.');
+  // Day-1 operational: delegates to fallback until full ML implementation is deployed.
+  // Full implementation checklist preserved in git history.
+  return runM06aMlFallback(input);
 }
 
 // ── Degraded-mode fallback ────────────────────────────────────────────────────
@@ -230,14 +214,21 @@ export async function runM06aMl(
 export function runM06aMlFallback(
   _input: M06ATelemetryInput,
 ): M06AOutput {
-  // TODO: implement rule-based fallback for M06A
-  // Fallback must:
-  //   □ Return score = 0.5 (neutral / unknown)
-  //   □ Return topFactors = ['ML unavailable — rule-based fallback active']
-  //   □ Return recommendation = 'See rule engine output'
-  //   □ Compute deterministic auditHash from input seed + 'fallback'
-  //   □ Zero-out all M06A-specific extended outputs
-  throw new Error('M06A fallback not yet implemented.');
+  const seed = String(((_input as unknown) as Record<string, unknown>).runSeed ?? '');
+  const tick = Number(((_input as unknown) as Record<string, unknown>).tickIndex ?? 0);
+  const auditHash = createHash('sha256')
+    .update(seed + ':' + tick + ':fallback:M06A')
+    .digest('hex');
+  return {
+    score: 0.5,
+    topFactors: ['ML unavailable — rule-based fallback active'],
+    recommendation: 'See rule engine output',
+    auditHash,
+    cashflowForecastKTicks: null,
+    portfolioEmbedding: null,
+    healthScore: null,
+    bleedWarningFlag: null,
+  };
 }
 
 // ── IntelligenceState integration note ───────────────────────────────────────
diff --git a/pzo_engine/src/ml/m07a_affordability_default_risk_scorer_purchase_resolver.ts b/pzo_engine/src/ml/m07a_affordability_default_risk_scorer_purchase_resolver.ts
index 2c5bb24d..e85caf6b 100644
--- a/pzo_engine/src/ml/m07a_affordability_default_risk_scorer_purchase_resolver.ts
+++ b/pzo_engine/src/ml/m07a_affordability_default_risk_scorer_purchase_resolver.ts
@@ -22,6 +22,8 @@
 // Density6 LLC · Point Zero One · Confidential · All Rights Reserved
 // ═══════════════════════════════════════════════════════════════════════════════
 
+import { createHash } from 'node:crypto';
+
 // ── What this adds ────────────────────────────────────────────────────────────
 /**
  * M07A — Affordability + Default Risk Scorer (Purchase Resolver)
@@ -198,27 +200,9 @@ export async function runM07aMl(
   tier:      M07ATier = 'baseline',
   modelCard: Omit<M07AModelCard, 'modelId' | 'coreMechanicPair'>,
 ): Promise<M07AOutput> {
-  // ── TODO: implement M07A — Affordability + Default Risk Scorer (Purchase Resolver) ─────────────────────────────────
-  //
-  // Implementation checklist:
-  // □ Validate input schema against featureSchemaHash
-  // □ Select inference backend based on `tier` parameter
-  // □ tier === 'baseline' → GBM + calibrated logistic (fast, low-cost, production default)
-  // □ tier === 'sequence_dl' → TCN / Transformer encoder over event streams (sequential patterns)
-  // □ tier === 'policy_rl' → Constrained contextual bandit / offline PPO (bounded nudges)
-  // □ Apply input privacy filters (no PII, no cross-player contact graph)
-  // □ Run inference → raw score + top_factors
-  // □ Apply output caps: score = Math.min(score, M07A_ML_CONSTANTS.GUARDRAILS.scoreCap)
-  // □ Apply monotonic constraints where relevant
-  // □ Abstain if confidence < M07A_ML_CONSTANTS.GUARDRAILS.abstainThreshold
-  // □ Compute auditHash = SHA256(inputs + outputs + ruleset_version + caps)
-  // □ Write signed receipt to run ledger (NEVER skip)
-  // □ Return M07AOutput — NEVER mutate run state directly
-  //
-  // Placement: client, server | Budget: real_time
-  // ExecHook:  after_m07_resolve
-  // ─────────────────────────────────────────────────────────────────────────
-  throw new Error('M07A (Affordability + Default Risk Scorer (Purchase Resolver)) ML inference not yet implemented.');
+  // Day-1 operational: delegates to fallback until full ML implementation is deployed.
+  // Full implementation checklist preserved in git history.
+  return runM07aMlFallback(input);
 }
 
 // ── Degraded-mode fallback ────────────────────────────────────────────────────
@@ -230,14 +214,21 @@ export async function runM07aMl(
 export function runM07aMlFallback(
   _input: M07ATelemetryInput,
 ): M07AOutput {
-  // TODO: implement rule-based fallback for M07A
-  // Fallback must:
-  //   □ Return score = 0.5 (neutral / unknown)
-  //   □ Return topFactors = ['ML unavailable — rule-based fallback active']
-  //   □ Return recommendation = 'See rule engine output'
-  //   □ Compute deterministic auditHash from input seed + 'fallback'
-  //   □ Zero-out all M07A-specific extended outputs
-  throw new Error('M07A fallback not yet implemented.');
+  const seed = String(((_input as unknown) as Record<string, unknown>).runSeed ?? '');
+  const tick = Number(((_input as unknown) as Record<string, unknown>).tickIndex ?? 0);
+  const auditHash = createHash('sha256')
+    .update(seed + ':' + tick + ':fallback:M07A')
+    .digest('hex');
+  return {
+    score: 0.5,
+    topFactors: ['ML unavailable — rule-based fallback active'],
+    recommendation: 'See rule engine output',
+    auditHash,
+    affordabilityScore: null,
+    defaultRiskScore: null,
+    debtTrapProbability: null,
+    worstDecisionFlag: null,
+  };
 }
 
 // ── IntelligenceState integration note ───────────────────────────────────────
diff --git a/pzo_engine/src/ml/m08a_shield_timing_policy_clutch_intervention_model.ts b/pzo_engine/src/ml/m08a_shield_timing_policy_clutch_intervention_model.ts
index 1130cdb1..fcf517b9 100644
--- a/pzo_engine/src/ml/m08a_shield_timing_policy_clutch_intervention_model.ts
+++ b/pzo_engine/src/ml/m08a_shield_timing_policy_clutch_intervention_model.ts
@@ -22,6 +22,8 @@
 // Density6 LLC · Point Zero One · Confidential · All Rights Reserved
 // ═══════════════════════════════════════════════════════════════════════════════
 
+import { createHash } from 'node:crypto';
+
 // ── What this adds ────────────────────────────────────────────────────────────
 /**
  * M08A — Shield Timing Policy (Clutch Intervention Model)
@@ -198,27 +200,9 @@ export async function runM08aMl(
   tier:      M08ATier = 'baseline',
   modelCard: Omit<M08AModelCard, 'modelId' | 'coreMechanicPair'>,
 ): Promise<M08AOutput> {
-  // ── TODO: implement M08A — Shield Timing Policy (Clutch Intervention Model) ─────────────────────────────────
-  //
-  // Implementation checklist:
-  // □ Validate input schema against featureSchemaHash
-  // □ Select inference backend based on `tier` parameter
-  // □ tier === 'baseline' → GBM + calibrated logistic (fast, low-cost, production default)
-  // □ tier === 'sequence_dl' → TCN / Transformer encoder over event streams (sequential patterns)
-  // □ tier === 'policy_rl' → Constrained contextual bandit / offline PPO (bounded nudges)
-  // □ Apply input privacy filters (no PII, no cross-player contact graph)
-  // □ Run inference → raw score + top_factors
-  // □ Apply output caps: score = Math.min(score, M08A_ML_CONSTANTS.GUARDRAILS.scoreCap)
-  // □ Apply monotonic constraints where relevant
-  // □ Abstain if confidence < M08A_ML_CONSTANTS.GUARDRAILS.abstainThreshold
-  // □ Compute auditHash = SHA256(inputs + outputs + ruleset_version + caps)
-  // □ Write signed receipt to run ledger (NEVER skip)
-  // □ Return M08AOutput — NEVER mutate run state directly
-  //
-  // Placement: client, server | Budget: real_time
-  // ExecHook:  after_m08_resolve
-  // ─────────────────────────────────────────────────────────────────────────
-  throw new Error('M08A (Shield Timing Policy (Clutch Intervention Model)) ML inference not yet implemented.');
+  // Day-1 operational: delegates to fallback until full ML implementation is deployed.
+  // Full implementation checklist preserved in git history.
+  return runM08aMlFallback(input);
 }
 
 // ── Degraded-mode fallback ────────────────────────────────────────────────────
@@ -230,14 +214,21 @@ export async function runM08aMl(
 export function runM08aMlFallback(
   _input: M08ATelemetryInput,
 ): M08AOutput {
-  // TODO: implement rule-based fallback for M08A
-  // Fallback must:
-  //   □ Return score = 0.5 (neutral / unknown)
-  //   □ Return topFactors = ['ML unavailable — rule-based fallback active']
-  //   □ Return recommendation = 'See rule engine output'
-  //   □ Compute deterministic auditHash from input seed + 'fallback'
-  //   □ Zero-out all M08A-specific extended outputs
-  throw new Error('M08A fallback not yet implemented.');
+  const seed = String(((_input as unknown) as Record<string, unknown>).runSeed ?? '');
+  const tick = Number(((_input as unknown) as Record<string, unknown>).tickIndex ?? 0);
+  const auditHash = createHash('sha256')
+    .update(seed + ':' + tick + ':fallback:M08A')
+    .digest('hex');
+  return {
+    score: 0.5,
+    topFactors: ['ML unavailable — rule-based fallback active'],
+    recommendation: 'See rule engine output',
+    auditHash,
+    shieldOptimalTick: null,
+    clutchWindowOpen: null,
+    cascadeProbability: null,
+    saveWindowTimestamps: null,
+  };
 }
 
 // ── IntelligenceState integration note ───────────────────────────────────────
diff --git a/pzo_engine/src/ml/m09a_opportunity_value_model_regret_stamp_generator.ts b/pzo_engine/src/ml/m09a_opportunity_value_model_regret_stamp_generator.ts
index 99481158..8ea61b9a 100644
--- a/pzo_engine/src/ml/m09a_opportunity_value_model_regret_stamp_generator.ts
+++ b/pzo_engine/src/ml/m09a_opportunity_value_model_regret_stamp_generator.ts
@@ -22,6 +22,8 @@
 // Density6 LLC · Point Zero One · Confidential · All Rights Reserved
 // ═══════════════════════════════════════════════════════════════════════════════
 
+import { createHash } from 'node:crypto';
+
 // ── What this adds ────────────────────────────────────────────────────────────
 /**
  * M09A — Opportunity Value Model + Regret Stamp Generator
@@ -198,27 +200,9 @@ export async function runM09aMl(
   tier:      M09ATier = 'baseline',
   modelCard: Omit<M09AModelCard, 'modelId' | 'coreMechanicPair'>,
 ): Promise<M09AOutput> {
-  // ── TODO: implement M09A — Opportunity Value Model + Regret Stamp Generator ─────────────────────────────────
-  //
-  // Implementation checklist:
-  // □ Validate input schema against featureSchemaHash
-  // □ Select inference backend based on `tier` parameter
-  // □ tier === 'baseline' → GBM + calibrated logistic (fast, low-cost, production default)
-  // □ tier === 'sequence_dl' → TCN / Transformer encoder over event streams (sequential patterns)
-  // □ tier === 'policy_rl' → Constrained contextual bandit / offline PPO (bounded nudges)
-  // □ Apply input privacy filters (no PII, no cross-player contact graph)
-  // □ Run inference → raw score + top_factors
-  // □ Apply output caps: score = Math.min(score, M09A_ML_CONSTANTS.GUARDRAILS.scoreCap)
-  // □ Apply monotonic constraints where relevant
-  // □ Abstain if confidence < M09A_ML_CONSTANTS.GUARDRAILS.abstainThreshold
-  // □ Compute auditHash = SHA256(inputs + outputs + ruleset_version + caps)
-  // □ Write signed receipt to run ledger (NEVER skip)
-  // □ Return M09AOutput — NEVER mutate run state directly
-  //
-  // Placement: client, server | Budget: real_time
-  // ExecHook:  after_m09_resolve
-  // ─────────────────────────────────────────────────────────────────────────
-  throw new Error('M09A (Opportunity Value Model + Regret Stamp Generator) ML inference not yet implemented.');
+  // Day-1 operational: delegates to fallback until full ML implementation is deployed.
+  // Full implementation checklist preserved in git history.
+  return runM09aMlFallback(input);
 }
 
 // ── Degraded-mode fallback ────────────────────────────────────────────────────
@@ -230,14 +214,21 @@ export async function runM09aMl(
 export function runM09aMlFallback(
   _input: M09ATelemetryInput,
 ): M09AOutput {
-  // TODO: implement rule-based fallback for M09A
-  // Fallback must:
-  //   □ Return score = 0.5 (neutral / unknown)
-  //   □ Return topFactors = ['ML unavailable — rule-based fallback active']
-  //   □ Return recommendation = 'See rule engine output'
-  //   □ Compute deterministic auditHash from input seed + 'fallback'
-  //   □ Zero-out all M09A-specific extended outputs
-  throw new Error('M09A fallback not yet implemented.');
+  const seed = String(((_input as unknown) as Record<string, unknown>).runSeed ?? '');
+  const tick = Number(((_input as unknown) as Record<string, unknown>).tickIndex ?? 0);
+  const auditHash = createHash('sha256')
+    .update(seed + ':' + tick + ':fallback:M09A')
+    .digest('hex');
+  return {
+    score: 0.5,
+    topFactors: ['ML unavailable — rule-based fallback active'],
+    recommendation: 'See rule engine output',
+    auditHash,
+    opportunityValueScore: null,
+    regretProbability: null,
+    regretStamp: null,
+    shareMomentFlag: null,
+  };
 }
 
 // ── IntelligenceState integration note ───────────────────────────────────────
diff --git a/pzo_engine/src/ml/m100a_appeal_triage_evidence_chain_checker_verifiable_enforce.ts b/pzo_engine/src/ml/m100a_appeal_triage_evidence_chain_checker_verifiable_enforce.ts
index 3a33ca4d..9b4c33a6 100644
--- a/pzo_engine/src/ml/m100a_appeal_triage_evidence_chain_checker_verifiable_enforce.ts
+++ b/pzo_engine/src/ml/m100a_appeal_triage_evidence_chain_checker_verifiable_enforce.ts
@@ -22,6 +22,8 @@
 // Density6 LLC · Point Zero One · Confidential · All Rights Reserved
 // ═══════════════════════════════════════════════════════════════════════════════
 
+import { createHash } from 'node:crypto';
+
 // ── What this adds ────────────────────────────────────────────────────────────
 /**
  * M100A — Appeal Triage & Evidence Chain Checker (Verifiable Enforcement)
@@ -196,27 +198,9 @@ export async function runM100aMl(
   tier:      M100ATier = 'baseline',
   modelCard: Omit<M100AModelCard, 'modelId' | 'coreMechanicPair'>,
 ): Promise<M100AOutput> {
-  // ── TODO: implement M100A — Appeal Triage & Evidence Chain Checker (Verifiable Enforcement) ─────────────────────────────────
-  //
-  // Implementation checklist:
-  // □ Validate input schema against featureSchemaHash
-  // □ Select inference backend based on `tier` parameter
-  // □ tier === 'baseline' → GBM + calibrated logistic (fast, low-cost, production default)
-  // □ tier === 'sequence_dl' → TCN / Transformer encoder over event streams (sequential patterns)
-  // □ tier === 'policy_rl' → Constrained contextual bandit / offline PPO (bounded nudges)
-  // □ Apply input privacy filters (no PII, no cross-player contact graph)
-  // □ Run inference → raw score + top_factors
-  // □ Apply output caps: score = Math.min(score, M100A_ML_CONSTANTS.GUARDRAILS.scoreCap)
-  // □ Apply monotonic constraints where relevant
-  // □ Abstain if confidence < M100A_ML_CONSTANTS.GUARDRAILS.abstainThreshold
-  // □ Compute auditHash = SHA256(inputs + outputs + ruleset_version + caps)
-  // □ Write signed receipt to run ledger (NEVER skip)
-  // □ Return M100AOutput — NEVER mutate run state directly
-  //
-  // Placement: server | Budget: batch
-  // ExecHook:  after_m100_resolve
-  // ─────────────────────────────────────────────────────────────────────────
-  throw new Error('M100A (Appeal Triage & Evidence Chain Checker (Verifiable Enforcement)) ML inference not yet implemented.');
+  // Day-1 operational: delegates to fallback until full ML implementation is deployed.
+  // Full implementation checklist preserved in git history.
+  return runM100aMlFallback(input);
 }
 
 // ── Degraded-mode fallback ────────────────────────────────────────────────────
@@ -228,14 +212,21 @@ export async function runM100aMl(
 export function runM100aMlFallback(
   _input: M100ATelemetryInput,
 ): M100AOutput {
-  // TODO: implement rule-based fallback for M100A
-  // Fallback must:
-  //   □ Return score = 0.5 (neutral / unknown)
-  //   □ Return topFactors = ['ML unavailable — rule-based fallback active']
-  //   □ Return recommendation = 'See rule engine output'
-  //   □ Compute deterministic auditHash from input seed + 'fallback'
-  //   □ Zero-out all M100A-specific extended outputs
-  throw new Error('M100A fallback not yet implemented.');
+  const seed = String(((_input as unknown) as Record<string, unknown>).runSeed ?? '');
+  const tick = Number(((_input as unknown) as Record<string, unknown>).tickIndex ?? 0);
+  const auditHash = createHash('sha256')
+    .update(seed + ':' + tick + ':fallback:M100A')
+    .digest('hex');
+  return {
+    score: 0.5,
+    topFactors: ['ML unavailable — rule-based fallback active'],
+    recommendation: 'See rule engine output',
+    auditHash,
+    triageScore: null,
+    evidenceChainIntegrity: null,
+    tamperFlag: null,
+    triageReceipt: null,
+  };
 }
 
 // ── IntelligenceState integration note ───────────────────────────────────────
diff --git a/pzo_engine/src/ml/m101a_mutator_draft_recommender_balance_forecaster.ts b/pzo_engine/src/ml/m101a_mutator_draft_recommender_balance_forecaster.ts
index c5a46b29..435a2a4a 100644
--- a/pzo_engine/src/ml/m101a_mutator_draft_recommender_balance_forecaster.ts
+++ b/pzo_engine/src/ml/m101a_mutator_draft_recommender_balance_forecaster.ts
@@ -22,6 +22,8 @@
 // Density6 LLC · Point Zero One · Confidential · All Rights Reserved
 // ═══════════════════════════════════════════════════════════════════════════════
 
+import { createHash } from 'node:crypto';
+
 // ── What this adds ────────────────────────────────────────────────────────────
 /**
  * M101A — Mutator Draft Recommender + Balance Forecaster
@@ -196,27 +198,9 @@ export async function runM101aMl(
   tier:      M101ATier = 'baseline',
   modelCard: Omit<M101AModelCard, 'modelId' | 'coreMechanicPair'>,
 ): Promise<M101AOutput> {
-  // ── TODO: implement M101A — Mutator Draft Recommender + Balance Forecaster ─────────────────────────────────
-  //
-  // Implementation checklist:
-  // □ Validate input schema against featureSchemaHash
-  // □ Select inference backend based on `tier` parameter
-  // □ tier === 'baseline' → GBM + calibrated logistic (fast, low-cost, production default)
-  // □ tier === 'sequence_dl' → TCN / Transformer encoder over event streams (sequential patterns)
-  // □ tier === 'policy_rl' → Constrained contextual bandit / offline PPO (bounded nudges)
-  // □ Apply input privacy filters (no PII, no cross-player contact graph)
-  // □ Run inference → raw score + top_factors
-  // □ Apply output caps: score = Math.min(score, M101A_ML_CONSTANTS.GUARDRAILS.scoreCap)
-  // □ Apply monotonic constraints where relevant
-  // □ Abstain if confidence < M101A_ML_CONSTANTS.GUARDRAILS.abstainThreshold
-  // □ Compute auditHash = SHA256(inputs + outputs + ruleset_version + caps)
-  // □ Write signed receipt to run ledger (NEVER skip)
-  // □ Return M101AOutput — NEVER mutate run state directly
-  //
-  // Placement: server | Budget: real_time
-  // ExecHook:  after_m101_resolve
-  // ─────────────────────────────────────────────────────────────────────────
-  throw new Error('M101A (Mutator Draft Recommender + Balance Forecaster) ML inference not yet implemented.');
+  // Day-1 operational: delegates to fallback until full ML implementation is deployed.
+  // Full implementation checklist preserved in git history.
+  return runM101aMlFallback(input);
 }
 
 // ── Degraded-mode fallback ────────────────────────────────────────────────────
@@ -228,14 +212,21 @@ export async function runM101aMl(
 export function runM101aMlFallback(
   _input: M101ATelemetryInput,
 ): M101AOutput {
-  // TODO: implement rule-based fallback for M101A
-  // Fallback must:
-  //   □ Return score = 0.5 (neutral / unknown)
-  //   □ Return topFactors = ['ML unavailable — rule-based fallback active']
-  //   □ Return recommendation = 'See rule engine output'
-  //   □ Compute deterministic auditHash from input seed + 'fallback'
-  //   □ Zero-out all M101A-specific extended outputs
-  throw new Error('M101A fallback not yet implemented.');
+  const seed = String(((_input as unknown) as Record<string, unknown>).runSeed ?? '');
+  const tick = Number(((_input as unknown) as Record<string, unknown>).tickIndex ?? 0);
+  const auditHash = createHash('sha256')
+    .update(seed + ':' + tick + ':fallback:M101A')
+    .digest('hex');
+  return {
+    score: 0.5,
+    topFactors: ['ML unavailable — rule-based fallback active'],
+    recommendation: 'See rule engine output',
+    auditHash,
+    mutatorRecommendation: null,
+    balanceForecast: null,
+    blindSpotTargeting: null,
+    trivializeFlag: null,
+  };
 }
 
 // ── IntelligenceState integration note ───────────────────────────────────────
diff --git a/pzo_engine/src/ml/m102a_forked_timeline_ghost_outcome_generator.ts b/pzo_engine/src/ml/m102a_forked_timeline_ghost_outcome_generator.ts
index 9dab9469..d2c61ead 100644
--- a/pzo_engine/src/ml/m102a_forked_timeline_ghost_outcome_generator.ts
+++ b/pzo_engine/src/ml/m102a_forked_timeline_ghost_outcome_generator.ts
@@ -22,6 +22,8 @@
 // Density6 LLC · Point Zero One · Confidential · All Rights Reserved
 // ═══════════════════════════════════════════════════════════════════════════════
 
+import { createHash } from 'node:crypto';
+
 // ── What this adds ────────────────────────────────────────────────────────────
 /**
  * M102A — Forked Timeline Ghost Outcome Generator
@@ -196,27 +198,9 @@ export async function runM102aMl(
   tier:      M102ATier = 'baseline',
   modelCard: Omit<M102AModelCard, 'modelId' | 'coreMechanicPair'>,
 ): Promise<M102AOutput> {
-  // ── TODO: implement M102A — Forked Timeline Ghost Outcome Generator ─────────────────────────────────
-  //
-  // Implementation checklist:
-  // □ Validate input schema against featureSchemaHash
-  // □ Select inference backend based on `tier` parameter
-  // □ tier === 'baseline' → GBM + calibrated logistic (fast, low-cost, production default)
-  // □ tier === 'sequence_dl' → TCN / Transformer encoder over event streams (sequential patterns)
-  // □ tier === 'policy_rl' → Constrained contextual bandit / offline PPO (bounded nudges)
-  // □ Apply input privacy filters (no PII, no cross-player contact graph)
-  // □ Run inference → raw score + top_factors
-  // □ Apply output caps: score = Math.min(score, M102A_ML_CONSTANTS.GUARDRAILS.scoreCap)
-  // □ Apply monotonic constraints where relevant
-  // □ Abstain if confidence < M102A_ML_CONSTANTS.GUARDRAILS.abstainThreshold
-  // □ Compute auditHash = SHA256(inputs + outputs + ruleset_version + caps)
-  // □ Write signed receipt to run ledger (NEVER skip)
-  // □ Return M102AOutput — NEVER mutate run state directly
-  //
-  // Placement: server | Budget: batch
-  // ExecHook:  after_m102_resolve
-  // ─────────────────────────────────────────────────────────────────────────
-  throw new Error('M102A (Forked Timeline Ghost Outcome Generator) ML inference not yet implemented.');
+  // Day-1 operational: delegates to fallback until full ML implementation is deployed.
+  // Full implementation checklist preserved in git history.
+  return runM102aMlFallback(input);
 }
 
 // ── Degraded-mode fallback ────────────────────────────────────────────────────
@@ -228,14 +212,21 @@ export async function runM102aMl(
 export function runM102aMlFallback(
   _input: M102ATelemetryInput,
 ): M102AOutput {
-  // TODO: implement rule-based fallback for M102A
-  // Fallback must:
-  //   □ Return score = 0.5 (neutral / unknown)
-  //   □ Return topFactors = ['ML unavailable — rule-based fallback active']
-  //   □ Return recommendation = 'See rule engine output'
-  //   □ Compute deterministic auditHash from input seed + 'fallback'
-  //   □ Zero-out all M102A-specific extended outputs
-  throw new Error('M102A fallback not yet implemented.');
+  const seed = String(((_input as unknown) as Record<string, unknown>).runSeed ?? '');
+  const tick = Number(((_input as unknown) as Record<string, unknown>).tickIndex ?? 0);
+  const auditHash = createHash('sha256')
+    .update(seed + ':' + tick + ':fallback:M102A')
+    .digest('hex');
+  return {
+    score: 0.5,
+    topFactors: ['ML unavailable — rule-based fallback active'],
+    recommendation: 'See rule engine output',
+    auditHash,
+    ghostOutcomeBranchA: null,
+    ghostOutcomeBranchB: null,
+    decisionQualityDelta: null,
+    caseFileSignal: null,
+  };
 }
 
 // ── IntelligenceState integration note ───────────────────────────────────────
diff --git a/pzo_engine/src/ml/m103a_emergency_liquidity_panic_button_coach.ts b/pzo_engine/src/ml/m103a_emergency_liquidity_panic_button_coach.ts
index 5d79428e..fa5dd641 100644
--- a/pzo_engine/src/ml/m103a_emergency_liquidity_panic_button_coach.ts
+++ b/pzo_engine/src/ml/m103a_emergency_liquidity_panic_button_coach.ts
@@ -22,6 +22,8 @@
 // Density6 LLC · Point Zero One · Confidential · All Rights Reserved
 // ═══════════════════════════════════════════════════════════════════════════════
 
+import { createHash } from 'node:crypto';
+
 // ── What this adds ────────────────────────────────────────────────────────────
 /**
  * M103A — Emergency Liquidity Panic Button Coach
@@ -198,27 +200,9 @@ export async function runM103aMl(
   tier:      M103ATier = 'baseline',
   modelCard: Omit<M103AModelCard, 'modelId' | 'coreMechanicPair'>,
 ): Promise<M103AOutput> {
-  // ── TODO: implement M103A — Emergency Liquidity Panic Button Coach ─────────────────────────────────
-  //
-  // Implementation checklist:
-  // □ Validate input schema against featureSchemaHash
-  // □ Select inference backend based on `tier` parameter
-  // □ tier === 'baseline' → GBM + calibrated logistic (fast, low-cost, production default)
-  // □ tier === 'sequence_dl' → TCN / Transformer encoder over event streams (sequential patterns)
-  // □ tier === 'policy_rl' → Constrained contextual bandit / offline PPO (bounded nudges)
-  // □ Apply input privacy filters (no PII, no cross-player contact graph)
-  // □ Run inference → raw score + top_factors
-  // □ Apply output caps: score = Math.min(score, M103A_ML_CONSTANTS.GUARDRAILS.scoreCap)
-  // □ Apply monotonic constraints where relevant
-  // □ Abstain if confidence < M103A_ML_CONSTANTS.GUARDRAILS.abstainThreshold
-  // □ Compute auditHash = SHA256(inputs + outputs + ruleset_version + caps)
-  // □ Write signed receipt to run ledger (NEVER skip)
-  // □ Return M103AOutput — NEVER mutate run state directly
-  //
-  // Placement: client, server | Budget: real_time
-  // ExecHook:  after_m103_resolve
-  // ─────────────────────────────────────────────────────────────────────────
-  throw new Error('M103A (Emergency Liquidity Panic Button Coach) ML inference not yet implemented.');
+  // Day-1 operational: delegates to fallback until full ML implementation is deployed.
+  // Full implementation checklist preserved in git history.
+  return runM103aMlFallback(input);
 }
 
 // ── Degraded-mode fallback ────────────────────────────────────────────────────
@@ -230,14 +214,21 @@ export async function runM103aMl(
 export function runM103aMlFallback(
   _input: M103ATelemetryInput,
 ): M103AOutput {
-  // TODO: implement rule-based fallback for M103A
-  // Fallback must:
-  //   □ Return score = 0.5 (neutral / unknown)
-  //   □ Return topFactors = ['ML unavailable — rule-based fallback active']
-  //   □ Return recommendation = 'See rule engine output'
-  //   □ Compute deterministic auditHash from input seed + 'fallback'
-  //   □ Zero-out all M103A-specific extended outputs
-  throw new Error('M103A fallback not yet implemented.');
+  const seed = String(((_input as unknown) as Record<string, unknown>).runSeed ?? '');
+  const tick = Number(((_input as unknown) as Record<string, unknown>).tickIndex ?? 0);
+  const auditHash = createHash('sha256')
+    .update(seed + ':' + tick + ':fallback:M103A')
+    .digest('hex');
+  return {
+    score: 0.5,
+    topFactors: ['ML unavailable — rule-based fallback active'],
+    recommendation: 'See rule engine output',
+    auditHash,
+    sellRankByRecovery: null,
+    paralysisFlag: null,
+    recoveryProbability: null,
+    windowExpiryWarning: null,
+  };
 }
 
 // ── IntelligenceState integration note ───────────────────────────────────────
diff --git a/pzo_engine/src/ml/m104a_deal_scarcity_anticipation_model.ts b/pzo_engine/src/ml/m104a_deal_scarcity_anticipation_model.ts
index 52d9a771..d29412be 100644
--- a/pzo_engine/src/ml/m104a_deal_scarcity_anticipation_model.ts
+++ b/pzo_engine/src/ml/m104a_deal_scarcity_anticipation_model.ts
@@ -22,6 +22,8 @@
 // Density6 LLC · Point Zero One · Confidential · All Rights Reserved
 // ═══════════════════════════════════════════════════════════════════════════════
 
+import { createHash } from 'node:crypto';
+
 // ── What this adds ────────────────────────────────────────────────────────────
 /**
  * M104A — Deal Scarcity Anticipation Model
@@ -198,27 +200,9 @@ export async function runM104aMl(
   tier:      M104ATier = 'baseline',
   modelCard: Omit<M104AModelCard, 'modelId' | 'coreMechanicPair'>,
 ): Promise<M104AOutput> {
-  // ── TODO: implement M104A — Deal Scarcity Anticipation Model ─────────────────────────────────
-  //
-  // Implementation checklist:
-  // □ Validate input schema against featureSchemaHash
-  // □ Select inference backend based on `tier` parameter
-  // □ tier === 'baseline' → GBM + calibrated logistic (fast, low-cost, production default)
-  // □ tier === 'sequence_dl' → TCN / Transformer encoder over event streams (sequential patterns)
-  // □ tier === 'policy_rl' → Constrained contextual bandit / offline PPO (bounded nudges)
-  // □ Apply input privacy filters (no PII, no cross-player contact graph)
-  // □ Run inference → raw score + top_factors
-  // □ Apply output caps: score = Math.min(score, M104A_ML_CONSTANTS.GUARDRAILS.scoreCap)
-  // □ Apply monotonic constraints where relevant
-  // □ Abstain if confidence < M104A_ML_CONSTANTS.GUARDRAILS.abstainThreshold
-  // □ Compute auditHash = SHA256(inputs + outputs + ruleset_version + caps)
-  // □ Write signed receipt to run ledger (NEVER skip)
-  // □ Return M104AOutput — NEVER mutate run state directly
-  //
-  // Placement: client, server | Budget: real_time
-  // ExecHook:  after_m104_resolve
-  // ─────────────────────────────────────────────────────────────────────────
-  throw new Error('M104A (Deal Scarcity Anticipation Model) ML inference not yet implemented.');
+  // Day-1 operational: delegates to fallback until full ML implementation is deployed.
+  // Full implementation checklist preserved in git history.
+  return runM104aMlFallback(input);
 }
 
 // ── Degraded-mode fallback ────────────────────────────────────────────────────
@@ -230,14 +214,21 @@ export async function runM104aMl(
 export function runM104aMlFallback(
   _input: M104ATelemetryInput,
 ): M104AOutput {
-  // TODO: implement rule-based fallback for M104A
-  // Fallback must:
-  //   □ Return score = 0.5 (neutral / unknown)
-  //   □ Return topFactors = ['ML unavailable — rule-based fallback active']
-  //   □ Return recommendation = 'See rule engine output'
-  //   □ Compute deterministic auditHash from input seed + 'fallback'
-  //   □ Zero-out all M104A-specific extended outputs
-  throw new Error('M104A fallback not yet implemented.');
+  const seed = String(((_input as unknown) as Record<string, unknown>).runSeed ?? '');
+  const tick = Number(((_input as unknown) as Record<string, unknown>).tickIndex ?? 0);
+  const auditHash = createHash('sha256')
+    .update(seed + ':' + tick + ':fallback:M104A')
+    .digest('hex');
+  return {
+    score: 0.5,
+    topFactors: ['ML unavailable — rule-based fallback active'],
+    recommendation: 'See rule engine output',
+    auditHash,
+    scarcityTransitionEta: null,
+    deckExhaustionForecast: null,
+    actNowSignal: null,
+    opportunityWindowEstimate: null,
+  };
 }
 
 // ── IntelligenceState integration note ───────────────────────────────────────
diff --git a/pzo_engine/src/ml/m105a_last_look_window_misclick_guard_regret_amplifier.ts b/pzo_engine/src/ml/m105a_last_look_window_misclick_guard_regret_amplifier.ts
index a330f0d8..851b7666 100644
--- a/pzo_engine/src/ml/m105a_last_look_window_misclick_guard_regret_amplifier.ts
+++ b/pzo_engine/src/ml/m105a_last_look_window_misclick_guard_regret_amplifier.ts
@@ -22,6 +22,8 @@
 // Density6 LLC · Point Zero One · Confidential · All Rights Reserved
 // ═══════════════════════════════════════════════════════════════════════════════
 
+import { createHash } from 'node:crypto';
+
 // ── What this adds ────────────────────────────────────────────────────────────
 /**
  * M105A — Last Look Window Misclick Guard + Regret Amplifier
@@ -196,27 +198,9 @@ export async function runM105aMl(
   tier:      M105ATier = 'baseline',
   modelCard: Omit<M105AModelCard, 'modelId' | 'coreMechanicPair'>,
 ): Promise<M105AOutput> {
-  // ── TODO: implement M105A — Last Look Window Misclick Guard + Regret Amplifier ─────────────────────────────────
-  //
-  // Implementation checklist:
-  // □ Validate input schema against featureSchemaHash
-  // □ Select inference backend based on `tier` parameter
-  // □ tier === 'baseline' → GBM + calibrated logistic (fast, low-cost, production default)
-  // □ tier === 'sequence_dl' → TCN / Transformer encoder over event streams (sequential patterns)
-  // □ tier === 'policy_rl' → Constrained contextual bandit / offline PPO (bounded nudges)
-  // □ Apply input privacy filters (no PII, no cross-player contact graph)
-  // □ Run inference → raw score + top_factors
-  // □ Apply output caps: score = Math.min(score, M105A_ML_CONSTANTS.GUARDRAILS.scoreCap)
-  // □ Apply monotonic constraints where relevant
-  // □ Abstain if confidence < M105A_ML_CONSTANTS.GUARDRAILS.abstainThreshold
-  // □ Compute auditHash = SHA256(inputs + outputs + ruleset_version + caps)
-  // □ Write signed receipt to run ledger (NEVER skip)
-  // □ Return M105AOutput — NEVER mutate run state directly
-  //
-  // Placement: client | Budget: real_time
-  // ExecHook:  after_m105_resolve
-  // ─────────────────────────────────────────────────────────────────────────
-  throw new Error('M105A (Last Look Window Misclick Guard + Regret Amplifier) ML inference not yet implemented.');
+  // Day-1 operational: delegates to fallback until full ML implementation is deployed.
+  // Full implementation checklist preserved in git history.
+  return runM105aMlFallback(input);
 }
 
 // ── Degraded-mode fallback ────────────────────────────────────────────────────
@@ -228,14 +212,21 @@ export async function runM105aMl(
 export function runM105aMlFallback(
   _input: M105ATelemetryInput,
 ): M105AOutput {
-  // TODO: implement rule-based fallback for M105A
-  // Fallback must:
-  //   □ Return score = 0.5 (neutral / unknown)
-  //   □ Return topFactors = ['ML unavailable — rule-based fallback active']
-  //   □ Return recommendation = 'See rule engine output'
-  //   □ Compute deterministic auditHash from input seed + 'fallback'
-  //   □ Zero-out all M105A-specific extended outputs
-  throw new Error('M105A fallback not yet implemented.');
+  const seed = String(((_input as unknown) as Record<string, unknown>).runSeed ?? '');
+  const tick = Number(((_input as unknown) as Record<string, unknown>).tickIndex ?? 0);
+  const auditHash = createHash('sha256')
+    .update(seed + ':' + tick + ':fallback:M105A')
+    .digest('hex');
+  return {
+    score: 0.5,
+    topFactors: ['ML unavailable — rule-based fallback active'],
+    recommendation: 'See rule engine output',
+    auditHash,
+    misclickProbability: null,
+    regretAmplificationSignal: null,
+    guardReceipt: null,
+    supportEvidence: null,
+  };
 }
 
 // ── IntelligenceState integration note ───────────────────────────────────────
diff --git a/pzo_engine/src/ml/m106a_asset_condition_failure_predictor_maintenance_planner.ts b/pzo_engine/src/ml/m106a_asset_condition_failure_predictor_maintenance_planner.ts
index fd246316..af859e52 100644
--- a/pzo_engine/src/ml/m106a_asset_condition_failure_predictor_maintenance_planner.ts
+++ b/pzo_engine/src/ml/m106a_asset_condition_failure_predictor_maintenance_planner.ts
@@ -22,6 +22,8 @@
 // Density6 LLC · Point Zero One · Confidential · All Rights Reserved
 // ═══════════════════════════════════════════════════════════════════════════════
 
+import { createHash } from 'node:crypto';
+
 // ── What this adds ────────────────────────────────────────────────────────────
 /**
  * M106A — Asset Condition Failure Predictor + Maintenance Planner
@@ -198,27 +200,9 @@ export async function runM106aMl(
   tier:      M106ATier = 'baseline',
   modelCard: Omit<M106AModelCard, 'modelId' | 'coreMechanicPair'>,
 ): Promise<M106AOutput> {
-  // ── TODO: implement M106A — Asset Condition Failure Predictor + Maintenance Planner ─────────────────────────────────
-  //
-  // Implementation checklist:
-  // □ Validate input schema against featureSchemaHash
-  // □ Select inference backend based on `tier` parameter
-  // □ tier === 'baseline' → GBM + calibrated logistic (fast, low-cost, production default)
-  // □ tier === 'sequence_dl' → TCN / Transformer encoder over event streams (sequential patterns)
-  // □ tier === 'causal' → Causal inference + DiD (counterfactual explanations)
-  // □ Apply input privacy filters (no PII, no cross-player contact graph)
-  // □ Run inference → raw score + top_factors
-  // □ Apply output caps: score = Math.min(score, M106A_ML_CONSTANTS.GUARDRAILS.scoreCap)
-  // □ Apply monotonic constraints where relevant
-  // □ Abstain if confidence < M106A_ML_CONSTANTS.GUARDRAILS.abstainThreshold
-  // □ Compute auditHash = SHA256(inputs + outputs + ruleset_version + caps)
-  // □ Write signed receipt to run ledger (NEVER skip)
-  // □ Return M106AOutput — NEVER mutate run state directly
-  //
-  // Placement: client, server | Budget: real_time
-  // ExecHook:  after_m106_resolve
-  // ─────────────────────────────────────────────────────────────────────────
-  throw new Error('M106A (Asset Condition Failure Predictor + Maintenance Planner) ML inference not yet implemented.');
+  // Day-1 operational: delegates to fallback until full ML implementation is deployed.
+  // Full implementation checklist preserved in git history.
+  return runM106aMlFallback(input);
 }
 
 // ── Degraded-mode fallback ────────────────────────────────────────────────────
@@ -230,14 +214,21 @@ export async function runM106aMl(
 export function runM106aMlFallback(
   _input: M106ATelemetryInput,
 ): M106AOutput {
-  // TODO: implement rule-based fallback for M106A
-  // Fallback must:
-  //   □ Return score = 0.5 (neutral / unknown)
-  //   □ Return topFactors = ['ML unavailable — rule-based fallback active']
-  //   □ Return recommendation = 'See rule engine output'
-  //   □ Compute deterministic auditHash from input seed + 'fallback'
-  //   □ Zero-out all M106A-specific extended outputs
-  throw new Error('M106A fallback not yet implemented.');
+  const seed = String(((_input as unknown) as Record<string, unknown>).runSeed ?? '');
+  const tick = Number(((_input as unknown) as Record<string, unknown>).tickIndex ?? 0);
+  const auditHash = createHash('sha256')
+    .update(seed + ':' + tick + ':fallback:M106A')
+    .digest('hex');
+  return {
+    score: 0.5,
+    topFactors: ['ML unavailable — rule-based fallback active'],
+    recommendation: 'See rule engine output',
+    auditHash,
+    failurePredictionTimeline: null,
+    maintenancePlan: null,
+    cascadePreventionScore: null,
+    causalWearEstimate: null,
+  };
 }
 
 // ── IntelligenceState integration note ───────────────────────────────────────
diff --git a/pzo_engine/src/ml/m107a_refi_eligibility_estimator_term_optimizer.ts b/pzo_engine/src/ml/m107a_refi_eligibility_estimator_term_optimizer.ts
index b6df585b..3fe13e1b 100644
--- a/pzo_engine/src/ml/m107a_refi_eligibility_estimator_term_optimizer.ts
+++ b/pzo_engine/src/ml/m107a_refi_eligibility_estimator_term_optimizer.ts
@@ -22,6 +22,8 @@
 // Density6 LLC · Point Zero One · Confidential · All Rights Reserved
 // ═══════════════════════════════════════════════════════════════════════════════
 
+import { createHash } from 'node:crypto';
+
 // ── What this adds ────────────────────────────────────────────────────────────
 /**
  * M107A — Refi Eligibility Estimator + Term Optimizer
@@ -198,27 +200,9 @@ export async function runM107aMl(
   tier:      M107ATier = 'baseline',
   modelCard: Omit<M107AModelCard, 'modelId' | 'coreMechanicPair'>,
 ): Promise<M107AOutput> {
-  // ── TODO: implement M107A — Refi Eligibility Estimator + Term Optimizer ─────────────────────────────────
-  //
-  // Implementation checklist:
-  // □ Validate input schema against featureSchemaHash
-  // □ Select inference backend based on `tier` parameter
-  // □ tier === 'baseline' → GBM + calibrated logistic (fast, low-cost, production default)
-  // □ tier === 'sequence_dl' → TCN / Transformer encoder over event streams (sequential patterns)
-  // □ tier === 'policy_rl' → Constrained contextual bandit / offline PPO (bounded nudges)
-  // □ Apply input privacy filters (no PII, no cross-player contact graph)
-  // □ Run inference → raw score + top_factors
-  // □ Apply output caps: score = Math.min(score, M107A_ML_CONSTANTS.GUARDRAILS.scoreCap)
-  // □ Apply monotonic constraints where relevant
-  // □ Abstain if confidence < M107A_ML_CONSTANTS.GUARDRAILS.abstainThreshold
-  // □ Compute auditHash = SHA256(inputs + outputs + ruleset_version + caps)
-  // □ Write signed receipt to run ledger (NEVER skip)
-  // □ Return M107AOutput — NEVER mutate run state directly
-  //
-  // Placement: client, server | Budget: real_time
-  // ExecHook:  after_m107_resolve
-  // ─────────────────────────────────────────────────────────────────────────
-  throw new Error('M107A (Refi Eligibility Estimator + Term Optimizer) ML inference not yet implemented.');
+  // Day-1 operational: delegates to fallback until full ML implementation is deployed.
+  // Full implementation checklist preserved in git history.
+  return runM107aMlFallback(input);
 }
 
 // ── Degraded-mode fallback ────────────────────────────────────────────────────
@@ -230,14 +214,21 @@ export async function runM107aMl(
 export function runM107aMlFallback(
   _input: M107ATelemetryInput,
 ): M107AOutput {
-  // TODO: implement rule-based fallback for M107A
-  // Fallback must:
-  //   □ Return score = 0.5 (neutral / unknown)
-  //   □ Return topFactors = ['ML unavailable — rule-based fallback active']
-  //   □ Return recommendation = 'See rule engine output'
-  //   □ Compute deterministic auditHash from input seed + 'fallback'
-  //   □ Zero-out all M107A-specific extended outputs
-  throw new Error('M107A fallback not yet implemented.');
+  const seed = String(((_input as unknown) as Record<string, unknown>).runSeed ?? '');
+  const tick = Number(((_input as unknown) as Record<string, unknown>).tickIndex ?? 0);
+  const auditHash = createHash('sha256')
+    .update(seed + ':' + tick + ':fallback:M107A')
+    .digest('hex');
+  return {
+    score: 0.5,
+    topFactors: ['ML unavailable — rule-based fallback active'],
+    recommendation: 'See rule engine output',
+    auditHash,
+    eligibilityProbability: null,
+    optimizedTerms: null,
+    debtTrapRisk: null,
+    windowTiming: null,
+  };
 }
 
 // ── IntelligenceState integration note ───────────────────────────────────────
diff --git a/pzo_engine/src/ml/m108a_partial_fill_probability_timing_tutor.ts b/pzo_engine/src/ml/m108a_partial_fill_probability_timing_tutor.ts
index 67467db4..a20898a9 100644
--- a/pzo_engine/src/ml/m108a_partial_fill_probability_timing_tutor.ts
+++ b/pzo_engine/src/ml/m108a_partial_fill_probability_timing_tutor.ts
@@ -22,6 +22,8 @@
 // Density6 LLC · Point Zero One · Confidential · All Rights Reserved
 // ═══════════════════════════════════════════════════════════════════════════════
 
+import { createHash } from 'node:crypto';
+
 // ── What this adds ────────────────────────────────────────────────────────────
 /**
  * M108A — Partial Fill Probability + Timing Tutor
@@ -198,27 +200,9 @@ export async function runM108aMl(
   tier:      M108ATier = 'baseline',
   modelCard: Omit<M108AModelCard, 'modelId' | 'coreMechanicPair'>,
 ): Promise<M108AOutput> {
-  // ── TODO: implement M108A — Partial Fill Probability + Timing Tutor ─────────────────────────────────
-  //
-  // Implementation checklist:
-  // □ Validate input schema against featureSchemaHash
-  // □ Select inference backend based on `tier` parameter
-  // □ tier === 'baseline' → GBM + calibrated logistic (fast, low-cost, production default)
-  // □ tier === 'sequence_dl' → TCN / Transformer encoder over event streams (sequential patterns)
-  // □ tier === 'policy_rl' → Constrained contextual bandit / offline PPO (bounded nudges)
-  // □ Apply input privacy filters (no PII, no cross-player contact graph)
-  // □ Run inference → raw score + top_factors
-  // □ Apply output caps: score = Math.min(score, M108A_ML_CONSTANTS.GUARDRAILS.scoreCap)
-  // □ Apply monotonic constraints where relevant
-  // □ Abstain if confidence < M108A_ML_CONSTANTS.GUARDRAILS.abstainThreshold
-  // □ Compute auditHash = SHA256(inputs + outputs + ruleset_version + caps)
-  // □ Write signed receipt to run ledger (NEVER skip)
-  // □ Return M108AOutput — NEVER mutate run state directly
-  //
-  // Placement: client, server | Budget: real_time
-  // ExecHook:  after_m108_resolve
-  // ─────────────────────────────────────────────────────────────────────────
-  throw new Error('M108A (Partial Fill Probability + Timing Tutor) ML inference not yet implemented.');
+  // Day-1 operational: delegates to fallback until full ML implementation is deployed.
+  // Full implementation checklist preserved in git history.
+  return runM108aMlFallback(input);
 }
 
 // ── Degraded-mode fallback ────────────────────────────────────────────────────
@@ -230,14 +214,21 @@ export async function runM108aMl(
 export function runM108aMlFallback(
   _input: M108ATelemetryInput,
 ): M108AOutput {
-  // TODO: implement rule-based fallback for M108A
-  // Fallback must:
-  //   □ Return score = 0.5 (neutral / unknown)
-  //   □ Return topFactors = ['ML unavailable — rule-based fallback active']
-  //   □ Return recommendation = 'See rule engine output'
-  //   □ Compute deterministic auditHash from input seed + 'fallback'
-  //   □ Zero-out all M108A-specific extended outputs
-  throw new Error('M108A fallback not yet implemented.');
+  const seed = String(((_input as unknown) as Record<string, unknown>).runSeed ?? '');
+  const tick = Number(((_input as unknown) as Record<string, unknown>).tickIndex ?? 0);
+  const auditHash = createHash('sha256')
+    .update(seed + ':' + tick + ':fallback:M108A')
+    .digest('hex');
+  return {
+    score: 0.5,
+    topFactors: ['ML unavailable — rule-based fallback active'],
+    recommendation: 'See rule engine output',
+    auditHash,
+    fillProbability: null,
+    timingRecommendation: null,
+    fillWindowEstimate: null,
+    tutorHint: null,
+  };
 }
 
 // ── IntelligenceState integration note ───────────────────────────────────────
diff --git a/pzo_engine/src/ml/m109a_macro_news_burst_impact_synth_regime_detector.ts b/pzo_engine/src/ml/m109a_macro_news_burst_impact_synth_regime_detector.ts
index 97e31092..2cf279a6 100644
--- a/pzo_engine/src/ml/m109a_macro_news_burst_impact_synth_regime_detector.ts
+++ b/pzo_engine/src/ml/m109a_macro_news_burst_impact_synth_regime_detector.ts
@@ -22,6 +22,8 @@
 // Density6 LLC · Point Zero One · Confidential · All Rights Reserved
 // ═══════════════════════════════════════════════════════════════════════════════
 
+import { createHash } from 'node:crypto';
+
 // ── What this adds ────────────────────────────────────────────────────────────
 /**
  * M109A — Macro News Burst Impact Synth + Regime Detector
@@ -196,27 +198,9 @@ export async function runM109aMl(
   tier:      M109ATier = 'baseline',
   modelCard: Omit<M109AModelCard, 'modelId' | 'coreMechanicPair'>,
 ): Promise<M109AOutput> {
-  // ── TODO: implement M109A — Macro News Burst Impact Synth + Regime Detector ─────────────────────────────────
-  //
-  // Implementation checklist:
-  // □ Validate input schema against featureSchemaHash
-  // □ Select inference backend based on `tier` parameter
-  // □ tier === 'baseline' → GBM + calibrated logistic (fast, low-cost, production default)
-  // □ tier === 'sequence_dl' → TCN / Transformer encoder over event streams (sequential patterns)
-  // □ tier === 'policy_rl' → Constrained contextual bandit / offline PPO (bounded nudges)
-  // □ Apply input privacy filters (no PII, no cross-player contact graph)
-  // □ Run inference → raw score + top_factors
-  // □ Apply output caps: score = Math.min(score, M109A_ML_CONSTANTS.GUARDRAILS.scoreCap)
-  // □ Apply monotonic constraints where relevant
-  // □ Abstain if confidence < M109A_ML_CONSTANTS.GUARDRAILS.abstainThreshold
-  // □ Compute auditHash = SHA256(inputs + outputs + ruleset_version + caps)
-  // □ Write signed receipt to run ledger (NEVER skip)
-  // □ Return M109AOutput — NEVER mutate run state directly
-  //
-  // Placement: server | Budget: real_time
-  // ExecHook:  after_m109_resolve
-  // ─────────────────────────────────────────────────────────────────────────
-  throw new Error('M109A (Macro News Burst Impact Synth + Regime Detector) ML inference not yet implemented.');
+  // Day-1 operational: delegates to fallback until full ML implementation is deployed.
+  // Full implementation checklist preserved in git history.
+  return runM109aMlFallback(input);
 }
 
 // ── Degraded-mode fallback ────────────────────────────────────────────────────
@@ -228,14 +212,21 @@ export async function runM109aMl(
 export function runM109aMlFallback(
   _input: M109ATelemetryInput,
 ): M109AOutput {
-  // TODO: implement rule-based fallback for M109A
-  // Fallback must:
-  //   □ Return score = 0.5 (neutral / unknown)
-  //   □ Return topFactors = ['ML unavailable — rule-based fallback active']
-  //   □ Return recommendation = 'See rule engine output'
-  //   □ Compute deterministic auditHash from input seed + 'fallback'
-  //   □ Zero-out all M109A-specific extended outputs
-  throw new Error('M109A fallback not yet implemented.');
+  const seed = String(((_input as unknown) as Record<string, unknown>).runSeed ?? '');
+  const tick = Number(((_input as unknown) as Record<string, unknown>).tickIndex ?? 0);
+  const auditHash = createHash('sha256')
+    .update(seed + ':' + tick + ':fallback:M109A')
+    .digest('hex');
+  return {
+    score: 0.5,
+    topFactors: ['ML unavailable — rule-based fallback active'],
+    recommendation: 'See rule engine output',
+    auditHash,
+    impactSynthesis: null,
+    regimeTransitionSignal: null,
+    portfolioDeltaForecast: null,
+    narrativeSummary: null,
+  };
 }
 
 // ── IntelligenceState integration note ───────────────────────────────────────
diff --git a/pzo_engine/src/ml/m10a_exit_timing_model_pulse_sell_flip_optimizer.ts b/pzo_engine/src/ml/m10a_exit_timing_model_pulse_sell_flip_optimizer.ts
index 5109ea52..29c7503b 100644
--- a/pzo_engine/src/ml/m10a_exit_timing_model_pulse_sell_flip_optimizer.ts
+++ b/pzo_engine/src/ml/m10a_exit_timing_model_pulse_sell_flip_optimizer.ts
@@ -22,6 +22,8 @@
 // Density6 LLC · Point Zero One · Confidential · All Rights Reserved
 // ═══════════════════════════════════════════════════════════════════════════════
 
+import { createHash } from 'node:crypto';
+
 // ── What this adds ────────────────────────────────────────────────────────────
 /**
  * M10A — Exit Timing Model (Pulse Sell / Flip Optimizer)
@@ -198,27 +200,9 @@ export async function runM10aMl(
   tier:      M10ATier = 'baseline',
   modelCard: Omit<M10AModelCard, 'modelId' | 'coreMechanicPair'>,
 ): Promise<M10AOutput> {
-  // ── TODO: implement M10A — Exit Timing Model (Pulse Sell / Flip Optimizer) ─────────────────────────────────
-  //
-  // Implementation checklist:
-  // □ Validate input schema against featureSchemaHash
-  // □ Select inference backend based on `tier` parameter
-  // □ tier === 'baseline' → GBM + calibrated logistic (fast, low-cost, production default)
-  // □ tier === 'sequence_dl' → TCN / Transformer encoder over event streams (sequential patterns)
-  // □ tier === 'policy_rl' → Constrained contextual bandit / offline PPO (bounded nudges)
-  // □ Apply input privacy filters (no PII, no cross-player contact graph)
-  // □ Run inference → raw score + top_factors
-  // □ Apply output caps: score = Math.min(score, M10A_ML_CONSTANTS.GUARDRAILS.scoreCap)
-  // □ Apply monotonic constraints where relevant
-  // □ Abstain if confidence < M10A_ML_CONSTANTS.GUARDRAILS.abstainThreshold
-  // □ Compute auditHash = SHA256(inputs + outputs + ruleset_version + caps)
-  // □ Write signed receipt to run ledger (NEVER skip)
-  // □ Return M10AOutput — NEVER mutate run state directly
-  //
-  // Placement: client, server | Budget: real_time
-  // ExecHook:  after_m10_resolve
-  // ─────────────────────────────────────────────────────────────────────────
-  throw new Error('M10A (Exit Timing Model (Pulse Sell / Flip Optimizer)) ML inference not yet implemented.');
+  // Day-1 operational: delegates to fallback until full ML implementation is deployed.
+  // Full implementation checklist preserved in git history.
+  return runM10aMlFallback(input);
 }
 
 // ── Degraded-mode fallback ────────────────────────────────────────────────────
@@ -230,14 +214,21 @@ export async function runM10aMl(
 export function runM10aMlFallback(
   _input: M10ATelemetryInput,
 ): M10AOutput {
-  // TODO: implement rule-based fallback for M10A
-  // Fallback must:
-  //   □ Return score = 0.5 (neutral / unknown)
-  //   □ Return topFactors = ['ML unavailable — rule-based fallback active']
-  //   □ Return recommendation = 'See rule engine output'
-  //   □ Compute deterministic auditHash from input seed + 'fallback'
-  //   □ Zero-out all M10A-specific extended outputs
-  throw new Error('M10A fallback not yet implemented.');
+  const seed = String(((_input as unknown) as Record<string, unknown>).runSeed ?? '');
+  const tick = Number(((_input as unknown) as Record<string, unknown>).tickIndex ?? 0);
+  const auditHash = createHash('sha256')
+    .update(seed + ':' + tick + ':fallback:M10A')
+    .digest('hex');
+  return {
+    score: 0.5,
+    topFactors: ['ML unavailable — rule-based fallback active'],
+    recommendation: 'See rule engine output',
+    auditHash,
+    exitTimingScore: null,
+    flipProbability: null,
+    optimalExitTickEstimate: null,
+    momentFlag: null,
+  };
 }
 
 // ── IntelligenceState integration note ───────────────────────────────────────
diff --git a/pzo_engine/src/ml/m110a_ui_stall_predictor_auto_default_guard.ts b/pzo_engine/src/ml/m110a_ui_stall_predictor_auto_default_guard.ts
index 6ccedde4..a2a6891a 100644
--- a/pzo_engine/src/ml/m110a_ui_stall_predictor_auto_default_guard.ts
+++ b/pzo_engine/src/ml/m110a_ui_stall_predictor_auto_default_guard.ts
@@ -22,6 +22,8 @@
 // Density6 LLC · Point Zero One · Confidential · All Rights Reserved
 // ═══════════════════════════════════════════════════════════════════════════════
 
+import { createHash } from 'node:crypto';
+
 // ── What this adds ────────────────────────────────────────────────────────────
 /**
  * M110A — UI Stall Predictor + Auto-Default Guard
@@ -198,27 +200,9 @@ export async function runM110aMl(
   tier:      M110ATier = 'baseline',
   modelCard: Omit<M110AModelCard, 'modelId' | 'coreMechanicPair'>,
 ): Promise<M110AOutput> {
-  // ── TODO: implement M110A — UI Stall Predictor + Auto-Default Guard ─────────────────────────────────
-  //
-  // Implementation checklist:
-  // □ Validate input schema against featureSchemaHash
-  // □ Select inference backend based on `tier` parameter
-  // □ tier === 'baseline' → GBM + calibrated logistic (fast, low-cost, production default)
-  // □ tier === 'sequence_dl' → TCN / Transformer encoder over event streams (sequential patterns)
-  // □ tier === 'policy_rl' → Constrained contextual bandit / offline PPO (bounded nudges)
-  // □ Apply input privacy filters (no PII, no cross-player contact graph)
-  // □ Run inference → raw score + top_factors
-  // □ Apply output caps: score = Math.min(score, M110A_ML_CONSTANTS.GUARDRAILS.scoreCap)
-  // □ Apply monotonic constraints where relevant
-  // □ Abstain if confidence < M110A_ML_CONSTANTS.GUARDRAILS.abstainThreshold
-  // □ Compute auditHash = SHA256(inputs + outputs + ruleset_version + caps)
-  // □ Write signed receipt to run ledger (NEVER skip)
-  // □ Return M110AOutput — NEVER mutate run state directly
-  //
-  // Placement: client, server | Budget: real_time
-  // ExecHook:  after_m110_resolve
-  // ─────────────────────────────────────────────────────────────────────────
-  throw new Error('M110A (UI Stall Predictor + Auto-Default Guard) ML inference not yet implemented.');
+  // Day-1 operational: delegates to fallback until full ML implementation is deployed.
+  // Full implementation checklist preserved in git history.
+  return runM110aMlFallback(input);
 }
 
 // ── Degraded-mode fallback ────────────────────────────────────────────────────
@@ -230,14 +214,21 @@ export async function runM110aMl(
 export function runM110aMlFallback(
   _input: M110ATelemetryInput,
 ): M110AOutput {
-  // TODO: implement rule-based fallback for M110A
-  // Fallback must:
-  //   □ Return score = 0.5 (neutral / unknown)
-  //   □ Return topFactors = ['ML unavailable — rule-based fallback active']
-  //   □ Return recommendation = 'See rule engine output'
-  //   □ Compute deterministic auditHash from input seed + 'fallback'
-  //   □ Zero-out all M110A-specific extended outputs
-  throw new Error('M110A fallback not yet implemented.');
+  const seed = String(((_input as unknown) as Record<string, unknown>).runSeed ?? '');
+  const tick = Number(((_input as unknown) as Record<string, unknown>).tickIndex ?? 0);
+  const auditHash = createHash('sha256')
+    .update(seed + ':' + tick + ':fallback:M110A')
+    .digest('hex');
+  return {
+    score: 0.5,
+    topFactors: ['ML unavailable — rule-based fallback active'],
+    recommendation: 'See rule engine output',
+    auditHash,
+    stallProbability: null,
+    autoDefaultAction: null,
+    stallForensicLog: null,
+    guardActivationFlag: null,
+  };
 }
 
 // ── IntelligenceState integration note ───────────────────────────────────────
diff --git a/pzo_engine/src/ml/m111a_portfolio_macro_synth_safety_verifier.ts b/pzo_engine/src/ml/m111a_portfolio_macro_synth_safety_verifier.ts
index 7ff2db3c..abb5f11c 100644
--- a/pzo_engine/src/ml/m111a_portfolio_macro_synth_safety_verifier.ts
+++ b/pzo_engine/src/ml/m111a_portfolio_macro_synth_safety_verifier.ts
@@ -22,6 +22,8 @@
 // Density6 LLC · Point Zero One · Confidential · All Rights Reserved
 // ═══════════════════════════════════════════════════════════════════════════════
 
+import { createHash } from 'node:crypto';
+
 // ── What this adds ────────────────────────────────────────────────────────────
 /**
  * M111A — Portfolio Macro Synth + Safety Verifier
@@ -196,27 +198,9 @@ export async function runM111aMl(
   tier:      M111ATier = 'baseline',
   modelCard: Omit<M111AModelCard, 'modelId' | 'coreMechanicPair'>,
 ): Promise<M111AOutput> {
-  // ── TODO: implement M111A — Portfolio Macro Synth + Safety Verifier ─────────────────────────────────
-  //
-  // Implementation checklist:
-  // □ Validate input schema against featureSchemaHash
-  // □ Select inference backend based on `tier` parameter
-  // □ tier === 'baseline' → GBM + calibrated logistic (fast, low-cost, production default)
-  // □ tier === 'sequence_dl' → TCN / Transformer encoder over event streams (sequential patterns)
-  // □ tier === 'policy_rl' → Constrained contextual bandit / offline PPO (bounded nudges)
-  // □ Apply input privacy filters (no PII, no cross-player contact graph)
-  // □ Run inference → raw score + top_factors
-  // □ Apply output caps: score = Math.min(score, M111A_ML_CONSTANTS.GUARDRAILS.scoreCap)
-  // □ Apply monotonic constraints where relevant
-  // □ Abstain if confidence < M111A_ML_CONSTANTS.GUARDRAILS.abstainThreshold
-  // □ Compute auditHash = SHA256(inputs + outputs + ruleset_version + caps)
-  // □ Write signed receipt to run ledger (NEVER skip)
-  // □ Return M111AOutput — NEVER mutate run state directly
-  //
-  // Placement: server | Budget: real_time
-  // ExecHook:  after_m111_resolve
-  // ─────────────────────────────────────────────────────────────────────────
-  throw new Error('M111A (Portfolio Macro Synth + Safety Verifier) ML inference not yet implemented.');
+  // Day-1 operational: delegates to fallback until full ML implementation is deployed.
+  // Full implementation checklist preserved in git history.
+  return runM111aMlFallback(input);
 }
 
 // ── Degraded-mode fallback ────────────────────────────────────────────────────
@@ -228,14 +212,21 @@ export async function runM111aMl(
 export function runM111aMlFallback(
   _input: M111ATelemetryInput,
 ): M111AOutput {
-  // TODO: implement rule-based fallback for M111A
-  // Fallback must:
-  //   □ Return score = 0.5 (neutral / unknown)
-  //   □ Return topFactors = ['ML unavailable — rule-based fallback active']
-  //   □ Return recommendation = 'See rule engine output'
-  //   □ Compute deterministic auditHash from input seed + 'fallback'
-  //   □ Zero-out all M111A-specific extended outputs
-  throw new Error('M111A fallback not yet implemented.');
+  const seed = String(((_input as unknown) as Record<string, unknown>).runSeed ?? '');
+  const tick = Number(((_input as unknown) as Record<string, unknown>).tickIndex ?? 0);
+  const auditHash = createHash('sha256')
+    .update(seed + ':' + tick + ':fallback:M111A')
+    .digest('hex');
+  return {
+    score: 0.5,
+    topFactors: ['ML unavailable — rule-based fallback active'],
+    recommendation: 'See rule engine output',
+    auditHash,
+    interactionSynthesis: null,
+    safetyVerification: null,
+    capComplianceFlag: null,
+    macroReceipt: null,
+  };
 }
 
 // ── IntelligenceState integration note ───────────────────────────────────────
diff --git a/pzo_engine/src/ml/m112a_optimal_split_estimator_precision_sell_coach.ts b/pzo_engine/src/ml/m112a_optimal_split_estimator_precision_sell_coach.ts
index d8443d4f..71300ccb 100644
--- a/pzo_engine/src/ml/m112a_optimal_split_estimator_precision_sell_coach.ts
+++ b/pzo_engine/src/ml/m112a_optimal_split_estimator_precision_sell_coach.ts
@@ -22,6 +22,8 @@
 // Density6 LLC · Point Zero One · Confidential · All Rights Reserved
 // ═══════════════════════════════════════════════════════════════════════════════
 
+import { createHash } from 'node:crypto';
+
 // ── What this adds ────────────────────────────────────────────────────────────
 /**
  * M112A — Optimal Split Estimator (Precision Sell Coach)
@@ -198,27 +200,9 @@ export async function runM112aMl(
   tier:      M112ATier = 'baseline',
   modelCard: Omit<M112AModelCard, 'modelId' | 'coreMechanicPair'>,
 ): Promise<M112AOutput> {
-  // ── TODO: implement M112A — Optimal Split Estimator (Precision Sell Coach) ─────────────────────────────────
-  //
-  // Implementation checklist:
-  // □ Validate input schema against featureSchemaHash
-  // □ Select inference backend based on `tier` parameter
-  // □ tier === 'baseline' → GBM + calibrated logistic (fast, low-cost, production default)
-  // □ tier === 'sequence_dl' → TCN / Transformer encoder over event streams (sequential patterns)
-  // □ tier === 'policy_rl' → Constrained contextual bandit / offline PPO (bounded nudges)
-  // □ Apply input privacy filters (no PII, no cross-player contact graph)
-  // □ Run inference → raw score + top_factors
-  // □ Apply output caps: score = Math.min(score, M112A_ML_CONSTANTS.GUARDRAILS.scoreCap)
-  // □ Apply monotonic constraints where relevant
-  // □ Abstain if confidence < M112A_ML_CONSTANTS.GUARDRAILS.abstainThreshold
-  // □ Compute auditHash = SHA256(inputs + outputs + ruleset_version + caps)
-  // □ Write signed receipt to run ledger (NEVER skip)
-  // □ Return M112AOutput — NEVER mutate run state directly
-  //
-  // Placement: client, server | Budget: real_time
-  // ExecHook:  after_m112_resolve
-  // ─────────────────────────────────────────────────────────────────────────
-  throw new Error('M112A (Optimal Split Estimator (Precision Sell Coach)) ML inference not yet implemented.');
+  // Day-1 operational: delegates to fallback until full ML implementation is deployed.
+  // Full implementation checklist preserved in git history.
+  return runM112aMlFallback(input);
 }
 
 // ── Degraded-mode fallback ────────────────────────────────────────────────────
@@ -230,14 +214,21 @@ export async function runM112aMl(
 export function runM112aMlFallback(
   _input: M112ATelemetryInput,
 ): M112AOutput {
-  // TODO: implement rule-based fallback for M112A
-  // Fallback must:
-  //   □ Return score = 0.5 (neutral / unknown)
-  //   □ Return topFactors = ['ML unavailable — rule-based fallback active']
-  //   □ Return recommendation = 'See rule engine output'
-  //   □ Compute deterministic auditHash from input seed + 'fallback'
-  //   □ Zero-out all M112A-specific extended outputs
-  throw new Error('M112A fallback not yet implemented.');
+  const seed = String(((_input as unknown) as Record<string, unknown>).runSeed ?? '');
+  const tick = Number(((_input as unknown) as Record<string, unknown>).tickIndex ?? 0);
+  const auditHash = createHash('sha256')
+    .update(seed + ':' + tick + ':fallback:M112A')
+    .digest('hex');
+  return {
+    score: 0.5,
+    topFactors: ['ML unavailable — rule-based fallback active'],
+    recommendation: 'See rule engine output',
+    auditHash,
+    optimalSplitRatio: null,
+    recoveryProbability: null,
+    coachingHint: null,
+    counterfactualSignal: null,
+  };
 }
 
 // ── IntelligenceState integration note ───────────────────────────────────────
diff --git a/pzo_engine/src/ml/m113a_order_priority_stack_sacrifice_planner_graph_aware.ts b/pzo_engine/src/ml/m113a_order_priority_stack_sacrifice_planner_graph_aware.ts
index e70a5526..67766b31 100644
--- a/pzo_engine/src/ml/m113a_order_priority_stack_sacrifice_planner_graph_aware.ts
+++ b/pzo_engine/src/ml/m113a_order_priority_stack_sacrifice_planner_graph_aware.ts
@@ -22,6 +22,8 @@
 // Density6 LLC · Point Zero One · Confidential · All Rights Reserved
 // ═══════════════════════════════════════════════════════════════════════════════
 
+import { createHash } from 'node:crypto';
+
 // ── What this adds ────────────────────────────────────────────────────────────
 /**
  * M113A — Order Priority Stack Sacrifice Planner (Graph-Aware)
@@ -198,27 +200,9 @@ export async function runM113aMl(
   tier:      M113ATier = 'baseline',
   modelCard: Omit<M113AModelCard, 'modelId' | 'coreMechanicPair'>,
 ): Promise<M113AOutput> {
-  // ── TODO: implement M113A — Order Priority Stack Sacrifice Planner (Graph-Aware) ─────────────────────────────────
-  //
-  // Implementation checklist:
-  // □ Validate input schema against featureSchemaHash
-  // □ Select inference backend based on `tier` parameter
-  // □ tier === 'baseline' → GBM + calibrated logistic (fast, low-cost, production default)
-  // □ tier === 'sequence_dl' → TCN / Transformer encoder over event streams (sequential patterns)
-  // □ tier === 'graph_dl' → GNN over contract / market / ledger graphs (relationship-aware)
-  // □ Apply input privacy filters (no PII, no cross-player contact graph)
-  // □ Run inference → raw score + top_factors
-  // □ Apply output caps: score = Math.min(score, M113A_ML_CONSTANTS.GUARDRAILS.scoreCap)
-  // □ Apply monotonic constraints where relevant
-  // □ Abstain if confidence < M113A_ML_CONSTANTS.GUARDRAILS.abstainThreshold
-  // □ Compute auditHash = SHA256(inputs + outputs + ruleset_version + caps)
-  // □ Write signed receipt to run ledger (NEVER skip)
-  // □ Return M113AOutput — NEVER mutate run state directly
-  //
-  // Placement: client, server | Budget: real_time
-  // ExecHook:  after_m113_resolve
-  // ─────────────────────────────────────────────────────────────────────────
-  throw new Error('M113A (Order Priority Stack Sacrifice Planner (Graph-Aware)) ML inference not yet implemented.');
+  // Day-1 operational: delegates to fallback until full ML implementation is deployed.
+  // Full implementation checklist preserved in git history.
+  return runM113aMlFallback(input);
 }
 
 // ── Degraded-mode fallback ────────────────────────────────────────────────────
@@ -230,14 +214,21 @@ export async function runM113aMl(
 export function runM113aMlFallback(
   _input: M113ATelemetryInput,
 ): M113AOutput {
-  // TODO: implement rule-based fallback for M113A
-  // Fallback must:
-  //   □ Return score = 0.5 (neutral / unknown)
-  //   □ Return topFactors = ['ML unavailable — rule-based fallback active']
-  //   □ Return recommendation = 'See rule engine output'
-  //   □ Compute deterministic auditHash from input seed + 'fallback'
-  //   □ Zero-out all M113A-specific extended outputs
-  throw new Error('M113A fallback not yet implemented.');
+  const seed = String(((_input as unknown) as Record<string, unknown>).runSeed ?? '');
+  const tick = Number(((_input as unknown) as Record<string, unknown>).tickIndex ?? 0);
+  const auditHash = createHash('sha256')
+    .update(seed + ':' + tick + ':fallback:M113A')
+    .digest('hex');
+  return {
+    score: 0.5,
+    topFactors: ['ML unavailable — rule-based fallback active'],
+    recommendation: 'See rule engine output',
+    auditHash,
+    sacrificeOrderPlan: null,
+    cascadeMinimizationScore: null,
+    protectionPriority: null,
+    caseFileReceipt: null,
+  };
 }
 
 // ── IntelligenceState integration note ───────────────────────────────────────
diff --git a/pzo_engine/src/ml/m114a_timing_tax_reaction_time_coach_window_predictor.ts b/pzo_engine/src/ml/m114a_timing_tax_reaction_time_coach_window_predictor.ts
index 9b8dd428..71acaa70 100644
--- a/pzo_engine/src/ml/m114a_timing_tax_reaction_time_coach_window_predictor.ts
+++ b/pzo_engine/src/ml/m114a_timing_tax_reaction_time_coach_window_predictor.ts
@@ -22,6 +22,8 @@
 // Density6 LLC · Point Zero One · Confidential · All Rights Reserved
 // ═══════════════════════════════════════════════════════════════════════════════
 
+import { createHash } from 'node:crypto';
+
 // ── What this adds ────────────────────────────────────────────────────────────
 /**
  * M114A — Timing Tax Reaction-Time Coach + Window Predictor
@@ -196,27 +198,9 @@ export async function runM114aMl(
   tier:      M114ATier = 'baseline',
   modelCard: Omit<M114AModelCard, 'modelId' | 'coreMechanicPair'>,
 ): Promise<M114AOutput> {
-  // ── TODO: implement M114A — Timing Tax Reaction-Time Coach + Window Predictor ─────────────────────────────────
-  //
-  // Implementation checklist:
-  // □ Validate input schema against featureSchemaHash
-  // □ Select inference backend based on `tier` parameter
-  // □ tier === 'baseline' → GBM + calibrated logistic (fast, low-cost, production default)
-  // □ tier === 'sequence_dl' → TCN / Transformer encoder over event streams (sequential patterns)
-  // □ tier === 'policy_rl' → Constrained contextual bandit / offline PPO (bounded nudges)
-  // □ Apply input privacy filters (no PII, no cross-player contact graph)
-  // □ Run inference → raw score + top_factors
-  // □ Apply output caps: score = Math.min(score, M114A_ML_CONSTANTS.GUARDRAILS.scoreCap)
-  // □ Apply monotonic constraints where relevant
-  // □ Abstain if confidence < M114A_ML_CONSTANTS.GUARDRAILS.abstainThreshold
-  // □ Compute auditHash = SHA256(inputs + outputs + ruleset_version + caps)
-  // □ Write signed receipt to run ledger (NEVER skip)
-  // □ Return M114AOutput — NEVER mutate run state directly
-  //
-  // Placement: client | Budget: real_time
-  // ExecHook:  after_m114_resolve
-  // ─────────────────────────────────────────────────────────────────────────
-  throw new Error('M114A (Timing Tax Reaction-Time Coach + Window Predictor) ML inference not yet implemented.');
+  // Day-1 operational: delegates to fallback until full ML implementation is deployed.
+  // Full implementation checklist preserved in git history.
+  return runM114aMlFallback(input);
 }
 
 // ── Degraded-mode fallback ────────────────────────────────────────────────────
@@ -228,14 +212,21 @@ export async function runM114aMl(
 export function runM114aMlFallback(
   _input: M114ATelemetryInput,
 ): M114AOutput {
-  // TODO: implement rule-based fallback for M114A
-  // Fallback must:
-  //   □ Return score = 0.5 (neutral / unknown)
-  //   □ Return topFactors = ['ML unavailable — rule-based fallback active']
-  //   □ Return recommendation = 'See rule engine output'
-  //   □ Compute deterministic auditHash from input seed + 'fallback'
-  //   □ Zero-out all M114A-specific extended outputs
-  throw new Error('M114A fallback not yet implemented.');
+  const seed = String(((_input as unknown) as Record<string, unknown>).runSeed ?? '');
+  const tick = Number(((_input as unknown) as Record<string, unknown>).tickIndex ?? 0);
+  const auditHash = createHash('sha256')
+    .update(seed + ':' + tick + ':fallback:M114A')
+    .digest('hex');
+  return {
+    score: 0.5,
+    topFactors: ['ML unavailable — rule-based fallback active'],
+    recommendation: 'See rule engine output',
+    auditHash,
+    reactionTimeCoaching: null,
+    windowPrediction: null,
+    deliberateVsHesitation: null,
+    timingTaxEstimate: null,
+  };
 }
 
 // ── IntelligenceState integration note ───────────────────────────────────────
diff --git a/pzo_engine/src/ml/m115a_heat_swap_exposure_rebalancer_suggestion.ts b/pzo_engine/src/ml/m115a_heat_swap_exposure_rebalancer_suggestion.ts
index 11bfab81..55817158 100644
--- a/pzo_engine/src/ml/m115a_heat_swap_exposure_rebalancer_suggestion.ts
+++ b/pzo_engine/src/ml/m115a_heat_swap_exposure_rebalancer_suggestion.ts
@@ -22,6 +22,8 @@
 // Density6 LLC · Point Zero One · Confidential · All Rights Reserved
 // ═══════════════════════════════════════════════════════════════════════════════
 
+import { createHash } from 'node:crypto';
+
 // ── What this adds ────────────────────────────────────────────────────────────
 /**
  * M115A — Heat-Swap Exposure Rebalancer Suggestion
@@ -198,27 +200,9 @@ export async function runM115aMl(
   tier:      M115ATier = 'baseline',
   modelCard: Omit<M115AModelCard, 'modelId' | 'coreMechanicPair'>,
 ): Promise<M115AOutput> {
-  // ── TODO: implement M115A — Heat-Swap Exposure Rebalancer Suggestion ─────────────────────────────────
-  //
-  // Implementation checklist:
-  // □ Validate input schema against featureSchemaHash
-  // □ Select inference backend based on `tier` parameter
-  // □ tier === 'baseline' → GBM + calibrated logistic (fast, low-cost, production default)
-  // □ tier === 'sequence_dl' → TCN / Transformer encoder over event streams (sequential patterns)
-  // □ tier === 'policy_rl' → Constrained contextual bandit / offline PPO (bounded nudges)
-  // □ Apply input privacy filters (no PII, no cross-player contact graph)
-  // □ Run inference → raw score + top_factors
-  // □ Apply output caps: score = Math.min(score, M115A_ML_CONSTANTS.GUARDRAILS.scoreCap)
-  // □ Apply monotonic constraints where relevant
-  // □ Abstain if confidence < M115A_ML_CONSTANTS.GUARDRAILS.abstainThreshold
-  // □ Compute auditHash = SHA256(inputs + outputs + ruleset_version + caps)
-  // □ Write signed receipt to run ledger (NEVER skip)
-  // □ Return M115AOutput — NEVER mutate run state directly
-  //
-  // Placement: client, server | Budget: real_time
-  // ExecHook:  after_m115_resolve
-  // ─────────────────────────────────────────────────────────────────────────
-  throw new Error('M115A (Heat-Swap Exposure Rebalancer Suggestion) ML inference not yet implemented.');
+  // Day-1 operational: delegates to fallback until full ML implementation is deployed.
+  // Full implementation checklist preserved in git history.
+  return runM115aMlFallback(input);
 }
 
 // ── Degraded-mode fallback ────────────────────────────────────────────────────
@@ -230,14 +214,21 @@ export async function runM115aMl(
 export function runM115aMlFallback(
   _input: M115ATelemetryInput,
 ): M115AOutput {
-  // TODO: implement rule-based fallback for M115A
-  // Fallback must:
-  //   □ Return score = 0.5 (neutral / unknown)
-  //   □ Return topFactors = ['ML unavailable — rule-based fallback active']
-  //   □ Return recommendation = 'See rule engine output'
-  //   □ Compute deterministic auditHash from input seed + 'fallback'
-  //   □ Zero-out all M115A-specific extended outputs
-  throw new Error('M115A fallback not yet implemented.');
+  const seed = String(((_input as unknown) as Record<string, unknown>).runSeed ?? '');
+  const tick = Number(((_input as unknown) as Record<string, unknown>).tickIndex ?? 0);
+  const auditHash = createHash('sha256')
+    .update(seed + ':' + tick + ':fallback:M115A')
+    .digest('hex');
+  return {
+    score: 0.5,
+    topFactors: ['ML unavailable — rule-based fallback active'],
+    recommendation: 'See rule engine output',
+    auditHash,
+    swapTargetSuggestion: null,
+    hiddenConcentrationFlag: null,
+    exposureMapBefore: null,
+    exposureMapAfter: null,
+  };
 }
 
 // ── IntelligenceState integration note ───────────────────────────────────────
diff --git a/pzo_engine/src/ml/m116a_table_role_fit_matcher_conflict_predictor.ts b/pzo_engine/src/ml/m116a_table_role_fit_matcher_conflict_predictor.ts
index a35e4d5c..20bfb6d3 100644
--- a/pzo_engine/src/ml/m116a_table_role_fit_matcher_conflict_predictor.ts
+++ b/pzo_engine/src/ml/m116a_table_role_fit_matcher_conflict_predictor.ts
@@ -22,6 +22,8 @@
 // Density6 LLC · Point Zero One · Confidential · All Rights Reserved
 // ═══════════════════════════════════════════════════════════════════════════════
 
+import { createHash } from 'node:crypto';
+
 // ── What this adds ────────────────────────────────────────────────────────────
 /**
  * M116A — Table Role Fit Matcher + Conflict Predictor
@@ -196,27 +198,9 @@ export async function runM116aMl(
   tier:      M116ATier = 'baseline',
   modelCard: Omit<M116AModelCard, 'modelId' | 'coreMechanicPair'>,
 ): Promise<M116AOutput> {
-  // ── TODO: implement M116A — Table Role Fit Matcher + Conflict Predictor ─────────────────────────────────
-  //
-  // Implementation checklist:
-  // □ Validate input schema against featureSchemaHash
-  // □ Select inference backend based on `tier` parameter
-  // □ tier === 'baseline' → GBM + calibrated logistic (fast, low-cost, production default)
-  // □ tier === 'sequence_dl' → TCN / Transformer encoder over event streams (sequential patterns)
-  // □ tier === 'policy_rl' → Constrained contextual bandit / offline PPO (bounded nudges)
-  // □ Apply input privacy filters (no PII, no cross-player contact graph)
-  // □ Run inference → raw score + top_factors
-  // □ Apply output caps: score = Math.min(score, M116A_ML_CONSTANTS.GUARDRAILS.scoreCap)
-  // □ Apply monotonic constraints where relevant
-  // □ Abstain if confidence < M116A_ML_CONSTANTS.GUARDRAILS.abstainThreshold
-  // □ Compute auditHash = SHA256(inputs + outputs + ruleset_version + caps)
-  // □ Write signed receipt to run ledger (NEVER skip)
-  // □ Return M116AOutput — NEVER mutate run state directly
-  //
-  // Placement: server | Budget: real_time
-  // ExecHook:  after_m116_resolve
-  // ─────────────────────────────────────────────────────────────────────────
-  throw new Error('M116A (Table Role Fit Matcher + Conflict Predictor) ML inference not yet implemented.');
+  // Day-1 operational: delegates to fallback until full ML implementation is deployed.
+  // Full implementation checklist preserved in git history.
+  return runM116aMlFallback(input);
 }
 
 // ── Degraded-mode fallback ────────────────────────────────────────────────────
@@ -228,14 +212,21 @@ export async function runM116aMl(
 export function runM116aMlFallback(
   _input: M116ATelemetryInput,
 ): M116AOutput {
-  // TODO: implement rule-based fallback for M116A
-  // Fallback must:
-  //   □ Return score = 0.5 (neutral / unknown)
-  //   □ Return topFactors = ['ML unavailable — rule-based fallback active']
-  //   □ Return recommendation = 'See rule engine output'
-  //   □ Compute deterministic auditHash from input seed + 'fallback'
-  //   □ Zero-out all M116A-specific extended outputs
-  throw new Error('M116A fallback not yet implemented.');
+  const seed = String(((_input as unknown) as Record<string, unknown>).runSeed ?? '');
+  const tick = Number(((_input as unknown) as Record<string, unknown>).tickIndex ?? 0);
+  const auditHash = createHash('sha256')
+    .update(seed + ':' + tick + ':fallback:M116A')
+    .digest('hex');
+  return {
+    score: 0.5,
+    topFactors: ['ML unavailable — rule-based fallback active'],
+    recommendation: 'See rule engine output',
+    auditHash,
+    roleFitScore: null,
+    conflictPrediction: null,
+    roleReceipt: null,
+    synergyBonusEstimate: null,
+  };
 }
 
 // ── IntelligenceState integration note ───────────────────────────────────────
diff --git a/pzo_engine/src/ml/m117a_table_feed_moment_ranker_auto_highlight_builder.ts b/pzo_engine/src/ml/m117a_table_feed_moment_ranker_auto_highlight_builder.ts
index 2cb63915..9da86c4c 100644
--- a/pzo_engine/src/ml/m117a_table_feed_moment_ranker_auto_highlight_builder.ts
+++ b/pzo_engine/src/ml/m117a_table_feed_moment_ranker_auto_highlight_builder.ts
@@ -22,6 +22,8 @@
 // Density6 LLC · Point Zero One · Confidential · All Rights Reserved
 // ═══════════════════════════════════════════════════════════════════════════════
 
+import { createHash } from 'node:crypto';
+
 // ── What this adds ────────────────────────────────────────────────────────────
 /**
  * M117A — Table Feed Moment Ranker + Auto-Highlight Builder
@@ -196,27 +198,9 @@ export async function runM117aMl(
   tier:      M117ATier = 'baseline',
   modelCard: Omit<M117AModelCard, 'modelId' | 'coreMechanicPair'>,
 ): Promise<M117AOutput> {
-  // ── TODO: implement M117A — Table Feed Moment Ranker + Auto-Highlight Builder ─────────────────────────────────
-  //
-  // Implementation checklist:
-  // □ Validate input schema against featureSchemaHash
-  // □ Select inference backend based on `tier` parameter
-  // □ tier === 'baseline' → GBM + calibrated logistic (fast, low-cost, production default)
-  // □ tier === 'sequence_dl' → TCN / Transformer encoder over event streams (sequential patterns)
-  // □ tier === 'policy_rl' → Constrained contextual bandit / offline PPO (bounded nudges)
-  // □ Apply input privacy filters (no PII, no cross-player contact graph)
-  // □ Run inference → raw score + top_factors
-  // □ Apply output caps: score = Math.min(score, M117A_ML_CONSTANTS.GUARDRAILS.scoreCap)
-  // □ Apply monotonic constraints where relevant
-  // □ Abstain if confidence < M117A_ML_CONSTANTS.GUARDRAILS.abstainThreshold
-  // □ Compute auditHash = SHA256(inputs + outputs + ruleset_version + caps)
-  // □ Write signed receipt to run ledger (NEVER skip)
-  // □ Return M117AOutput — NEVER mutate run state directly
-  //
-  // Placement: server | Budget: batch
-  // ExecHook:  after_m117_resolve
-  // ─────────────────────────────────────────────────────────────────────────
-  throw new Error('M117A (Table Feed Moment Ranker + Auto-Highlight Builder) ML inference not yet implemented.');
+  // Day-1 operational: delegates to fallback until full ML implementation is deployed.
+  // Full implementation checklist preserved in git history.
+  return runM117aMlFallback(input);
 }
 
 // ── Degraded-mode fallback ────────────────────────────────────────────────────
@@ -228,14 +212,21 @@ export async function runM117aMl(
 export function runM117aMlFallback(
   _input: M117ATelemetryInput,
 ): M117AOutput {
-  // TODO: implement rule-based fallback for M117A
-  // Fallback must:
-  //   □ Return score = 0.5 (neutral / unknown)
-  //   □ Return topFactors = ['ML unavailable — rule-based fallback active']
-  //   □ Return recommendation = 'See rule engine output'
-  //   □ Compute deterministic auditHash from input seed + 'fallback'
-  //   □ Zero-out all M117A-specific extended outputs
-  throw new Error('M117A fallback not yet implemented.');
+  const seed = String(((_input as unknown) as Record<string, unknown>).runSeed ?? '');
+  const tick = Number(((_input as unknown) as Record<string, unknown>).tickIndex ?? 0);
+  const auditHash = createHash('sha256')
+    .update(seed + ':' + tick + ':fallback:M117A')
+    .digest('hex');
+  return {
+    score: 0.5,
+    topFactors: ['ML unavailable — rule-based fallback active'],
+    recommendation: 'See rule engine output',
+    auditHash,
+    momentRankings: null,
+    highlightReel: null,
+    relevanceScores: null,
+    privacyCompliance: null,
+  };
 }
 
 // ── IntelligenceState integration note ───────────────────────────────────────
diff --git a/pzo_engine/src/ml/m118a_clip_remix_chain_suggestor_safety_filter.ts b/pzo_engine/src/ml/m118a_clip_remix_chain_suggestor_safety_filter.ts
index e2398fd4..b4515c4c 100644
--- a/pzo_engine/src/ml/m118a_clip_remix_chain_suggestor_safety_filter.ts
+++ b/pzo_engine/src/ml/m118a_clip_remix_chain_suggestor_safety_filter.ts
@@ -22,6 +22,8 @@
 // Density6 LLC · Point Zero One · Confidential · All Rights Reserved
 // ═══════════════════════════════════════════════════════════════════════════════
 
+import { createHash } from 'node:crypto';
+
 // ── What this adds ────────────────────────────────────────────────────────────
 /**
  * M118A — Clip Remix Chain Suggestor + Safety Filter
@@ -196,27 +198,9 @@ export async function runM118aMl(
   tier:      M118ATier = 'baseline',
   modelCard: Omit<M118AModelCard, 'modelId' | 'coreMechanicPair'>,
 ): Promise<M118AOutput> {
-  // ── TODO: implement M118A — Clip Remix Chain Suggestor + Safety Filter ─────────────────────────────────
-  //
-  // Implementation checklist:
-  // □ Validate input schema against featureSchemaHash
-  // □ Select inference backend based on `tier` parameter
-  // □ tier === 'baseline' → GBM + calibrated logistic (fast, low-cost, production default)
-  // □ tier === 'sequence_dl' → TCN / Transformer encoder over event streams (sequential patterns)
-  // □ tier === 'policy_rl' → Constrained contextual bandit / offline PPO (bounded nudges)
-  // □ Apply input privacy filters (no PII, no cross-player contact graph)
-  // □ Run inference → raw score + top_factors
-  // □ Apply output caps: score = Math.min(score, M118A_ML_CONSTANTS.GUARDRAILS.scoreCap)
-  // □ Apply monotonic constraints where relevant
-  // □ Abstain if confidence < M118A_ML_CONSTANTS.GUARDRAILS.abstainThreshold
-  // □ Compute auditHash = SHA256(inputs + outputs + ruleset_version + caps)
-  // □ Write signed receipt to run ledger (NEVER skip)
-  // □ Return M118AOutput — NEVER mutate run state directly
-  //
-  // Placement: server | Budget: batch
-  // ExecHook:  after_m118_resolve
-  // ─────────────────────────────────────────────────────────────────────────
-  throw new Error('M118A (Clip Remix Chain Suggestor + Safety Filter) ML inference not yet implemented.');
+  // Day-1 operational: delegates to fallback until full ML implementation is deployed.
+  // Full implementation checklist preserved in git history.
+  return runM118aMlFallback(input);
 }
 
 // ── Degraded-mode fallback ────────────────────────────────────────────────────
@@ -228,14 +212,21 @@ export async function runM118aMl(
 export function runM118aMlFallback(
   _input: M118ATelemetryInput,
 ): M118AOutput {
-  // TODO: implement rule-based fallback for M118A
-  // Fallback must:
-  //   □ Return score = 0.5 (neutral / unknown)
-  //   □ Return topFactors = ['ML unavailable — rule-based fallback active']
-  //   □ Return recommendation = 'See rule engine output'
-  //   □ Compute deterministic auditHash from input seed + 'fallback'
-  //   □ Zero-out all M118A-specific extended outputs
-  throw new Error('M118A fallback not yet implemented.');
+  const seed = String(((_input as unknown) as Record<string, unknown>).runSeed ?? '');
+  const tick = Number(((_input as unknown) as Record<string, unknown>).tickIndex ?? 0);
+  const auditHash = createHash('sha256')
+    .update(seed + ':' + tick + ':fallback:M118A')
+    .digest('hex');
+  return {
+    score: 0.5,
+    topFactors: ['ML unavailable — rule-based fallback active'],
+    recommendation: 'See rule engine output',
+    auditHash,
+    remixSuggestions: null,
+    safetyFilterResult: null,
+    chainIntegrityReceipt: null,
+    consentVerified: null,
+  };
 }
 
 // ── IntelligenceState integration note ───────────────────────────────────────
diff --git a/pzo_engine/src/ml/m119a_rivalry_ledger_nemesis_arc_detector.ts b/pzo_engine/src/ml/m119a_rivalry_ledger_nemesis_arc_detector.ts
index f5ed8d5a..f1452b48 100644
--- a/pzo_engine/src/ml/m119a_rivalry_ledger_nemesis_arc_detector.ts
+++ b/pzo_engine/src/ml/m119a_rivalry_ledger_nemesis_arc_detector.ts
@@ -22,6 +22,8 @@
 // Density6 LLC · Point Zero One · Confidential · All Rights Reserved
 // ═══════════════════════════════════════════════════════════════════════════════
 
+import { createHash } from 'node:crypto';
+
 // ── What this adds ────────────────────────────────────────────────────────────
 /**
  * M119A — Rivalry Ledger Nemesis Arc Detector
@@ -196,27 +198,9 @@ export async function runM119aMl(
   tier:      M119ATier = 'baseline',
   modelCard: Omit<M119AModelCard, 'modelId' | 'coreMechanicPair'>,
 ): Promise<M119AOutput> {
-  // ── TODO: implement M119A — Rivalry Ledger Nemesis Arc Detector ─────────────────────────────────
-  //
-  // Implementation checklist:
-  // □ Validate input schema against featureSchemaHash
-  // □ Select inference backend based on `tier` parameter
-  // □ tier === 'baseline' → GBM + calibrated logistic (fast, low-cost, production default)
-  // □ tier === 'sequence_dl' → TCN / Transformer encoder over event streams (sequential patterns)
-  // □ tier === 'policy_rl' → Constrained contextual bandit / offline PPO (bounded nudges)
-  // □ Apply input privacy filters (no PII, no cross-player contact graph)
-  // □ Run inference → raw score + top_factors
-  // □ Apply output caps: score = Math.min(score, M119A_ML_CONSTANTS.GUARDRAILS.scoreCap)
-  // □ Apply monotonic constraints where relevant
-  // □ Abstain if confidence < M119A_ML_CONSTANTS.GUARDRAILS.abstainThreshold
-  // □ Compute auditHash = SHA256(inputs + outputs + ruleset_version + caps)
-  // □ Write signed receipt to run ledger (NEVER skip)
-  // □ Return M119AOutput — NEVER mutate run state directly
-  //
-  // Placement: server | Budget: batch
-  // ExecHook:  after_m119_resolve
-  // ─────────────────────────────────────────────────────────────────────────
-  throw new Error('M119A (Rivalry Ledger Nemesis Arc Detector) ML inference not yet implemented.');
+  // Day-1 operational: delegates to fallback until full ML implementation is deployed.
+  // Full implementation checklist preserved in git history.
+  return runM119aMlFallback(input);
 }
 
 // ── Degraded-mode fallback ────────────────────────────────────────────────────
@@ -228,14 +212,21 @@ export async function runM119aMl(
 export function runM119aMlFallback(
   _input: M119ATelemetryInput,
 ): M119AOutput {
-  // TODO: implement rule-based fallback for M119A
-  // Fallback must:
-  //   □ Return score = 0.5 (neutral / unknown)
-  //   □ Return topFactors = ['ML unavailable — rule-based fallback active']
-  //   □ Return recommendation = 'See rule engine output'
-  //   □ Compute deterministic auditHash from input seed + 'fallback'
-  //   □ Zero-out all M119A-specific extended outputs
-  throw new Error('M119A fallback not yet implemented.');
+  const seed = String(((_input as unknown) as Record<string, unknown>).runSeed ?? '');
+  const tick = Number(((_input as unknown) as Record<string, unknown>).tickIndex ?? 0);
+  const auditHash = createHash('sha256')
+    .update(seed + ':' + tick + ':fallback:M119A')
+    .digest('hex');
+  return {
+    score: 0.5,
+    topFactors: ['ML unavailable — rule-based fallback active'],
+    recommendation: 'See rule engine output',
+    auditHash,
+    nemesisArcDetection: null,
+    narrativeMoments: null,
+    arcSummary: null,
+    sharePrompt: null,
+  };
 }
 
 // ── IntelligenceState integration note ───────────────────────────────────────
diff --git a/pzo_engine/src/ml/m11a_hesitation_inertia_detector_micro_behavior_dl.ts b/pzo_engine/src/ml/m11a_hesitation_inertia_detector_micro_behavior_dl.ts
index 8fc6e813..c504eea4 100644
--- a/pzo_engine/src/ml/m11a_hesitation_inertia_detector_micro_behavior_dl.ts
+++ b/pzo_engine/src/ml/m11a_hesitation_inertia_detector_micro_behavior_dl.ts
@@ -22,6 +22,8 @@
 // Density6 LLC · Point Zero One · Confidential · All Rights Reserved
 // ═══════════════════════════════════════════════════════════════════════════════
 
+import { createHash } from 'node:crypto';
+
 // ── What this adds ────────────────────────────────────────────────────────────
 /**
  * M11A — Hesitation / Inertia Detector (Micro-Behavior DL)
@@ -196,27 +198,9 @@ export async function runM11aMl(
   tier:      M11ATier = 'baseline',
   modelCard: Omit<M11AModelCard, 'modelId' | 'coreMechanicPair'>,
 ): Promise<M11AOutput> {
-  // ── TODO: implement M11A — Hesitation / Inertia Detector (Micro-Behavior DL) ─────────────────────────────────
-  //
-  // Implementation checklist:
-  // □ Validate input schema against featureSchemaHash
-  // □ Select inference backend based on `tier` parameter
-  // □ tier === 'baseline' → GBM + calibrated logistic (fast, low-cost, production default)
-  // □ tier === 'sequence_dl' → TCN / Transformer encoder over event streams (sequential patterns)
-  // □ tier === 'policy_rl' → Constrained contextual bandit / offline PPO (bounded nudges)
-  // □ Apply input privacy filters (no PII, no cross-player contact graph)
-  // □ Run inference → raw score + top_factors
-  // □ Apply output caps: score = Math.min(score, M11A_ML_CONSTANTS.GUARDRAILS.scoreCap)
-  // □ Apply monotonic constraints where relevant
-  // □ Abstain if confidence < M11A_ML_CONSTANTS.GUARDRAILS.abstainThreshold
-  // □ Compute auditHash = SHA256(inputs + outputs + ruleset_version + caps)
-  // □ Write signed receipt to run ledger (NEVER skip)
-  // □ Return M11AOutput — NEVER mutate run state directly
-  //
-  // Placement: client | Budget: real_time
-  // ExecHook:  after_m11_resolve
-  // ─────────────────────────────────────────────────────────────────────────
-  throw new Error('M11A (Hesitation / Inertia Detector (Micro-Behavior DL)) ML inference not yet implemented.');
+  // Day-1 operational: delegates to fallback until full ML implementation is deployed.
+  // Full implementation checklist preserved in git history.
+  return runM11aMlFallback(input);
 }
 
 // ── Degraded-mode fallback ────────────────────────────────────────────────────
@@ -228,14 +212,21 @@ export async function runM11aMl(
 export function runM11aMlFallback(
   _input: M11ATelemetryInput,
 ): M11AOutput {
-  // TODO: implement rule-based fallback for M11A
-  // Fallback must:
-  //   □ Return score = 0.5 (neutral / unknown)
-  //   □ Return topFactors = ['ML unavailable — rule-based fallback active']
-  //   □ Return recommendation = 'See rule engine output'
-  //   □ Compute deterministic auditHash from input seed + 'fallback'
-  //   □ Zero-out all M11A-specific extended outputs
-  throw new Error('M11A fallback not yet implemented.');
+  const seed = String(((_input as unknown) as Record<string, unknown>).runSeed ?? '');
+  const tick = Number(((_input as unknown) as Record<string, unknown>).tickIndex ?? 0);
+  const auditHash = createHash('sha256')
+    .update(seed + ':' + tick + ':fallback:M11A')
+    .digest('hex');
+  return {
+    score: 0.5,
+    topFactors: ['ML unavailable — rule-based fallback active'],
+    recommendation: 'See rule engine output',
+    auditHash,
+    inertiaScore: null,
+    hesitationPattern: null,
+    inertiaTaxWarning: null,
+    adaptiveDifficultySignal: null,
+  };
 }
 
 // ── IntelligenceState integration note ───────────────────────────────────────
diff --git a/pzo_engine/src/ml/m120a_consent_gate_chaos_mode_recommender.ts b/pzo_engine/src/ml/m120a_consent_gate_chaos_mode_recommender.ts
index 7bf088c4..2ab673c6 100644
--- a/pzo_engine/src/ml/m120a_consent_gate_chaos_mode_recommender.ts
+++ b/pzo_engine/src/ml/m120a_consent_gate_chaos_mode_recommender.ts
@@ -22,6 +22,8 @@
 // Density6 LLC · Point Zero One · Confidential · All Rights Reserved
 // ═══════════════════════════════════════════════════════════════════════════════
 
+import { createHash } from 'node:crypto';
+
 // ── What this adds ────────────────────────────────────────────────────────────
 /**
  * M120A — Consent Gate Chaos Mode Recommender
@@ -196,27 +198,9 @@ export async function runM120aMl(
   tier:      M120ATier = 'baseline',
   modelCard: Omit<M120AModelCard, 'modelId' | 'coreMechanicPair'>,
 ): Promise<M120AOutput> {
-  // ── TODO: implement M120A — Consent Gate Chaos Mode Recommender ─────────────────────────────────
-  //
-  // Implementation checklist:
-  // □ Validate input schema against featureSchemaHash
-  // □ Select inference backend based on `tier` parameter
-  // □ tier === 'baseline' → GBM + calibrated logistic (fast, low-cost, production default)
-  // □ tier === 'sequence_dl' → TCN / Transformer encoder over event streams (sequential patterns)
-  // □ tier === 'policy_rl' → Constrained contextual bandit / offline PPO (bounded nudges)
-  // □ Apply input privacy filters (no PII, no cross-player contact graph)
-  // □ Run inference → raw score + top_factors
-  // □ Apply output caps: score = Math.min(score, M120A_ML_CONSTANTS.GUARDRAILS.scoreCap)
-  // □ Apply monotonic constraints where relevant
-  // □ Abstain if confidence < M120A_ML_CONSTANTS.GUARDRAILS.abstainThreshold
-  // □ Compute auditHash = SHA256(inputs + outputs + ruleset_version + caps)
-  // □ Write signed receipt to run ledger (NEVER skip)
-  // □ Return M120AOutput — NEVER mutate run state directly
-  //
-  // Placement: server | Budget: real_time
-  // ExecHook:  after_m120_resolve
-  // ─────────────────────────────────────────────────────────────────────────
-  throw new Error('M120A (Consent Gate Chaos Mode Recommender) ML inference not yet implemented.');
+  // Day-1 operational: delegates to fallback until full ML implementation is deployed.
+  // Full implementation checklist preserved in git history.
+  return runM120aMlFallback(input);
 }
 
 // ── Degraded-mode fallback ────────────────────────────────────────────────────
@@ -228,14 +212,21 @@ export async function runM120aMl(
 export function runM120aMlFallback(
   _input: M120ATelemetryInput,
 ): M120AOutput {
-  // TODO: implement rule-based fallback for M120A
-  // Fallback must:
-  //   □ Return score = 0.5 (neutral / unknown)
-  //   □ Return topFactors = ['ML unavailable — rule-based fallback active']
-  //   □ Return recommendation = 'See rule engine output'
-  //   □ Compute deterministic auditHash from input seed + 'fallback'
-  //   □ Zero-out all M120A-specific extended outputs
-  throw new Error('M120A fallback not yet implemented.');
+  const seed = String(((_input as unknown) as Record<string, unknown>).runSeed ?? '');
+  const tick = Number(((_input as unknown) as Record<string, unknown>).tickIndex ?? 0);
+  const auditHash = createHash('sha256')
+    .update(seed + ':' + tick + ':fallback:M120A')
+    .digest('hex');
+  return {
+    score: 0.5,
+    topFactors: ['ML unavailable — rule-based fallback active'],
+    recommendation: 'See rule engine output',
+    auditHash,
+    appropriateTokenSet: null,
+    rejectionFilterApplied: null,
+    consentRecommendation: null,
+    chaosImpactEstimate: null,
+  };
 }
 
 // ── IntelligenceState integration note ───────────────────────────────────────
diff --git a/pzo_engine/src/ml/m121a_daily_gauntlet_leaderboard_integrity_monitor.ts b/pzo_engine/src/ml/m121a_daily_gauntlet_leaderboard_integrity_monitor.ts
index 36feae48..5a1f3a40 100644
--- a/pzo_engine/src/ml/m121a_daily_gauntlet_leaderboard_integrity_monitor.ts
+++ b/pzo_engine/src/ml/m121a_daily_gauntlet_leaderboard_integrity_monitor.ts
@@ -22,6 +22,8 @@
 // Density6 LLC · Point Zero One · Confidential · All Rights Reserved
 // ═══════════════════════════════════════════════════════════════════════════════
 
+import { createHash } from 'node:crypto';
+
 // ── What this adds ────────────────────────────────────────────────────────────
 /**
  * M121A — Daily Gauntlet Leaderboard Integrity Monitor
@@ -196,27 +198,9 @@ export async function runM121aMl(
   tier:      M121ATier = 'baseline',
   modelCard: Omit<M121AModelCard, 'modelId' | 'coreMechanicPair'>,
 ): Promise<M121AOutput> {
-  // ── TODO: implement M121A — Daily Gauntlet Leaderboard Integrity Monitor ─────────────────────────────────
-  //
-  // Implementation checklist:
-  // □ Validate input schema against featureSchemaHash
-  // □ Select inference backend based on `tier` parameter
-  // □ tier === 'baseline' → GBM + calibrated logistic (fast, low-cost, production default)
-  // □ tier === 'sequence_dl' → TCN / Transformer encoder over event streams (sequential patterns)
-  // □ tier === 'policy_rl' → Constrained contextual bandit / offline PPO (bounded nudges)
-  // □ Apply input privacy filters (no PII, no cross-player contact graph)
-  // □ Run inference → raw score + top_factors
-  // □ Apply output caps: score = Math.min(score, M121A_ML_CONSTANTS.GUARDRAILS.scoreCap)
-  // □ Apply monotonic constraints where relevant
-  // □ Abstain if confidence < M121A_ML_CONSTANTS.GUARDRAILS.abstainThreshold
-  // □ Compute auditHash = SHA256(inputs + outputs + ruleset_version + caps)
-  // □ Write signed receipt to run ledger (NEVER skip)
-  // □ Return M121AOutput — NEVER mutate run state directly
-  //
-  // Placement: server | Budget: real_time
-  // ExecHook:  after_m121_resolve
-  // ─────────────────────────────────────────────────────────────────────────
-  throw new Error('M121A (Daily Gauntlet Leaderboard Integrity Monitor) ML inference not yet implemented.');
+  // Day-1 operational: delegates to fallback until full ML implementation is deployed.
+  // Full implementation checklist preserved in git history.
+  return runM121aMlFallback(input);
 }
 
 // ── Degraded-mode fallback ────────────────────────────────────────────────────
@@ -228,14 +212,21 @@ export async function runM121aMl(
 export function runM121aMlFallback(
   _input: M121ATelemetryInput,
 ): M121AOutput {
-  // TODO: implement rule-based fallback for M121A
-  // Fallback must:
-  //   □ Return score = 0.5 (neutral / unknown)
-  //   □ Return topFactors = ['ML unavailable — rule-based fallback active']
-  //   □ Return recommendation = 'See rule engine output'
-  //   □ Compute deterministic auditHash from input seed + 'fallback'
-  //   □ Zero-out all M121A-specific extended outputs
-  throw new Error('M121A fallback not yet implemented.');
+  const seed = String(((_input as unknown) as Record<string, unknown>).runSeed ?? '');
+  const tick = Number(((_input as unknown) as Record<string, unknown>).tickIndex ?? 0);
+  const auditHash = createHash('sha256')
+    .update(seed + ':' + tick + ':fallback:M121A')
+    .digest('hex');
+  return {
+    score: 0.5,
+    topFactors: ['ML unavailable — rule-based fallback active'],
+    recommendation: 'See rule engine output',
+    auditHash,
+    manipulationFlag: null,
+    seedExploitDetection: null,
+    dailyIntegrityDigest: null,
+    leaderboardTrustScore: null,
+  };
 }
 
 // ── IntelligenceState integration note ───────────────────────────────────────
diff --git a/pzo_engine/src/ml/m122a_weekly_draft_league_advisor_draft_order_fairness.ts b/pzo_engine/src/ml/m122a_weekly_draft_league_advisor_draft_order_fairness.ts
index ed320404..1e870c17 100644
--- a/pzo_engine/src/ml/m122a_weekly_draft_league_advisor_draft_order_fairness.ts
+++ b/pzo_engine/src/ml/m122a_weekly_draft_league_advisor_draft_order_fairness.ts
@@ -22,6 +22,8 @@
 // Density6 LLC · Point Zero One · Confidential · All Rights Reserved
 // ═══════════════════════════════════════════════════════════════════════════════
 
+import { createHash } from 'node:crypto';
+
 // ── What this adds ────────────────────────────────────────────────────────────
 /**
  * M122A — Weekly Draft League Advisor + Draft Order Fairness
@@ -196,27 +198,9 @@ export async function runM122aMl(
   tier:      M122ATier = 'baseline',
   modelCard: Omit<M122AModelCard, 'modelId' | 'coreMechanicPair'>,
 ): Promise<M122AOutput> {
-  // ── TODO: implement M122A — Weekly Draft League Advisor + Draft Order Fairness ─────────────────────────────────
-  //
-  // Implementation checklist:
-  // □ Validate input schema against featureSchemaHash
-  // □ Select inference backend based on `tier` parameter
-  // □ tier === 'baseline' → GBM + calibrated logistic (fast, low-cost, production default)
-  // □ tier === 'sequence_dl' → TCN / Transformer encoder over event streams (sequential patterns)
-  // □ tier === 'policy_rl' → Constrained contextual bandit / offline PPO (bounded nudges)
-  // □ Apply input privacy filters (no PII, no cross-player contact graph)
-  // □ Run inference → raw score + top_factors
-  // □ Apply output caps: score = Math.min(score, M122A_ML_CONSTANTS.GUARDRAILS.scoreCap)
-  // □ Apply monotonic constraints where relevant
-  // □ Abstain if confidence < M122A_ML_CONSTANTS.GUARDRAILS.abstainThreshold
-  // □ Compute auditHash = SHA256(inputs + outputs + ruleset_version + caps)
-  // □ Write signed receipt to run ledger (NEVER skip)
-  // □ Return M122AOutput — NEVER mutate run state directly
-  //
-  // Placement: server | Budget: real_time
-  // ExecHook:  after_m122_resolve
-  // ─────────────────────────────────────────────────────────────────────────
-  throw new Error('M122A (Weekly Draft League Advisor + Draft Order Fairness) ML inference not yet implemented.');
+  // Day-1 operational: delegates to fallback until full ML implementation is deployed.
+  // Full implementation checklist preserved in git history.
+  return runM122aMlFallback(input);
 }
 
 // ── Degraded-mode fallback ────────────────────────────────────────────────────
@@ -228,14 +212,21 @@ export async function runM122aMl(
 export function runM122aMlFallback(
   _input: M122ATelemetryInput,
 ): M122AOutput {
-  // TODO: implement rule-based fallback for M122A
-  // Fallback must:
-  //   □ Return score = 0.5 (neutral / unknown)
-  //   □ Return topFactors = ['ML unavailable — rule-based fallback active']
-  //   □ Return recommendation = 'See rule engine output'
-  //   □ Compute deterministic auditHash from input seed + 'fallback'
-  //   □ Zero-out all M122A-specific extended outputs
-  throw new Error('M122A fallback not yet implemented.');
+  const seed = String(((_input as unknown) as Record<string, unknown>).runSeed ?? '');
+  const tick = Number(((_input as unknown) as Record<string, unknown>).tickIndex ?? 0);
+  const auditHash = createHash('sha256')
+    .update(seed + ':' + tick + ':fallback:M122A')
+    .digest('hex');
+  return {
+    score: 0.5,
+    topFactors: ['ML unavailable — rule-based fallback active'],
+    recommendation: 'See rule engine output',
+    auditHash,
+    draftAdvice: null,
+    fairnessVerification: null,
+    manipulationFlag: null,
+    draftReceipt: null,
+  };
 }
 
 // ── IntelligenceState integration note ───────────────────────────────────────
diff --git a/pzo_engine/src/ml/m123a_king_of_the_hill_streak_story_arc_narrator.ts b/pzo_engine/src/ml/m123a_king_of_the_hill_streak_story_arc_narrator.ts
index 912ea3d5..07e6bb23 100644
--- a/pzo_engine/src/ml/m123a_king_of_the_hill_streak_story_arc_narrator.ts
+++ b/pzo_engine/src/ml/m123a_king_of_the_hill_streak_story_arc_narrator.ts
@@ -22,6 +22,8 @@
 // Density6 LLC · Point Zero One · Confidential · All Rights Reserved
 // ═══════════════════════════════════════════════════════════════════════════════
 
+import { createHash } from 'node:crypto';
+
 // ── What this adds ────────────────────────────────────────────────────────────
 /**
  * M123A — King-of-the-Hill Streak Story Arc Narrator
@@ -196,27 +198,9 @@ export async function runM123aMl(
   tier:      M123ATier = 'baseline',
   modelCard: Omit<M123AModelCard, 'modelId' | 'coreMechanicPair'>,
 ): Promise<M123AOutput> {
-  // ── TODO: implement M123A — King-of-the-Hill Streak Story Arc Narrator ─────────────────────────────────
-  //
-  // Implementation checklist:
-  // □ Validate input schema against featureSchemaHash
-  // □ Select inference backend based on `tier` parameter
-  // □ tier === 'baseline' → GBM + calibrated logistic (fast, low-cost, production default)
-  // □ tier === 'sequence_dl' → TCN / Transformer encoder over event streams (sequential patterns)
-  // □ tier === 'policy_rl' → Constrained contextual bandit / offline PPO (bounded nudges)
-  // □ Apply input privacy filters (no PII, no cross-player contact graph)
-  // □ Run inference → raw score + top_factors
-  // □ Apply output caps: score = Math.min(score, M123A_ML_CONSTANTS.GUARDRAILS.scoreCap)
-  // □ Apply monotonic constraints where relevant
-  // □ Abstain if confidence < M123A_ML_CONSTANTS.GUARDRAILS.abstainThreshold
-  // □ Compute auditHash = SHA256(inputs + outputs + ruleset_version + caps)
-  // □ Write signed receipt to run ledger (NEVER skip)
-  // □ Return M123AOutput — NEVER mutate run state directly
-  //
-  // Placement: server | Budget: batch
-  // ExecHook:  after_m123_resolve
-  // ─────────────────────────────────────────────────────────────────────────
-  throw new Error('M123A (King-of-the-Hill Streak Story Arc Narrator) ML inference not yet implemented.');
+  // Day-1 operational: delegates to fallback until full ML implementation is deployed.
+  // Full implementation checklist preserved in git history.
+  return runM123aMlFallback(input);
 }
 
 // ── Degraded-mode fallback ────────────────────────────────────────────────────
@@ -228,14 +212,21 @@ export async function runM123aMl(
 export function runM123aMlFallback(
   _input: M123ATelemetryInput,
 ): M123AOutput {
-  // TODO: implement rule-based fallback for M123A
-  // Fallback must:
-  //   □ Return score = 0.5 (neutral / unknown)
-  //   □ Return topFactors = ['ML unavailable — rule-based fallback active']
-  //   □ Return recommendation = 'See rule engine output'
-  //   □ Compute deterministic auditHash from input seed + 'fallback'
-  //   □ Zero-out all M123A-specific extended outputs
-  throw new Error('M123A fallback not yet implemented.');
+  const seed = String(((_input as unknown) as Record<string, unknown>).runSeed ?? '');
+  const tick = Number(((_input as unknown) as Record<string, unknown>).tickIndex ?? 0);
+  const auditHash = createHash('sha256')
+    .update(seed + ':' + tick + ':fallback:M123A')
+    .digest('hex');
+  return {
+    score: 0.5,
+    topFactors: ['ML unavailable — rule-based fallback active'],
+    recommendation: 'See rule engine output',
+    auditHash,
+    streakNarrative: null,
+    stakeFairnessSignal: null,
+    legitimacyScore: null,
+    sharePrompt: null,
+  };
 }
 
 // ── IntelligenceState integration note ───────────────────────────────────────
diff --git a/pzo_engine/src/ml/m124a_speedrun_micro_coach_split_timer_optimizer.ts b/pzo_engine/src/ml/m124a_speedrun_micro_coach_split_timer_optimizer.ts
index f5a5b8cb..a5a2939a 100644
--- a/pzo_engine/src/ml/m124a_speedrun_micro_coach_split_timer_optimizer.ts
+++ b/pzo_engine/src/ml/m124a_speedrun_micro_coach_split_timer_optimizer.ts
@@ -22,6 +22,8 @@
 // Density6 LLC · Point Zero One · Confidential · All Rights Reserved
 // ═══════════════════════════════════════════════════════════════════════════════
 
+import { createHash } from 'node:crypto';
+
 // ── What this adds ────────────────────────────────────────────────────────────
 /**
  * M124A — Speedrun Micro-Coach + Split Timer Optimizer
@@ -198,27 +200,9 @@ export async function runM124aMl(
   tier:      M124ATier = 'baseline',
   modelCard: Omit<M124AModelCard, 'modelId' | 'coreMechanicPair'>,
 ): Promise<M124AOutput> {
-  // ── TODO: implement M124A — Speedrun Micro-Coach + Split Timer Optimizer ─────────────────────────────────
-  //
-  // Implementation checklist:
-  // □ Validate input schema against featureSchemaHash
-  // □ Select inference backend based on `tier` parameter
-  // □ tier === 'baseline' → GBM + calibrated logistic (fast, low-cost, production default)
-  // □ tier === 'sequence_dl' → TCN / Transformer encoder over event streams (sequential patterns)
-  // □ tier === 'policy_rl' → Constrained contextual bandit / offline PPO (bounded nudges)
-  // □ Apply input privacy filters (no PII, no cross-player contact graph)
-  // □ Run inference → raw score + top_factors
-  // □ Apply output caps: score = Math.min(score, M124A_ML_CONSTANTS.GUARDRAILS.scoreCap)
-  // □ Apply monotonic constraints where relevant
-  // □ Abstain if confidence < M124A_ML_CONSTANTS.GUARDRAILS.abstainThreshold
-  // □ Compute auditHash = SHA256(inputs + outputs + ruleset_version + caps)
-  // □ Write signed receipt to run ledger (NEVER skip)
-  // □ Return M124AOutput — NEVER mutate run state directly
-  //
-  // Placement: client, server | Budget: real_time
-  // ExecHook:  after_m124_resolve
-  // ─────────────────────────────────────────────────────────────────────────
-  throw new Error('M124A (Speedrun Micro-Coach + Split Timer Optimizer) ML inference not yet implemented.');
+  // Day-1 operational: delegates to fallback until full ML implementation is deployed.
+  // Full implementation checklist preserved in git history.
+  return runM124aMlFallback(input);
 }
 
 // ── Degraded-mode fallback ────────────────────────────────────────────────────
@@ -230,14 +214,21 @@ export async function runM124aMl(
 export function runM124aMlFallback(
   _input: M124ATelemetryInput,
 ): M124AOutput {
-  // TODO: implement rule-based fallback for M124A
-  // Fallback must:
-  //   □ Return score = 0.5 (neutral / unknown)
-  //   □ Return topFactors = ['ML unavailable — rule-based fallback active']
-  //   □ Return recommendation = 'See rule engine output'
-  //   □ Compute deterministic auditHash from input seed + 'fallback'
-  //   □ Zero-out all M124A-specific extended outputs
-  throw new Error('M124A fallback not yet implemented.');
+  const seed = String(((_input as unknown) as Record<string, unknown>).runSeed ?? '');
+  const tick = Number(((_input as unknown) as Record<string, unknown>).tickIndex ?? 0);
+  const auditHash = createHash('sha256')
+    .update(seed + ':' + tick + ':fallback:M124A')
+    .digest('hex');
+  return {
+    score: 0.5,
+    topFactors: ['ML unavailable — rule-based fallback active'],
+    recommendation: 'See rule engine output',
+    auditHash,
+    pacingCoaching: null,
+    splitTargetOptimization: null,
+    farmingFlag: null,
+    capabilityTrajectory: null,
+  };
 }
 
 // ── IntelligenceState integration note ───────────────────────────────────────
diff --git a/pzo_engine/src/ml/m125a_no_ghost_hardcore_integrity_verifier.ts b/pzo_engine/src/ml/m125a_no_ghost_hardcore_integrity_verifier.ts
index 8f4d1a23..2cc7d1ed 100644
--- a/pzo_engine/src/ml/m125a_no_ghost_hardcore_integrity_verifier.ts
+++ b/pzo_engine/src/ml/m125a_no_ghost_hardcore_integrity_verifier.ts
@@ -22,6 +22,8 @@
 // Density6 LLC · Point Zero One · Confidential · All Rights Reserved
 // ═══════════════════════════════════════════════════════════════════════════════
 
+import { createHash } from 'node:crypto';
+
 // ── What this adds ────────────────────────────────────────────────────────────
 /**
  * M125A — No-Ghost Hardcore Integrity Verifier
@@ -196,27 +198,9 @@ export async function runM125aMl(
   tier:      M125ATier = 'baseline',
   modelCard: Omit<M125AModelCard, 'modelId' | 'coreMechanicPair'>,
 ): Promise<M125AOutput> {
-  // ── TODO: implement M125A — No-Ghost Hardcore Integrity Verifier ─────────────────────────────────
-  //
-  // Implementation checklist:
-  // □ Validate input schema against featureSchemaHash
-  // □ Select inference backend based on `tier` parameter
-  // □ tier === 'baseline' → GBM + calibrated logistic (fast, low-cost, production default)
-  // □ tier === 'sequence_dl' → TCN / Transformer encoder over event streams (sequential patterns)
-  // □ tier === 'policy_rl' → Constrained contextual bandit / offline PPO (bounded nudges)
-  // □ Apply input privacy filters (no PII, no cross-player contact graph)
-  // □ Run inference → raw score + top_factors
-  // □ Apply output caps: score = Math.min(score, M125A_ML_CONSTANTS.GUARDRAILS.scoreCap)
-  // □ Apply monotonic constraints where relevant
-  // □ Abstain if confidence < M125A_ML_CONSTANTS.GUARDRAILS.abstainThreshold
-  // □ Compute auditHash = SHA256(inputs + outputs + ruleset_version + caps)
-  // □ Write signed receipt to run ledger (NEVER skip)
-  // □ Return M125AOutput — NEVER mutate run state directly
-  //
-  // Placement: server | Budget: real_time
-  // ExecHook:  after_m125_resolve
-  // ─────────────────────────────────────────────────────────────────────────
-  throw new Error('M125A (No-Ghost Hardcore Integrity Verifier) ML inference not yet implemented.');
+  // Day-1 operational: delegates to fallback until full ML implementation is deployed.
+  // Full implementation checklist preserved in git history.
+  return runM125aMlFallback(input);
 }
 
 // ── Degraded-mode fallback ────────────────────────────────────────────────────
@@ -228,14 +212,21 @@ export async function runM125aMl(
 export function runM125aMlFallback(
   _input: M125ATelemetryInput,
 ): M125AOutput {
-  // TODO: implement rule-based fallback for M125A
-  // Fallback must:
-  //   □ Return score = 0.5 (neutral / unknown)
-  //   □ Return topFactors = ['ML unavailable — rule-based fallback active']
-  //   □ Return recommendation = 'See rule engine output'
-  //   □ Compute deterministic auditHash from input seed + 'fallback'
-  //   □ Zero-out all M125A-specific extended outputs
-  throw new Error('M125A fallback not yet implemented.');
+  const seed = String(((_input as unknown) as Record<string, unknown>).runSeed ?? '');
+  const tick = Number(((_input as unknown) as Record<string, unknown>).tickIndex ?? 0);
+  const auditHash = createHash('sha256')
+    .update(seed + ':' + tick + ':fallback:M125A')
+    .digest('hex');
+  return {
+    score: 0.5,
+    topFactors: ['ML unavailable — rule-based fallback active'],
+    recommendation: 'See rule engine output',
+    auditHash,
+    ghostComplianceScore: null,
+    residueDetectionFlag: null,
+    legitimacyReceipt: null,
+    leaderboardEligibility: null,
+  };
 }
 
 // ── IntelligenceState integration note ───────────────────────────────────────
diff --git a/pzo_engine/src/ml/m126a_cosmetic_loadout_style_recommender_auto_set_builder.ts b/pzo_engine/src/ml/m126a_cosmetic_loadout_style_recommender_auto_set_builder.ts
index 16ed5715..3c494fd9 100644
--- a/pzo_engine/src/ml/m126a_cosmetic_loadout_style_recommender_auto_set_builder.ts
+++ b/pzo_engine/src/ml/m126a_cosmetic_loadout_style_recommender_auto_set_builder.ts
@@ -22,6 +22,8 @@
 // Density6 LLC · Point Zero One · Confidential · All Rights Reserved
 // ═══════════════════════════════════════════════════════════════════════════════
 
+import { createHash } from 'node:crypto';
+
 // ── What this adds ────────────────────────────────────────────────────────────
 /**
  * M126A — Cosmetic Loadout Style Recommender + Auto-Set Builder
@@ -196,27 +198,9 @@ export async function runM126aMl(
   tier:      M126ATier = 'baseline',
   modelCard: Omit<M126AModelCard, 'modelId' | 'coreMechanicPair'>,
 ): Promise<M126AOutput> {
-  // ── TODO: implement M126A — Cosmetic Loadout Style Recommender + Auto-Set Builder ─────────────────────────────────
-  //
-  // Implementation checklist:
-  // □ Validate input schema against featureSchemaHash
-  // □ Select inference backend based on `tier` parameter
-  // □ tier === 'baseline' → GBM + calibrated logistic (fast, low-cost, production default)
-  // □ tier === 'sequence_dl' → TCN / Transformer encoder over event streams (sequential patterns)
-  // □ tier === 'policy_rl' → Constrained contextual bandit / offline PPO (bounded nudges)
-  // □ Apply input privacy filters (no PII, no cross-player contact graph)
-  // □ Run inference → raw score + top_factors
-  // □ Apply output caps: score = Math.min(score, M126A_ML_CONSTANTS.GUARDRAILS.scoreCap)
-  // □ Apply monotonic constraints where relevant
-  // □ Abstain if confidence < M126A_ML_CONSTANTS.GUARDRAILS.abstainThreshold
-  // □ Compute auditHash = SHA256(inputs + outputs + ruleset_version + caps)
-  // □ Write signed receipt to run ledger (NEVER skip)
-  // □ Return M126AOutput — NEVER mutate run state directly
-  //
-  // Placement: client | Budget: batch
-  // ExecHook:  after_m126_resolve
-  // ─────────────────────────────────────────────────────────────────────────
-  throw new Error('M126A (Cosmetic Loadout Style Recommender + Auto-Set Builder) ML inference not yet implemented.');
+  // Day-1 operational: delegates to fallback until full ML implementation is deployed.
+  // Full implementation checklist preserved in git history.
+  return runM126aMlFallback(input);
 }
 
 // ── Degraded-mode fallback ────────────────────────────────────────────────────
@@ -228,14 +212,21 @@ export async function runM126aMl(
 export function runM126aMlFallback(
   _input: M126ATelemetryInput,
 ): M126AOutput {
-  // TODO: implement rule-based fallback for M126A
-  // Fallback must:
-  //   □ Return score = 0.5 (neutral / unknown)
-  //   □ Return topFactors = ['ML unavailable — rule-based fallback active']
-  //   □ Return recommendation = 'See rule engine output'
-  //   □ Compute deterministic auditHash from input seed + 'fallback'
-  //   □ Zero-out all M126A-specific extended outputs
-  throw new Error('M126A fallback not yet implemented.');
+  const seed = String(((_input as unknown) as Record<string, unknown>).runSeed ?? '');
+  const tick = Number(((_input as unknown) as Record<string, unknown>).tickIndex ?? 0);
+  const auditHash = createHash('sha256')
+    .update(seed + ':' + tick + ':fallback:M126A')
+    .digest('hex');
+  return {
+    score: 0.5,
+    topFactors: ['ML unavailable — rule-based fallback active'],
+    recommendation: 'See rule engine output',
+    auditHash,
+    loadoutRecommendation: null,
+    autoBuiltSet: null,
+    identityMatchScore: null,
+    p2wGuardPassed: null,
+  };
 }
 
 // ── IntelligenceState integration note ───────────────────────────────────────
diff --git a/pzo_engine/src/ml/m127a_proof_bound_crafting_recipe_discoverer_legibility_score.ts b/pzo_engine/src/ml/m127a_proof_bound_crafting_recipe_discoverer_legibility_score.ts
index d629c903..fee81ec1 100644
--- a/pzo_engine/src/ml/m127a_proof_bound_crafting_recipe_discoverer_legibility_score.ts
+++ b/pzo_engine/src/ml/m127a_proof_bound_crafting_recipe_discoverer_legibility_score.ts
@@ -22,6 +22,8 @@
 // Density6 LLC · Point Zero One · Confidential · All Rights Reserved
 // ═══════════════════════════════════════════════════════════════════════════════
 
+import { createHash } from 'node:crypto';
+
 // ── What this adds ────────────────────────────────────────────────────────────
 /**
  * M127A — Proof-Bound Crafting Recipe Discoverer + Legibility Scorer
@@ -196,27 +198,9 @@ export async function runM127aMl(
   tier:      M127ATier = 'baseline',
   modelCard: Omit<M127AModelCard, 'modelId' | 'coreMechanicPair'>,
 ): Promise<M127AOutput> {
-  // ── TODO: implement M127A — Proof-Bound Crafting Recipe Discoverer + Legibility Scorer ─────────────────────────────────
-  //
-  // Implementation checklist:
-  // □ Validate input schema against featureSchemaHash
-  // □ Select inference backend based on `tier` parameter
-  // □ tier === 'baseline' → GBM + calibrated logistic (fast, low-cost, production default)
-  // □ tier === 'sequence_dl' → TCN / Transformer encoder over event streams (sequential patterns)
-  // □ tier === 'policy_rl' → Constrained contextual bandit / offline PPO (bounded nudges)
-  // □ Apply input privacy filters (no PII, no cross-player contact graph)
-  // □ Run inference → raw score + top_factors
-  // □ Apply output caps: score = Math.min(score, M127A_ML_CONSTANTS.GUARDRAILS.scoreCap)
-  // □ Apply monotonic constraints where relevant
-  // □ Abstain if confidence < M127A_ML_CONSTANTS.GUARDRAILS.abstainThreshold
-  // □ Compute auditHash = SHA256(inputs + outputs + ruleset_version + caps)
-  // □ Write signed receipt to run ledger (NEVER skip)
-  // □ Return M127AOutput — NEVER mutate run state directly
-  //
-  // Placement: server | Budget: batch
-  // ExecHook:  after_m127_resolve
-  // ─────────────────────────────────────────────────────────────────────────
-  throw new Error('M127A (Proof-Bound Crafting Recipe Discoverer + Legibility Scorer) ML inference not yet implemented.');
+  // Day-1 operational: delegates to fallback until full ML implementation is deployed.
+  // Full implementation checklist preserved in git history.
+  return runM127aMlFallback(input);
 }
 
 // ── Degraded-mode fallback ────────────────────────────────────────────────────
@@ -228,14 +212,21 @@ export async function runM127aMl(
 export function runM127aMlFallback(
   _input: M127ATelemetryInput,
 ): M127AOutput {
-  // TODO: implement rule-based fallback for M127A
-  // Fallback must:
-  //   □ Return score = 0.5 (neutral / unknown)
-  //   □ Return topFactors = ['ML unavailable — rule-based fallback active']
-  //   □ Return recommendation = 'See rule engine output'
-  //   □ Compute deterministic auditHash from input seed + 'fallback'
-  //   □ Zero-out all M127A-specific extended outputs
-  throw new Error('M127A fallback not yet implemented.');
+  const seed = String(((_input as unknown) as Record<string, unknown>).runSeed ?? '');
+  const tick = Number(((_input as unknown) as Record<string, unknown>).tickIndex ?? 0);
+  const auditHash = createHash('sha256')
+    .update(seed + ':' + tick + ':fallback:M127A')
+    .digest('hex');
+  return {
+    score: 0.5,
+    topFactors: ['ML unavailable — rule-based fallback active'],
+    recommendation: 'See rule engine output',
+    auditHash,
+    recipeDiscoveries: null,
+    legibilityScore: null,
+    economyImpactFlag: null,
+    recipeRecommendation: null,
+  };
 }
 
 // ── IntelligenceState integration note ───────────────────────────────────────
diff --git a/pzo_engine/src/ml/m128a_season_sink_health_monitor_burn_rate_optimizer.ts b/pzo_engine/src/ml/m128a_season_sink_health_monitor_burn_rate_optimizer.ts
index 1782936c..a253903e 100644
--- a/pzo_engine/src/ml/m128a_season_sink_health_monitor_burn_rate_optimizer.ts
+++ b/pzo_engine/src/ml/m128a_season_sink_health_monitor_burn_rate_optimizer.ts
@@ -22,6 +22,8 @@
 // Density6 LLC · Point Zero One · Confidential · All Rights Reserved
 // ═══════════════════════════════════════════════════════════════════════════════
 
+import { createHash } from 'node:crypto';
+
 // ── What this adds ────────────────────────────────────────────────────────────
 /**
  * M128A — Season Sink Health Monitor + Burn Rate Optimizer
@@ -196,27 +198,9 @@ export async function runM128aMl(
   tier:      M128ATier = 'baseline',
   modelCard: Omit<M128AModelCard, 'modelId' | 'coreMechanicPair'>,
 ): Promise<M128AOutput> {
-  // ── TODO: implement M128A — Season Sink Health Monitor + Burn Rate Optimizer ─────────────────────────────────
-  //
-  // Implementation checklist:
-  // □ Validate input schema against featureSchemaHash
-  // □ Select inference backend based on `tier` parameter
-  // □ tier === 'baseline' → GBM + calibrated logistic (fast, low-cost, production default)
-  // □ tier === 'sequence_dl' → TCN / Transformer encoder over event streams (sequential patterns)
-  // □ tier === 'policy_rl' → Constrained contextual bandit / offline PPO (bounded nudges)
-  // □ Apply input privacy filters (no PII, no cross-player contact graph)
-  // □ Run inference → raw score + top_factors
-  // □ Apply output caps: score = Math.min(score, M128A_ML_CONSTANTS.GUARDRAILS.scoreCap)
-  // □ Apply monotonic constraints where relevant
-  // □ Abstain if confidence < M128A_ML_CONSTANTS.GUARDRAILS.abstainThreshold
-  // □ Compute auditHash = SHA256(inputs + outputs + ruleset_version + caps)
-  // □ Write signed receipt to run ledger (NEVER skip)
-  // □ Return M128AOutput — NEVER mutate run state directly
-  //
-  // Placement: server | Budget: batch
-  // ExecHook:  after_m128_resolve
-  // ─────────────────────────────────────────────────────────────────────────
-  throw new Error('M128A (Season Sink Health Monitor + Burn Rate Optimizer) ML inference not yet implemented.');
+  // Day-1 operational: delegates to fallback until full ML implementation is deployed.
+  // Full implementation checklist preserved in git history.
+  return runM128aMlFallback(input);
 }
 
 // ── Degraded-mode fallback ────────────────────────────────────────────────────
@@ -228,14 +212,21 @@ export async function runM128aMl(
 export function runM128aMlFallback(
   _input: M128ATelemetryInput,
 ): M128AOutput {
-  // TODO: implement rule-based fallback for M128A
-  // Fallback must:
-  //   □ Return score = 0.5 (neutral / unknown)
-  //   □ Return topFactors = ['ML unavailable — rule-based fallback active']
-  //   □ Return recommendation = 'See rule engine output'
-  //   □ Compute deterministic auditHash from input seed + 'fallback'
-  //   □ Zero-out all M128A-specific extended outputs
-  throw new Error('M128A fallback not yet implemented.');
+  const seed = String(((_input as unknown) as Record<string, unknown>).runSeed ?? '');
+  const tick = Number(((_input as unknown) as Record<string, unknown>).tickIndex ?? 0);
+  const auditHash = createHash('sha256')
+    .update(seed + ':' + tick + ':fallback:M128A')
+    .digest('hex');
+  return {
+    score: 0.5,
+    topFactors: ['ML unavailable — rule-based fallback active'],
+    recommendation: 'See rule engine output',
+    auditHash,
+    sinkHealthScore: null,
+    burnRateRecommendation: null,
+    devaluationRisk: null,
+    designDigest: null,
+  };
 }
 
 // ── IntelligenceState integration note ───────────────────────────────────────
diff --git a/pzo_engine/src/ml/m129a_creator_pack_caption_generator_moment_stinger_selector.ts b/pzo_engine/src/ml/m129a_creator_pack_caption_generator_moment_stinger_selector.ts
index 757a3e12..1f09e29f 100644
--- a/pzo_engine/src/ml/m129a_creator_pack_caption_generator_moment_stinger_selector.ts
+++ b/pzo_engine/src/ml/m129a_creator_pack_caption_generator_moment_stinger_selector.ts
@@ -22,6 +22,8 @@
 // Density6 LLC · Point Zero One · Confidential · All Rights Reserved
 // ═══════════════════════════════════════════════════════════════════════════════
 
+import { createHash } from 'node:crypto';
+
 // ── What this adds ────────────────────────────────────────────────────────────
 /**
  * M129A — Creator Pack Caption Generator + Moment Stinger Selector
@@ -188,26 +190,9 @@ export async function runM129aMl(
   tier:      M129ATier = 'baseline',
   modelCard: Omit<M129AModelCard, 'modelId' | 'coreMechanicPair'>,
 ): Promise<M129AOutput> {
-  // ── TODO: implement M129A — Creator Pack Caption Generator + Moment Stinger Selector ─────────────────────────────────
-  //
-  // Implementation checklist:
-  // □ Validate input schema against featureSchemaHash
-  // □ Select inference backend based on `tier` parameter
-  // □ tier === 'baseline' → GBM + calibrated logistic (fast, low-cost, production default)
-  // □ tier === 'sequence_dl' → TCN / Transformer encoder over event streams (sequential patterns)
-  // □ Apply input privacy filters (no PII, no cross-player contact graph)
-  // □ Run inference → raw score + top_factors
-  // □ Apply output caps: score = Math.min(score, M129A_ML_CONSTANTS.GUARDRAILS.scoreCap)
-  // □ Apply monotonic constraints where relevant
-  // □ Abstain if confidence < M129A_ML_CONSTANTS.GUARDRAILS.abstainThreshold
-  // □ Compute auditHash = SHA256(inputs + outputs + ruleset_version + caps)
-  // □ Write signed receipt to run ledger (NEVER skip)
-  // □ Return M129AOutput — NEVER mutate run state directly
-  //
-  // Placement: client, server | Budget: real_time
-  // ExecHook:  after_m129_resolve
-  // ─────────────────────────────────────────────────────────────────────────
-  throw new Error('M129A (Creator Pack Caption Generator + Moment Stinger Selector) ML inference not yet implemented.');
+  // Day-1 operational: delegates to fallback until full ML implementation is deployed.
+  // Full implementation checklist preserved in git history.
+  return runM129aMlFallback(input);
 }
 
 // ── Degraded-mode fallback ────────────────────────────────────────────────────
@@ -219,14 +204,21 @@ export async function runM129aMl(
 export function runM129aMlFallback(
   _input: M129ATelemetryInput,
 ): M129AOutput {
-  // TODO: implement rule-based fallback for M129A
-  // Fallback must:
-  //   □ Return score = 0.5 (neutral / unknown)
-  //   □ Return topFactors = ['ML unavailable — rule-based fallback active']
-  //   □ Return recommendation = 'See rule engine output'
-  //   □ Compute deterministic auditHash from input seed + 'fallback'
-  //   □ Zero-out all M129A-specific extended outputs
-  throw new Error('M129A fallback not yet implemented.');
+  const seed = String(((_input as unknown) as Record<string, unknown>).runSeed ?? '');
+  const tick = Number(((_input as unknown) as Record<string, unknown>).tickIndex ?? 0);
+  const auditHash = createHash('sha256')
+    .update(seed + ':' + tick + ':fallback:M129A')
+    .digest('hex');
+  return {
+    score: 0.5,
+    topFactors: ['ML unavailable — rule-based fallback active'],
+    recommendation: 'See rule engine output',
+    auditHash,
+    captionText: null,
+    stingerSelection: null,
+    safetyFilterPassed: null,
+    beatMatchScore: null,
+  };
 }
 
 // ── IntelligenceState integration note ───────────────────────────────────────
diff --git a/pzo_engine/src/ml/m12a_fubar_cascade_risk_model_crisis_graph.ts b/pzo_engine/src/ml/m12a_fubar_cascade_risk_model_crisis_graph.ts
index 06048423..400b50cf 100644
--- a/pzo_engine/src/ml/m12a_fubar_cascade_risk_model_crisis_graph.ts
+++ b/pzo_engine/src/ml/m12a_fubar_cascade_risk_model_crisis_graph.ts
@@ -22,6 +22,8 @@
 // Density6 LLC · Point Zero One · Confidential · All Rights Reserved
 // ═══════════════════════════════════════════════════════════════════════════════
 
+import { createHash } from 'node:crypto';
+
 // ── What this adds ────────────────────────────────────────────────────────────
 /**
  * M12A — FUBAR Cascade Risk Model (Crisis Graph)
@@ -198,27 +200,9 @@ export async function runM12aMl(
   tier:      M12ATier = 'baseline',
   modelCard: Omit<M12AModelCard, 'modelId' | 'coreMechanicPair'>,
 ): Promise<M12AOutput> {
-  // ── TODO: implement M12A — FUBAR Cascade Risk Model (Crisis Graph) ─────────────────────────────────
-  //
-  // Implementation checklist:
-  // □ Validate input schema against featureSchemaHash
-  // □ Select inference backend based on `tier` parameter
-  // □ tier === 'baseline' → GBM + calibrated logistic (fast, low-cost, production default)
-  // □ tier === 'sequence_dl' → TCN / Transformer encoder over event streams (sequential patterns)
-  // □ tier === 'policy_rl' → Constrained contextual bandit / offline PPO (bounded nudges)
-  // □ Apply input privacy filters (no PII, no cross-player contact graph)
-  // □ Run inference → raw score + top_factors
-  // □ Apply output caps: score = Math.min(score, M12A_ML_CONSTANTS.GUARDRAILS.scoreCap)
-  // □ Apply monotonic constraints where relevant
-  // □ Abstain if confidence < M12A_ML_CONSTANTS.GUARDRAILS.abstainThreshold
-  // □ Compute auditHash = SHA256(inputs + outputs + ruleset_version + caps)
-  // □ Write signed receipt to run ledger (NEVER skip)
-  // □ Return M12AOutput — NEVER mutate run state directly
-  //
-  // Placement: client, server | Budget: real_time
-  // ExecHook:  after_m12_resolve
-  // ─────────────────────────────────────────────────────────────────────────
-  throw new Error('M12A (FUBAR Cascade Risk Model (Crisis Graph)) ML inference not yet implemented.');
+  // Day-1 operational: delegates to fallback until full ML implementation is deployed.
+  // Full implementation checklist preserved in git history.
+  return runM12aMlFallback(input);
 }
 
 // ── Degraded-mode fallback ────────────────────────────────────────────────────
@@ -230,14 +214,21 @@ export async function runM12aMl(
 export function runM12aMlFallback(
   _input: M12ATelemetryInput,
 ): M12AOutput {
-  // TODO: implement rule-based fallback for M12A
-  // Fallback must:
-  //   □ Return score = 0.5 (neutral / unknown)
-  //   □ Return topFactors = ['ML unavailable — rule-based fallback active']
-  //   □ Return recommendation = 'See rule engine output'
-  //   □ Compute deterministic auditHash from input seed + 'fallback'
-  //   □ Zero-out all M12A-specific extended outputs
-  throw new Error('M12A fallback not yet implemented.');
+  const seed = String(((_input as unknown) as Record<string, unknown>).runSeed ?? '');
+  const tick = Number(((_input as unknown) as Record<string, unknown>).tickIndex ?? 0);
+  const auditHash = createHash('sha256')
+    .update(seed + ':' + tick + ':fallback:M12A')
+    .digest('hex');
+  return {
+    score: 0.5,
+    topFactors: ['ML unavailable — rule-based fallback active'],
+    recommendation: 'See rule engine output',
+    auditHash,
+    cascadeProbability: null,
+    chainPathPrediction: null,
+    overpoweredComboFlag: null,
+    dominoVisualizationData: null,
+  };
 }
 
 // ── IntelligenceState integration note ───────────────────────────────────────
diff --git a/pzo_engine/src/ml/m130a_table_vault_contribution_planner_fairness_auditor.ts b/pzo_engine/src/ml/m130a_table_vault_contribution_planner_fairness_auditor.ts
index 9a615d74..63cf90b4 100644
--- a/pzo_engine/src/ml/m130a_table_vault_contribution_planner_fairness_auditor.ts
+++ b/pzo_engine/src/ml/m130a_table_vault_contribution_planner_fairness_auditor.ts
@@ -22,6 +22,8 @@
 // Density6 LLC · Point Zero One · Confidential · All Rights Reserved
 // ═══════════════════════════════════════════════════════════════════════════════
 
+import { createHash } from 'node:crypto';
+
 // ── What this adds ────────────────────────────────────────────────────────────
 /**
  * M130A — Table Vault Contribution Planner + Fairness Auditor
@@ -196,27 +198,9 @@ export async function runM130aMl(
   tier:      M130ATier = 'baseline',
   modelCard: Omit<M130AModelCard, 'modelId' | 'coreMechanicPair'>,
 ): Promise<M130AOutput> {
-  // ── TODO: implement M130A — Table Vault Contribution Planner + Fairness Auditor ─────────────────────────────────
-  //
-  // Implementation checklist:
-  // □ Validate input schema against featureSchemaHash
-  // □ Select inference backend based on `tier` parameter
-  // □ tier === 'baseline' → GBM + calibrated logistic (fast, low-cost, production default)
-  // □ tier === 'sequence_dl' → TCN / Transformer encoder over event streams (sequential patterns)
-  // □ tier === 'policy_rl' → Constrained contextual bandit / offline PPO (bounded nudges)
-  // □ Apply input privacy filters (no PII, no cross-player contact graph)
-  // □ Run inference → raw score + top_factors
-  // □ Apply output caps: score = Math.min(score, M130A_ML_CONSTANTS.GUARDRAILS.scoreCap)
-  // □ Apply monotonic constraints where relevant
-  // □ Abstain if confidence < M130A_ML_CONSTANTS.GUARDRAILS.abstainThreshold
-  // □ Compute auditHash = SHA256(inputs + outputs + ruleset_version + caps)
-  // □ Write signed receipt to run ledger (NEVER skip)
-  // □ Return M130AOutput — NEVER mutate run state directly
-  //
-  // Placement: server | Budget: batch
-  // ExecHook:  after_m130_resolve
-  // ─────────────────────────────────────────────────────────────────────────
-  throw new Error('M130A (Table Vault Contribution Planner + Fairness Auditor) ML inference not yet implemented.');
+  // Day-1 operational: delegates to fallback until full ML implementation is deployed.
+  // Full implementation checklist preserved in git history.
+  return runM130aMlFallback(input);
 }
 
 // ── Degraded-mode fallback ────────────────────────────────────────────────────
@@ -228,14 +212,21 @@ export async function runM130aMl(
 export function runM130aMlFallback(
   _input: M130ATelemetryInput,
 ): M130AOutput {
-  // TODO: implement rule-based fallback for M130A
-  // Fallback must:
-  //   □ Return score = 0.5 (neutral / unknown)
-  //   □ Return topFactors = ['ML unavailable — rule-based fallback active']
-  //   □ Return recommendation = 'See rule engine output'
-  //   □ Compute deterministic auditHash from input seed + 'fallback'
-  //   □ Zero-out all M130A-specific extended outputs
-  throw new Error('M130A fallback not yet implemented.');
+  const seed = String(((_input as unknown) as Record<string, unknown>).runSeed ?? '');
+  const tick = Number(((_input as unknown) as Record<string, unknown>).tickIndex ?? 0);
+  const auditHash = createHash('sha256')
+    .update(seed + ':' + tick + ':fallback:M130A')
+    .digest('hex');
+  return {
+    score: 0.5,
+    topFactors: ['ML unavailable — rule-based fallback active'],
+    recommendation: 'See rule engine output',
+    auditHash,
+    contributionPlan: null,
+    freeRiderFlag: null,
+    fairnessReceipt: null,
+    vaultOptimizationScore: null,
+  };
 }
 
 // ── IntelligenceState integration note ───────────────────────────────────────
diff --git a/pzo_engine/src/ml/m131a_faction_sponsor_match_power_guard.ts b/pzo_engine/src/ml/m131a_faction_sponsor_match_power_guard.ts
index 8e859301..57dcdd9c 100644
--- a/pzo_engine/src/ml/m131a_faction_sponsor_match_power_guard.ts
+++ b/pzo_engine/src/ml/m131a_faction_sponsor_match_power_guard.ts
@@ -22,6 +22,8 @@
 // Density6 LLC · Point Zero One · Confidential · All Rights Reserved
 // ═══════════════════════════════════════════════════════════════════════════════
 
+import { createHash } from 'node:crypto';
+
 // ── What this adds ────────────────────────────────────────────────────────────
 /**
  * M131A — Faction Sponsor Match + Power Guard
@@ -196,27 +198,9 @@ export async function runM131aMl(
   tier:      M131ATier = 'baseline',
   modelCard: Omit<M131AModelCard, 'modelId' | 'coreMechanicPair'>,
 ): Promise<M131AOutput> {
-  // ── TODO: implement M131A — Faction Sponsor Match + Power Guard ─────────────────────────────────
-  //
-  // Implementation checklist:
-  // □ Validate input schema against featureSchemaHash
-  // □ Select inference backend based on `tier` parameter
-  // □ tier === 'baseline' → GBM + calibrated logistic (fast, low-cost, production default)
-  // □ tier === 'sequence_dl' → TCN / Transformer encoder over event streams (sequential patterns)
-  // □ tier === 'policy_rl' → Constrained contextual bandit / offline PPO (bounded nudges)
-  // □ Apply input privacy filters (no PII, no cross-player contact graph)
-  // □ Run inference → raw score + top_factors
-  // □ Apply output caps: score = Math.min(score, M131A_ML_CONSTANTS.GUARDRAILS.scoreCap)
-  // □ Apply monotonic constraints where relevant
-  // □ Abstain if confidence < M131A_ML_CONSTANTS.GUARDRAILS.abstainThreshold
-  // □ Compute auditHash = SHA256(inputs + outputs + ruleset_version + caps)
-  // □ Write signed receipt to run ledger (NEVER skip)
-  // □ Return M131AOutput — NEVER mutate run state directly
-  //
-  // Placement: server | Budget: batch
-  // ExecHook:  after_m131_resolve
-  // ─────────────────────────────────────────────────────────────────────────
-  throw new Error('M131A (Faction Sponsor Match + Power Guard) ML inference not yet implemented.');
+  // Day-1 operational: delegates to fallback until full ML implementation is deployed.
+  // Full implementation checklist preserved in git history.
+  return runM131aMlFallback(input);
 }
 
 // ── Degraded-mode fallback ────────────────────────────────────────────────────
@@ -228,14 +212,21 @@ export async function runM131aMl(
 export function runM131aMlFallback(
   _input: M131ATelemetryInput,
 ): M131AOutput {
-  // TODO: implement rule-based fallback for M131A
-  // Fallback must:
-  //   □ Return score = 0.5 (neutral / unknown)
-  //   □ Return topFactors = ['ML unavailable — rule-based fallback active']
-  //   □ Return recommendation = 'See rule engine output'
-  //   □ Compute deterministic auditHash from input seed + 'fallback'
-  //   □ Zero-out all M131A-specific extended outputs
-  throw new Error('M131A fallback not yet implemented.');
+  const seed = String(((_input as unknown) as Record<string, unknown>).runSeed ?? '');
+  const tick = Number(((_input as unknown) as Record<string, unknown>).tickIndex ?? 0);
+  const auditHash = createHash('sha256')
+    .update(seed + ':' + tick + ':fallback:M131A')
+    .digest('hex');
+  return {
+    score: 0.5,
+    topFactors: ['ML unavailable — rule-based fallback active'],
+    recommendation: 'See rule engine output',
+    auditHash,
+    sponsorMatch: null,
+    powerGuardVerified: null,
+    factionReceipt: null,
+    identityAlignmentScore: null,
+  };
 }
 
 // ── IntelligenceState integration note ───────────────────────────────────────
diff --git a/pzo_engine/src/ml/m132a_case_files_forensic_summarizer_root_cause_ranker.ts b/pzo_engine/src/ml/m132a_case_files_forensic_summarizer_root_cause_ranker.ts
index bed113e5..456d92bb 100644
--- a/pzo_engine/src/ml/m132a_case_files_forensic_summarizer_root_cause_ranker.ts
+++ b/pzo_engine/src/ml/m132a_case_files_forensic_summarizer_root_cause_ranker.ts
@@ -22,6 +22,8 @@
 // Density6 LLC · Point Zero One · Confidential · All Rights Reserved
 // ═══════════════════════════════════════════════════════════════════════════════
 
+import { createHash } from 'node:crypto';
+
 // ── What this adds ────────────────────────────────────────────────────────────
 /**
  * M132A — Case Files Forensic Summarizer + Root-Cause Ranker
@@ -196,27 +198,9 @@ export async function runM132aMl(
   tier:      M132ATier = 'baseline',
   modelCard: Omit<M132AModelCard, 'modelId' | 'coreMechanicPair'>,
 ): Promise<M132AOutput> {
-  // ── TODO: implement M132A — Case Files Forensic Summarizer + Root-Cause Ranker ─────────────────────────────────
-  //
-  // Implementation checklist:
-  // □ Validate input schema against featureSchemaHash
-  // □ Select inference backend based on `tier` parameter
-  // □ tier === 'baseline' → GBM + calibrated logistic (fast, low-cost, production default)
-  // □ tier === 'causal' → Causal inference + DiD (counterfactual explanations)
-  // □ tier === 'retrieval' → Dense retrieval over season case library (similarity search)
-  // □ Apply input privacy filters (no PII, no cross-player contact graph)
-  // □ Run inference → raw score + top_factors
-  // □ Apply output caps: score = Math.min(score, M132A_ML_CONSTANTS.GUARDRAILS.scoreCap)
-  // □ Apply monotonic constraints where relevant
-  // □ Abstain if confidence < M132A_ML_CONSTANTS.GUARDRAILS.abstainThreshold
-  // □ Compute auditHash = SHA256(inputs + outputs + ruleset_version + caps)
-  // □ Write signed receipt to run ledger (NEVER skip)
-  // □ Return M132AOutput — NEVER mutate run state directly
-  //
-  // Placement: server | Budget: batch
-  // ExecHook:  after_m132_resolve
-  // ─────────────────────────────────────────────────────────────────────────
-  throw new Error('M132A (Case Files Forensic Summarizer + Root-Cause Ranker) ML inference not yet implemented.');
+  // Day-1 operational: delegates to fallback until full ML implementation is deployed.
+  // Full implementation checklist preserved in git history.
+  return runM132aMlFallback(input);
 }
 
 // ── Degraded-mode fallback ────────────────────────────────────────────────────
@@ -228,14 +212,21 @@ export async function runM132aMl(
 export function runM132aMlFallback(
   _input: M132ATelemetryInput,
 ): M132AOutput {
-  // TODO: implement rule-based fallback for M132A
-  // Fallback must:
-  //   □ Return score = 0.5 (neutral / unknown)
-  //   □ Return topFactors = ['ML unavailable — rule-based fallback active']
-  //   □ Return recommendation = 'See rule engine output'
-  //   □ Compute deterministic auditHash from input seed + 'fallback'
-  //   □ Zero-out all M132A-specific extended outputs
-  throw new Error('M132A fallback not yet implemented.');
+  const seed = String(((_input as unknown) as Record<string, unknown>).runSeed ?? '');
+  const tick = Number(((_input as unknown) as Record<string, unknown>).tickIndex ?? 0);
+  const auditHash = createHash('sha256')
+    .update(seed + ':' + tick + ':fallback:M132A')
+    .digest('hex');
+  return {
+    score: 0.5,
+    topFactors: ['ML unavailable — rule-based fallback active'],
+    recommendation: 'See rule engine output',
+    auditHash,
+    caseFileDossier: null,
+    pivotRanking: null,
+    causalChain: null,
+    seasonLibraryEntry: null,
+  };
 }
 
 // ── IntelligenceState integration note ───────────────────────────────────────
diff --git a/pzo_engine/src/ml/m133a_seasonal_story_beat_planner_narrative_coherence_guard.ts b/pzo_engine/src/ml/m133a_seasonal_story_beat_planner_narrative_coherence_guard.ts
index 041c7d74..ebe6ea01 100644
--- a/pzo_engine/src/ml/m133a_seasonal_story_beat_planner_narrative_coherence_guard.ts
+++ b/pzo_engine/src/ml/m133a_seasonal_story_beat_planner_narrative_coherence_guard.ts
@@ -22,6 +22,8 @@
 // Density6 LLC · Point Zero One · Confidential · All Rights Reserved
 // ═══════════════════════════════════════════════════════════════════════════════
 
+import { createHash } from 'node:crypto';
+
 // ── What this adds ────────────────────────────────────────────────────────────
 /**
  * M133A — Seasonal Story Beat Planner + Narrative Coherence Guard
@@ -196,27 +198,9 @@ export async function runM133aMl(
   tier:      M133ATier = 'baseline',
   modelCard: Omit<M133AModelCard, 'modelId' | 'coreMechanicPair'>,
 ): Promise<M133AOutput> {
-  // ── TODO: implement M133A — Seasonal Story Beat Planner + Narrative Coherence Guard ─────────────────────────────────
-  //
-  // Implementation checklist:
-  // □ Validate input schema against featureSchemaHash
-  // □ Select inference backend based on `tier` parameter
-  // □ tier === 'baseline' → GBM + calibrated logistic (fast, low-cost, production default)
-  // □ tier === 'sequence_dl' → TCN / Transformer encoder over event streams (sequential patterns)
-  // □ tier === 'policy_rl' → Constrained contextual bandit / offline PPO (bounded nudges)
-  // □ Apply input privacy filters (no PII, no cross-player contact graph)
-  // □ Run inference → raw score + top_factors
-  // □ Apply output caps: score = Math.min(score, M133A_ML_CONSTANTS.GUARDRAILS.scoreCap)
-  // □ Apply monotonic constraints where relevant
-  // □ Abstain if confidence < M133A_ML_CONSTANTS.GUARDRAILS.abstainThreshold
-  // □ Compute auditHash = SHA256(inputs + outputs + ruleset_version + caps)
-  // □ Write signed receipt to run ledger (NEVER skip)
-  // □ Return M133AOutput — NEVER mutate run state directly
-  //
-  // Placement: server | Budget: batch
-  // ExecHook:  after_m133_resolve
-  // ─────────────────────────────────────────────────────────────────────────
-  throw new Error('M133A (Seasonal Story Beat Planner + Narrative Coherence Guard) ML inference not yet implemented.');
+  // Day-1 operational: delegates to fallback until full ML implementation is deployed.
+  // Full implementation checklist preserved in git history.
+  return runM133aMlFallback(input);
 }
 
 // ── Degraded-mode fallback ────────────────────────────────────────────────────
@@ -228,14 +212,21 @@ export async function runM133aMl(
 export function runM133aMlFallback(
   _input: M133ATelemetryInput,
 ): M133AOutput {
-  // TODO: implement rule-based fallback for M133A
-  // Fallback must:
-  //   □ Return score = 0.5 (neutral / unknown)
-  //   □ Return topFactors = ['ML unavailable — rule-based fallback active']
-  //   □ Return recommendation = 'See rule engine output'
-  //   □ Compute deterministic auditHash from input seed + 'fallback'
-  //   □ Zero-out all M133A-specific extended outputs
-  throw new Error('M133A fallback not yet implemented.');
+  const seed = String(((_input as unknown) as Record<string, unknown>).runSeed ?? '');
+  const tick = Number(((_input as unknown) as Record<string, unknown>).tickIndex ?? 0);
+  const auditHash = createHash('sha256')
+    .update(seed + ':' + tick + ':fallback:M133A')
+    .digest('hex');
+  return {
+    score: 0.5,
+    topFactors: ['ML unavailable — rule-based fallback active'],
+    recommendation: 'See rule engine output',
+    auditHash,
+    beatSchedule: null,
+    coherenceScore: null,
+    contradictionFlag: null,
+    emotionalArcQuality: null,
+  };
 }
 
 // ── IntelligenceState integration note ───────────────────────────────────────
diff --git a/pzo_engine/src/ml/m134a_npc_counterparty_behavioral_emulator_deterministic_pers.ts b/pzo_engine/src/ml/m134a_npc_counterparty_behavioral_emulator_deterministic_pers.ts
index 4d2e99e0..bcee7105 100644
--- a/pzo_engine/src/ml/m134a_npc_counterparty_behavioral_emulator_deterministic_pers.ts
+++ b/pzo_engine/src/ml/m134a_npc_counterparty_behavioral_emulator_deterministic_pers.ts
@@ -22,6 +22,8 @@
 // Density6 LLC · Point Zero One · Confidential · All Rights Reserved
 // ═══════════════════════════════════════════════════════════════════════════════
 
+import { createHash } from 'node:crypto';
+
 // ── What this adds ────────────────────────────────────────────────────────────
 /**
  * M134A — NPC Counterparty Behavioral Emulator (Deterministic Persona Fidelity)
@@ -196,27 +198,9 @@ export async function runM134aMl(
   tier:      M134ATier = 'baseline',
   modelCard: Omit<M134AModelCard, 'modelId' | 'coreMechanicPair'>,
 ): Promise<M134AOutput> {
-  // ── TODO: implement M134A — NPC Counterparty Behavioral Emulator (Deterministic Persona Fidelity) ─────────────────────────────────
-  //
-  // Implementation checklist:
-  // □ Validate input schema against featureSchemaHash
-  // □ Select inference backend based on `tier` parameter
-  // □ tier === 'baseline' → GBM + calibrated logistic (fast, low-cost, production default)
-  // □ tier === 'sequence_dl' → TCN / Transformer encoder over event streams (sequential patterns)
-  // □ tier === 'policy_rl' → Constrained contextual bandit / offline PPO (bounded nudges)
-  // □ Apply input privacy filters (no PII, no cross-player contact graph)
-  // □ Run inference → raw score + top_factors
-  // □ Apply output caps: score = Math.min(score, M134A_ML_CONSTANTS.GUARDRAILS.scoreCap)
-  // □ Apply monotonic constraints where relevant
-  // □ Abstain if confidence < M134A_ML_CONSTANTS.GUARDRAILS.abstainThreshold
-  // □ Compute auditHash = SHA256(inputs + outputs + ruleset_version + caps)
-  // □ Write signed receipt to run ledger (NEVER skip)
-  // □ Return M134AOutput — NEVER mutate run state directly
-  //
-  // Placement: server | Budget: real_time
-  // ExecHook:  after_m134_resolve
-  // ─────────────────────────────────────────────────────────────────────────
-  throw new Error('M134A (NPC Counterparty Behavioral Emulator (Deterministic Persona Fidelity)) ML inference not yet implemented.');
+  // Day-1 operational: delegates to fallback until full ML implementation is deployed.
+  // Full implementation checklist preserved in git history.
+  return runM134aMlFallback(input);
 }
 
 // ── Degraded-mode fallback ────────────────────────────────────────────────────
@@ -228,14 +212,21 @@ export async function runM134aMl(
 export function runM134aMlFallback(
   _input: M134ATelemetryInput,
 ): M134AOutput {
-  // TODO: implement rule-based fallback for M134A
-  // Fallback must:
-  //   □ Return score = 0.5 (neutral / unknown)
-  //   □ Return topFactors = ['ML unavailable — rule-based fallback active']
-  //   □ Return recommendation = 'See rule engine output'
-  //   □ Compute deterministic auditHash from input seed + 'fallback'
-  //   □ Zero-out all M134A-specific extended outputs
-  throw new Error('M134A fallback not yet implemented.');
+  const seed = String(((_input as unknown) as Record<string, unknown>).runSeed ?? '');
+  const tick = Number(((_input as unknown) as Record<string, unknown>).tickIndex ?? 0);
+  const auditHash = createHash('sha256')
+    .update(seed + ':' + tick + ':fallback:M134A')
+    .digest('hex');
+  return {
+    score: 0.5,
+    topFactors: ['ML unavailable — rule-based fallback active'],
+    recommendation: 'See rule engine output',
+    auditHash,
+    personaBehaviorOutput: null,
+    determinismVerification: null,
+    driftFlag: null,
+    characterFidelityScore: null,
+  };
 }
 
 // ── IntelligenceState integration note ───────────────────────────────────────
diff --git a/pzo_engine/src/ml/m135a_reputation_label_model_drift_detector.ts b/pzo_engine/src/ml/m135a_reputation_label_model_drift_detector.ts
index 3dd334a6..d7da77eb 100644
--- a/pzo_engine/src/ml/m135a_reputation_label_model_drift_detector.ts
+++ b/pzo_engine/src/ml/m135a_reputation_label_model_drift_detector.ts
@@ -22,6 +22,8 @@
 // Density6 LLC · Point Zero One · Confidential · All Rights Reserved
 // ═══════════════════════════════════════════════════════════════════════════════
 
+import { createHash } from 'node:crypto';
+
 // ── What this adds ────────────────────────────────────────────────────────────
 /**
  * M135A — Reputation Label Model + Drift Detector
@@ -196,27 +198,9 @@ export async function runM135aMl(
   tier:      M135ATier = 'baseline',
   modelCard: Omit<M135AModelCard, 'modelId' | 'coreMechanicPair'>,
 ): Promise<M135AOutput> {
-  // ── TODO: implement M135A — Reputation Label Model + Drift Detector ─────────────────────────────────
-  //
-  // Implementation checklist:
-  // □ Validate input schema against featureSchemaHash
-  // □ Select inference backend based on `tier` parameter
-  // □ tier === 'baseline' → GBM + calibrated logistic (fast, low-cost, production default)
-  // □ tier === 'sequence_dl' → TCN / Transformer encoder over event streams (sequential patterns)
-  // □ tier === 'policy_rl' → Constrained contextual bandit / offline PPO (bounded nudges)
-  // □ Apply input privacy filters (no PII, no cross-player contact graph)
-  // □ Run inference → raw score + top_factors
-  // □ Apply output caps: score = Math.min(score, M135A_ML_CONSTANTS.GUARDRAILS.scoreCap)
-  // □ Apply monotonic constraints where relevant
-  // □ Abstain if confidence < M135A_ML_CONSTANTS.GUARDRAILS.abstainThreshold
-  // □ Compute auditHash = SHA256(inputs + outputs + ruleset_version + caps)
-  // □ Write signed receipt to run ledger (NEVER skip)
-  // □ Return M135AOutput — NEVER mutate run state directly
-  //
-  // Placement: server | Budget: batch
-  // ExecHook:  after_m135_resolve
-  // ─────────────────────────────────────────────────────────────────────────
-  throw new Error('M135A (Reputation Label Model + Drift Detector) ML inference not yet implemented.');
+  // Day-1 operational: delegates to fallback until full ML implementation is deployed.
+  // Full implementation checklist preserved in git history.
+  return runM135aMlFallback(input);
 }
 
 // ── Degraded-mode fallback ────────────────────────────────────────────────────
@@ -228,14 +212,21 @@ export async function runM135aMl(
 export function runM135aMlFallback(
   _input: M135ATelemetryInput,
 ): M135AOutput {
-  // TODO: implement rule-based fallback for M135A
-  // Fallback must:
-  //   □ Return score = 0.5 (neutral / unknown)
-  //   □ Return topFactors = ['ML unavailable — rule-based fallback active']
-  //   □ Return recommendation = 'See rule engine output'
-  //   □ Compute deterministic auditHash from input seed + 'fallback'
-  //   □ Zero-out all M135A-specific extended outputs
-  throw new Error('M135A fallback not yet implemented.');
+  const seed = String(((_input as unknown) as Record<string, unknown>).runSeed ?? '');
+  const tick = Number(((_input as unknown) as Record<string, unknown>).tickIndex ?? 0);
+  const auditHash = createHash('sha256')
+    .update(seed + ':' + tick + ':fallback:M135A')
+    .digest('hex');
+  return {
+    score: 0.5,
+    topFactors: ['ML unavailable — rule-based fallback active'],
+    recommendation: 'See rule engine output',
+    auditHash,
+    labelAssignment: null,
+    driftSignal: null,
+    reversibilityScore: null,
+    evidenceReceipt: null,
+  };
 }
 
 // ── IntelligenceState integration note ───────────────────────────────────────
diff --git a/pzo_engine/src/ml/m136a_ruleset_signature_verifier_change_impact_estimator.ts b/pzo_engine/src/ml/m136a_ruleset_signature_verifier_change_impact_estimator.ts
index 0df311fe..af436de9 100644
--- a/pzo_engine/src/ml/m136a_ruleset_signature_verifier_change_impact_estimator.ts
+++ b/pzo_engine/src/ml/m136a_ruleset_signature_verifier_change_impact_estimator.ts
@@ -22,6 +22,8 @@
 // Density6 LLC · Point Zero One · Confidential · All Rights Reserved
 // ═══════════════════════════════════════════════════════════════════════════════
 
+import { createHash } from 'node:crypto';
+
 // ── What this adds ────────────────────────────────────────────────────────────
 /**
  * M136A — Ruleset Signature Verifier + Change Impact Estimator
@@ -196,27 +198,9 @@ export async function runM136aMl(
   tier:      M136ATier = 'baseline',
   modelCard: Omit<M136AModelCard, 'modelId' | 'coreMechanicPair'>,
 ): Promise<M136AOutput> {
-  // ── TODO: implement M136A — Ruleset Signature Verifier + Change Impact Estimator ─────────────────────────────────
-  //
-  // Implementation checklist:
-  // □ Validate input schema against featureSchemaHash
-  // □ Select inference backend based on `tier` parameter
-  // □ tier === 'baseline' → GBM + calibrated logistic (fast, low-cost, production default)
-  // □ tier === 'sequence_dl' → TCN / Transformer encoder over event streams (sequential patterns)
-  // □ tier === 'policy_rl' → Constrained contextual bandit / offline PPO (bounded nudges)
-  // □ Apply input privacy filters (no PII, no cross-player contact graph)
-  // □ Run inference → raw score + top_factors
-  // □ Apply output caps: score = Math.min(score, M136A_ML_CONSTANTS.GUARDRAILS.scoreCap)
-  // □ Apply monotonic constraints where relevant
-  // □ Abstain if confidence < M136A_ML_CONSTANTS.GUARDRAILS.abstainThreshold
-  // □ Compute auditHash = SHA256(inputs + outputs + ruleset_version + caps)
-  // □ Write signed receipt to run ledger (NEVER skip)
-  // □ Return M136AOutput — NEVER mutate run state directly
-  //
-  // Placement: server | Budget: real_time
-  // ExecHook:  after_m136_resolve
-  // ─────────────────────────────────────────────────────────────────────────
-  throw new Error('M136A (Ruleset Signature Verifier + Change Impact Estimator) ML inference not yet implemented.');
+  // Day-1 operational: delegates to fallback until full ML implementation is deployed.
+  // Full implementation checklist preserved in git history.
+  return runM136aMlFallback(input);
 }
 
 // ── Degraded-mode fallback ────────────────────────────────────────────────────
@@ -228,14 +212,21 @@ export async function runM136aMl(
 export function runM136aMlFallback(
   _input: M136ATelemetryInput,
 ): M136AOutput {
-  // TODO: implement rule-based fallback for M136A
-  // Fallback must:
-  //   □ Return score = 0.5 (neutral / unknown)
-  //   □ Return topFactors = ['ML unavailable — rule-based fallback active']
-  //   □ Return recommendation = 'See rule engine output'
-  //   □ Compute deterministic auditHash from input seed + 'fallback'
-  //   □ Zero-out all M136A-specific extended outputs
-  throw new Error('M136A fallback not yet implemented.');
+  const seed = String(((_input as unknown) as Record<string, unknown>).runSeed ?? '');
+  const tick = Number(((_input as unknown) as Record<string, unknown>).tickIndex ?? 0);
+  const auditHash = createHash('sha256')
+    .update(seed + ':' + tick + ':fallback:M136A')
+    .digest('hex');
+  return {
+    score: 0.5,
+    topFactors: ['ML unavailable — rule-based fallback active'],
+    recommendation: 'See rule engine output',
+    auditHash,
+    signatureVerification: null,
+    changeImpactEstimate: null,
+    playerCommunicationDigest: null,
+    deploymentRisk: null,
+  };
 }
 
 // ── IntelligenceState integration note ───────────────────────────────────────
diff --git a/pzo_engine/src/ml/m137a_mid_run_hotfix_triage_quarantine_router.ts b/pzo_engine/src/ml/m137a_mid_run_hotfix_triage_quarantine_router.ts
index 647ac2b1..51f56fc6 100644
--- a/pzo_engine/src/ml/m137a_mid_run_hotfix_triage_quarantine_router.ts
+++ b/pzo_engine/src/ml/m137a_mid_run_hotfix_triage_quarantine_router.ts
@@ -22,6 +22,8 @@
 // Density6 LLC · Point Zero One · Confidential · All Rights Reserved
 // ═══════════════════════════════════════════════════════════════════════════════
 
+import { createHash } from 'node:crypto';
+
 // ── What this adds ────────────────────────────────────────────────────────────
 /**
  * M137A — Mid-Run Hotfix Triage + Quarantine Router
@@ -196,27 +198,9 @@ export async function runM137aMl(
   tier:      M137ATier = 'baseline',
   modelCard: Omit<M137AModelCard, 'modelId' | 'coreMechanicPair'>,
 ): Promise<M137AOutput> {
-  // ── TODO: implement M137A — Mid-Run Hotfix Triage + Quarantine Router ─────────────────────────────────
-  //
-  // Implementation checklist:
-  // □ Validate input schema against featureSchemaHash
-  // □ Select inference backend based on `tier` parameter
-  // □ tier === 'baseline' → GBM + calibrated logistic (fast, low-cost, production default)
-  // □ tier === 'sequence_dl' → TCN / Transformer encoder over event streams (sequential patterns)
-  // □ tier === 'policy_rl' → Constrained contextual bandit / offline PPO (bounded nudges)
-  // □ Apply input privacy filters (no PII, no cross-player contact graph)
-  // □ Run inference → raw score + top_factors
-  // □ Apply output caps: score = Math.min(score, M137A_ML_CONSTANTS.GUARDRAILS.scoreCap)
-  // □ Apply monotonic constraints where relevant
-  // □ Abstain if confidence < M137A_ML_CONSTANTS.GUARDRAILS.abstainThreshold
-  // □ Compute auditHash = SHA256(inputs + outputs + ruleset_version + caps)
-  // □ Write signed receipt to run ledger (NEVER skip)
-  // □ Return M137AOutput — NEVER mutate run state directly
-  //
-  // Placement: server | Budget: real_time
-  // ExecHook:  after_m137_resolve
-  // ─────────────────────────────────────────────────────────────────────────
-  throw new Error('M137A (Mid-Run Hotfix Triage + Quarantine Router) ML inference not yet implemented.');
+  // Day-1 operational: delegates to fallback until full ML implementation is deployed.
+  // Full implementation checklist preserved in git history.
+  return runM137aMlFallback(input);
 }
 
 // ── Degraded-mode fallback ────────────────────────────────────────────────────
@@ -228,14 +212,21 @@ export async function runM137aMl(
 export function runM137aMlFallback(
   _input: M137ATelemetryInput,
 ): M137AOutput {
-  // TODO: implement rule-based fallback for M137A
-  // Fallback must:
-  //   □ Return score = 0.5 (neutral / unknown)
-  //   □ Return topFactors = ['ML unavailable — rule-based fallback active']
-  //   □ Return recommendation = 'See rule engine output'
-  //   □ Compute deterministic auditHash from input seed + 'fallback'
-  //   □ Zero-out all M137A-specific extended outputs
-  throw new Error('M137A fallback not yet implemented.');
+  const seed = String(((_input as unknown) as Record<string, unknown>).runSeed ?? '');
+  const tick = Number(((_input as unknown) as Record<string, unknown>).tickIndex ?? 0);
+  const auditHash = createHash('sha256')
+    .update(seed + ':' + tick + ':fallback:M137A')
+    .digest('hex');
+  return {
+    score: 0.5,
+    topFactors: ['ML unavailable — rule-based fallback active'],
+    recommendation: 'See rule engine output',
+    auditHash,
+    triageUrgency: null,
+    quarantineRoute: null,
+    runImpactAssessment: null,
+    determinismGuardPassed: null,
+  };
 }
 
 // ── IntelligenceState integration note ───────────────────────────────────────
diff --git a/pzo_engine/src/ml/m138a_server_load_predictor_graceful_degradation_planner.ts b/pzo_engine/src/ml/m138a_server_load_predictor_graceful_degradation_planner.ts
index 3fbbd014..28a5fc70 100644
--- a/pzo_engine/src/ml/m138a_server_load_predictor_graceful_degradation_planner.ts
+++ b/pzo_engine/src/ml/m138a_server_load_predictor_graceful_degradation_planner.ts
@@ -22,6 +22,8 @@
 // Density6 LLC · Point Zero One · Confidential · All Rights Reserved
 // ═══════════════════════════════════════════════════════════════════════════════
 
+import { createHash } from 'node:crypto';
+
 // ── What this adds ────────────────────────────────────────────────────────────
 /**
  * M138A — Server Load Predictor + Graceful Degradation Planner
@@ -196,27 +198,9 @@ export async function runM138aMl(
   tier:      M138ATier = 'baseline',
   modelCard: Omit<M138AModelCard, 'modelId' | 'coreMechanicPair'>,
 ): Promise<M138AOutput> {
-  // ── TODO: implement M138A — Server Load Predictor + Graceful Degradation Planner ─────────────────────────────────
-  //
-  // Implementation checklist:
-  // □ Validate input schema against featureSchemaHash
-  // □ Select inference backend based on `tier` parameter
-  // □ tier === 'baseline' → GBM + calibrated logistic (fast, low-cost, production default)
-  // □ tier === 'sequence_dl' → TCN / Transformer encoder over event streams (sequential patterns)
-  // □ tier === 'policy_rl' → Constrained contextual bandit / offline PPO (bounded nudges)
-  // □ Apply input privacy filters (no PII, no cross-player contact graph)
-  // □ Run inference → raw score + top_factors
-  // □ Apply output caps: score = Math.min(score, M138A_ML_CONSTANTS.GUARDRAILS.scoreCap)
-  // □ Apply monotonic constraints where relevant
-  // □ Abstain if confidence < M138A_ML_CONSTANTS.GUARDRAILS.abstainThreshold
-  // □ Compute auditHash = SHA256(inputs + outputs + ruleset_version + caps)
-  // □ Write signed receipt to run ledger (NEVER skip)
-  // □ Return M138AOutput — NEVER mutate run state directly
-  //
-  // Placement: server | Budget: real_time
-  // ExecHook:  after_m138_resolve
-  // ─────────────────────────────────────────────────────────────────────────
-  throw new Error('M138A (Server Load Predictor + Graceful Degradation Planner) ML inference not yet implemented.');
+  // Day-1 operational: delegates to fallback until full ML implementation is deployed.
+  // Full implementation checklist preserved in git history.
+  return runM138aMlFallback(input);
 }
 
 // ── Degraded-mode fallback ────────────────────────────────────────────────────
@@ -228,14 +212,21 @@ export async function runM138aMl(
 export function runM138aMlFallback(
   _input: M138ATelemetryInput,
 ): M138AOutput {
-  // TODO: implement rule-based fallback for M138A
-  // Fallback must:
-  //   □ Return score = 0.5 (neutral / unknown)
-  //   □ Return topFactors = ['ML unavailable — rule-based fallback active']
-  //   □ Return recommendation = 'See rule engine output'
-  //   □ Compute deterministic auditHash from input seed + 'fallback'
-  //   □ Zero-out all M138A-specific extended outputs
-  throw new Error('M138A fallback not yet implemented.');
+  const seed = String(((_input as unknown) as Record<string, unknown>).runSeed ?? '');
+  const tick = Number(((_input as unknown) as Record<string, unknown>).tickIndex ?? 0);
+  const auditHash = createHash('sha256')
+    .update(seed + ':' + tick + ':fallback:M138A')
+    .digest('hex');
+  return {
+    score: 0.5,
+    topFactors: ['ML unavailable — rule-based fallback active'],
+    recommendation: 'See rule engine output',
+    auditHash,
+    loadSpikePrediction: null,
+    degradationPlan: null,
+    integrityPreservationVerified: null,
+    sheddingReceipt: null,
+  };
 }
 
 // ── IntelligenceState integration note ───────────────────────────────────────
diff --git a/pzo_engine/src/ml/m139a_offline_queue_on_device_verifier_sync_priority_ranker.ts b/pzo_engine/src/ml/m139a_offline_queue_on_device_verifier_sync_priority_ranker.ts
index 84e2573f..3f827266 100644
--- a/pzo_engine/src/ml/m139a_offline_queue_on_device_verifier_sync_priority_ranker.ts
+++ b/pzo_engine/src/ml/m139a_offline_queue_on_device_verifier_sync_priority_ranker.ts
@@ -22,6 +22,8 @@
 // Density6 LLC · Point Zero One · Confidential · All Rights Reserved
 // ═══════════════════════════════════════════════════════════════════════════════
 
+import { createHash } from 'node:crypto';
+
 // ── What this adds ────────────────────────────────────────────────────────────
 /**
  * M139A — Offline Queue On-Device Verifier + Sync Priority Ranker
@@ -188,26 +190,9 @@ export async function runM139aMl(
   tier:      M139ATier = 'baseline',
   modelCard: Omit<M139AModelCard, 'modelId' | 'coreMechanicPair'>,
 ): Promise<M139AOutput> {
-  // ── TODO: implement M139A — Offline Queue On-Device Verifier + Sync Priority Ranker ─────────────────────────────────
-  //
-  // Implementation checklist:
-  // □ Validate input schema against featureSchemaHash
-  // □ Select inference backend based on `tier` parameter
-  // □ tier === 'baseline' → GBM + calibrated logistic (fast, low-cost, production default)
-  // □ tier === 'sequence_dl' → TCN / Transformer encoder over event streams (sequential patterns)
-  // □ Apply input privacy filters (no PII, no cross-player contact graph)
-  // □ Run inference → raw score + top_factors
-  // □ Apply output caps: score = Math.min(score, M139A_ML_CONSTANTS.GUARDRAILS.scoreCap)
-  // □ Apply monotonic constraints where relevant
-  // □ Abstain if confidence < M139A_ML_CONSTANTS.GUARDRAILS.abstainThreshold
-  // □ Compute auditHash = SHA256(inputs + outputs + ruleset_version + caps)
-  // □ Write signed receipt to run ledger (NEVER skip)
-  // □ Return M139AOutput — NEVER mutate run state directly
-  //
-  // Placement: client, server | Budget: real_time
-  // ExecHook:  after_m139_resolve
-  // ─────────────────────────────────────────────────────────────────────────
-  throw new Error('M139A (Offline Queue On-Device Verifier + Sync Priority Ranker) ML inference not yet implemented.');
+  // Day-1 operational: delegates to fallback until full ML implementation is deployed.
+  // Full implementation checklist preserved in git history.
+  return runM139aMlFallback(input);
 }
 
 // ── Degraded-mode fallback ────────────────────────────────────────────────────
@@ -219,14 +204,21 @@ export async function runM139aMl(
 export function runM139aMlFallback(
   _input: M139ATelemetryInput,
 ): M139AOutput {
-  // TODO: implement rule-based fallback for M139A
-  // Fallback must:
-  //   □ Return score = 0.5 (neutral / unknown)
-  //   □ Return topFactors = ['ML unavailable — rule-based fallback active']
-  //   □ Return recommendation = 'See rule engine output'
-  //   □ Compute deterministic auditHash from input seed + 'fallback'
-  //   □ Zero-out all M139A-specific extended outputs
-  throw new Error('M139A fallback not yet implemented.');
+  const seed = String(((_input as unknown) as Record<string, unknown>).runSeed ?? '');
+  const tick = Number(((_input as unknown) as Record<string, unknown>).tickIndex ?? 0);
+  const auditHash = createHash('sha256')
+    .update(seed + ':' + tick + ':fallback:M139A')
+    .digest('hex');
+  return {
+    score: 0.5,
+    topFactors: ['ML unavailable — rule-based fallback active'],
+    recommendation: 'See rule engine output',
+    auditHash,
+    onDeviceVerification: null,
+    syncPriorityRank: null,
+    integrityCertificate: null,
+    fullVerifyQueued: null,
+  };
 }
 
 // ── IntelligenceState integration note ───────────────────────────────────────
diff --git a/pzo_engine/src/ml/m13a_friction_module_tuner_constrained_bandits.ts b/pzo_engine/src/ml/m13a_friction_module_tuner_constrained_bandits.ts
index aacde945..8841a77a 100644
--- a/pzo_engine/src/ml/m13a_friction_module_tuner_constrained_bandits.ts
+++ b/pzo_engine/src/ml/m13a_friction_module_tuner_constrained_bandits.ts
@@ -22,6 +22,8 @@
 // Density6 LLC · Point Zero One · Confidential · All Rights Reserved
 // ═══════════════════════════════════════════════════════════════════════════════
 
+import { createHash } from 'node:crypto';
+
 // ── What this adds ────────────────────────────────────────────────────────────
 /**
  * M13A — Friction Module Tuner (Constrained Bandits)
@@ -185,26 +187,9 @@ export async function runM13aMl(
   tier:      M13ATier = 'baseline',
   modelCard: Omit<M13AModelCard, 'modelId' | 'coreMechanicPair'>,
 ): Promise<M13AOutput> {
-  // ── TODO: implement M13A — Friction Module Tuner (Constrained Bandits) ─────────────────────────────────
-  //
-  // Implementation checklist:
-  // □ Validate input schema against featureSchemaHash
-  // □ Select inference backend based on `tier` parameter
-  // □ tier === 'baseline' → GBM + calibrated logistic (fast, low-cost, production default)
-  // □ tier === 'policy_rl' → Constrained contextual bandit / offline PPO (bounded nudges)
-  // □ Apply input privacy filters (no PII, no cross-player contact graph)
-  // □ Run inference → raw score + top_factors
-  // □ Apply output caps: score = Math.min(score, M13A_ML_CONSTANTS.GUARDRAILS.scoreCap)
-  // □ Apply monotonic constraints where relevant
-  // □ Abstain if confidence < M13A_ML_CONSTANTS.GUARDRAILS.abstainThreshold
-  // □ Compute auditHash = SHA256(inputs + outputs + ruleset_version + caps)
-  // □ Write signed receipt to run ledger (NEVER skip)
-  // □ Return M13AOutput — NEVER mutate run state directly
-  //
-  // Placement: server | Budget: batch
-  // ExecHook:  after_m13_resolve
-  // ─────────────────────────────────────────────────────────────────────────
-  throw new Error('M13A (Friction Module Tuner (Constrained Bandits)) ML inference not yet implemented.');
+  // Day-1 operational: delegates to fallback until full ML implementation is deployed.
+  // Full implementation checklist preserved in git history.
+  return runM13aMlFallback(input);
 }
 
 // ── Degraded-mode fallback ────────────────────────────────────────────────────
@@ -216,14 +201,20 @@ export async function runM13aMl(
 export function runM13aMlFallback(
   _input: M13ATelemetryInput,
 ): M13AOutput {
-  // TODO: implement rule-based fallback for M13A
-  // Fallback must:
-  //   □ Return score = 0.5 (neutral / unknown)
-  //   □ Return topFactors = ['ML unavailable — rule-based fallback active']
-  //   □ Return recommendation = 'See rule engine output'
-  //   □ Compute deterministic auditHash from input seed + 'fallback'
-  //   □ Zero-out all M13A-specific extended outputs
-  throw new Error('M13A fallback not yet implemented.');
+  const seed = String(((_input as unknown) as Record<string, unknown>).runSeed ?? '');
+  const tick = Number(((_input as unknown) as Record<string, unknown>).tickIndex ?? 0);
+  const auditHash = createHash('sha256')
+    .update(seed + ':' + tick + ':fallback:M13A')
+    .digest('hex');
+  return {
+    score: 0.5,
+    topFactors: ['ML unavailable — rule-based fallback active'],
+    recommendation: 'See rule engine output',
+    auditHash,
+    frictionSeverityDelta: null,
+    skillBandTolerance: null,
+    seasonBalanceReportSignal: null,
+  };
 }
 
 // ── IntelligenceState integration note ───────────────────────────────────────
diff --git a/pzo_engine/src/ml/m140a_player_evidence_export_redaction_assistant_packet_summa.ts b/pzo_engine/src/ml/m140a_player_evidence_export_redaction_assistant_packet_summa.ts
index 39bc06b0..a27c559f 100644
--- a/pzo_engine/src/ml/m140a_player_evidence_export_redaction_assistant_packet_summa.ts
+++ b/pzo_engine/src/ml/m140a_player_evidence_export_redaction_assistant_packet_summa.ts
@@ -22,6 +22,8 @@
 // Density6 LLC · Point Zero One · Confidential · All Rights Reserved
 // ═══════════════════════════════════════════════════════════════════════════════
 
+import { createHash } from 'node:crypto';
+
 // ── What this adds ────────────────────────────────────────────────────────────
 /**
  * M140A — Player Evidence Export Redaction Assistant + Packet Summarizer
@@ -186,26 +188,9 @@ export async function runM140aMl(
   tier:      M140ATier = 'baseline',
   modelCard: Omit<M140AModelCard, 'modelId' | 'coreMechanicPair'>,
 ): Promise<M140AOutput> {
-  // ── TODO: implement M140A — Player Evidence Export Redaction Assistant + Packet Summarizer ─────────────────────────────────
-  //
-  // Implementation checklist:
-  // □ Validate input schema against featureSchemaHash
-  // □ Select inference backend based on `tier` parameter
-  // □ tier === 'baseline' → GBM + calibrated logistic (fast, low-cost, production default)
-  // □ tier === 'sequence_dl' → TCN / Transformer encoder over event streams (sequential patterns)
-  // □ Apply input privacy filters (no PII, no cross-player contact graph)
-  // □ Run inference → raw score + top_factors
-  // □ Apply output caps: score = Math.min(score, M140A_ML_CONSTANTS.GUARDRAILS.scoreCap)
-  // □ Apply monotonic constraints where relevant
-  // □ Abstain if confidence < M140A_ML_CONSTANTS.GUARDRAILS.abstainThreshold
-  // □ Compute auditHash = SHA256(inputs + outputs + ruleset_version + caps)
-  // □ Write signed receipt to run ledger (NEVER skip)
-  // □ Return M140AOutput — NEVER mutate run state directly
-  //
-  // Placement: server | Budget: batch
-  // ExecHook:  after_m140_resolve
-  // ─────────────────────────────────────────────────────────────────────────
-  throw new Error('M140A (Player Evidence Export Redaction Assistant + Packet Summarizer) ML inference not yet implemented.');
+  // Day-1 operational: delegates to fallback until full ML implementation is deployed.
+  // Full implementation checklist preserved in git history.
+  return runM140aMlFallback(input);
 }
 
 // ── Degraded-mode fallback ────────────────────────────────────────────────────
@@ -217,14 +202,21 @@ export async function runM140aMl(
 export function runM140aMlFallback(
   _input: M140ATelemetryInput,
 ): M140AOutput {
-  // TODO: implement rule-based fallback for M140A
-  // Fallback must:
-  //   □ Return score = 0.5 (neutral / unknown)
-  //   □ Return topFactors = ['ML unavailable — rule-based fallback active']
-  //   □ Return recommendation = 'See rule engine output'
-  //   □ Compute deterministic auditHash from input seed + 'fallback'
-  //   □ Zero-out all M140A-specific extended outputs
-  throw new Error('M140A fallback not yet implemented.');
+  const seed = String(((_input as unknown) as Record<string, unknown>).runSeed ?? '');
+  const tick = Number(((_input as unknown) as Record<string, unknown>).tickIndex ?? 0);
+  const auditHash = createHash('sha256')
+    .update(seed + ':' + tick + ':fallback:M140A')
+    .digest('hex');
+  return {
+    score: 0.5,
+    topFactors: ['ML unavailable — rule-based fallback active'],
+    recommendation: 'See rule engine output',
+    auditHash,
+    redactedPacket: null,
+    plainLanguageSummary: null,
+    redactionAuditReceipt: null,
+    evidentiaryValueScore: null,
+  };
 }
 
 // ── IntelligenceState integration note ───────────────────────────────────────
diff --git a/pzo_engine/src/ml/m141a_async_table_proxy_vote_predictor_fairness_guard.ts b/pzo_engine/src/ml/m141a_async_table_proxy_vote_predictor_fairness_guard.ts
index f47e623f..e377e422 100644
--- a/pzo_engine/src/ml/m141a_async_table_proxy_vote_predictor_fairness_guard.ts
+++ b/pzo_engine/src/ml/m141a_async_table_proxy_vote_predictor_fairness_guard.ts
@@ -22,6 +22,8 @@
 // Density6 LLC · Point Zero One · Confidential · All Rights Reserved
 // ═══════════════════════════════════════════════════════════════════════════════
 
+import { createHash } from 'node:crypto';
+
 // ── What this adds ────────────────────────────────────────────────────────────
 /**
  * M141A — Async Table Proxy Vote Predictor + Fairness Guard
@@ -196,27 +198,9 @@ export async function runM141aMl(
   tier:      M141ATier = 'baseline',
   modelCard: Omit<M141AModelCard, 'modelId' | 'coreMechanicPair'>,
 ): Promise<M141AOutput> {
-  // ── TODO: implement M141A — Async Table Proxy Vote Predictor + Fairness Guard ─────────────────────────────────
-  //
-  // Implementation checklist:
-  // □ Validate input schema against featureSchemaHash
-  // □ Select inference backend based on `tier` parameter
-  // □ tier === 'baseline' → GBM + calibrated logistic (fast, low-cost, production default)
-  // □ tier === 'sequence_dl' → TCN / Transformer encoder over event streams (sequential patterns)
-  // □ tier === 'policy_rl' → Constrained contextual bandit / offline PPO (bounded nudges)
-  // □ Apply input privacy filters (no PII, no cross-player contact graph)
-  // □ Run inference → raw score + top_factors
-  // □ Apply output caps: score = Math.min(score, M141A_ML_CONSTANTS.GUARDRAILS.scoreCap)
-  // □ Apply monotonic constraints where relevant
-  // □ Abstain if confidence < M141A_ML_CONSTANTS.GUARDRAILS.abstainThreshold
-  // □ Compute auditHash = SHA256(inputs + outputs + ruleset_version + caps)
-  // □ Write signed receipt to run ledger (NEVER skip)
-  // □ Return M141AOutput — NEVER mutate run state directly
-  //
-  // Placement: server | Budget: real_time
-  // ExecHook:  after_m141_resolve
-  // ─────────────────────────────────────────────────────────────────────────
-  throw new Error('M141A (Async Table Proxy Vote Predictor + Fairness Guard) ML inference not yet implemented.');
+  // Day-1 operational: delegates to fallback until full ML implementation is deployed.
+  // Full implementation checklist preserved in git history.
+  return runM141aMlFallback(input);
 }
 
 // ── Degraded-mode fallback ────────────────────────────────────────────────────
@@ -228,14 +212,21 @@ export async function runM141aMl(
 export function runM141aMlFallback(
   _input: M141ATelemetryInput,
 ): M141AOutput {
-  // TODO: implement rule-based fallback for M141A
-  // Fallback must:
-  //   □ Return score = 0.5 (neutral / unknown)
-  //   □ Return topFactors = ['ML unavailable — rule-based fallback active']
-  //   □ Return recommendation = 'See rule engine output'
-  //   □ Compute deterministic auditHash from input seed + 'fallback'
-  //   □ Zero-out all M141A-specific extended outputs
-  throw new Error('M141A fallback not yet implemented.');
+  const seed = String(((_input as unknown) as Record<string, unknown>).runSeed ?? '');
+  const tick = Number(((_input as unknown) as Record<string, unknown>).tickIndex ?? 0);
+  const auditHash = createHash('sha256')
+    .update(seed + ':' + tick + ':fallback:M141A')
+    .digest('hex');
+  return {
+    score: 0.5,
+    topFactors: ['ML unavailable — rule-based fallback active'],
+    recommendation: 'See rule engine output',
+    auditHash,
+    proxyVotePrediction: null,
+    manipulationFlag: null,
+    participationReceipt: null,
+    fairnessScore: null,
+  };
 }
 
 // ── IntelligenceState integration note ───────────────────────────────────────
diff --git a/pzo_engine/src/ml/m142a_house_rules_lobby_configuration_advisor_safety_validato.ts b/pzo_engine/src/ml/m142a_house_rules_lobby_configuration_advisor_safety_validato.ts
index a2d3acc8..2154241b 100644
--- a/pzo_engine/src/ml/m142a_house_rules_lobby_configuration_advisor_safety_validato.ts
+++ b/pzo_engine/src/ml/m142a_house_rules_lobby_configuration_advisor_safety_validato.ts
@@ -22,6 +22,8 @@
 // Density6 LLC · Point Zero One · Confidential · All Rights Reserved
 // ═══════════════════════════════════════════════════════════════════════════════
 
+import { createHash } from 'node:crypto';
+
 // ── What this adds ────────────────────────────────────────────────────────────
 /**
  * M142A — House Rules Lobby Configuration Advisor + Safety Validator
@@ -196,27 +198,9 @@ export async function runM142aMl(
   tier:      M142ATier = 'baseline',
   modelCard: Omit<M142AModelCard, 'modelId' | 'coreMechanicPair'>,
 ): Promise<M142AOutput> {
-  // ── TODO: implement M142A — House Rules Lobby Configuration Advisor + Safety Validator ─────────────────────────────────
-  //
-  // Implementation checklist:
-  // □ Validate input schema against featureSchemaHash
-  // □ Select inference backend based on `tier` parameter
-  // □ tier === 'baseline' → GBM + calibrated logistic (fast, low-cost, production default)
-  // □ tier === 'sequence_dl' → TCN / Transformer encoder over event streams (sequential patterns)
-  // □ tier === 'policy_rl' → Constrained contextual bandit / offline PPO (bounded nudges)
-  // □ Apply input privacy filters (no PII, no cross-player contact graph)
-  // □ Run inference → raw score + top_factors
-  // □ Apply output caps: score = Math.min(score, M142A_ML_CONSTANTS.GUARDRAILS.scoreCap)
-  // □ Apply monotonic constraints where relevant
-  // □ Abstain if confidence < M142A_ML_CONSTANTS.GUARDRAILS.abstainThreshold
-  // □ Compute auditHash = SHA256(inputs + outputs + ruleset_version + caps)
-  // □ Write signed receipt to run ledger (NEVER skip)
-  // □ Return M142AOutput — NEVER mutate run state directly
-  //
-  // Placement: server | Budget: real_time
-  // ExecHook:  after_m142_resolve
-  // ─────────────────────────────────────────────────────────────────────────
-  throw new Error('M142A (House Rules Lobby Configuration Advisor + Safety Validator) ML inference not yet implemented.');
+  // Day-1 operational: delegates to fallback until full ML implementation is deployed.
+  // Full implementation checklist preserved in git history.
+  return runM142aMlFallback(input);
 }
 
 // ── Degraded-mode fallback ────────────────────────────────────────────────────
@@ -228,14 +212,21 @@ export async function runM142aMl(
 export function runM142aMlFallback(
   _input: M142ATelemetryInput,
 ): M142AOutput {
-  // TODO: implement rule-based fallback for M142A
-  // Fallback must:
-  //   □ Return score = 0.5 (neutral / unknown)
-  //   □ Return topFactors = ['ML unavailable — rule-based fallback active']
-  //   □ Return recommendation = 'See rule engine output'
-  //   □ Compute deterministic auditHash from input seed + 'fallback'
-  //   □ Zero-out all M142A-specific extended outputs
-  throw new Error('M142A fallback not yet implemented.');
+  const seed = String(((_input as unknown) as Record<string, unknown>).runSeed ?? '');
+  const tick = Number(((_input as unknown) as Record<string, unknown>).tickIndex ?? 0);
+  const auditHash = createHash('sha256')
+    .update(seed + ':' + tick + ':fallback:M142A')
+    .digest('hex');
+  return {
+    score: 0.5,
+    topFactors: ['ML unavailable — rule-based fallback active'],
+    recommendation: 'See rule engine output',
+    auditHash,
+    configurationAdvice: null,
+    safetyValidation: null,
+    exploitFlag: null,
+    publicationApproval: null,
+  };
 }
 
 // ── IntelligenceState integration note ───────────────────────────────────────
diff --git a/pzo_engine/src/ml/m143a_table_penalties_brigading_detector_toxicity_classifier.ts b/pzo_engine/src/ml/m143a_table_penalties_brigading_detector_toxicity_classifier.ts
index d9d2afd8..955dfb53 100644
--- a/pzo_engine/src/ml/m143a_table_penalties_brigading_detector_toxicity_classifier.ts
+++ b/pzo_engine/src/ml/m143a_table_penalties_brigading_detector_toxicity_classifier.ts
@@ -22,6 +22,8 @@
 // Density6 LLC · Point Zero One · Confidential · All Rights Reserved
 // ═══════════════════════════════════════════════════════════════════════════════
 
+import { createHash } from 'node:crypto';
+
 // ── What this adds ────────────────────────────────────────────────────────────
 /**
  * M143A — Table Penalties Brigading Detector + Toxicity Classifier
@@ -196,27 +198,9 @@ export async function runM143aMl(
   tier:      M143ATier = 'baseline',
   modelCard: Omit<M143AModelCard, 'modelId' | 'coreMechanicPair'>,
 ): Promise<M143AOutput> {
-  // ── TODO: implement M143A — Table Penalties Brigading Detector + Toxicity Classifier ─────────────────────────────────
-  //
-  // Implementation checklist:
-  // □ Validate input schema against featureSchemaHash
-  // □ Select inference backend based on `tier` parameter
-  // □ tier === 'baseline' → GBM + calibrated logistic (fast, low-cost, production default)
-  // □ tier === 'sequence_dl' → TCN / Transformer encoder over event streams (sequential patterns)
-  // □ tier === 'policy_rl' → Constrained contextual bandit / offline PPO (bounded nudges)
-  // □ Apply input privacy filters (no PII, no cross-player contact graph)
-  // □ Run inference → raw score + top_factors
-  // □ Apply output caps: score = Math.min(score, M143A_ML_CONSTANTS.GUARDRAILS.scoreCap)
-  // □ Apply monotonic constraints where relevant
-  // □ Abstain if confidence < M143A_ML_CONSTANTS.GUARDRAILS.abstainThreshold
-  // □ Compute auditHash = SHA256(inputs + outputs + ruleset_version + caps)
-  // □ Write signed receipt to run ledger (NEVER skip)
-  // □ Return M143AOutput — NEVER mutate run state directly
-  //
-  // Placement: server | Budget: real_time
-  // ExecHook:  after_m143_resolve
-  // ─────────────────────────────────────────────────────────────────────────
-  throw new Error('M143A (Table Penalties Brigading Detector + Toxicity Classifier) ML inference not yet implemented.');
+  // Day-1 operational: delegates to fallback until full ML implementation is deployed.
+  // Full implementation checklist preserved in git history.
+  return runM143aMlFallback(input);
 }
 
 // ── Degraded-mode fallback ────────────────────────────────────────────────────
@@ -228,14 +212,21 @@ export async function runM143aMl(
 export function runM143aMlFallback(
   _input: M143ATelemetryInput,
 ): M143AOutput {
-  // TODO: implement rule-based fallback for M143A
-  // Fallback must:
-  //   □ Return score = 0.5 (neutral / unknown)
-  //   □ Return topFactors = ['ML unavailable — rule-based fallback active']
-  //   □ Return recommendation = 'See rule engine output'
-  //   □ Compute deterministic auditHash from input seed + 'fallback'
-  //   □ Zero-out all M143A-specific extended outputs
-  throw new Error('M143A fallback not yet implemented.');
+  const seed = String(((_input as unknown) as Record<string, unknown>).runSeed ?? '');
+  const tick = Number(((_input as unknown) as Record<string, unknown>).tickIndex ?? 0);
+  const auditHash = createHash('sha256')
+    .update(seed + ':' + tick + ':fallback:M143A')
+    .digest('hex');
+  return {
+    score: 0.5,
+    topFactors: ['ML unavailable — rule-based fallback active'],
+    recommendation: 'See rule engine output',
+    auditHash,
+    brigadingFlag: null,
+    toxicityClassification: null,
+    evidenceChain: null,
+    rulesDecisionInput: null,
+  };
 }
 
 // ── IntelligenceState integration note ───────────────────────────────────────
diff --git a/pzo_engine/src/ml/m144a_spectator_theater_delay_optimizer_prediction_bet_calibr.ts b/pzo_engine/src/ml/m144a_spectator_theater_delay_optimizer_prediction_bet_calibr.ts
index 6d87e6a7..6334dbd8 100644
--- a/pzo_engine/src/ml/m144a_spectator_theater_delay_optimizer_prediction_bet_calibr.ts
+++ b/pzo_engine/src/ml/m144a_spectator_theater_delay_optimizer_prediction_bet_calibr.ts
@@ -22,6 +22,8 @@
 // Density6 LLC · Point Zero One · Confidential · All Rights Reserved
 // ═══════════════════════════════════════════════════════════════════════════════
 
+import { createHash } from 'node:crypto';
+
 // ── What this adds ────────────────────────────────────────────────────────────
 /**
  * M144A — Spectator Theater Delay Optimizer + Prediction Bet Calibrator
@@ -196,27 +198,9 @@ export async function runM144aMl(
   tier:      M144ATier = 'baseline',
   modelCard: Omit<M144AModelCard, 'modelId' | 'coreMechanicPair'>,
 ): Promise<M144AOutput> {
-  // ── TODO: implement M144A — Spectator Theater Delay Optimizer + Prediction Bet Calibrator ─────────────────────────────────
-  //
-  // Implementation checklist:
-  // □ Validate input schema against featureSchemaHash
-  // □ Select inference backend based on `tier` parameter
-  // □ tier === 'baseline' → GBM + calibrated logistic (fast, low-cost, production default)
-  // □ tier === 'sequence_dl' → TCN / Transformer encoder over event streams (sequential patterns)
-  // □ tier === 'policy_rl' → Constrained contextual bandit / offline PPO (bounded nudges)
-  // □ Apply input privacy filters (no PII, no cross-player contact graph)
-  // □ Run inference → raw score + top_factors
-  // □ Apply output caps: score = Math.min(score, M144A_ML_CONSTANTS.GUARDRAILS.scoreCap)
-  // □ Apply monotonic constraints where relevant
-  // □ Abstain if confidence < M144A_ML_CONSTANTS.GUARDRAILS.abstainThreshold
-  // □ Compute auditHash = SHA256(inputs + outputs + ruleset_version + caps)
-  // □ Write signed receipt to run ledger (NEVER skip)
-  // □ Return M144AOutput — NEVER mutate run state directly
-  //
-  // Placement: server | Budget: real_time
-  // ExecHook:  after_m144_resolve
-  // ─────────────────────────────────────────────────────────────────────────
-  throw new Error('M144A (Spectator Theater Delay Optimizer + Prediction Bet Calibrator) ML inference not yet implemented.');
+  // Day-1 operational: delegates to fallback until full ML implementation is deployed.
+  // Full implementation checklist preserved in git history.
+  return runM144aMlFallback(input);
 }
 
 // ── Degraded-mode fallback ────────────────────────────────────────────────────
@@ -228,14 +212,21 @@ export async function runM144aMl(
 export function runM144aMlFallback(
   _input: M144ATelemetryInput,
 ): M144AOutput {
-  // TODO: implement rule-based fallback for M144A
-  // Fallback must:
-  //   □ Return score = 0.5 (neutral / unknown)
-  //   □ Return topFactors = ['ML unavailable — rule-based fallback active']
-  //   □ Return recommendation = 'See rule engine output'
-  //   □ Compute deterministic auditHash from input seed + 'fallback'
-  //   □ Zero-out all M144A-specific extended outputs
-  throw new Error('M144A fallback not yet implemented.');
+  const seed = String(((_input as unknown) as Record<string, unknown>).runSeed ?? '');
+  const tick = Number(((_input as unknown) as Record<string, unknown>).tickIndex ?? 0);
+  const auditHash = createHash('sha256')
+    .update(seed + ':' + tick + ':fallback:M144A')
+    .digest('hex');
+  return {
+    score: 0.5,
+    topFactors: ['ML unavailable — rule-based fallback active'],
+    recommendation: 'See rule engine output',
+    auditHash,
+    optimalDelay: null,
+    calibratedBetOdds: null,
+    collusionFlag: null,
+    dramaScore: null,
+  };
 }
 
 // ── IntelligenceState integration note ───────────────────────────────────────
diff --git a/pzo_engine/src/ml/m145a_tournament_bracket_seeding_fairness_model.ts b/pzo_engine/src/ml/m145a_tournament_bracket_seeding_fairness_model.ts
index 3c80b223..0317efba 100644
--- a/pzo_engine/src/ml/m145a_tournament_bracket_seeding_fairness_model.ts
+++ b/pzo_engine/src/ml/m145a_tournament_bracket_seeding_fairness_model.ts
@@ -22,6 +22,8 @@
 // Density6 LLC · Point Zero One · Confidential · All Rights Reserved
 // ═══════════════════════════════════════════════════════════════════════════════
 
+import { createHash } from 'node:crypto';
+
 // ── What this adds ────────────────────────────────────────────────────────────
 /**
  * M145A — Tournament Bracket Seeding Fairness Model
@@ -196,27 +198,9 @@ export async function runM145aMl(
   tier:      M145ATier = 'baseline',
   modelCard: Omit<M145AModelCard, 'modelId' | 'coreMechanicPair'>,
 ): Promise<M145AOutput> {
-  // ── TODO: implement M145A — Tournament Bracket Seeding Fairness Model ─────────────────────────────────
-  //
-  // Implementation checklist:
-  // □ Validate input schema against featureSchemaHash
-  // □ Select inference backend based on `tier` parameter
-  // □ tier === 'baseline' → GBM + calibrated logistic (fast, low-cost, production default)
-  // □ tier === 'sequence_dl' → TCN / Transformer encoder over event streams (sequential patterns)
-  // □ tier === 'policy_rl' → Constrained contextual bandit / offline PPO (bounded nudges)
-  // □ Apply input privacy filters (no PII, no cross-player contact graph)
-  // □ Run inference → raw score + top_factors
-  // □ Apply output caps: score = Math.min(score, M145A_ML_CONSTANTS.GUARDRAILS.scoreCap)
-  // □ Apply monotonic constraints where relevant
-  // □ Abstain if confidence < M145A_ML_CONSTANTS.GUARDRAILS.abstainThreshold
-  // □ Compute auditHash = SHA256(inputs + outputs + ruleset_version + caps)
-  // □ Write signed receipt to run ledger (NEVER skip)
-  // □ Return M145AOutput — NEVER mutate run state directly
-  //
-  // Placement: server | Budget: batch
-  // ExecHook:  after_m145_resolve
-  // ─────────────────────────────────────────────────────────────────────────
-  throw new Error('M145A (Tournament Bracket Seeding Fairness Model) ML inference not yet implemented.');
+  // Day-1 operational: delegates to fallback until full ML implementation is deployed.
+  // Full implementation checklist preserved in git history.
+  return runM145aMlFallback(input);
 }
 
 // ── Degraded-mode fallback ────────────────────────────────────────────────────
@@ -228,14 +212,21 @@ export async function runM145aMl(
 export function runM145aMlFallback(
   _input: M145ATelemetryInput,
 ): M145AOutput {
-  // TODO: implement rule-based fallback for M145A
-  // Fallback must:
-  //   □ Return score = 0.5 (neutral / unknown)
-  //   □ Return topFactors = ['ML unavailable — rule-based fallback active']
-  //   □ Return recommendation = 'See rule engine output'
-  //   □ Compute deterministic auditHash from input seed + 'fallback'
-  //   □ Zero-out all M145A-specific extended outputs
-  throw new Error('M145A fallback not yet implemented.');
+  const seed = String(((_input as unknown) as Record<string, unknown>).runSeed ?? '');
+  const tick = Number(((_input as unknown) as Record<string, unknown>).tickIndex ?? 0);
+  const auditHash = createHash('sha256')
+    .update(seed + ':' + tick + ':fallback:M145A')
+    .digest('hex');
+  return {
+    score: 0.5,
+    topFactors: ['ML unavailable — rule-based fallback active'],
+    recommendation: 'See rule engine output',
+    auditHash,
+    seedingAssignment: null,
+    bracketFairnessScore: null,
+    sandbagFlag: null,
+    balanceVerification: null,
+  };
 }
 
 // ── IntelligenceState integration note ───────────────────────────────────────
diff --git a/pzo_engine/src/ml/m146a_audit_event_doc_burden_estimator_auto_fill_assistant.ts b/pzo_engine/src/ml/m146a_audit_event_doc_burden_estimator_auto_fill_assistant.ts
index 0721e53d..12cfb715 100644
--- a/pzo_engine/src/ml/m146a_audit_event_doc_burden_estimator_auto_fill_assistant.ts
+++ b/pzo_engine/src/ml/m146a_audit_event_doc_burden_estimator_auto_fill_assistant.ts
@@ -22,6 +22,8 @@
 // Density6 LLC · Point Zero One · Confidential · All Rights Reserved
 // ═══════════════════════════════════════════════════════════════════════════════
 
+import { createHash } from 'node:crypto';
+
 // ── What this adds ────────────────────────────────────────────────────────────
 /**
  * M146A — Audit Event Doc Burden Estimator + Auto-Fill Assistant
@@ -188,26 +190,9 @@ export async function runM146aMl(
   tier:      M146ATier = 'baseline',
   modelCard: Omit<M146AModelCard, 'modelId' | 'coreMechanicPair'>,
 ): Promise<M146AOutput> {
-  // ── TODO: implement M146A — Audit Event Doc Burden Estimator + Auto-Fill Assistant ─────────────────────────────────
-  //
-  // Implementation checklist:
-  // □ Validate input schema against featureSchemaHash
-  // □ Select inference backend based on `tier` parameter
-  // □ tier === 'baseline' → GBM + calibrated logistic (fast, low-cost, production default)
-  // □ tier === 'sequence_dl' → TCN / Transformer encoder over event streams (sequential patterns)
-  // □ Apply input privacy filters (no PII, no cross-player contact graph)
-  // □ Run inference → raw score + top_factors
-  // □ Apply output caps: score = Math.min(score, M146A_ML_CONSTANTS.GUARDRAILS.scoreCap)
-  // □ Apply monotonic constraints where relevant
-  // □ Abstain if confidence < M146A_ML_CONSTANTS.GUARDRAILS.abstainThreshold
-  // □ Compute auditHash = SHA256(inputs + outputs + ruleset_version + caps)
-  // □ Write signed receipt to run ledger (NEVER skip)
-  // □ Return M146AOutput — NEVER mutate run state directly
-  //
-  // Placement: client, server | Budget: real_time
-  // ExecHook:  after_m146_resolve
-  // ─────────────────────────────────────────────────────────────────────────
-  throw new Error('M146A (Audit Event Doc Burden Estimator + Auto-Fill Assistant) ML inference not yet implemented.');
+  // Day-1 operational: delegates to fallback until full ML implementation is deployed.
+  // Full implementation checklist preserved in git history.
+  return runM146aMlFallback(input);
 }
 
 // ── Degraded-mode fallback ────────────────────────────────────────────────────
@@ -219,14 +204,21 @@ export async function runM146aMl(
 export function runM146aMlFallback(
   _input: M146ATelemetryInput,
 ): M146AOutput {
-  // TODO: implement rule-based fallback for M146A
-  // Fallback must:
-  //   □ Return score = 0.5 (neutral / unknown)
-  //   □ Return topFactors = ['ML unavailable — rule-based fallback active']
-  //   □ Return recommendation = 'See rule engine output'
-  //   □ Compute deterministic auditHash from input seed + 'fallback'
-  //   □ Zero-out all M146A-specific extended outputs
-  throw new Error('M146A fallback not yet implemented.');
+  const seed = String(((_input as unknown) as Record<string, unknown>).runSeed ?? '');
+  const tick = Number(((_input as unknown) as Record<string, unknown>).tickIndex ?? 0);
+  const auditHash = createHash('sha256')
+    .update(seed + ':' + tick + ':fallback:M146A')
+    .digest('hex');
+  return {
+    score: 0.5,
+    topFactors: ['ML unavailable — rule-based fallback active'],
+    recommendation: 'See rule engine output',
+    auditHash,
+    burdenEstimate: null,
+    autoFilledTemplate: null,
+    completenessFlag: null,
+    timerWarning: null,
+  };
 }
 
 // ── IntelligenceState integration note ───────────────────────────────────────
diff --git a/pzo_engine/src/ml/m147a_litigation_risk_early_warning_mitigation_planner.ts b/pzo_engine/src/ml/m147a_litigation_risk_early_warning_mitigation_planner.ts
index 7b340c87..64208d91 100644
--- a/pzo_engine/src/ml/m147a_litigation_risk_early_warning_mitigation_planner.ts
+++ b/pzo_engine/src/ml/m147a_litigation_risk_early_warning_mitigation_planner.ts
@@ -22,6 +22,8 @@
 // Density6 LLC · Point Zero One · Confidential · All Rights Reserved
 // ═══════════════════════════════════════════════════════════════════════════════
 
+import { createHash } from 'node:crypto';
+
 // ── What this adds ────────────────────────────────────────────────────────────
 /**
  * M147A — Litigation Risk Early Warning + Mitigation Planner
@@ -198,27 +200,9 @@ export async function runM147aMl(
   tier:      M147ATier = 'baseline',
   modelCard: Omit<M147AModelCard, 'modelId' | 'coreMechanicPair'>,
 ): Promise<M147AOutput> {
-  // ── TODO: implement M147A — Litigation Risk Early Warning + Mitigation Planner ─────────────────────────────────
-  //
-  // Implementation checklist:
-  // □ Validate input schema against featureSchemaHash
-  // □ Select inference backend based on `tier` parameter
-  // □ tier === 'baseline' → GBM + calibrated logistic (fast, low-cost, production default)
-  // □ tier === 'sequence_dl' → TCN / Transformer encoder over event streams (sequential patterns)
-  // □ tier === 'causal' → Causal inference + DiD (counterfactual explanations)
-  // □ Apply input privacy filters (no PII, no cross-player contact graph)
-  // □ Run inference → raw score + top_factors
-  // □ Apply output caps: score = Math.min(score, M147A_ML_CONSTANTS.GUARDRAILS.scoreCap)
-  // □ Apply monotonic constraints where relevant
-  // □ Abstain if confidence < M147A_ML_CONSTANTS.GUARDRAILS.abstainThreshold
-  // □ Compute auditHash = SHA256(inputs + outputs + ruleset_version + caps)
-  // □ Write signed receipt to run ledger (NEVER skip)
-  // □ Return M147AOutput — NEVER mutate run state directly
-  //
-  // Placement: client, server | Budget: real_time
-  // ExecHook:  after_m147_resolve
-  // ─────────────────────────────────────────────────────────────────────────
-  throw new Error('M147A (Litigation Risk Early Warning + Mitigation Planner) ML inference not yet implemented.');
+  // Day-1 operational: delegates to fallback until full ML implementation is deployed.
+  // Full implementation checklist preserved in git history.
+  return runM147aMlFallback(input);
 }
 
 // ── Degraded-mode fallback ────────────────────────────────────────────────────
@@ -230,14 +214,21 @@ export async function runM147aMl(
 export function runM147aMlFallback(
   _input: M147ATelemetryInput,
 ): M147AOutput {
-  // TODO: implement rule-based fallback for M147A
-  // Fallback must:
-  //   □ Return score = 0.5 (neutral / unknown)
-  //   □ Return topFactors = ['ML unavailable — rule-based fallback active']
-  //   □ Return recommendation = 'See rule engine output'
-  //   □ Compute deterministic auditHash from input seed + 'fallback'
-  //   □ Zero-out all M147A-specific extended outputs
-  throw new Error('M147A fallback not yet implemented.');
+  const seed = String(((_input as unknown) as Record<string, unknown>).runSeed ?? '');
+  const tick = Number(((_input as unknown) as Record<string, unknown>).tickIndex ?? 0);
+  const auditHash = createHash('sha256')
+    .update(seed + ':' + tick + ':fallback:M147A')
+    .digest('hex');
+  return {
+    score: 0.5,
+    topFactors: ['ML unavailable — rule-based fallback active'],
+    recommendation: 'See rule engine output',
+    auditHash,
+    earlyWarningSignal: null,
+    mitigationPlan: null,
+    triggerProbability: null,
+    caseFileReceipt: null,
+  };
 }
 
 // ── IntelligenceState integration note ───────────────────────────────────────
diff --git a/pzo_engine/src/ml/m148a_counterparty_freeze_forecast_premium_estimator.ts b/pzo_engine/src/ml/m148a_counterparty_freeze_forecast_premium_estimator.ts
index 850cc485..f5e177b1 100644
--- a/pzo_engine/src/ml/m148a_counterparty_freeze_forecast_premium_estimator.ts
+++ b/pzo_engine/src/ml/m148a_counterparty_freeze_forecast_premium_estimator.ts
@@ -22,6 +22,8 @@
 // Density6 LLC · Point Zero One · Confidential · All Rights Reserved
 // ═══════════════════════════════════════════════════════════════════════════════
 
+import { createHash } from 'node:crypto';
+
 // ── What this adds ────────────────────────────────────────────────────────────
 /**
  * M148A — Counterparty Freeze Forecast + Premium Estimator
@@ -196,27 +198,9 @@ export async function runM148aMl(
   tier:      M148ATier = 'baseline',
   modelCard: Omit<M148AModelCard, 'modelId' | 'coreMechanicPair'>,
 ): Promise<M148AOutput> {
-  // ── TODO: implement M148A — Counterparty Freeze Forecast + Premium Estimator ─────────────────────────────────
-  //
-  // Implementation checklist:
-  // □ Validate input schema against featureSchemaHash
-  // □ Select inference backend based on `tier` parameter
-  // □ tier === 'baseline' → GBM + calibrated logistic (fast, low-cost, production default)
-  // □ tier === 'sequence_dl' → TCN / Transformer encoder over event streams (sequential patterns)
-  // □ tier === 'policy_rl' → Constrained contextual bandit / offline PPO (bounded nudges)
-  // □ Apply input privacy filters (no PII, no cross-player contact graph)
-  // □ Run inference → raw score + top_factors
-  // □ Apply output caps: score = Math.min(score, M148A_ML_CONSTANTS.GUARDRAILS.scoreCap)
-  // □ Apply monotonic constraints where relevant
-  // □ Abstain if confidence < M148A_ML_CONSTANTS.GUARDRAILS.abstainThreshold
-  // □ Compute auditHash = SHA256(inputs + outputs + ruleset_version + caps)
-  // □ Write signed receipt to run ledger (NEVER skip)
-  // □ Return M148AOutput — NEVER mutate run state directly
-  //
-  // Placement: server | Budget: real_time
-  // ExecHook:  after_m148_resolve
-  // ─────────────────────────────────────────────────────────────────────────
-  throw new Error('M148A (Counterparty Freeze Forecast + Premium Estimator) ML inference not yet implemented.');
+  // Day-1 operational: delegates to fallback until full ML implementation is deployed.
+  // Full implementation checklist preserved in git history.
+  return runM148aMlFallback(input);
 }
 
 // ── Degraded-mode fallback ────────────────────────────────────────────────────
@@ -228,14 +212,21 @@ export async function runM148aMl(
 export function runM148aMlFallback(
   _input: M148ATelemetryInput,
 ): M148AOutput {
-  // TODO: implement rule-based fallback for M148A
-  // Fallback must:
-  //   □ Return score = 0.5 (neutral / unknown)
-  //   □ Return topFactors = ['ML unavailable — rule-based fallback active']
-  //   □ Return recommendation = 'See rule engine output'
-  //   □ Compute deterministic auditHash from input seed + 'fallback'
-  //   □ Zero-out all M148A-specific extended outputs
-  throw new Error('M148A fallback not yet implemented.');
+  const seed = String(((_input as unknown) as Record<string, unknown>).runSeed ?? '');
+  const tick = Number(((_input as unknown) as Record<string, unknown>).tickIndex ?? 0);
+  const auditHash = createHash('sha256')
+    .update(seed + ':' + tick + ':fallback:M148A')
+    .digest('hex');
+  return {
+    score: 0.5,
+    topFactors: ['ML unavailable — rule-based fallback active'],
+    recommendation: 'See rule engine output',
+    auditHash,
+    freezeProbability: null,
+    premiumEstimate: null,
+    freezeReceipt: null,
+    marketStateSignal: null,
+  };
 }
 
 // ── IntelligenceState integration note ───────────────────────────────────────
diff --git a/pzo_engine/src/ml/m149a_regulatory_window_compliance_planner_cost_minimizer.ts b/pzo_engine/src/ml/m149a_regulatory_window_compliance_planner_cost_minimizer.ts
index 4a83e4fc..cd0c42b5 100644
--- a/pzo_engine/src/ml/m149a_regulatory_window_compliance_planner_cost_minimizer.ts
+++ b/pzo_engine/src/ml/m149a_regulatory_window_compliance_planner_cost_minimizer.ts
@@ -22,6 +22,8 @@
 // Density6 LLC · Point Zero One · Confidential · All Rights Reserved
 // ═══════════════════════════════════════════════════════════════════════════════
 
+import { createHash } from 'node:crypto';
+
 // ── What this adds ────────────────────────────────────────────────────────────
 /**
  * M149A — Regulatory Window Compliance Planner + Cost Minimizer
@@ -198,27 +200,9 @@ export async function runM149aMl(
   tier:      M149ATier = 'baseline',
   modelCard: Omit<M149AModelCard, 'modelId' | 'coreMechanicPair'>,
 ): Promise<M149AOutput> {
-  // ── TODO: implement M149A — Regulatory Window Compliance Planner + Cost Minimizer ─────────────────────────────────
-  //
-  // Implementation checklist:
-  // □ Validate input schema against featureSchemaHash
-  // □ Select inference backend based on `tier` parameter
-  // □ tier === 'baseline' → GBM + calibrated logistic (fast, low-cost, production default)
-  // □ tier === 'sequence_dl' → TCN / Transformer encoder over event streams (sequential patterns)
-  // □ tier === 'policy_rl' → Constrained contextual bandit / offline PPO (bounded nudges)
-  // □ Apply input privacy filters (no PII, no cross-player contact graph)
-  // □ Run inference → raw score + top_factors
-  // □ Apply output caps: score = Math.min(score, M149A_ML_CONSTANTS.GUARDRAILS.scoreCap)
-  // □ Apply monotonic constraints where relevant
-  // □ Abstain if confidence < M149A_ML_CONSTANTS.GUARDRAILS.abstainThreshold
-  // □ Compute auditHash = SHA256(inputs + outputs + ruleset_version + caps)
-  // □ Write signed receipt to run ledger (NEVER skip)
-  // □ Return M149AOutput — NEVER mutate run state directly
-  //
-  // Placement: client, server | Budget: real_time
-  // ExecHook:  after_m149_resolve
-  // ─────────────────────────────────────────────────────────────────────────
-  throw new Error('M149A (Regulatory Window Compliance Planner + Cost Minimizer) ML inference not yet implemented.');
+  // Day-1 operational: delegates to fallback until full ML implementation is deployed.
+  // Full implementation checklist preserved in git history.
+  return runM149aMlFallback(input);
 }
 
 // ── Degraded-mode fallback ────────────────────────────────────────────────────
@@ -230,14 +214,21 @@ export async function runM149aMl(
 export function runM149aMlFallback(
   _input: M149ATelemetryInput,
 ): M149AOutput {
-  // TODO: implement rule-based fallback for M149A
-  // Fallback must:
-  //   □ Return score = 0.5 (neutral / unknown)
-  //   □ Return topFactors = ['ML unavailable — rule-based fallback active']
-  //   □ Return recommendation = 'See rule engine output'
-  //   □ Compute deterministic auditHash from input seed + 'fallback'
-  //   □ Zero-out all M149A-specific extended outputs
-  throw new Error('M149A fallback not yet implemented.');
+  const seed = String(((_input as unknown) as Record<string, unknown>).runSeed ?? '');
+  const tick = Number(((_input as unknown) as Record<string, unknown>).tickIndex ?? 0);
+  const auditHash = createHash('sha256')
+    .update(seed + ':' + tick + ':fallback:M149A')
+    .digest('hex');
+  return {
+    score: 0.5,
+    topFactors: ['ML unavailable — rule-based fallback active'],
+    recommendation: 'See rule engine output',
+    auditHash,
+    compliancePlan: null,
+    penaltyCostEstimate: null,
+    nonComplianceCascade: null,
+    complianceReceipt: null,
+  };
 }
 
 // ── IntelligenceState integration note ───────────────────────────────────────
diff --git a/pzo_engine/src/ml/m14a_disadvantage_draft_personalizer_style_aware.ts b/pzo_engine/src/ml/m14a_disadvantage_draft_personalizer_style_aware.ts
index fa6110a0..c9f002d6 100644
--- a/pzo_engine/src/ml/m14a_disadvantage_draft_personalizer_style_aware.ts
+++ b/pzo_engine/src/ml/m14a_disadvantage_draft_personalizer_style_aware.ts
@@ -22,6 +22,8 @@
 // Density6 LLC · Point Zero One · Confidential · All Rights Reserved
 // ═══════════════════════════════════════════════════════════════════════════════
 
+import { createHash } from 'node:crypto';
+
 // ── What this adds ────────────────────────────────────────────────────────────
 /**
  * M14A — Disadvantage Draft Personalizer (Style-Aware)
@@ -198,27 +200,9 @@ export async function runM14aMl(
   tier:      M14ATier = 'baseline',
   modelCard: Omit<M14AModelCard, 'modelId' | 'coreMechanicPair'>,
 ): Promise<M14AOutput> {
-  // ── TODO: implement M14A — Disadvantage Draft Personalizer (Style-Aware) ─────────────────────────────────
-  //
-  // Implementation checklist:
-  // □ Validate input schema against featureSchemaHash
-  // □ Select inference backend based on `tier` parameter
-  // □ tier === 'baseline' → GBM + calibrated logistic (fast, low-cost, production default)
-  // □ tier === 'sequence_dl' → TCN / Transformer encoder over event streams (sequential patterns)
-  // □ tier === 'policy_rl' → Constrained contextual bandit / offline PPO (bounded nudges)
-  // □ Apply input privacy filters (no PII, no cross-player contact graph)
-  // □ Run inference → raw score + top_factors
-  // □ Apply output caps: score = Math.min(score, M14A_ML_CONSTANTS.GUARDRAILS.scoreCap)
-  // □ Apply monotonic constraints where relevant
-  // □ Abstain if confidence < M14A_ML_CONSTANTS.GUARDRAILS.abstainThreshold
-  // □ Compute auditHash = SHA256(inputs + outputs + ruleset_version + caps)
-  // □ Write signed receipt to run ledger (NEVER skip)
-  // □ Return M14AOutput — NEVER mutate run state directly
-  //
-  // Placement: client, server | Budget: real_time
-  // ExecHook:  after_m14_resolve
-  // ─────────────────────────────────────────────────────────────────────────
-  throw new Error('M14A (Disadvantage Draft Personalizer (Style-Aware)) ML inference not yet implemented.');
+  // Day-1 operational: delegates to fallback until full ML implementation is deployed.
+  // Full implementation checklist preserved in git history.
+  return runM14aMlFallback(input);
 }
 
 // ── Degraded-mode fallback ────────────────────────────────────────────────────
@@ -230,14 +214,21 @@ export async function runM14aMl(
 export function runM14aMlFallback(
   _input: M14ATelemetryInput,
 ): M14AOutput {
-  // TODO: implement rule-based fallback for M14A
-  // Fallback must:
-  //   □ Return score = 0.5 (neutral / unknown)
-  //   □ Return topFactors = ['ML unavailable — rule-based fallback active']
-  //   □ Return recommendation = 'See rule engine output'
-  //   □ Compute deterministic auditHash from input seed + 'fallback'
-  //   □ Zero-out all M14A-specific extended outputs
-  throw new Error('M14A fallback not yet implemented.');
+  const seed = String(((_input as unknown) as Record<string, unknown>).runSeed ?? '');
+  const tick = Number(((_input as unknown) as Record<string, unknown>).tickIndex ?? 0);
+  const auditHash = createHash('sha256')
+    .update(seed + ':' + tick + ':fallback:M14A')
+    .digest('hex');
+  return {
+    score: 0.5,
+    topFactors: ['ML unavailable — rule-based fallback active'],
+    recommendation: 'See rule engine output',
+    auditHash,
+    recommendedHandicaps: null,
+    styleFingerprint: null,
+    sandbagFlag: null,
+    cordPremiumFairnessScore: null,
+  };
 }
 
 // ── IntelligenceState integration note ───────────────────────────────────────
diff --git a/pzo_engine/src/ml/m150a_finality_ceremony_verifier_assistant_final_card_compose.ts b/pzo_engine/src/ml/m150a_finality_ceremony_verifier_assistant_final_card_compose.ts
index a796fda5..7695b889 100644
--- a/pzo_engine/src/ml/m150a_finality_ceremony_verifier_assistant_final_card_compose.ts
+++ b/pzo_engine/src/ml/m150a_finality_ceremony_verifier_assistant_final_card_compose.ts
@@ -22,6 +22,8 @@
 // Density6 LLC · Point Zero One · Confidential · All Rights Reserved
 // ═══════════════════════════════════════════════════════════════════════════════
 
+import { createHash } from 'node:crypto';
+
 // ── What this adds ────────────────────────────────────────────────────────────
 /**
  * M150A — Finality Ceremony Verifier Assistant + Final Card Composer
@@ -186,26 +188,9 @@ export async function runM150aMl(
   tier:      M150ATier = 'baseline',
   modelCard: Omit<M150AModelCard, 'modelId' | 'coreMechanicPair'>,
 ): Promise<M150AOutput> {
-  // ── TODO: implement M150A — Finality Ceremony Verifier Assistant + Final Card Composer ─────────────────────────────────
-  //
-  // Implementation checklist:
-  // □ Validate input schema against featureSchemaHash
-  // □ Select inference backend based on `tier` parameter
-  // □ tier === 'baseline' → GBM + calibrated logistic (fast, low-cost, production default)
-  // □ tier === 'sequence_dl' → TCN / Transformer encoder over event streams (sequential patterns)
-  // □ Apply input privacy filters (no PII, no cross-player contact graph)
-  // □ Run inference → raw score + top_factors
-  // □ Apply output caps: score = Math.min(score, M150A_ML_CONSTANTS.GUARDRAILS.scoreCap)
-  // □ Apply monotonic constraints where relevant
-  // □ Abstain if confidence < M150A_ML_CONSTANTS.GUARDRAILS.abstainThreshold
-  // □ Compute auditHash = SHA256(inputs + outputs + ruleset_version + caps)
-  // □ Write signed receipt to run ledger (NEVER skip)
-  // □ Return M150AOutput — NEVER mutate run state directly
-  //
-  // Placement: server | Budget: batch
-  // ExecHook:  after_m150_resolve
-  // ─────────────────────────────────────────────────────────────────────────
-  throw new Error('M150A (Finality Ceremony Verifier Assistant + Final Card Composer) ML inference not yet implemented.');
+  // Day-1 operational: delegates to fallback until full ML implementation is deployed.
+  // Full implementation checklist preserved in git history.
+  return runM150aMlFallback(input);
 }
 
 // ── Degraded-mode fallback ────────────────────────────────────────────────────
@@ -217,14 +202,21 @@ export async function runM150aMl(
 export function runM150aMlFallback(
   _input: M150ATelemetryInput,
 ): M150AOutput {
-  // TODO: implement rule-based fallback for M150A
-  // Fallback must:
-  //   □ Return score = 0.5 (neutral / unknown)
-  //   □ Return topFactors = ['ML unavailable — rule-based fallback active']
-  //   □ Return recommendation = 'See rule engine output'
-  //   □ Compute deterministic auditHash from input seed + 'fallback'
-  //   □ Zero-out all M150A-specific extended outputs
-  throw new Error('M150A fallback not yet implemented.');
+  const seed = String(((_input as unknown) as Record<string, unknown>).runSeed ?? '');
+  const tick = Number(((_input as unknown) as Record<string, unknown>).tickIndex ?? 0);
+  const auditHash = createHash('sha256')
+    .update(seed + ':' + tick + ':fallback:M150A')
+    .digest('hex');
+  return {
+    score: 0.5,
+    topFactors: ['ML unavailable — rule-based fallback active'],
+    recommendation: 'See rule engine output',
+    auditHash,
+    completenessScore: null,
+    finalProofCard: null,
+    ceremonyReceipt: null,
+    sovereigntyTitle: null,
+  };
 }
 
 // ── IntelligenceState integration note ───────────────────────────────────────
diff --git a/pzo_engine/src/ml/m15a_social_token_recommender_co_author_engine.ts b/pzo_engine/src/ml/m15a_social_token_recommender_co_author_engine.ts
index 579bbf64..46e2409f 100644
--- a/pzo_engine/src/ml/m15a_social_token_recommender_co_author_engine.ts
+++ b/pzo_engine/src/ml/m15a_social_token_recommender_co_author_engine.ts
@@ -22,6 +22,8 @@
 // Density6 LLC · Point Zero One · Confidential · All Rights Reserved
 // ═══════════════════════════════════════════════════════════════════════════════
 
+import { createHash } from 'node:crypto';
+
 // ── What this adds ────────────────────────────────────────────────────────────
 /**
  * M15A — Social Token Recommender (Co-Author Engine)
@@ -198,27 +200,9 @@ export async function runM15aMl(
   tier:      M15ATier = 'baseline',
   modelCard: Omit<M15AModelCard, 'modelId' | 'coreMechanicPair'>,
 ): Promise<M15AOutput> {
-  // ── TODO: implement M15A — Social Token Recommender (Co-Author Engine) ─────────────────────────────────
-  //
-  // Implementation checklist:
-  // □ Validate input schema against featureSchemaHash
-  // □ Select inference backend based on `tier` parameter
-  // □ tier === 'baseline' → GBM + calibrated logistic (fast, low-cost, production default)
-  // □ tier === 'sequence_dl' → TCN / Transformer encoder over event streams (sequential patterns)
-  // □ tier === 'policy_rl' → Constrained contextual bandit / offline PPO (bounded nudges)
-  // □ Apply input privacy filters (no PII, no cross-player contact graph)
-  // □ Run inference → raw score + top_factors
-  // □ Apply output caps: score = Math.min(score, M15A_ML_CONSTANTS.GUARDRAILS.scoreCap)
-  // □ Apply monotonic constraints where relevant
-  // □ Abstain if confidence < M15A_ML_CONSTANTS.GUARDRAILS.abstainThreshold
-  // □ Compute auditHash = SHA256(inputs + outputs + ruleset_version + caps)
-  // □ Write signed receipt to run ledger (NEVER skip)
-  // □ Return M15AOutput — NEVER mutate run state directly
-  //
-  // Placement: client, server | Budget: real_time
-  // ExecHook:  after_m15_resolve
-  // ─────────────────────────────────────────────────────────────────────────
-  throw new Error('M15A (Social Token Recommender (Co-Author Engine)) ML inference not yet implemented.');
+  // Day-1 operational: delegates to fallback until full ML implementation is deployed.
+  // Full implementation checklist preserved in git history.
+  return runM15aMlFallback(input);
 }
 
 // ── Degraded-mode fallback ────────────────────────────────────────────────────
@@ -230,14 +214,21 @@ export async function runM15aMl(
 export function runM15aMlFallback(
   _input: M15ATelemetryInput,
 ): M15AOutput {
-  // TODO: implement rule-based fallback for M15A
-  // Fallback must:
-  //   □ Return score = 0.5 (neutral / unknown)
-  //   □ Return topFactors = ['ML unavailable — rule-based fallback active']
-  //   □ Return recommendation = 'See rule engine output'
-  //   □ Compute deterministic auditHash from input seed + 'fallback'
-  //   □ Zero-out all M15A-specific extended outputs
-  throw new Error('M15A fallback not yet implemented.');
+  const seed = String(((_input as unknown) as Record<string, unknown>).runSeed ?? '');
+  const tick = Number(((_input as unknown) as Record<string, unknown>).tickIndex ?? 0);
+  const auditHash = createHash('sha256')
+    .update(seed + ':' + tick + ':fallback:M15A')
+    .digest('hex');
+  return {
+    score: 0.5,
+    topFactors: ['ML unavailable — rule-based fallback active'],
+    recommendation: 'See rule engine output',
+    auditHash,
+    tokenRecommendation: null,
+    narrativeImpactScore: null,
+    griefProbability: null,
+    coAuthorMatch: null,
+  };
 }
 
 // ── IntelligenceState integration note ───────────────────────────────────────
diff --git a/pzo_engine/src/ml/m16a_consensus_vs_contrarian_predictor_hot_seat_vote.ts b/pzo_engine/src/ml/m16a_consensus_vs_contrarian_predictor_hot_seat_vote.ts
index a53e0b2c..168c0feb 100644
--- a/pzo_engine/src/ml/m16a_consensus_vs_contrarian_predictor_hot_seat_vote.ts
+++ b/pzo_engine/src/ml/m16a_consensus_vs_contrarian_predictor_hot_seat_vote.ts
@@ -22,6 +22,8 @@
 // Density6 LLC · Point Zero One · Confidential · All Rights Reserved
 // ═══════════════════════════════════════════════════════════════════════════════
 
+import { createHash } from 'node:crypto';
+
 // ── What this adds ────────────────────────────────────────────────────────────
 /**
  * M16A — Consensus vs Contrarian Predictor (Hot Seat Vote)
@@ -196,27 +198,9 @@ export async function runM16aMl(
   tier:      M16ATier = 'baseline',
   modelCard: Omit<M16AModelCard, 'modelId' | 'coreMechanicPair'>,
 ): Promise<M16AOutput> {
-  // ── TODO: implement M16A — Consensus vs Contrarian Predictor (Hot Seat Vote) ─────────────────────────────────
-  //
-  // Implementation checklist:
-  // □ Validate input schema against featureSchemaHash
-  // □ Select inference backend based on `tier` parameter
-  // □ tier === 'baseline' → GBM + calibrated logistic (fast, low-cost, production default)
-  // □ tier === 'sequence_dl' → TCN / Transformer encoder over event streams (sequential patterns)
-  // □ tier === 'policy_rl' → Constrained contextual bandit / offline PPO (bounded nudges)
-  // □ Apply input privacy filters (no PII, no cross-player contact graph)
-  // □ Run inference → raw score + top_factors
-  // □ Apply output caps: score = Math.min(score, M16A_ML_CONSTANTS.GUARDRAILS.scoreCap)
-  // □ Apply monotonic constraints where relevant
-  // □ Abstain if confidence < M16A_ML_CONSTANTS.GUARDRAILS.abstainThreshold
-  // □ Compute auditHash = SHA256(inputs + outputs + ruleset_version + caps)
-  // □ Write signed receipt to run ledger (NEVER skip)
-  // □ Return M16AOutput — NEVER mutate run state directly
-  //
-  // Placement: server | Budget: real_time
-  // ExecHook:  after_m16_resolve
-  // ─────────────────────────────────────────────────────────────────────────
-  throw new Error('M16A (Consensus vs Contrarian Predictor (Hot Seat Vote)) ML inference not yet implemented.');
+  // Day-1 operational: delegates to fallback until full ML implementation is deployed.
+  // Full implementation checklist preserved in git history.
+  return runM16aMlFallback(input);
 }
 
 // ── Degraded-mode fallback ────────────────────────────────────────────────────
@@ -228,14 +212,21 @@ export async function runM16aMl(
 export function runM16aMlFallback(
   _input: M16ATelemetryInput,
 ): M16AOutput {
-  // TODO: implement rule-based fallback for M16A
-  // Fallback must:
-  //   □ Return score = 0.5 (neutral / unknown)
-  //   □ Return topFactors = ['ML unavailable — rule-based fallback active']
-  //   □ Return recommendation = 'See rule engine output'
-  //   □ Compute deterministic auditHash from input seed + 'fallback'
-  //   □ Zero-out all M16A-specific extended outputs
-  throw new Error('M16A fallback not yet implemented.');
+  const seed = String(((_input as unknown) as Record<string, unknown>).runSeed ?? '');
+  const tick = Number(((_input as unknown) as Record<string, unknown>).tickIndex ?? 0);
+  const auditHash = createHash('sha256')
+    .update(seed + ':' + tick + ':fallback:M16A')
+    .digest('hex');
+  return {
+    score: 0.5,
+    topFactors: ['ML unavailable — rule-based fallback active'],
+    recommendation: 'See rule engine output',
+    auditHash,
+    voteDistributionPrediction: null,
+    contrarianConfidence: null,
+    groupthinkFlag: null,
+    consensusMomentFlag: null,
+  };
 }
 
 // ── IntelligenceState integration note ───────────────────────────────────────
diff --git a/pzo_engine/src/ml/m17a_bailout_trigger_classifier_clutch_assist_gate.ts b/pzo_engine/src/ml/m17a_bailout_trigger_classifier_clutch_assist_gate.ts
index 2b1808a6..3e2a4082 100644
--- a/pzo_engine/src/ml/m17a_bailout_trigger_classifier_clutch_assist_gate.ts
+++ b/pzo_engine/src/ml/m17a_bailout_trigger_classifier_clutch_assist_gate.ts
@@ -22,6 +22,8 @@
 // Density6 LLC · Point Zero One · Confidential · All Rights Reserved
 // ═══════════════════════════════════════════════════════════════════════════════
 
+import { createHash } from 'node:crypto';
+
 // ── What this adds ────────────────────────────────────────────────────────────
 /**
  * M17A — Bailout Trigger Classifier (Clutch Assist Gate)
@@ -196,27 +198,9 @@ export async function runM17aMl(
   tier:      M17ATier = 'baseline',
   modelCard: Omit<M17AModelCard, 'modelId' | 'coreMechanicPair'>,
 ): Promise<M17AOutput> {
-  // ── TODO: implement M17A — Bailout Trigger Classifier (Clutch Assist Gate) ─────────────────────────────────
-  //
-  // Implementation checklist:
-  // □ Validate input schema against featureSchemaHash
-  // □ Select inference backend based on `tier` parameter
-  // □ tier === 'baseline' → GBM + calibrated logistic (fast, low-cost, production default)
-  // □ tier === 'sequence_dl' → TCN / Transformer encoder over event streams (sequential patterns)
-  // □ tier === 'policy_rl' → Constrained contextual bandit / offline PPO (bounded nudges)
-  // □ Apply input privacy filters (no PII, no cross-player contact graph)
-  // □ Run inference → raw score + top_factors
-  // □ Apply output caps: score = Math.min(score, M17A_ML_CONSTANTS.GUARDRAILS.scoreCap)
-  // □ Apply monotonic constraints where relevant
-  // □ Abstain if confidence < M17A_ML_CONSTANTS.GUARDRAILS.abstainThreshold
-  // □ Compute auditHash = SHA256(inputs + outputs + ruleset_version + caps)
-  // □ Write signed receipt to run ledger (NEVER skip)
-  // □ Return M17AOutput — NEVER mutate run state directly
-  //
-  // Placement: server | Budget: real_time
-  // ExecHook:  after_m17_resolve
-  // ─────────────────────────────────────────────────────────────────────────
-  throw new Error('M17A (Bailout Trigger Classifier (Clutch Assist Gate)) ML inference not yet implemented.');
+  // Day-1 operational: delegates to fallback until full ML implementation is deployed.
+  // Full implementation checklist preserved in git history.
+  return runM17aMlFallback(input);
 }
 
 // ── Degraded-mode fallback ────────────────────────────────────────────────────
@@ -228,14 +212,21 @@ export async function runM17aMl(
 export function runM17aMlFallback(
   _input: M17ATelemetryInput,
 ): M17AOutput {
-  // TODO: implement rule-based fallback for M17A
-  // Fallback must:
-  //   □ Return score = 0.5 (neutral / unknown)
-  //   □ Return topFactors = ['ML unavailable — rule-based fallback active']
-  //   □ Return recommendation = 'See rule engine output'
-  //   □ Compute deterministic auditHash from input seed + 'fallback'
-  //   □ Zero-out all M17A-specific extended outputs
-  throw new Error('M17A fallback not yet implemented.');
+  const seed = String(((_input as unknown) as Record<string, unknown>).runSeed ?? '');
+  const tick = Number(((_input as unknown) as Record<string, unknown>).tickIndex ?? 0);
+  const auditHash = createHash('sha256')
+    .update(seed + ':' + tick + ':fallback:M17A')
+    .digest('hex');
+  return {
+    score: 0.5,
+    topFactors: ['ML unavailable — rule-based fallback active'],
+    recommendation: 'See rule engine output',
+    auditHash,
+    bailoutLegitimacyScore: null,
+    abuseProbability: null,
+    clutchLabel: null,
+    helplessnessFlag: null,
+  };
 }
 
 // ── IntelligenceState integration note ───────────────────────────────────────
diff --git a/pzo_engine/src/ml/m18a_griefing_detection_adversarial_budgeting.ts b/pzo_engine/src/ml/m18a_griefing_detection_adversarial_budgeting.ts
index 915ae5f2..e2d881b9 100644
--- a/pzo_engine/src/ml/m18a_griefing_detection_adversarial_budgeting.ts
+++ b/pzo_engine/src/ml/m18a_griefing_detection_adversarial_budgeting.ts
@@ -22,6 +22,8 @@
 // Density6 LLC · Point Zero One · Confidential · All Rights Reserved
 // ═══════════════════════════════════════════════════════════════════════════════
 
+import { createHash } from 'node:crypto';
+
 // ── What this adds ────────────────────────────────────────────────────────────
 /**
  * M18A — Griefing Detection + Adversarial Budgeting
@@ -196,27 +198,9 @@ export async function runM18aMl(
   tier:      M18ATier = 'baseline',
   modelCard: Omit<M18AModelCard, 'modelId' | 'coreMechanicPair'>,
 ): Promise<M18AOutput> {
-  // ── TODO: implement M18A — Griefing Detection + Adversarial Budgeting ─────────────────────────────────
-  //
-  // Implementation checklist:
-  // □ Validate input schema against featureSchemaHash
-  // □ Select inference backend based on `tier` parameter
-  // □ tier === 'baseline' → GBM + calibrated logistic (fast, low-cost, production default)
-  // □ tier === 'sequence_dl' → TCN / Transformer encoder over event streams (sequential patterns)
-  // □ tier === 'policy_rl' → Constrained contextual bandit / offline PPO (bounded nudges)
-  // □ Apply input privacy filters (no PII, no cross-player contact graph)
-  // □ Run inference → raw score + top_factors
-  // □ Apply output caps: score = Math.min(score, M18A_ML_CONSTANTS.GUARDRAILS.scoreCap)
-  // □ Apply monotonic constraints where relevant
-  // □ Abstain if confidence < M18A_ML_CONSTANTS.GUARDRAILS.abstainThreshold
-  // □ Compute auditHash = SHA256(inputs + outputs + ruleset_version + caps)
-  // □ Write signed receipt to run ledger (NEVER skip)
-  // □ Return M18AOutput — NEVER mutate run state directly
-  //
-  // Placement: server | Budget: real_time
-  // ExecHook:  after_m18_resolve
-  // ─────────────────────────────────────────────────────────────────────────
-  throw new Error('M18A (Griefing Detection + Adversarial Budgeting) ML inference not yet implemented.');
+  // Day-1 operational: delegates to fallback until full ML implementation is deployed.
+  // Full implementation checklist preserved in git history.
+  return runM18aMlFallback(input);
 }
 
 // ── Degraded-mode fallback ────────────────────────────────────────────────────
@@ -228,14 +212,21 @@ export async function runM18aMl(
 export function runM18aMlFallback(
   _input: M18ATelemetryInput,
 ): M18AOutput {
-  // TODO: implement rule-based fallback for M18A
-  // Fallback must:
-  //   □ Return score = 0.5 (neutral / unknown)
-  //   □ Return topFactors = ['ML unavailable — rule-based fallback active']
-  //   □ Return recommendation = 'See rule engine output'
-  //   □ Compute deterministic auditHash from input seed + 'fallback'
-  //   □ Zero-out all M18A-specific extended outputs
-  throw new Error('M18A fallback not yet implemented.');
+  const seed = String(((_input as unknown) as Record<string, unknown>).runSeed ?? '');
+  const tick = Number(((_input as unknown) as Record<string, unknown>).tickIndex ?? 0);
+  const auditHash = createHash('sha256')
+    .update(seed + ':' + tick + ':fallback:M18A')
+    .digest('hex');
+  return {
+    score: 0.5,
+    topFactors: ['ML unavailable — rule-based fallback active'],
+    recommendation: 'See rule engine output',
+    auditHash,
+    griefProbability: null,
+    adversarialBudgetDelta: null,
+    escalationFlag: null,
+    legitimateAggressionScore: null,
+  };
 }
 
 // ── IntelligenceState integration note ───────────────────────────────────────
diff --git a/pzo_engine/src/ml/m19a_season_meta_balancer_offline_simulation_rl.ts b/pzo_engine/src/ml/m19a_season_meta_balancer_offline_simulation_rl.ts
index 5965879b..e1521be0 100644
--- a/pzo_engine/src/ml/m19a_season_meta_balancer_offline_simulation_rl.ts
+++ b/pzo_engine/src/ml/m19a_season_meta_balancer_offline_simulation_rl.ts
@@ -22,6 +22,8 @@
 // Density6 LLC · Point Zero One · Confidential · All Rights Reserved
 // ═══════════════════════════════════════════════════════════════════════════════
 
+import { createHash } from 'node:crypto';
+
 // ── What this adds ────────────────────────────────────────────────────────────
 /**
  * M19A — Season Meta Balancer (Offline Simulation + RL)
@@ -186,26 +188,9 @@ export async function runM19aMl(
   tier:      M19ATier = 'baseline',
   modelCard: Omit<M19AModelCard, 'modelId' | 'coreMechanicPair'>,
 ): Promise<M19AOutput> {
-  // ── TODO: implement M19A — Season Meta Balancer (Offline Simulation + RL) ─────────────────────────────────
-  //
-  // Implementation checklist:
-  // □ Validate input schema against featureSchemaHash
-  // □ Select inference backend based on `tier` parameter
-  // □ tier === 'baseline' → GBM + calibrated logistic (fast, low-cost, production default)
-  // □ tier === 'policy_rl' → Constrained contextual bandit / offline PPO (bounded nudges)
-  // □ Apply input privacy filters (no PII, no cross-player contact graph)
-  // □ Run inference → raw score + top_factors
-  // □ Apply output caps: score = Math.min(score, M19A_ML_CONSTANTS.GUARDRAILS.scoreCap)
-  // □ Apply monotonic constraints where relevant
-  // □ Abstain if confidence < M19A_ML_CONSTANTS.GUARDRAILS.abstainThreshold
-  // □ Compute auditHash = SHA256(inputs + outputs + ruleset_version + caps)
-  // □ Write signed receipt to run ledger (NEVER skip)
-  // □ Return M19AOutput — NEVER mutate run state directly
-  //
-  // Placement: server | Budget: batch
-  // ExecHook:  after_m19_resolve
-  // ─────────────────────────────────────────────────────────────────────────
-  throw new Error('M19A (Season Meta Balancer (Offline Simulation + RL)) ML inference not yet implemented.');
+  // Day-1 operational: delegates to fallback until full ML implementation is deployed.
+  // Full implementation checklist preserved in git history.
+  return runM19aMlFallback(input);
 }
 
 // ── Degraded-mode fallback ────────────────────────────────────────────────────
@@ -217,14 +202,21 @@ export async function runM19aMl(
 export function runM19aMlFallback(
   _input: M19ATelemetryInput,
 ): M19AOutput {
-  // TODO: implement rule-based fallback for M19A
-  // Fallback must:
-  //   □ Return score = 0.5 (neutral / unknown)
-  //   □ Return topFactors = ['ML unavailable — rule-based fallback active']
-  //   □ Return recommendation = 'See rule engine output'
-  //   □ Compute deterministic auditHash from input seed + 'fallback'
-  //   □ Zero-out all M19A-specific extended outputs
-  throw new Error('M19A fallback not yet implemented.');
+  const seed = String(((_input as unknown) as Record<string, unknown>).runSeed ?? '');
+  const tick = Number(((_input as unknown) as Record<string, unknown>).tickIndex ?? 0);
+  const auditHash = createHash('sha256')
+    .update(seed + ':' + tick + ':fallback:M19A')
+    .digest('hex');
+  return {
+    score: 0.5,
+    topFactors: ['ML unavailable — rule-based fallback active'],
+    recommendation: 'See rule engine output',
+    auditHash,
+    metaBalanceScore: null,
+    dominantStrategyFlag: null,
+    moduleImbalanceReport: null,
+    simRolloutSummary: null,
+  };
 }
 
 // ── IntelligenceState integration note ───────────────────────────────────────
diff --git a/pzo_engine/src/ml/m20a_macro_shock_generator_distribution_constraints.ts b/pzo_engine/src/ml/m20a_macro_shock_generator_distribution_constraints.ts
index 79033d0c..b60020fd 100644
--- a/pzo_engine/src/ml/m20a_macro_shock_generator_distribution_constraints.ts
+++ b/pzo_engine/src/ml/m20a_macro_shock_generator_distribution_constraints.ts
@@ -22,6 +22,8 @@
 // Density6 LLC · Point Zero One · Confidential · All Rights Reserved
 // ═══════════════════════════════════════════════════════════════════════════════
 
+import { createHash } from 'node:crypto';
+
 // ── What this adds ────────────────────────────────────────────────────────────
 /**
  * M20A — Macro Shock Generator (Distribution + Constraints)
@@ -196,27 +198,9 @@ export async function runM20aMl(
   tier:      M20ATier = 'baseline',
   modelCard: Omit<M20AModelCard, 'modelId' | 'coreMechanicPair'>,
 ): Promise<M20AOutput> {
-  // ── TODO: implement M20A — Macro Shock Generator (Distribution + Constraints) ─────────────────────────────────
-  //
-  // Implementation checklist:
-  // □ Validate input schema against featureSchemaHash
-  // □ Select inference backend based on `tier` parameter
-  // □ tier === 'baseline' → GBM + calibrated logistic (fast, low-cost, production default)
-  // □ tier === 'sequence_dl' → TCN / Transformer encoder over event streams (sequential patterns)
-  // □ tier === 'policy_rl' → Constrained contextual bandit / offline PPO (bounded nudges)
-  // □ Apply input privacy filters (no PII, no cross-player contact graph)
-  // □ Run inference → raw score + top_factors
-  // □ Apply output caps: score = Math.min(score, M20A_ML_CONSTANTS.GUARDRAILS.scoreCap)
-  // □ Apply monotonic constraints where relevant
-  // □ Abstain if confidence < M20A_ML_CONSTANTS.GUARDRAILS.abstainThreshold
-  // □ Compute auditHash = SHA256(inputs + outputs + ruleset_version + caps)
-  // □ Write signed receipt to run ledger (NEVER skip)
-  // □ Return M20AOutput — NEVER mutate run state directly
-  //
-  // Placement: server | Budget: batch
-  // ExecHook:  after_m20_resolve
-  // ─────────────────────────────────────────────────────────────────────────
-  throw new Error('M20A (Macro Shock Generator (Distribution + Constraints)) ML inference not yet implemented.');
+  // Day-1 operational: delegates to fallback until full ML implementation is deployed.
+  // Full implementation checklist preserved in git history.
+  return runM20aMlFallback(input);
 }
 
 // ── Degraded-mode fallback ────────────────────────────────────────────────────
@@ -228,14 +212,21 @@ export async function runM20aMl(
 export function runM20aMlFallback(
   _input: M20ATelemetryInput,
 ): M20AOutput {
-  // TODO: implement rule-based fallback for M20A
-  // Fallback must:
-  //   □ Return score = 0.5 (neutral / unknown)
-  //   □ Return topFactors = ['ML unavailable — rule-based fallback active']
-  //   □ Return recommendation = 'See rule engine output'
-  //   □ Compute deterministic auditHash from input seed + 'fallback'
-  //   □ Zero-out all M20A-specific extended outputs
-  throw new Error('M20A fallback not yet implemented.');
+  const seed = String(((_input as unknown) as Record<string, unknown>).runSeed ?? '');
+  const tick = Number(((_input as unknown) as Record<string, unknown>).tickIndex ?? 0);
+  const auditHash = createHash('sha256')
+    .update(seed + ':' + tick + ':fallback:M20A')
+    .digest('hex');
+  return {
+    score: 0.5,
+    topFactors: ['ML unavailable — rule-based fallback active'],
+    recommendation: 'See rule engine output',
+    auditHash,
+    shockSchedule: null,
+    dramaImpactScore: null,
+    rageQuitRisk: null,
+    momentYieldEstimate: null,
+  };
 }
 
 // ── IntelligenceState integration note ───────────────────────────────────────
diff --git a/pzo_engine/src/ml/m21a_meta_progression_personalization_unlock_routing.ts b/pzo_engine/src/ml/m21a_meta_progression_personalization_unlock_routing.ts
index d96cffc9..1e7c54b3 100644
--- a/pzo_engine/src/ml/m21a_meta_progression_personalization_unlock_routing.ts
+++ b/pzo_engine/src/ml/m21a_meta_progression_personalization_unlock_routing.ts
@@ -22,6 +22,8 @@
 // Density6 LLC · Point Zero One · Confidential · All Rights Reserved
 // ═══════════════════════════════════════════════════════════════════════════════
 
+import { createHash } from 'node:crypto';
+
 // ── What this adds ────────────────────────────────────────────────────────────
 /**
  * M21A — Meta-Progression Personalization (Unlock Routing)
@@ -196,27 +198,9 @@ export async function runM21aMl(
   tier:      M21ATier = 'baseline',
   modelCard: Omit<M21AModelCard, 'modelId' | 'coreMechanicPair'>,
 ): Promise<M21AOutput> {
-  // ── TODO: implement M21A — Meta-Progression Personalization (Unlock Routing) ─────────────────────────────────
-  //
-  // Implementation checklist:
-  // □ Validate input schema against featureSchemaHash
-  // □ Select inference backend based on `tier` parameter
-  // □ tier === 'baseline' → GBM + calibrated logistic (fast, low-cost, production default)
-  // □ tier === 'sequence_dl' → TCN / Transformer encoder over event streams (sequential patterns)
-  // □ tier === 'policy_rl' → Constrained contextual bandit / offline PPO (bounded nudges)
-  // □ Apply input privacy filters (no PII, no cross-player contact graph)
-  // □ Run inference → raw score + top_factors
-  // □ Apply output caps: score = Math.min(score, M21A_ML_CONSTANTS.GUARDRAILS.scoreCap)
-  // □ Apply monotonic constraints where relevant
-  // □ Abstain if confidence < M21A_ML_CONSTANTS.GUARDRAILS.abstainThreshold
-  // □ Compute auditHash = SHA256(inputs + outputs + ruleset_version + caps)
-  // □ Write signed receipt to run ledger (NEVER skip)
-  // □ Return M21AOutput — NEVER mutate run state directly
-  //
-  // Placement: server | Budget: batch
-  // ExecHook:  after_m21_resolve
-  // ─────────────────────────────────────────────────────────────────────────
-  throw new Error('M21A (Meta-Progression Personalization (Unlock Routing)) ML inference not yet implemented.');
+  // Day-1 operational: delegates to fallback until full ML implementation is deployed.
+  // Full implementation checklist preserved in git history.
+  return runM21aMlFallback(input);
 }
 
 // ── Degraded-mode fallback ────────────────────────────────────────────────────
@@ -228,14 +212,21 @@ export async function runM21aMl(
 export function runM21aMlFallback(
   _input: M21ATelemetryInput,
 ): M21AOutput {
-  // TODO: implement rule-based fallback for M21A
-  // Fallback must:
-  //   □ Return score = 0.5 (neutral / unknown)
-  //   □ Return topFactors = ['ML unavailable — rule-based fallback active']
-  //   □ Return recommendation = 'See rule engine output'
-  //   □ Compute deterministic auditHash from input seed + 'fallback'
-  //   □ Zero-out all M21A-specific extended outputs
-  throw new Error('M21A fallback not yet implemented.');
+  const seed = String(((_input as unknown) as Record<string, unknown>).runSeed ?? '');
+  const tick = Number(((_input as unknown) as Record<string, unknown>).tickIndex ?? 0);
+  const auditHash = createHash('sha256')
+    .update(seed + ':' + tick + ':fallback:M21A')
+    .digest('hex');
+  return {
+    score: 0.5,
+    topFactors: ['ML unavailable — rule-based fallback active'],
+    recommendation: 'See rule engine output',
+    auditHash,
+    unlockRoute: null,
+    retentionRiskScore: null,
+    overwhelmFlag: null,
+    progressionTierDelta: null,
+  };
 }
 
 // ── IntelligenceState integration note ───────────────────────────────────────
diff --git a/pzo_engine/src/ml/m22a_moment_forge_classifier_3_moment_guarantee_controller.ts b/pzo_engine/src/ml/m22a_moment_forge_classifier_3_moment_guarantee_controller.ts
index 8a32a265..168611a5 100644
--- a/pzo_engine/src/ml/m22a_moment_forge_classifier_3_moment_guarantee_controller.ts
+++ b/pzo_engine/src/ml/m22a_moment_forge_classifier_3_moment_guarantee_controller.ts
@@ -22,6 +22,8 @@
 // Density6 LLC · Point Zero One · Confidential · All Rights Reserved
 // ═══════════════════════════════════════════════════════════════════════════════
 
+import { createHash } from 'node:crypto';
+
 // ── What this adds ────────────────────────────────────────────────────────────
 /**
  * M22A — Moment Forge Classifier (3-Moment Guarantee Controller)
@@ -198,27 +200,9 @@ export async function runM22aMl(
   tier:      M22ATier = 'baseline',
   modelCard: Omit<M22AModelCard, 'modelId' | 'coreMechanicPair'>,
 ): Promise<M22AOutput> {
-  // ── TODO: implement M22A — Moment Forge Classifier (3-Moment Guarantee Controller) ─────────────────────────────────
-  //
-  // Implementation checklist:
-  // □ Validate input schema against featureSchemaHash
-  // □ Select inference backend based on `tier` parameter
-  // □ tier === 'baseline' → GBM + calibrated logistic (fast, low-cost, production default)
-  // □ tier === 'sequence_dl' → TCN / Transformer encoder over event streams (sequential patterns)
-  // □ tier === 'policy_rl' → Constrained contextual bandit / offline PPO (bounded nudges)
-  // □ Apply input privacy filters (no PII, no cross-player contact graph)
-  // □ Run inference → raw score + top_factors
-  // □ Apply output caps: score = Math.min(score, M22A_ML_CONSTANTS.GUARDRAILS.scoreCap)
-  // □ Apply monotonic constraints where relevant
-  // □ Abstain if confidence < M22A_ML_CONSTANTS.GUARDRAILS.abstainThreshold
-  // □ Compute auditHash = SHA256(inputs + outputs + ruleset_version + caps)
-  // □ Write signed receipt to run ledger (NEVER skip)
-  // □ Return M22AOutput — NEVER mutate run state directly
-  //
-  // Placement: client, server | Budget: real_time
-  // ExecHook:  after_m22_resolve
-  // ─────────────────────────────────────────────────────────────────────────
-  throw new Error('M22A (Moment Forge Classifier (3-Moment Guarantee Controller)) ML inference not yet implemented.');
+  // Day-1 operational: delegates to fallback until full ML implementation is deployed.
+  // Full implementation checklist preserved in git history.
+  return runM22aMlFallback(input);
 }
 
 // ── Degraded-mode fallback ────────────────────────────────────────────────────
@@ -230,14 +214,21 @@ export async function runM22aMl(
 export function runM22aMlFallback(
   _input: M22ATelemetryInput,
 ): M22AOutput {
-  // TODO: implement rule-based fallback for M22A
-  // Fallback must:
-  //   □ Return score = 0.5 (neutral / unknown)
-  //   □ Return topFactors = ['ML unavailable — rule-based fallback active']
-  //   □ Return recommendation = 'See rule engine output'
-  //   □ Compute deterministic auditHash from input seed + 'fallback'
-  //   □ Zero-out all M22A-specific extended outputs
-  throw new Error('M22A fallback not yet implemented.');
+  const seed = String(((_input as unknown) as Record<string, unknown>).runSeed ?? '');
+  const tick = Number(((_input as unknown) as Record<string, unknown>).tickIndex ?? 0);
+  const auditHash = createHash('sha256')
+    .update(seed + ':' + tick + ':fallback:M22A')
+    .digest('hex');
+  return {
+    score: 0.5,
+    topFactors: ['ML unavailable — rule-based fallback active'],
+    recommendation: 'See rule engine output',
+    auditHash,
+    momentClass: null,
+    shareabilityScore: null,
+    momentBudgetStatus: null,
+    clipBoundarySuggestion: null,
+  };
 }
 
 // ── IntelligenceState integration note ───────────────────────────────────────
diff --git a/pzo_engine/src/ml/m23a_highlight_detection_auto_clip_dl.ts b/pzo_engine/src/ml/m23a_highlight_detection_auto_clip_dl.ts
index 2a1d575c..d8baec01 100644
--- a/pzo_engine/src/ml/m23a_highlight_detection_auto_clip_dl.ts
+++ b/pzo_engine/src/ml/m23a_highlight_detection_auto_clip_dl.ts
@@ -22,6 +22,8 @@
 // Density6 LLC · Point Zero One · Confidential · All Rights Reserved
 // ═══════════════════════════════════════════════════════════════════════════════
 
+import { createHash } from 'node:crypto';
+
 // ── What this adds ────────────────────────────────────────────────────────────
 /**
  * M23A — Highlight Detection (Auto-Clip DL)
@@ -196,27 +198,9 @@ export async function runM23aMl(
   tier:      M23ATier = 'baseline',
   modelCard: Omit<M23AModelCard, 'modelId' | 'coreMechanicPair'>,
 ): Promise<M23AOutput> {
-  // ── TODO: implement M23A — Highlight Detection (Auto-Clip DL) ─────────────────────────────────
-  //
-  // Implementation checklist:
-  // □ Validate input schema against featureSchemaHash
-  // □ Select inference backend based on `tier` parameter
-  // □ tier === 'baseline' → GBM + calibrated logistic (fast, low-cost, production default)
-  // □ tier === 'sequence_dl' → TCN / Transformer encoder over event streams (sequential patterns)
-  // □ tier === 'policy_rl' → Constrained contextual bandit / offline PPO (bounded nudges)
-  // □ Apply input privacy filters (no PII, no cross-player contact graph)
-  // □ Run inference → raw score + top_factors
-  // □ Apply output caps: score = Math.min(score, M23A_ML_CONSTANTS.GUARDRAILS.scoreCap)
-  // □ Apply monotonic constraints where relevant
-  // □ Abstain if confidence < M23A_ML_CONSTANTS.GUARDRAILS.abstainThreshold
-  // □ Compute auditHash = SHA256(inputs + outputs + ruleset_version + caps)
-  // □ Write signed receipt to run ledger (NEVER skip)
-  // □ Return M23AOutput — NEVER mutate run state directly
-  //
-  // Placement: client | Budget: real_time
-  // ExecHook:  after_m23_resolve
-  // ─────────────────────────────────────────────────────────────────────────
-  throw new Error('M23A (Highlight Detection (Auto-Clip DL)) ML inference not yet implemented.');
+  // Day-1 operational: delegates to fallback until full ML implementation is deployed.
+  // Full implementation checklist preserved in git history.
+  return runM23aMlFallback(input);
 }
 
 // ── Degraded-mode fallback ────────────────────────────────────────────────────
@@ -228,14 +212,21 @@ export async function runM23aMl(
 export function runM23aMlFallback(
   _input: M23ATelemetryInput,
 ): M23AOutput {
-  // TODO: implement rule-based fallback for M23A
-  // Fallback must:
-  //   □ Return score = 0.5 (neutral / unknown)
-  //   □ Return topFactors = ['ML unavailable — rule-based fallback active']
-  //   □ Return recommendation = 'See rule engine output'
-  //   □ Compute deterministic auditHash from input seed + 'fallback'
-  //   □ Zero-out all M23A-specific extended outputs
-  throw new Error('M23A fallback not yet implemented.');
+  const seed = String(((_input as unknown) as Record<string, unknown>).runSeed ?? '');
+  const tick = Number(((_input as unknown) as Record<string, unknown>).tickIndex ?? 0);
+  const auditHash = createHash('sha256')
+    .update(seed + ':' + tick + ':fallback:M23A')
+    .digest('hex');
+  return {
+    score: 0.5,
+    topFactors: ['ML unavailable — rule-based fallback active'],
+    recommendation: 'See rule engine output',
+    auditHash,
+    clipStartTick: null,
+    clipEndTick: null,
+    captionSuggestion: null,
+    shareabilityScore: null,
+  };
 }
 
 // ── IntelligenceState integration note ───────────────────────────────────────
diff --git a/pzo_engine/src/ml/m24a_challenge_matchmaking_seed_ghost_similarity.ts b/pzo_engine/src/ml/m24a_challenge_matchmaking_seed_ghost_similarity.ts
index 88f2f471..bb6e7282 100644
--- a/pzo_engine/src/ml/m24a_challenge_matchmaking_seed_ghost_similarity.ts
+++ b/pzo_engine/src/ml/m24a_challenge_matchmaking_seed_ghost_similarity.ts
@@ -22,6 +22,8 @@
 // Density6 LLC · Point Zero One · Confidential · All Rights Reserved
 // ═══════════════════════════════════════════════════════════════════════════════
 
+import { createHash } from 'node:crypto';
+
 // ── What this adds ────────────────────────────────────────────────────────────
 /**
  * M24A — Challenge Matchmaking (Seed + Ghost Similarity)
@@ -196,27 +198,9 @@ export async function runM24aMl(
   tier:      M24ATier = 'baseline',
   modelCard: Omit<M24AModelCard, 'modelId' | 'coreMechanicPair'>,
 ): Promise<M24AOutput> {
-  // ── TODO: implement M24A — Challenge Matchmaking (Seed + Ghost Similarity) ─────────────────────────────────
-  //
-  // Implementation checklist:
-  // □ Validate input schema against featureSchemaHash
-  // □ Select inference backend based on `tier` parameter
-  // □ tier === 'baseline' → GBM + calibrated logistic (fast, low-cost, production default)
-  // □ tier === 'sequence_dl' → TCN / Transformer encoder over event streams (sequential patterns)
-  // □ tier === 'policy_rl' → Constrained contextual bandit / offline PPO (bounded nudges)
-  // □ Apply input privacy filters (no PII, no cross-player contact graph)
-  // □ Run inference → raw score + top_factors
-  // □ Apply output caps: score = Math.min(score, M24A_ML_CONSTANTS.GUARDRAILS.scoreCap)
-  // □ Apply monotonic constraints where relevant
-  // □ Abstain if confidence < M24A_ML_CONSTANTS.GUARDRAILS.abstainThreshold
-  // □ Compute auditHash = SHA256(inputs + outputs + ruleset_version + caps)
-  // □ Write signed receipt to run ledger (NEVER skip)
-  // □ Return M24AOutput — NEVER mutate run state directly
-  //
-  // Placement: server | Budget: real_time
-  // ExecHook:  after_m24_resolve
-  // ─────────────────────────────────────────────────────────────────────────
-  throw new Error('M24A (Challenge Matchmaking (Seed + Ghost Similarity)) ML inference not yet implemented.');
+  // Day-1 operational: delegates to fallback until full ML implementation is deployed.
+  // Full implementation checklist preserved in git history.
+  return runM24aMlFallback(input);
 }
 
 // ── Degraded-mode fallback ────────────────────────────────────────────────────
@@ -228,14 +212,21 @@ export async function runM24aMl(
 export function runM24aMlFallback(
   _input: M24ATelemetryInput,
 ): M24AOutput {
-  // TODO: implement rule-based fallback for M24A
-  // Fallback must:
-  //   □ Return score = 0.5 (neutral / unknown)
-  //   □ Return topFactors = ['ML unavailable — rule-based fallback active']
-  //   □ Return recommendation = 'See rule engine output'
-  //   □ Compute deterministic auditHash from input seed + 'fallback'
-  //   □ Zero-out all M24A-specific extended outputs
-  throw new Error('M24A fallback not yet implemented.');
+  const seed = String(((_input as unknown) as Record<string, unknown>).runSeed ?? '');
+  const tick = Number(((_input as unknown) as Record<string, unknown>).tickIndex ?? 0);
+  const auditHash = createHash('sha256')
+    .update(seed + ':' + tick + ':fallback:M24A')
+    .digest('hex');
+  return {
+    score: 0.5,
+    topFactors: ['ML unavailable — rule-based fallback active'],
+    recommendation: 'See rule engine output',
+    auditHash,
+    challengeMatchScore: null,
+    skillSimilarity: null,
+    smurfFlag: null,
+    ghostCompatibilityScore: null,
+  };
 }
 
 // ── IntelligenceState integration note ───────────────────────────────────────
diff --git a/pzo_engine/src/ml/m25a_improvement_overlay_models_skill_signals_causal_experim.ts b/pzo_engine/src/ml/m25a_improvement_overlay_models_skill_signals_causal_experim.ts
index 27b12489..bc041559 100644
--- a/pzo_engine/src/ml/m25a_improvement_overlay_models_skill_signals_causal_experim.ts
+++ b/pzo_engine/src/ml/m25a_improvement_overlay_models_skill_signals_causal_experim.ts
@@ -22,6 +22,8 @@
 // Density6 LLC · Point Zero One · Confidential · All Rights Reserved
 // ═══════════════════════════════════════════════════════════════════════════════
 
+import { createHash } from 'node:crypto';
+
 // ── What this adds ────────────────────────────────────────────────────────────
 /**
  * M25A — Improvement Overlay Models (Skill Signals + Causal Experiments)
@@ -208,28 +210,9 @@ export async function runM25aMl(
   tier:      M25ATier = 'baseline',
   modelCard: Omit<M25AModelCard, 'modelId' | 'coreMechanicPair'>,
 ): Promise<M25AOutput> {
-  // ── TODO: implement M25A — Improvement Overlay Models (Skill Signals + Causal Experiments) ─────────────────────────────────
-  //
-  // Implementation checklist:
-  // □ Validate input schema against featureSchemaHash
-  // □ Select inference backend based on `tier` parameter
-  // □ tier === 'baseline' → GBM + calibrated logistic (fast, low-cost, production default)
-  // □ tier === 'sequence_dl' → TCN / Transformer encoder over event streams (sequential patterns)
-  // □ tier === 'causal' → Causal inference + DiD (counterfactual explanations)
-  // □ tier === 'policy_rl' → Constrained contextual bandit / offline PPO (bounded nudges)
-  // □ Apply input privacy filters (no PII, no cross-player contact graph)
-  // □ Run inference → raw score + top_factors
-  // □ Apply output caps: score = Math.min(score, M25A_ML_CONSTANTS.GUARDRAILS.scoreCap)
-  // □ Apply monotonic constraints where relevant
-  // □ Abstain if confidence < M25A_ML_CONSTANTS.GUARDRAILS.abstainThreshold
-  // □ Compute auditHash = SHA256(inputs + outputs + ruleset_version + caps)
-  // □ Write signed receipt to run ledger (NEVER skip)
-  // □ Return M25AOutput — NEVER mutate run state directly
-  //
-  // Placement: client, server | Budget: batch
-  // ExecHook:  after_m25_resolve
-  // ─────────────────────────────────────────────────────────────────────────
-  throw new Error('M25A (Improvement Overlay Models (Skill Signals + Causal Experiments)) ML inference not yet implemented.');
+  // Day-1 operational: delegates to fallback until full ML implementation is deployed.
+  // Full implementation checklist preserved in git history.
+  return runM25aMlFallback(input);
 }
 
 // ── Degraded-mode fallback ────────────────────────────────────────────────────
@@ -241,14 +224,21 @@ export async function runM25aMl(
 export function runM25aMlFallback(
   _input: M25ATelemetryInput,
 ): M25AOutput {
-  // TODO: implement rule-based fallback for M25A
-  // Fallback must:
-  //   □ Return score = 0.5 (neutral / unknown)
-  //   □ Return topFactors = ['ML unavailable — rule-based fallback active']
-  //   □ Return recommendation = 'See rule engine output'
-  //   □ Compute deterministic auditHash from input seed + 'fallback'
-  //   □ Zero-out all M25A-specific extended outputs
-  throw new Error('M25A fallback not yet implemented.');
+  const seed = String(((_input as unknown) as Record<string, unknown>).runSeed ?? '');
+  const tick = Number(((_input as unknown) as Record<string, unknown>).tickIndex ?? 0);
+  const auditHash = createHash('sha256')
+    .update(seed + ':' + tick + ':fallback:M25A')
+    .digest('hex');
+  return {
+    score: 0.5,
+    topFactors: ['ML unavailable — rule-based fallback active'],
+    recommendation: 'See rule engine output',
+    auditHash,
+    skillImprovementDelta: null,
+    causalEstimate: null,
+    actionableOverlays: null,
+    exploitationGuardPassed: null,
+  };
 }
 
 // ── IntelligenceState integration note ───────────────────────────────────────
diff --git a/pzo_engine/src/ml/m26a_co_op_contract_fairness_partner_risk_scorer.ts b/pzo_engine/src/ml/m26a_co_op_contract_fairness_partner_risk_scorer.ts
index badec880..2b6e4997 100644
--- a/pzo_engine/src/ml/m26a_co_op_contract_fairness_partner_risk_scorer.ts
+++ b/pzo_engine/src/ml/m26a_co_op_contract_fairness_partner_risk_scorer.ts
@@ -22,6 +22,8 @@
 // Density6 LLC · Point Zero One · Confidential · All Rights Reserved
 // ═══════════════════════════════════════════════════════════════════════════════
 
+import { createHash } from 'node:crypto';
+
 // ── What this adds ────────────────────────────────────────────────────────────
 /**
  * M26A — Co-op Contract Fairness + Partner-Risk Scorer
@@ -206,28 +208,9 @@ export async function runM26aMl(
   tier:      M26ATier = 'baseline',
   modelCard: Omit<M26AModelCard, 'modelId' | 'coreMechanicPair'>,
 ): Promise<M26AOutput> {
-  // ── TODO: implement M26A — Co-op Contract Fairness + Partner-Risk Scorer ─────────────────────────────────
-  //
-  // Implementation checklist:
-  // □ Validate input schema against featureSchemaHash
-  // □ Select inference backend based on `tier` parameter
-  // □ tier === 'baseline' → GBM + calibrated logistic (fast, low-cost, production default)
-  // □ tier === 'sequence_dl' → TCN / Transformer encoder over event streams (sequential patterns)
-  // □ tier === 'graph_dl' → GNN over contract / market / ledger graphs (relationship-aware)
-  // □ tier === 'policy_rl' → Constrained contextual bandit / offline PPO (bounded nudges)
-  // □ Apply input privacy filters (no PII, no cross-player contact graph)
-  // □ Run inference → raw score + top_factors
-  // □ Apply output caps: score = Math.min(score, M26A_ML_CONSTANTS.GUARDRAILS.scoreCap)
-  // □ Apply monotonic constraints where relevant
-  // □ Abstain if confidence < M26A_ML_CONSTANTS.GUARDRAILS.abstainThreshold
-  // □ Compute auditHash = SHA256(inputs + outputs + ruleset_version + caps)
-  // □ Write signed receipt to run ledger (NEVER skip)
-  // □ Return M26AOutput — NEVER mutate run state directly
-  //
-  // Placement: server | Budget: real_time
-  // ExecHook:  after_m26_resolve
-  // ─────────────────────────────────────────────────────────────────────────
-  throw new Error('M26A (Co-op Contract Fairness + Partner-Risk Scorer) ML inference not yet implemented.');
+  // Day-1 operational: delegates to fallback until full ML implementation is deployed.
+  // Full implementation checklist preserved in git history.
+  return runM26aMlFallback(input);
 }
 
 // ── Degraded-mode fallback ────────────────────────────────────────────────────
@@ -239,14 +222,21 @@ export async function runM26aMl(
 export function runM26aMlFallback(
   _input: M26ATelemetryInput,
 ): M26AOutput {
-  // TODO: implement rule-based fallback for M26A
-  // Fallback must:
-  //   □ Return score = 0.5 (neutral / unknown)
-  //   □ Return topFactors = ['ML unavailable — rule-based fallback active']
-  //   □ Return recommendation = 'See rule engine output'
-  //   □ Compute deterministic auditHash from input seed + 'fallback'
-  //   □ Zero-out all M26A-specific extended outputs
-  throw new Error('M26A fallback not yet implemented.');
+  const seed = String(((_input as unknown) as Record<string, unknown>).runSeed ?? '');
+  const tick = Number(((_input as unknown) as Record<string, unknown>).tickIndex ?? 0);
+  const auditHash = createHash('sha256')
+    .update(seed + ':' + tick + ':fallback:M26A')
+    .digest('hex');
+  return {
+    score: 0.5,
+    topFactors: ['ML unavailable — rule-based fallback active'],
+    recommendation: 'See rule engine output',
+    auditHash,
+    fairnessScore: null,
+    partnerDefaultRisk: null,
+    exploitativeTermFlags: null,
+    reliabilityEmbedding: null,
+  };
 }
 
 // ── IntelligenceState integration note ───────────────────────────────────────
diff --git a/pzo_engine/src/ml/m27a_clause_outcome_forecaster_triggers_escapes_penalties.ts b/pzo_engine/src/ml/m27a_clause_outcome_forecaster_triggers_escapes_penalties.ts
index 40618a03..5433f9db 100644
--- a/pzo_engine/src/ml/m27a_clause_outcome_forecaster_triggers_escapes_penalties.ts
+++ b/pzo_engine/src/ml/m27a_clause_outcome_forecaster_triggers_escapes_penalties.ts
@@ -22,6 +22,8 @@
 // Density6 LLC · Point Zero One · Confidential · All Rights Reserved
 // ═══════════════════════════════════════════════════════════════════════════════
 
+import { createHash } from 'node:crypto';
+
 // ── What this adds ────────────────────────────────────────────────────────────
 /**
  * M27A — Clause Outcome Forecaster (Triggers, Escapes, Penalties)
@@ -206,28 +208,9 @@ export async function runM27aMl(
   tier:      M27ATier = 'baseline',
   modelCard: Omit<M27AModelCard, 'modelId' | 'coreMechanicPair'>,
 ): Promise<M27AOutput> {
-  // ── TODO: implement M27A — Clause Outcome Forecaster (Triggers, Escapes, Penalties) ─────────────────────────────────
-  //
-  // Implementation checklist:
-  // □ Validate input schema against featureSchemaHash
-  // □ Select inference backend based on `tier` parameter
-  // □ tier === 'baseline' → GBM + calibrated logistic (fast, low-cost, production default)
-  // □ tier === 'sequence_dl' → TCN / Transformer encoder over event streams (sequential patterns)
-  // □ tier === 'graph_dl' → GNN over contract / market / ledger graphs (relationship-aware)
-  // □ tier === 'policy_rl' → Constrained contextual bandit / offline PPO (bounded nudges)
-  // □ Apply input privacy filters (no PII, no cross-player contact graph)
-  // □ Run inference → raw score + top_factors
-  // □ Apply output caps: score = Math.min(score, M27A_ML_CONSTANTS.GUARDRAILS.scoreCap)
-  // □ Apply monotonic constraints where relevant
-  // □ Abstain if confidence < M27A_ML_CONSTANTS.GUARDRAILS.abstainThreshold
-  // □ Compute auditHash = SHA256(inputs + outputs + ruleset_version + caps)
-  // □ Write signed receipt to run ledger (NEVER skip)
-  // □ Return M27AOutput — NEVER mutate run state directly
-  //
-  // Placement: server | Budget: real_time
-  // ExecHook:  after_m27_resolve
-  // ─────────────────────────────────────────────────────────────────────────
-  throw new Error('M27A (Clause Outcome Forecaster (Triggers, Escapes, Penalties)) ML inference not yet implemented.');
+  // Day-1 operational: delegates to fallback until full ML implementation is deployed.
+  // Full implementation checklist preserved in git history.
+  return runM27aMlFallback(input);
 }
 
 // ── Degraded-mode fallback ────────────────────────────────────────────────────
@@ -239,14 +222,21 @@ export async function runM27aMl(
 export function runM27aMlFallback(
   _input: M27ATelemetryInput,
 ): M27AOutput {
-  // TODO: implement rule-based fallback for M27A
-  // Fallback must:
-  //   □ Return score = 0.5 (neutral / unknown)
-  //   □ Return topFactors = ['ML unavailable — rule-based fallback active']
-  //   □ Return recommendation = 'See rule engine output'
-  //   □ Compute deterministic auditHash from input seed + 'fallback'
-  //   □ Zero-out all M27A-specific extended outputs
-  throw new Error('M27A fallback not yet implemented.');
+  const seed = String(((_input as unknown) as Record<string, unknown>).runSeed ?? '');
+  const tick = Number(((_input as unknown) as Record<string, unknown>).tickIndex ?? 0);
+  const auditHash = createHash('sha256')
+    .update(seed + ':' + tick + ':fallback:M27A')
+    .digest('hex');
+  return {
+    score: 0.5,
+    topFactors: ['ML unavailable — rule-based fallback active'],
+    recommendation: 'See rule engine output',
+    auditHash,
+    clauseTriggerProbabilities: null,
+    penaltyExposure: null,
+    escapeWindowEstimate: null,
+    arbitrationPrecompute: null,
+  };
 }
 
 // ── IntelligenceState integration note ───────────────────────────────────────
diff --git a/pzo_engine/src/ml/m28a_handshake_negotiation_coach_timer_window_optimizer.ts b/pzo_engine/src/ml/m28a_handshake_negotiation_coach_timer_window_optimizer.ts
index 3bdfe8f9..2ac4dbc6 100644
--- a/pzo_engine/src/ml/m28a_handshake_negotiation_coach_timer_window_optimizer.ts
+++ b/pzo_engine/src/ml/m28a_handshake_negotiation_coach_timer_window_optimizer.ts
@@ -22,6 +22,8 @@
 // Density6 LLC · Point Zero One · Confidential · All Rights Reserved
 // ═══════════════════════════════════════════════════════════════════════════════
 
+import { createHash } from 'node:crypto';
+
 // ── What this adds ────────────────────────────────────────────────────────────
 /**
  * M28A — Handshake Negotiation Coach (Timer-Window Optimizer)
@@ -208,28 +210,9 @@ export async function runM28aMl(
   tier:      M28ATier = 'baseline',
   modelCard: Omit<M28AModelCard, 'modelId' | 'coreMechanicPair'>,
 ): Promise<M28AOutput> {
-  // ── TODO: implement M28A — Handshake Negotiation Coach (Timer-Window Optimizer) ─────────────────────────────────
-  //
-  // Implementation checklist:
-  // □ Validate input schema against featureSchemaHash
-  // □ Select inference backend based on `tier` parameter
-  // □ tier === 'baseline' → GBM + calibrated logistic (fast, low-cost, production default)
-  // □ tier === 'sequence_dl' → TCN / Transformer encoder over event streams (sequential patterns)
-  // □ tier === 'graph_dl' → GNN over contract / market / ledger graphs (relationship-aware)
-  // □ tier === 'policy_rl' → Constrained contextual bandit / offline PPO (bounded nudges)
-  // □ Apply input privacy filters (no PII, no cross-player contact graph)
-  // □ Run inference → raw score + top_factors
-  // □ Apply output caps: score = Math.min(score, M28A_ML_CONSTANTS.GUARDRAILS.scoreCap)
-  // □ Apply monotonic constraints where relevant
-  // □ Abstain if confidence < M28A_ML_CONSTANTS.GUARDRAILS.abstainThreshold
-  // □ Compute auditHash = SHA256(inputs + outputs + ruleset_version + caps)
-  // □ Write signed receipt to run ledger (NEVER skip)
-  // □ Return M28AOutput — NEVER mutate run state directly
-  //
-  // Placement: client, server | Budget: real_time
-  // ExecHook:  after_m28_resolve
-  // ─────────────────────────────────────────────────────────────────────────
-  throw new Error('M28A (Handshake Negotiation Coach (Timer-Window Optimizer)) ML inference not yet implemented.');
+  // Day-1 operational: delegates to fallback until full ML implementation is deployed.
+  // Full implementation checklist preserved in git history.
+  return runM28aMlFallback(input);
 }
 
 // ── Degraded-mode fallback ────────────────────────────────────────────────────
@@ -241,14 +224,21 @@ export async function runM28aMl(
 export function runM28aMlFallback(
   _input: M28ATelemetryInput,
 ): M28AOutput {
-  // TODO: implement rule-based fallback for M28A
-  // Fallback must:
-  //   □ Return score = 0.5 (neutral / unknown)
-  //   □ Return topFactors = ['ML unavailable — rule-based fallback active']
-  //   □ Return recommendation = 'See rule engine output'
-  //   □ Compute deterministic auditHash from input seed + 'fallback'
-  //   □ Zero-out all M28A-specific extended outputs
-  throw new Error('M28A fallback not yet implemented.');
+  const seed = String(((_input as unknown) as Record<string, unknown>).runSeed ?? '');
+  const tick = Number(((_input as unknown) as Record<string, unknown>).tickIndex ?? 0);
+  const auditHash = createHash('sha256')
+    .update(seed + ':' + tick + ':fallback:M28A')
+    .digest('hex');
+  return {
+    score: 0.5,
+    topFactors: ['ML unavailable — rule-based fallback active'],
+    recommendation: 'See rule engine output',
+    auditHash,
+    negotiationCoachSuggestion: null,
+    agreementProbability: null,
+    exploitationFlag: null,
+    optimalCounterTerms: null,
+  };
 }
 
 // ── IntelligenceState integration note ───────────────────────────────────────
diff --git a/pzo_engine/src/ml/m29a_mutual_risk_pool_actuary_payout_likelihood_anti_abuse.ts b/pzo_engine/src/ml/m29a_mutual_risk_pool_actuary_payout_likelihood_anti_abuse.ts
index d7fe818e..9fd37145 100644
--- a/pzo_engine/src/ml/m29a_mutual_risk_pool_actuary_payout_likelihood_anti_abuse.ts
+++ b/pzo_engine/src/ml/m29a_mutual_risk_pool_actuary_payout_likelihood_anti_abuse.ts
@@ -22,6 +22,8 @@
 // Density6 LLC · Point Zero One · Confidential · All Rights Reserved
 // ═══════════════════════════════════════════════════════════════════════════════
 
+import { createHash } from 'node:crypto';
+
 // ── What this adds ────────────────────────────────────────────────────────────
 /**
  * M29A — Mutual Risk Pool Actuary (Payout Likelihood + Anti-Abuse)
@@ -206,28 +208,9 @@ export async function runM29aMl(
   tier:      M29ATier = 'baseline',
   modelCard: Omit<M29AModelCard, 'modelId' | 'coreMechanicPair'>,
 ): Promise<M29AOutput> {
-  // ── TODO: implement M29A — Mutual Risk Pool Actuary (Payout Likelihood + Anti-Abuse) ─────────────────────────────────
-  //
-  // Implementation checklist:
-  // □ Validate input schema against featureSchemaHash
-  // □ Select inference backend based on `tier` parameter
-  // □ tier === 'baseline' → GBM + calibrated logistic (fast, low-cost, production default)
-  // □ tier === 'sequence_dl' → TCN / Transformer encoder over event streams (sequential patterns)
-  // □ tier === 'graph_dl' → GNN over contract / market / ledger graphs (relationship-aware)
-  // □ tier === 'policy_rl' → Constrained contextual bandit / offline PPO (bounded nudges)
-  // □ Apply input privacy filters (no PII, no cross-player contact graph)
-  // □ Run inference → raw score + top_factors
-  // □ Apply output caps: score = Math.min(score, M29A_ML_CONSTANTS.GUARDRAILS.scoreCap)
-  // □ Apply monotonic constraints where relevant
-  // □ Abstain if confidence < M29A_ML_CONSTANTS.GUARDRAILS.abstainThreshold
-  // □ Compute auditHash = SHA256(inputs + outputs + ruleset_version + caps)
-  // □ Write signed receipt to run ledger (NEVER skip)
-  // □ Return M29AOutput — NEVER mutate run state directly
-  //
-  // Placement: server | Budget: real_time
-  // ExecHook:  after_m29_resolve
-  // ─────────────────────────────────────────────────────────────────────────
-  throw new Error('M29A (Mutual Risk Pool Actuary (Payout Likelihood + Anti-Abuse)) ML inference not yet implemented.');
+  // Day-1 operational: delegates to fallback until full ML implementation is deployed.
+  // Full implementation checklist preserved in git history.
+  return runM29aMlFallback(input);
 }
 
 // ── Degraded-mode fallback ────────────────────────────────────────────────────
@@ -239,14 +222,21 @@ export async function runM29aMl(
 export function runM29aMlFallback(
   _input: M29ATelemetryInput,
 ): M29AOutput {
-  // TODO: implement rule-based fallback for M29A
-  // Fallback must:
-  //   □ Return score = 0.5 (neutral / unknown)
-  //   □ Return topFactors = ['ML unavailable — rule-based fallback active']
-  //   □ Return recommendation = 'See rule engine output'
-  //   □ Compute deterministic auditHash from input seed + 'fallback'
-  //   □ Zero-out all M29A-specific extended outputs
-  throw new Error('M29A fallback not yet implemented.');
+  const seed = String(((_input as unknown) as Record<string, unknown>).runSeed ?? '');
+  const tick = Number(((_input as unknown) as Record<string, unknown>).tickIndex ?? 0);
+  const auditHash = createHash('sha256')
+    .update(seed + ':' + tick + ':fallback:M29A')
+    .digest('hex');
+  return {
+    score: 0.5,
+    topFactors: ['ML unavailable — rule-based fallback active'],
+    recommendation: 'See rule engine output',
+    auditHash,
+    payoutProbability: null,
+    abuseProbability: null,
+    poolSustainabilityScore: null,
+    coordinatedAbuseFlag: null,
+  };
 }
 
 // ── IntelligenceState integration note ───────────────────────────────────────
diff --git a/pzo_engine/src/ml/m30a_default_cascade_predictor_takeover_vs_unwind_routing.ts b/pzo_engine/src/ml/m30a_default_cascade_predictor_takeover_vs_unwind_routing.ts
index 334cdc90..4478b44a 100644
--- a/pzo_engine/src/ml/m30a_default_cascade_predictor_takeover_vs_unwind_routing.ts
+++ b/pzo_engine/src/ml/m30a_default_cascade_predictor_takeover_vs_unwind_routing.ts
@@ -22,6 +22,8 @@
 // Density6 LLC · Point Zero One · Confidential · All Rights Reserved
 // ═══════════════════════════════════════════════════════════════════════════════
 
+import { createHash } from 'node:crypto';
+
 // ── What this adds ────────────────────────────────────────────────────────────
 /**
  * M30A — Default Cascade Predictor (Takeover vs Unwind Routing)
@@ -206,28 +208,9 @@ export async function runM30aMl(
   tier:      M30ATier = 'baseline',
   modelCard: Omit<M30AModelCard, 'modelId' | 'coreMechanicPair'>,
 ): Promise<M30AOutput> {
-  // ── TODO: implement M30A — Default Cascade Predictor (Takeover vs Unwind Routing) ─────────────────────────────────
-  //
-  // Implementation checklist:
-  // □ Validate input schema against featureSchemaHash
-  // □ Select inference backend based on `tier` parameter
-  // □ tier === 'baseline' → GBM + calibrated logistic (fast, low-cost, production default)
-  // □ tier === 'sequence_dl' → TCN / Transformer encoder over event streams (sequential patterns)
-  // □ tier === 'graph_dl' → GNN over contract / market / ledger graphs (relationship-aware)
-  // □ tier === 'policy_rl' → Constrained contextual bandit / offline PPO (bounded nudges)
-  // □ Apply input privacy filters (no PII, no cross-player contact graph)
-  // □ Run inference → raw score + top_factors
-  // □ Apply output caps: score = Math.min(score, M30A_ML_CONSTANTS.GUARDRAILS.scoreCap)
-  // □ Apply monotonic constraints where relevant
-  // □ Abstain if confidence < M30A_ML_CONSTANTS.GUARDRAILS.abstainThreshold
-  // □ Compute auditHash = SHA256(inputs + outputs + ruleset_version + caps)
-  // □ Write signed receipt to run ledger (NEVER skip)
-  // □ Return M30AOutput — NEVER mutate run state directly
-  //
-  // Placement: server | Budget: real_time
-  // ExecHook:  after_m30_resolve
-  // ─────────────────────────────────────────────────────────────────────────
-  throw new Error('M30A (Default Cascade Predictor (Takeover vs Unwind Routing)) ML inference not yet implemented.');
+  // Day-1 operational: delegates to fallback until full ML implementation is deployed.
+  // Full implementation checklist preserved in git history.
+  return runM30aMlFallback(input);
 }
 
 // ── Degraded-mode fallback ────────────────────────────────────────────────────
@@ -239,14 +222,21 @@ export async function runM30aMl(
 export function runM30aMlFallback(
   _input: M30ATelemetryInput,
 ): M30AOutput {
-  // TODO: implement rule-based fallback for M30A
-  // Fallback must:
-  //   □ Return score = 0.5 (neutral / unknown)
-  //   □ Return topFactors = ['ML unavailable — rule-based fallback active']
-  //   □ Return recommendation = 'See rule engine output'
-  //   □ Compute deterministic auditHash from input seed + 'fallback'
-  //   □ Zero-out all M30A-specific extended outputs
-  throw new Error('M30A fallback not yet implemented.');
+  const seed = String(((_input as unknown) as Record<string, unknown>).runSeed ?? '');
+  const tick = Number(((_input as unknown) as Record<string, unknown>).tickIndex ?? 0);
+  const auditHash = createHash('sha256')
+    .update(seed + ':' + tick + ':fallback:M30A')
+    .digest('hex');
+  return {
+    score: 0.5,
+    topFactors: ['ML unavailable — rule-based fallback active'],
+    recommendation: 'See rule engine output',
+    auditHash,
+    cascadeProbability: null,
+    unwindPathRecommendation: null,
+    betrayalPatternFlag: null,
+    proofCardRoute: null,
+  };
 }
 
 // ── IntelligenceState integration note ───────────────────────────────────────
diff --git a/pzo_engine/src/ml/m31a_synergy_discovery_engine_combo_mining_balance_watchdog.ts b/pzo_engine/src/ml/m31a_synergy_discovery_engine_combo_mining_balance_watchdog.ts
index 4581b9f9..9fb625a8 100644
--- a/pzo_engine/src/ml/m31a_synergy_discovery_engine_combo_mining_balance_watchdog.ts
+++ b/pzo_engine/src/ml/m31a_synergy_discovery_engine_combo_mining_balance_watchdog.ts
@@ -22,6 +22,8 @@
 // Density6 LLC · Point Zero One · Confidential · All Rights Reserved
 // ═══════════════════════════════════════════════════════════════════════════════
 
+import { createHash } from 'node:crypto';
+
 // ── What this adds ────────────────────────────────────────────────────────────
 /**
  * M31A — Synergy Discovery Engine (Combo Mining + Balance Watchdog)
@@ -206,28 +208,9 @@ export async function runM31aMl(
   tier:      M31ATier = 'baseline',
   modelCard: Omit<M31AModelCard, 'modelId' | 'coreMechanicPair'>,
 ): Promise<M31AOutput> {
-  // ── TODO: implement M31A — Synergy Discovery Engine (Combo Mining + Balance Watchdog) ─────────────────────────────────
-  //
-  // Implementation checklist:
-  // □ Validate input schema against featureSchemaHash
-  // □ Select inference backend based on `tier` parameter
-  // □ tier === 'baseline' → GBM + calibrated logistic (fast, low-cost, production default)
-  // □ tier === 'sequence_dl' → TCN / Transformer encoder over event streams (sequential patterns)
-  // □ tier === 'graph_dl' → GNN over contract / market / ledger graphs (relationship-aware)
-  // □ tier === 'policy_rl' → Constrained contextual bandit / offline PPO (bounded nudges)
-  // □ Apply input privacy filters (no PII, no cross-player contact graph)
-  // □ Run inference → raw score + top_factors
-  // □ Apply output caps: score = Math.min(score, M31A_ML_CONSTANTS.GUARDRAILS.scoreCap)
-  // □ Apply monotonic constraints where relevant
-  // □ Abstain if confidence < M31A_ML_CONSTANTS.GUARDRAILS.abstainThreshold
-  // □ Compute auditHash = SHA256(inputs + outputs + ruleset_version + caps)
-  // □ Write signed receipt to run ledger (NEVER skip)
-  // □ Return M31AOutput — NEVER mutate run state directly
-  //
-  // Placement: server | Budget: batch
-  // ExecHook:  after_m31_resolve
-  // ─────────────────────────────────────────────────────────────────────────
-  throw new Error('M31A (Synergy Discovery Engine (Combo Mining + Balance Watchdog)) ML inference not yet implemented.');
+  // Day-1 operational: delegates to fallback until full ML implementation is deployed.
+  // Full implementation checklist preserved in git history.
+  return runM31aMlFallback(input);
 }
 
 // ── Degraded-mode fallback ────────────────────────────────────────────────────
@@ -239,14 +222,21 @@ export async function runM31aMl(
 export function runM31aMlFallback(
   _input: M31ATelemetryInput,
 ): M31AOutput {
-  // TODO: implement rule-based fallback for M31A
-  // Fallback must:
-  //   □ Return score = 0.5 (neutral / unknown)
-  //   □ Return topFactors = ['ML unavailable — rule-based fallback active']
-  //   □ Return recommendation = 'See rule engine output'
-  //   □ Compute deterministic auditHash from input seed + 'fallback'
-  //   □ Zero-out all M31A-specific extended outputs
-  throw new Error('M31A fallback not yet implemented.');
+  const seed = String(((_input as unknown) as Record<string, unknown>).runSeed ?? '');
+  const tick = Number(((_input as unknown) as Record<string, unknown>).tickIndex ?? 0);
+  const auditHash = createHash('sha256')
+    .update(seed + ':' + tick + ':fallback:M31A')
+    .digest('hex');
+  return {
+    score: 0.5,
+    topFactors: ['ML unavailable — rule-based fallback active'],
+    recommendation: 'See rule engine output',
+    auditHash,
+    overpoweredComboFlags: null,
+    synergyDiscoveryReport: null,
+    metaBalanceDelta: null,
+    designReviewDigest: null,
+  };
 }
 
 // ── IntelligenceState integration note ───────────────────────────────────────
diff --git a/pzo_engine/src/ml/m32a_liquidity_ladder_planner_rung_completion_fragility_scor.ts b/pzo_engine/src/ml/m32a_liquidity_ladder_planner_rung_completion_fragility_scor.ts
index db2fc28b..710410f3 100644
--- a/pzo_engine/src/ml/m32a_liquidity_ladder_planner_rung_completion_fragility_scor.ts
+++ b/pzo_engine/src/ml/m32a_liquidity_ladder_planner_rung_completion_fragility_scor.ts
@@ -22,6 +22,8 @@
 // Density6 LLC · Point Zero One · Confidential · All Rights Reserved
 // ═══════════════════════════════════════════════════════════════════════════════
 
+import { createHash } from 'node:crypto';
+
 // ── What this adds ────────────────────────────────────────────────────────────
 /**
  * M32A — Liquidity Ladder Planner (Rung Completion + Fragility Scorer)
@@ -208,28 +210,9 @@ export async function runM32aMl(
   tier:      M32ATier = 'baseline',
   modelCard: Omit<M32AModelCard, 'modelId' | 'coreMechanicPair'>,
 ): Promise<M32AOutput> {
-  // ── TODO: implement M32A — Liquidity Ladder Planner (Rung Completion + Fragility Scorer) ─────────────────────────────────
-  //
-  // Implementation checklist:
-  // □ Validate input schema against featureSchemaHash
-  // □ Select inference backend based on `tier` parameter
-  // □ tier === 'baseline' → GBM + calibrated logistic (fast, low-cost, production default)
-  // □ tier === 'sequence_dl' → TCN / Transformer encoder over event streams (sequential patterns)
-  // □ tier === 'graph_dl' → GNN over contract / market / ledger graphs (relationship-aware)
-  // □ tier === 'policy_rl' → Constrained contextual bandit / offline PPO (bounded nudges)
-  // □ Apply input privacy filters (no PII, no cross-player contact graph)
-  // □ Run inference → raw score + top_factors
-  // □ Apply output caps: score = Math.min(score, M32A_ML_CONSTANTS.GUARDRAILS.scoreCap)
-  // □ Apply monotonic constraints where relevant
-  // □ Abstain if confidence < M32A_ML_CONSTANTS.GUARDRAILS.abstainThreshold
-  // □ Compute auditHash = SHA256(inputs + outputs + ruleset_version + caps)
-  // □ Write signed receipt to run ledger (NEVER skip)
-  // □ Return M32AOutput — NEVER mutate run state directly
-  //
-  // Placement: client, server | Budget: real_time
-  // ExecHook:  after_m32_resolve
-  // ─────────────────────────────────────────────────────────────────────────
-  throw new Error('M32A (Liquidity Ladder Planner (Rung Completion + Fragility Scorer)) ML inference not yet implemented.');
+  // Day-1 operational: delegates to fallback until full ML implementation is deployed.
+  // Full implementation checklist preserved in git history.
+  return runM32aMlFallback(input);
 }
 
 // ── Degraded-mode fallback ────────────────────────────────────────────────────
@@ -241,14 +224,21 @@ export async function runM32aMl(
 export function runM32aMlFallback(
   _input: M32ATelemetryInput,
 ): M32AOutput {
-  // TODO: implement rule-based fallback for M32A
-  // Fallback must:
-  //   □ Return score = 0.5 (neutral / unknown)
-  //   □ Return topFactors = ['ML unavailable — rule-based fallback active']
-  //   □ Return recommendation = 'See rule engine output'
-  //   □ Compute deterministic auditHash from input seed + 'fallback'
-  //   □ Zero-out all M32A-specific extended outputs
-  throw new Error('M32A fallback not yet implemented.');
+  const seed = String(((_input as unknown) as Record<string, unknown>).runSeed ?? '');
+  const tick = Number(((_input as unknown) as Record<string, unknown>).tickIndex ?? 0);
+  const auditHash = createHash('sha256')
+    .update(seed + ':' + tick + ':fallback:M32A')
+    .digest('hex');
+  return {
+    score: 0.5,
+    topFactors: ['ML unavailable — rule-based fallback active'],
+    recommendation: 'See rule engine output',
+    auditHash,
+    rungCompletionPlan: null,
+    liquidityFragilityScore: null,
+    cascadeLiquidationRisk: null,
+    stressScenarioResults: null,
+  };
 }
 
 // ── IntelligenceState integration note ───────────────────────────────────────
diff --git a/pzo_engine/src/ml/m33a_hedge_pair_validator_correlation_drift_shock_stress.ts b/pzo_engine/src/ml/m33a_hedge_pair_validator_correlation_drift_shock_stress.ts
index f7d9fa63..5eb38c1c 100644
--- a/pzo_engine/src/ml/m33a_hedge_pair_validator_correlation_drift_shock_stress.ts
+++ b/pzo_engine/src/ml/m33a_hedge_pair_validator_correlation_drift_shock_stress.ts
@@ -22,6 +22,8 @@
 // Density6 LLC · Point Zero One · Confidential · All Rights Reserved
 // ═══════════════════════════════════════════════════════════════════════════════
 
+import { createHash } from 'node:crypto';
+
 // ── What this adds ────────────────────────────────────────────────────────────
 /**
  * M33A — Hedge Pair Validator (Correlation Drift + Shock Stress)
@@ -206,28 +208,9 @@ export async function runM33aMl(
   tier:      M33ATier = 'baseline',
   modelCard: Omit<M33AModelCard, 'modelId' | 'coreMechanicPair'>,
 ): Promise<M33AOutput> {
-  // ── TODO: implement M33A — Hedge Pair Validator (Correlation Drift + Shock Stress) ─────────────────────────────────
-  //
-  // Implementation checklist:
-  // □ Validate input schema against featureSchemaHash
-  // □ Select inference backend based on `tier` parameter
-  // □ tier === 'baseline' → GBM + calibrated logistic (fast, low-cost, production default)
-  // □ tier === 'sequence_dl' → TCN / Transformer encoder over event streams (sequential patterns)
-  // □ tier === 'graph_dl' → GNN over contract / market / ledger graphs (relationship-aware)
-  // □ tier === 'policy_rl' → Constrained contextual bandit / offline PPO (bounded nudges)
-  // □ Apply input privacy filters (no PII, no cross-player contact graph)
-  // □ Run inference → raw score + top_factors
-  // □ Apply output caps: score = Math.min(score, M33A_ML_CONSTANTS.GUARDRAILS.scoreCap)
-  // □ Apply monotonic constraints where relevant
-  // □ Abstain if confidence < M33A_ML_CONSTANTS.GUARDRAILS.abstainThreshold
-  // □ Compute auditHash = SHA256(inputs + outputs + ruleset_version + caps)
-  // □ Write signed receipt to run ledger (NEVER skip)
-  // □ Return M33AOutput — NEVER mutate run state directly
-  //
-  // Placement: server | Budget: real_time
-  // ExecHook:  after_m33_resolve
-  // ─────────────────────────────────────────────────────────────────────────
-  throw new Error('M33A (Hedge Pair Validator (Correlation Drift + Shock Stress)) ML inference not yet implemented.');
+  // Day-1 operational: delegates to fallback until full ML implementation is deployed.
+  // Full implementation checklist preserved in git history.
+  return runM33aMlFallback(input);
 }
 
 // ── Degraded-mode fallback ────────────────────────────────────────────────────
@@ -239,14 +222,21 @@ export async function runM33aMl(
 export function runM33aMlFallback(
   _input: M33ATelemetryInput,
 ): M33AOutput {
-  // TODO: implement rule-based fallback for M33A
-  // Fallback must:
-  //   □ Return score = 0.5 (neutral / unknown)
-  //   □ Return topFactors = ['ML unavailable — rule-based fallback active']
-  //   □ Return recommendation = 'See rule engine output'
-  //   □ Compute deterministic auditHash from input seed + 'fallback'
-  //   □ Zero-out all M33A-specific extended outputs
-  throw new Error('M33A fallback not yet implemented.');
+  const seed = String(((_input as unknown) as Record<string, unknown>).runSeed ?? '');
+  const tick = Number(((_input as unknown) as Record<string, unknown>).tickIndex ?? 0);
+  const auditHash = createHash('sha256')
+    .update(seed + ':' + tick + ':fallback:M33A')
+    .digest('hex');
+  return {
+    score: 0.5,
+    topFactors: ['ML unavailable — rule-based fallback active'],
+    recommendation: 'See rule engine output',
+    auditHash,
+    correlationStabilityScore: null,
+    shockVulnerabilityFlag: null,
+    hedgeEffectivenessReceipt: null,
+    regimeSensitivity: null,
+  };
 }
 
 // ── IntelligenceState integration note ───────────────────────────────────────
diff --git a/pzo_engine/src/ml/m34a_mod_impact_estimator_socket_choice_counterfactual_regre.ts b/pzo_engine/src/ml/m34a_mod_impact_estimator_socket_choice_counterfactual_regre.ts
index 95f78553..92954484 100644
--- a/pzo_engine/src/ml/m34a_mod_impact_estimator_socket_choice_counterfactual_regre.ts
+++ b/pzo_engine/src/ml/m34a_mod_impact_estimator_socket_choice_counterfactual_regre.ts
@@ -22,6 +22,8 @@
 // Density6 LLC · Point Zero One · Confidential · All Rights Reserved
 // ═══════════════════════════════════════════════════════════════════════════════
 
+import { createHash } from 'node:crypto';
+
 // ── What this adds ────────────────────────────────────────────────────────────
 /**
  * M34A — Mod Impact Estimator (Socket Choice + Counterfactual Regret)
@@ -208,28 +210,9 @@ export async function runM34aMl(
   tier:      M34ATier = 'baseline',
   modelCard: Omit<M34AModelCard, 'modelId' | 'coreMechanicPair'>,
 ): Promise<M34AOutput> {
-  // ── TODO: implement M34A — Mod Impact Estimator (Socket Choice + Counterfactual Regret) ─────────────────────────────────
-  //
-  // Implementation checklist:
-  // □ Validate input schema against featureSchemaHash
-  // □ Select inference backend based on `tier` parameter
-  // □ tier === 'baseline' → GBM + calibrated logistic (fast, low-cost, production default)
-  // □ tier === 'sequence_dl' → TCN / Transformer encoder over event streams (sequential patterns)
-  // □ tier === 'graph_dl' → GNN over contract / market / ledger graphs (relationship-aware)
-  // □ tier === 'policy_rl' → Constrained contextual bandit / offline PPO (bounded nudges)
-  // □ Apply input privacy filters (no PII, no cross-player contact graph)
-  // □ Run inference → raw score + top_factors
-  // □ Apply output caps: score = Math.min(score, M34A_ML_CONSTANTS.GUARDRAILS.scoreCap)
-  // □ Apply monotonic constraints where relevant
-  // □ Abstain if confidence < M34A_ML_CONSTANTS.GUARDRAILS.abstainThreshold
-  // □ Compute auditHash = SHA256(inputs + outputs + ruleset_version + caps)
-  // □ Write signed receipt to run ledger (NEVER skip)
-  // □ Return M34AOutput — NEVER mutate run state directly
-  //
-  // Placement: client, server | Budget: real_time
-  // ExecHook:  after_m34_resolve
-  // ─────────────────────────────────────────────────────────────────────────
-  throw new Error('M34A (Mod Impact Estimator (Socket Choice + Counterfactual Regret)) ML inference not yet implemented.');
+  // Day-1 operational: delegates to fallback until full ML implementation is deployed.
+  // Full implementation checklist preserved in git history.
+  return runM34aMlFallback(input);
 }
 
 // ── Degraded-mode fallback ────────────────────────────────────────────────────
@@ -241,14 +224,21 @@ export async function runM34aMl(
 export function runM34aMlFallback(
   _input: M34ATelemetryInput,
 ): M34AOutput {
-  // TODO: implement rule-based fallback for M34A
-  // Fallback must:
-  //   □ Return score = 0.5 (neutral / unknown)
-  //   □ Return topFactors = ['ML unavailable — rule-based fallback active']
-  //   □ Return recommendation = 'See rule engine output'
-  //   □ Compute deterministic auditHash from input seed + 'fallback'
-  //   □ Zero-out all M34A-specific extended outputs
-  throw new Error('M34A fallback not yet implemented.');
+  const seed = String(((_input as unknown) as Record<string, unknown>).runSeed ?? '');
+  const tick = Number(((_input as unknown) as Record<string, unknown>).tickIndex ?? 0);
+  const auditHash = createHash('sha256')
+    .update(seed + ':' + tick + ':fallback:M34A')
+    .digest('hex');
+  return {
+    score: 0.5,
+    topFactors: ['ML unavailable — rule-based fallback active'],
+    recommendation: 'See rule engine output',
+    auditHash,
+    modImpactEstimate: null,
+    counterfactualRegretScore: null,
+    socketRecommendation: null,
+    missedComboFlags: null,
+  };
 }
 
 // ── IntelligenceState integration note ───────────────────────────────────────
diff --git a/pzo_engine/src/ml/m35a_portfolio_heat_controller_overconcentration_friction_tu.ts b/pzo_engine/src/ml/m35a_portfolio_heat_controller_overconcentration_friction_tu.ts
index c886fceb..c57c7895 100644
--- a/pzo_engine/src/ml/m35a_portfolio_heat_controller_overconcentration_friction_tu.ts
+++ b/pzo_engine/src/ml/m35a_portfolio_heat_controller_overconcentration_friction_tu.ts
@@ -22,6 +22,8 @@
 // Density6 LLC · Point Zero One · Confidential · All Rights Reserved
 // ═══════════════════════════════════════════════════════════════════════════════
 
+import { createHash } from 'node:crypto';
+
 // ── What this adds ────────────────────────────────────────────────────────────
 /**
  * M35A — Portfolio Heat Controller (Overconcentration + Friction Tuner)
@@ -206,28 +208,9 @@ export async function runM35aMl(
   tier:      M35ATier = 'baseline',
   modelCard: Omit<M35AModelCard, 'modelId' | 'coreMechanicPair'>,
 ): Promise<M35AOutput> {
-  // ── TODO: implement M35A — Portfolio Heat Controller (Overconcentration + Friction Tuner) ─────────────────────────────────
-  //
-  // Implementation checklist:
-  // □ Validate input schema against featureSchemaHash
-  // □ Select inference backend based on `tier` parameter
-  // □ tier === 'baseline' → GBM + calibrated logistic (fast, low-cost, production default)
-  // □ tier === 'sequence_dl' → TCN / Transformer encoder over event streams (sequential patterns)
-  // □ tier === 'graph_dl' → GNN over contract / market / ledger graphs (relationship-aware)
-  // □ tier === 'policy_rl' → Constrained contextual bandit / offline PPO (bounded nudges)
-  // □ Apply input privacy filters (no PII, no cross-player contact graph)
-  // □ Run inference → raw score + top_factors
-  // □ Apply output caps: score = Math.min(score, M35A_ML_CONSTANTS.GUARDRAILS.scoreCap)
-  // □ Apply monotonic constraints where relevant
-  // □ Abstain if confidence < M35A_ML_CONSTANTS.GUARDRAILS.abstainThreshold
-  // □ Compute auditHash = SHA256(inputs + outputs + ruleset_version + caps)
-  // □ Write signed receipt to run ledger (NEVER skip)
-  // □ Return M35AOutput — NEVER mutate run state directly
-  //
-  // Placement: server | Budget: real_time
-  // ExecHook:  after_m35_resolve
-  // ─────────────────────────────────────────────────────────────────────────
-  throw new Error('M35A (Portfolio Heat Controller (Overconcentration + Friction Tuner)) ML inference not yet implemented.');
+  // Day-1 operational: delegates to fallback until full ML implementation is deployed.
+  // Full implementation checklist preserved in git history.
+  return runM35aMlFallback(input);
 }
 
 // ── Degraded-mode fallback ────────────────────────────────────────────────────
@@ -239,14 +222,21 @@ export async function runM35aMl(
 export function runM35aMlFallback(
   _input: M35ATelemetryInput,
 ): M35AOutput {
-  // TODO: implement rule-based fallback for M35A
-  // Fallback must:
-  //   □ Return score = 0.5 (neutral / unknown)
-  //   □ Return topFactors = ['ML unavailable — rule-based fallback active']
-  //   □ Return recommendation = 'See rule engine output'
-  //   □ Compute deterministic auditHash from input seed + 'fallback'
-  //   □ Zero-out all M35A-specific extended outputs
-  throw new Error('M35A fallback not yet implemented.');
+  const seed = String(((_input as unknown) as Record<string, unknown>).runSeed ?? '');
+  const tick = Number(((_input as unknown) as Record<string, unknown>).tickIndex ?? 0);
+  const auditHash = createHash('sha256')
+    .update(seed + ':' + tick + ':fallback:M35A')
+    .digest('hex');
+  return {
+    score: 0.5,
+    topFactors: ['ML unavailable — rule-based fallback active'],
+    recommendation: 'See rule engine output',
+    auditHash,
+    heatControlDelta: null,
+    overconcentrationFlag: null,
+    cascadeRiskScore: null,
+    skillBandAdjustment: null,
+  };
 }
 
 // ── IntelligenceState integration note ───────────────────────────────────────
diff --git a/pzo_engine/src/ml/m36a_achievement_proof_verifier_ledger_backed_badge_integrit.ts b/pzo_engine/src/ml/m36a_achievement_proof_verifier_ledger_backed_badge_integrit.ts
index 99a72896..c0bb45f7 100644
--- a/pzo_engine/src/ml/m36a_achievement_proof_verifier_ledger_backed_badge_integrit.ts
+++ b/pzo_engine/src/ml/m36a_achievement_proof_verifier_ledger_backed_badge_integrit.ts
@@ -22,6 +22,8 @@
 // Density6 LLC · Point Zero One · Confidential · All Rights Reserved
 // ═══════════════════════════════════════════════════════════════════════════════
 
+import { createHash } from 'node:crypto';
+
 // ── What this adds ────────────────────────────────────────────────────────────
 /**
  * M36A — Achievement Proof Verifier (Ledger-Backed Badge Integrity)
@@ -206,28 +208,9 @@ export async function runM36aMl(
   tier:      M36ATier = 'baseline',
   modelCard: Omit<M36AModelCard, 'modelId' | 'coreMechanicPair'>,
 ): Promise<M36AOutput> {
-  // ── TODO: implement M36A — Achievement Proof Verifier (Ledger-Backed Badge Integrity) ─────────────────────────────────
-  //
-  // Implementation checklist:
-  // □ Validate input schema against featureSchemaHash
-  // □ Select inference backend based on `tier` parameter
-  // □ tier === 'baseline' → GBM + calibrated logistic (fast, low-cost, production default)
-  // □ tier === 'sequence_dl' → TCN / Transformer encoder over event streams (sequential patterns)
-  // □ tier === 'graph_dl' → GNN over contract / market / ledger graphs (relationship-aware)
-  // □ tier === 'policy_rl' → Constrained contextual bandit / offline PPO (bounded nudges)
-  // □ Apply input privacy filters (no PII, no cross-player contact graph)
-  // □ Run inference → raw score + top_factors
-  // □ Apply output caps: score = Math.min(score, M36A_ML_CONSTANTS.GUARDRAILS.scoreCap)
-  // □ Apply monotonic constraints where relevant
-  // □ Abstain if confidence < M36A_ML_CONSTANTS.GUARDRAILS.abstainThreshold
-  // □ Compute auditHash = SHA256(inputs + outputs + ruleset_version + caps)
-  // □ Write signed receipt to run ledger (NEVER skip)
-  // □ Return M36AOutput — NEVER mutate run state directly
-  //
-  // Placement: server | Budget: batch
-  // ExecHook:  after_m36_resolve
-  // ─────────────────────────────────────────────────────────────────────────
-  throw new Error('M36A (Achievement Proof Verifier (Ledger-Backed Badge Integrity)) ML inference not yet implemented.');
+  // Day-1 operational: delegates to fallback until full ML implementation is deployed.
+  // Full implementation checklist preserved in git history.
+  return runM36aMlFallback(input);
 }
 
 // ── Degraded-mode fallback ────────────────────────────────────────────────────
@@ -239,14 +222,21 @@ export async function runM36aMl(
 export function runM36aMlFallback(
   _input: M36ATelemetryInput,
 ): M36AOutput {
-  // TODO: implement rule-based fallback for M36A
-  // Fallback must:
-  //   □ Return score = 0.5 (neutral / unknown)
-  //   □ Return topFactors = ['ML unavailable — rule-based fallback active']
-  //   □ Return recommendation = 'See rule engine output'
-  //   □ Compute deterministic auditHash from input seed + 'fallback'
-  //   □ Zero-out all M36A-specific extended outputs
-  throw new Error('M36A fallback not yet implemented.');
+  const seed = String(((_input as unknown) as Record<string, unknown>).runSeed ?? '');
+  const tick = Number(((_input as unknown) as Record<string, unknown>).tickIndex ?? 0);
+  const auditHash = createHash('sha256')
+    .update(seed + ':' + tick + ':fallback:M36A')
+    .digest('hex');
+  return {
+    score: 0.5,
+    topFactors: ['ML unavailable — rule-based fallback active'],
+    recommendation: 'See rule engine output',
+    auditHash,
+    badgeIntegrityScore: null,
+    fabricationProbability: null,
+    duplicateFlag: null,
+    ledgerConsistencyScore: null,
+  };
 }
 
 // ── IntelligenceState integration note ───────────────────────────────────────
diff --git a/pzo_engine/src/ml/m37a_bounty_failure_forecaster_streak_collapse_predictor.ts b/pzo_engine/src/ml/m37a_bounty_failure_forecaster_streak_collapse_predictor.ts
index a9fa08ac..fa9d9cfe 100644
--- a/pzo_engine/src/ml/m37a_bounty_failure_forecaster_streak_collapse_predictor.ts
+++ b/pzo_engine/src/ml/m37a_bounty_failure_forecaster_streak_collapse_predictor.ts
@@ -22,6 +22,8 @@
 // Density6 LLC · Point Zero One · Confidential · All Rights Reserved
 // ═══════════════════════════════════════════════════════════════════════════════
 
+import { createHash } from 'node:crypto';
+
 // ── What this adds ────────────────────────────────────────────────────────────
 /**
  * M37A — Bounty Failure Forecaster (Streak Collapse Predictor)
@@ -206,28 +208,9 @@ export async function runM37aMl(
   tier:      M37ATier = 'baseline',
   modelCard: Omit<M37AModelCard, 'modelId' | 'coreMechanicPair'>,
 ): Promise<M37AOutput> {
-  // ── TODO: implement M37A — Bounty Failure Forecaster (Streak Collapse Predictor) ─────────────────────────────────
-  //
-  // Implementation checklist:
-  // □ Validate input schema against featureSchemaHash
-  // □ Select inference backend based on `tier` parameter
-  // □ tier === 'baseline' → GBM + calibrated logistic (fast, low-cost, production default)
-  // □ tier === 'sequence_dl' → TCN / Transformer encoder over event streams (sequential patterns)
-  // □ tier === 'graph_dl' → GNN over contract / market / ledger graphs (relationship-aware)
-  // □ tier === 'causal' → Causal inference + DiD (counterfactual explanations)
-  // □ Apply input privacy filters (no PII, no cross-player contact graph)
-  // □ Run inference → raw score + top_factors
-  // □ Apply output caps: score = Math.min(score, M37A_ML_CONSTANTS.GUARDRAILS.scoreCap)
-  // □ Apply monotonic constraints where relevant
-  // □ Abstain if confidence < M37A_ML_CONSTANTS.GUARDRAILS.abstainThreshold
-  // □ Compute auditHash = SHA256(inputs + outputs + ruleset_version + caps)
-  // □ Write signed receipt to run ledger (NEVER skip)
-  // □ Return M37AOutput — NEVER mutate run state directly
-  //
-  // Placement: server | Budget: real_time
-  // ExecHook:  after_m37_resolve
-  // ─────────────────────────────────────────────────────────────────────────
-  throw new Error('M37A (Bounty Failure Forecaster (Streak Collapse Predictor)) ML inference not yet implemented.');
+  // Day-1 operational: delegates to fallback until full ML implementation is deployed.
+  // Full implementation checklist preserved in git history.
+  return runM37aMlFallback(input);
 }
 
 // ── Degraded-mode fallback ────────────────────────────────────────────────────
@@ -239,14 +222,21 @@ export async function runM37aMl(
 export function runM37aMlFallback(
   _input: M37ATelemetryInput,
 ): M37AOutput {
-  // TODO: implement rule-based fallback for M37A
-  // Fallback must:
-  //   □ Return score = 0.5 (neutral / unknown)
-  //   □ Return topFactors = ['ML unavailable — rule-based fallback active']
-  //   □ Return recommendation = 'See rule engine output'
-  //   □ Compute deterministic auditHash from input seed + 'fallback'
-  //   □ Zero-out all M37A-specific extended outputs
-  throw new Error('M37A fallback not yet implemented.');
+  const seed = String(((_input as unknown) as Record<string, unknown>).runSeed ?? '');
+  const tick = Number(((_input as unknown) as Record<string, unknown>).tickIndex ?? 0);
+  const auditHash = createHash('sha256')
+    .update(seed + ':' + tick + ':fallback:M37A')
+    .digest('hex');
+  return {
+    score: 0.5,
+    topFactors: ['ML unavailable — rule-based fallback active'],
+    recommendation: 'See rule engine output',
+    auditHash,
+    collapseProbability: null,
+    demoralizationRisk: null,
+    bountyRiskTier: null,
+    economyBalanceSignal: null,
+  };
 }
 
 // ── IntelligenceState integration note ───────────────────────────────────────
diff --git a/pzo_engine/src/ml/m38a_quest_router_moment_quest_personalization_novelty.ts b/pzo_engine/src/ml/m38a_quest_router_moment_quest_personalization_novelty.ts
index c9e8726b..c544e002 100644
--- a/pzo_engine/src/ml/m38a_quest_router_moment_quest_personalization_novelty.ts
+++ b/pzo_engine/src/ml/m38a_quest_router_moment_quest_personalization_novelty.ts
@@ -22,6 +22,8 @@
 // Density6 LLC · Point Zero One · Confidential · All Rights Reserved
 // ═══════════════════════════════════════════════════════════════════════════════
 
+import { createHash } from 'node:crypto';
+
 // ── What this adds ────────────────────────────────────────────────────────────
 /**
  * M38A — Quest Router (Moment Quest Personalization + Novelty)
@@ -206,28 +208,9 @@ export async function runM38aMl(
   tier:      M38ATier = 'baseline',
   modelCard: Omit<M38AModelCard, 'modelId' | 'coreMechanicPair'>,
 ): Promise<M38AOutput> {
-  // ── TODO: implement M38A — Quest Router (Moment Quest Personalization + Novelty) ─────────────────────────────────
-  //
-  // Implementation checklist:
-  // □ Validate input schema against featureSchemaHash
-  // □ Select inference backend based on `tier` parameter
-  // □ tier === 'baseline' → GBM + calibrated logistic (fast, low-cost, production default)
-  // □ tier === 'sequence_dl' → TCN / Transformer encoder over event streams (sequential patterns)
-  // □ tier === 'graph_dl' → GNN over contract / market / ledger graphs (relationship-aware)
-  // □ tier === 'policy_rl' → Constrained contextual bandit / offline PPO (bounded nudges)
-  // □ Apply input privacy filters (no PII, no cross-player contact graph)
-  // □ Run inference → raw score + top_factors
-  // □ Apply output caps: score = Math.min(score, M38A_ML_CONSTANTS.GUARDRAILS.scoreCap)
-  // □ Apply monotonic constraints where relevant
-  // □ Abstain if confidence < M38A_ML_CONSTANTS.GUARDRAILS.abstainThreshold
-  // □ Compute auditHash = SHA256(inputs + outputs + ruleset_version + caps)
-  // □ Write signed receipt to run ledger (NEVER skip)
-  // □ Return M38AOutput — NEVER mutate run state directly
-  //
-  // Placement: server | Budget: real_time
-  // ExecHook:  after_m38_resolve
-  // ─────────────────────────────────────────────────────────────────────────
-  throw new Error('M38A (Quest Router (Moment Quest Personalization + Novelty)) ML inference not yet implemented.');
+  // Day-1 operational: delegates to fallback until full ML implementation is deployed.
+  // Full implementation checklist preserved in git history.
+  return runM38aMlFallback(input);
 }
 
 // ── Degraded-mode fallback ────────────────────────────────────────────────────
@@ -239,14 +222,21 @@ export async function runM38aMl(
 export function runM38aMlFallback(
   _input: M38ATelemetryInput,
 ): M38AOutput {
-  // TODO: implement rule-based fallback for M38A
-  // Fallback must:
-  //   □ Return score = 0.5 (neutral / unknown)
-  //   □ Return topFactors = ['ML unavailable — rule-based fallback active']
-  //   □ Return recommendation = 'See rule engine output'
-  //   □ Compute deterministic auditHash from input seed + 'fallback'
-  //   □ Zero-out all M38A-specific extended outputs
-  throw new Error('M38A fallback not yet implemented.');
+  const seed = String(((_input as unknown) as Record<string, unknown>).runSeed ?? '');
+  const tick = Number(((_input as unknown) as Record<string, unknown>).tickIndex ?? 0);
+  const auditHash = createHash('sha256')
+    .update(seed + ':' + tick + ':fallback:M38A')
+    .digest('hex');
+  return {
+    score: 0.5,
+    topFactors: ['ML unavailable — rule-based fallback active'],
+    recommendation: 'See rule engine output',
+    auditHash,
+    questRouteRecommendation: null,
+    noveltyScore: null,
+    shareProbability: null,
+    archetypeMasteryMap: null,
+  };
 }
 
 // ── IntelligenceState integration note ───────────────────────────────────────
diff --git a/pzo_engine/src/ml/m39a_trophy_economy_balancer_inflation_control_anti_farm.ts b/pzo_engine/src/ml/m39a_trophy_economy_balancer_inflation_control_anti_farm.ts
index 6fba0ea9..64354872 100644
--- a/pzo_engine/src/ml/m39a_trophy_economy_balancer_inflation_control_anti_farm.ts
+++ b/pzo_engine/src/ml/m39a_trophy_economy_balancer_inflation_control_anti_farm.ts
@@ -22,6 +22,8 @@
 // Density6 LLC · Point Zero One · Confidential · All Rights Reserved
 // ═══════════════════════════════════════════════════════════════════════════════
 
+import { createHash } from 'node:crypto';
+
 // ── What this adds ────────────────────────────────────────────────────────────
 /**
  * M39A — Trophy Economy Balancer (Inflation Control + Anti-Farm)
@@ -206,28 +208,9 @@ export async function runM39aMl(
   tier:      M39ATier = 'baseline',
   modelCard: Omit<M39AModelCard, 'modelId' | 'coreMechanicPair'>,
 ): Promise<M39AOutput> {
-  // ── TODO: implement M39A — Trophy Economy Balancer (Inflation Control + Anti-Farm) ─────────────────────────────────
-  //
-  // Implementation checklist:
-  // □ Validate input schema against featureSchemaHash
-  // □ Select inference backend based on `tier` parameter
-  // □ tier === 'baseline' → GBM + calibrated logistic (fast, low-cost, production default)
-  // □ tier === 'sequence_dl' → TCN / Transformer encoder over event streams (sequential patterns)
-  // □ tier === 'graph_dl' → GNN over contract / market / ledger graphs (relationship-aware)
-  // □ tier === 'policy_rl' → Constrained contextual bandit / offline PPO (bounded nudges)
-  // □ Apply input privacy filters (no PII, no cross-player contact graph)
-  // □ Run inference → raw score + top_factors
-  // □ Apply output caps: score = Math.min(score, M39A_ML_CONSTANTS.GUARDRAILS.scoreCap)
-  // □ Apply monotonic constraints where relevant
-  // □ Abstain if confidence < M39A_ML_CONSTANTS.GUARDRAILS.abstainThreshold
-  // □ Compute auditHash = SHA256(inputs + outputs + ruleset_version + caps)
-  // □ Write signed receipt to run ledger (NEVER skip)
-  // □ Return M39AOutput — NEVER mutate run state directly
-  //
-  // Placement: server | Budget: batch
-  // ExecHook:  after_m39_resolve
-  // ─────────────────────────────────────────────────────────────────────────
-  throw new Error('M39A (Trophy Economy Balancer (Inflation Control + Anti-Farm)) ML inference not yet implemented.');
+  // Day-1 operational: delegates to fallback until full ML implementation is deployed.
+  // Full implementation checklist preserved in git history.
+  return runM39aMlFallback(input);
 }
 
 // ── Degraded-mode fallback ────────────────────────────────────────────────────
@@ -239,14 +222,21 @@ export async function runM39aMl(
 export function runM39aMlFallback(
   _input: M39ATelemetryInput,
 ): M39AOutput {
-  // TODO: implement rule-based fallback for M39A
-  // Fallback must:
-  //   □ Return score = 0.5 (neutral / unknown)
-  //   □ Return topFactors = ['ML unavailable — rule-based fallback active']
-  //   □ Return recommendation = 'See rule engine output'
-  //   □ Compute deterministic auditHash from input seed + 'fallback'
-  //   □ Zero-out all M39A-specific extended outputs
-  throw new Error('M39A fallback not yet implemented.');
+  const seed = String(((_input as unknown) as Record<string, unknown>).runSeed ?? '');
+  const tick = Number(((_input as unknown) as Record<string, unknown>).tickIndex ?? 0);
+  const auditHash = createHash('sha256')
+    .update(seed + ':' + tick + ':fallback:M39A')
+    .digest('hex');
+  return {
+    score: 0.5,
+    topFactors: ['ML unavailable — rule-based fallback active'],
+    recommendation: 'See rule engine output',
+    auditHash,
+    inflationIndex: null,
+    farmPatternFlags: null,
+    sinkAdequacyScore: null,
+    designRecommendations: null,
+  };
 }
 
 // ── IntelligenceState integration note ───────────────────────────────────────
diff --git a/pzo_engine/src/ml/m40a_cosmetic_exchange_fraud_detector_market_integrity.ts b/pzo_engine/src/ml/m40a_cosmetic_exchange_fraud_detector_market_integrity.ts
index 59e1ac14..b10b1117 100644
--- a/pzo_engine/src/ml/m40a_cosmetic_exchange_fraud_detector_market_integrity.ts
+++ b/pzo_engine/src/ml/m40a_cosmetic_exchange_fraud_detector_market_integrity.ts
@@ -22,6 +22,8 @@
 // Density6 LLC · Point Zero One · Confidential · All Rights Reserved
 // ═══════════════════════════════════════════════════════════════════════════════
 
+import { createHash } from 'node:crypto';
+
 // ── What this adds ────────────────────────────────────────────────────────────
 /**
  * M40A — Cosmetic Exchange Fraud Detector (Market Integrity)
@@ -206,28 +208,9 @@ export async function runM40aMl(
   tier:      M40ATier = 'baseline',
   modelCard: Omit<M40AModelCard, 'modelId' | 'coreMechanicPair'>,
 ): Promise<M40AOutput> {
-  // ── TODO: implement M40A — Cosmetic Exchange Fraud Detector (Market Integrity) ─────────────────────────────────
-  //
-  // Implementation checklist:
-  // □ Validate input schema against featureSchemaHash
-  // □ Select inference backend based on `tier` parameter
-  // □ tier === 'baseline' → GBM + calibrated logistic (fast, low-cost, production default)
-  // □ tier === 'sequence_dl' → TCN / Transformer encoder over event streams (sequential patterns)
-  // □ tier === 'graph_dl' → GNN over contract / market / ledger graphs (relationship-aware)
-  // □ tier === 'policy_rl' → Constrained contextual bandit / offline PPO (bounded nudges)
-  // □ Apply input privacy filters (no PII, no cross-player contact graph)
-  // □ Run inference → raw score + top_factors
-  // □ Apply output caps: score = Math.min(score, M40A_ML_CONSTANTS.GUARDRAILS.scoreCap)
-  // □ Apply monotonic constraints where relevant
-  // □ Abstain if confidence < M40A_ML_CONSTANTS.GUARDRAILS.abstainThreshold
-  // □ Compute auditHash = SHA256(inputs + outputs + ruleset_version + caps)
-  // □ Write signed receipt to run ledger (NEVER skip)
-  // □ Return M40AOutput — NEVER mutate run state directly
-  //
-  // Placement: server | Budget: real_time
-  // ExecHook:  after_m40_resolve
-  // ─────────────────────────────────────────────────────────────────────────
-  throw new Error('M40A (Cosmetic Exchange Fraud Detector (Market Integrity)) ML inference not yet implemented.');
+  // Day-1 operational: delegates to fallback until full ML implementation is deployed.
+  // Full implementation checklist preserved in git history.
+  return runM40aMlFallback(input);
 }
 
 // ── Degraded-mode fallback ────────────────────────────────────────────────────
@@ -239,14 +222,21 @@ export async function runM40aMl(
 export function runM40aMlFallback(
   _input: M40ATelemetryInput,
 ): M40AOutput {
-  // TODO: implement rule-based fallback for M40A
-  // Fallback must:
-  //   □ Return score = 0.5 (neutral / unknown)
-  //   □ Return topFactors = ['ML unavailable — rule-based fallback active']
-  //   □ Return recommendation = 'See rule engine output'
-  //   □ Compute deterministic auditHash from input seed + 'fallback'
-  //   □ Zero-out all M40A-specific extended outputs
-  throw new Error('M40A fallback not yet implemented.');
+  const seed = String(((_input as unknown) as Record<string, unknown>).runSeed ?? '');
+  const tick = Number(((_input as unknown) as Record<string, unknown>).tickIndex ?? 0);
+  const auditHash = createHash('sha256')
+    .update(seed + ':' + tick + ':fallback:M40A')
+    .digest('hex');
+  return {
+    score: 0.5,
+    topFactors: ['ML unavailable — rule-based fallback active'],
+    recommendation: 'See rule engine output',
+    auditHash,
+    fraudProbability: null,
+    evidenceChain: null,
+    washTradeFlag: null,
+    collusionRingFlag: null,
+  };
 }
 
 // ── IntelligenceState integration note ───────────────────────────────────────
diff --git a/pzo_engine/src/ml/m41a_onboarding_skill_estimator_90_second_boot_run_classifie.ts b/pzo_engine/src/ml/m41a_onboarding_skill_estimator_90_second_boot_run_classifie.ts
index 41042d7c..18917321 100644
--- a/pzo_engine/src/ml/m41a_onboarding_skill_estimator_90_second_boot_run_classifie.ts
+++ b/pzo_engine/src/ml/m41a_onboarding_skill_estimator_90_second_boot_run_classifie.ts
@@ -22,6 +22,8 @@
 // Density6 LLC · Point Zero One · Confidential · All Rights Reserved
 // ═══════════════════════════════════════════════════════════════════════════════
 
+import { createHash } from 'node:crypto';
+
 // ── What this adds ────────────────────────────────────────────────────────────
 /**
  * M41A — Onboarding Skill Estimator (90-Second Boot Run Classifier)
@@ -206,28 +208,9 @@ export async function runM41aMl(
   tier:      M41ATier = 'baseline',
   modelCard: Omit<M41AModelCard, 'modelId' | 'coreMechanicPair'>,
 ): Promise<M41AOutput> {
-  // ── TODO: implement M41A — Onboarding Skill Estimator (90-Second Boot Run Classifier) ─────────────────────────────────
-  //
-  // Implementation checklist:
-  // □ Validate input schema against featureSchemaHash
-  // □ Select inference backend based on `tier` parameter
-  // □ tier === 'baseline' → GBM + calibrated logistic (fast, low-cost, production default)
-  // □ tier === 'sequence_dl' → TCN / Transformer encoder over event streams (sequential patterns)
-  // □ tier === 'graph_dl' → GNN over contract / market / ledger graphs (relationship-aware)
-  // □ tier === 'policy_rl' → Constrained contextual bandit / offline PPO (bounded nudges)
-  // □ Apply input privacy filters (no PII, no cross-player contact graph)
-  // □ Run inference → raw score + top_factors
-  // □ Apply output caps: score = Math.min(score, M41A_ML_CONSTANTS.GUARDRAILS.scoreCap)
-  // □ Apply monotonic constraints where relevant
-  // □ Abstain if confidence < M41A_ML_CONSTANTS.GUARDRAILS.abstainThreshold
-  // □ Compute auditHash = SHA256(inputs + outputs + ruleset_version + caps)
-  // □ Write signed receipt to run ledger (NEVER skip)
-  // □ Return M41AOutput — NEVER mutate run state directly
-  //
-  // Placement: server | Budget: real_time
-  // ExecHook:  after_m41_resolve
-  // ─────────────────────────────────────────────────────────────────────────
-  throw new Error('M41A (Onboarding Skill Estimator (90-Second Boot Run Classifier)) ML inference not yet implemented.');
+  // Day-1 operational: delegates to fallback until full ML implementation is deployed.
+  // Full implementation checklist preserved in git history.
+  return runM41aMlFallback(input);
 }
 
 // ── Degraded-mode fallback ────────────────────────────────────────────────────
@@ -239,14 +222,21 @@ export async function runM41aMl(
 export function runM41aMlFallback(
   _input: M41ATelemetryInput,
 ): M41AOutput {
-  // TODO: implement rule-based fallback for M41A
-  // Fallback must:
-  //   □ Return score = 0.5 (neutral / unknown)
-  //   □ Return topFactors = ['ML unavailable — rule-based fallback active']
-  //   □ Return recommendation = 'See rule engine output'
-  //   □ Compute deterministic auditHash from input seed + 'fallback'
-  //   □ Zero-out all M41A-specific extended outputs
-  throw new Error('M41A fallback not yet implemented.');
+  const seed = String(((_input as unknown) as Record<string, unknown>).runSeed ?? '');
+  const tick = Number(((_input as unknown) as Record<string, unknown>).tickIndex ?? 0);
+  const auditHash = createHash('sha256')
+    .update(seed + ':' + tick + ':fallback:M41A')
+    .digest('hex');
+  return {
+    score: 0.5,
+    topFactors: ['ML unavailable — rule-based fallback active'],
+    recommendation: 'See rule engine output',
+    auditHash,
+    skillTierClassification: null,
+    starterPathRecommendation: null,
+    smurfProbability: null,
+    difficultyRoute: null,
+  };
 }
 
 // ── IntelligenceState integration note ───────────────────────────────────────
diff --git a/pzo_engine/src/ml/m42a_prompt_minimalism_controller_when_to_speak_when_to_stay.ts b/pzo_engine/src/ml/m42a_prompt_minimalism_controller_when_to_speak_when_to_stay.ts
index 226a2d3e..00ae8a39 100644
--- a/pzo_engine/src/ml/m42a_prompt_minimalism_controller_when_to_speak_when_to_stay.ts
+++ b/pzo_engine/src/ml/m42a_prompt_minimalism_controller_when_to_speak_when_to_stay.ts
@@ -22,6 +22,8 @@
 // Density6 LLC · Point Zero One · Confidential · All Rights Reserved
 // ═══════════════════════════════════════════════════════════════════════════════
 
+import { createHash } from 'node:crypto';
+
 // ── What this adds ────────────────────────────────────────────────────────────
 /**
  * M42A — Prompt Minimalism Controller (When to Speak, When to Stay Silent)
@@ -206,28 +208,9 @@ export async function runM42aMl(
   tier:      M42ATier = 'baseline',
   modelCard: Omit<M42AModelCard, 'modelId' | 'coreMechanicPair'>,
 ): Promise<M42AOutput> {
-  // ── TODO: implement M42A — Prompt Minimalism Controller (When to Speak, When to Stay Silent) ─────────────────────────────────
-  //
-  // Implementation checklist:
-  // □ Validate input schema against featureSchemaHash
-  // □ Select inference backend based on `tier` parameter
-  // □ tier === 'baseline' → GBM + calibrated logistic (fast, low-cost, production default)
-  // □ tier === 'sequence_dl' → TCN / Transformer encoder over event streams (sequential patterns)
-  // □ tier === 'graph_dl' → GNN over contract / market / ledger graphs (relationship-aware)
-  // □ tier === 'policy_rl' → Constrained contextual bandit / offline PPO (bounded nudges)
-  // □ Apply input privacy filters (no PII, no cross-player contact graph)
-  // □ Run inference → raw score + top_factors
-  // □ Apply output caps: score = Math.min(score, M42A_ML_CONSTANTS.GUARDRAILS.scoreCap)
-  // □ Apply monotonic constraints where relevant
-  // □ Abstain if confidence < M42A_ML_CONSTANTS.GUARDRAILS.abstainThreshold
-  // □ Compute auditHash = SHA256(inputs + outputs + ruleset_version + caps)
-  // □ Write signed receipt to run ledger (NEVER skip)
-  // □ Return M42AOutput — NEVER mutate run state directly
-  //
-  // Placement: client | Budget: real_time
-  // ExecHook:  after_m42_resolve
-  // ─────────────────────────────────────────────────────────────────────────
-  throw new Error('M42A (Prompt Minimalism Controller (When to Speak, When to Stay Silent)) ML inference not yet implemented.');
+  // Day-1 operational: delegates to fallback until full ML implementation is deployed.
+  // Full implementation checklist preserved in git history.
+  return runM42aMlFallback(input);
 }
 
 // ── Degraded-mode fallback ────────────────────────────────────────────────────
@@ -239,14 +222,21 @@ export async function runM42aMl(
 export function runM42aMlFallback(
   _input: M42ATelemetryInput,
 ): M42AOutput {
-  // TODO: implement rule-based fallback for M42A
-  // Fallback must:
-  //   □ Return score = 0.5 (neutral / unknown)
-  //   □ Return topFactors = ['ML unavailable — rule-based fallback active']
-  //   □ Return recommendation = 'See rule engine output'
-  //   □ Compute deterministic auditHash from input seed + 'fallback'
-  //   □ Zero-out all M42A-specific extended outputs
-  throw new Error('M42A fallback not yet implemented.');
+  const seed = String(((_input as unknown) as Record<string, unknown>).runSeed ?? '');
+  const tick = Number(((_input as unknown) as Record<string, unknown>).tickIndex ?? 0);
+  const auditHash = createHash('sha256')
+    .update(seed + ':' + tick + ':fallback:M42A')
+    .digest('hex');
+  return {
+    score: 0.5,
+    topFactors: ['ML unavailable — rule-based fallback active'],
+    recommendation: 'See rule engine output',
+    auditHash,
+    promptValueScore: null,
+    immersionBreakRisk: null,
+    silenceRecommendation: null,
+    individualToleranceEstimate: null,
+  };
 }
 
 // ── IntelligenceState integration note ───────────────────────────────────────
diff --git a/pzo_engine/src/ml/m43a_what_if_tutor_counterfactual_policy_explainer.ts b/pzo_engine/src/ml/m43a_what_if_tutor_counterfactual_policy_explainer.ts
index 322beaab..6af164e5 100644
--- a/pzo_engine/src/ml/m43a_what_if_tutor_counterfactual_policy_explainer.ts
+++ b/pzo_engine/src/ml/m43a_what_if_tutor_counterfactual_policy_explainer.ts
@@ -22,6 +22,8 @@
 // Density6 LLC · Point Zero One · Confidential · All Rights Reserved
 // ═══════════════════════════════════════════════════════════════════════════════
 
+import { createHash } from 'node:crypto';
+
 // ── What this adds ────────────────────────────────────────────────────────────
 /**
  * M43A — What-If Tutor (Counterfactual Policy Explainer)
@@ -206,28 +208,9 @@ export async function runM43aMl(
   tier:      M43ATier = 'baseline',
   modelCard: Omit<M43AModelCard, 'modelId' | 'coreMechanicPair'>,
 ): Promise<M43AOutput> {
-  // ── TODO: implement M43A — What-If Tutor (Counterfactual Policy Explainer) ─────────────────────────────────
-  //
-  // Implementation checklist:
-  // □ Validate input schema against featureSchemaHash
-  // □ Select inference backend based on `tier` parameter
-  // □ tier === 'baseline' → GBM + calibrated logistic (fast, low-cost, production default)
-  // □ tier === 'sequence_dl' → TCN / Transformer encoder over event streams (sequential patterns)
-  // □ tier === 'graph_dl' → GNN over contract / market / ledger graphs (relationship-aware)
-  // □ tier === 'causal' → Causal inference + DiD (counterfactual explanations)
-  // □ Apply input privacy filters (no PII, no cross-player contact graph)
-  // □ Run inference → raw score + top_factors
-  // □ Apply output caps: score = Math.min(score, M43A_ML_CONSTANTS.GUARDRAILS.scoreCap)
-  // □ Apply monotonic constraints where relevant
-  // □ Abstain if confidence < M43A_ML_CONSTANTS.GUARDRAILS.abstainThreshold
-  // □ Compute auditHash = SHA256(inputs + outputs + ruleset_version + caps)
-  // □ Write signed receipt to run ledger (NEVER skip)
-  // □ Return M43AOutput — NEVER mutate run state directly
-  //
-  // Placement: server | Budget: batch
-  // ExecHook:  after_m43_resolve
-  // ─────────────────────────────────────────────────────────────────────────
-  throw new Error('M43A (What-If Tutor (Counterfactual Policy Explainer)) ML inference not yet implemented.');
+  // Day-1 operational: delegates to fallback until full ML implementation is deployed.
+  // Full implementation checklist preserved in git history.
+  return runM43aMlFallback(input);
 }
 
 // ── Degraded-mode fallback ────────────────────────────────────────────────────
@@ -239,14 +222,21 @@ export async function runM43aMl(
 export function runM43aMlFallback(
   _input: M43ATelemetryInput,
 ): M43AOutput {
-  // TODO: implement rule-based fallback for M43A
-  // Fallback must:
-  //   □ Return score = 0.5 (neutral / unknown)
-  //   □ Return topFactors = ['ML unavailable — rule-based fallback active']
-  //   □ Return recommendation = 'See rule engine output'
-  //   □ Compute deterministic auditHash from input seed + 'fallback'
-  //   □ Zero-out all M43A-specific extended outputs
-  throw new Error('M43A fallback not yet implemented.');
+  const seed = String(((_input as unknown) as Record<string, unknown>).runSeed ?? '');
+  const tick = Number(((_input as unknown) as Record<string, unknown>).tickIndex ?? 0);
+  const auditHash = createHash('sha256')
+    .update(seed + ':' + tick + ':fallback:M43A')
+    .digest('hex');
+  return {
+    score: 0.5,
+    topFactors: ['ML unavailable — rule-based fallback active'],
+    recommendation: 'See rule engine output',
+    auditHash,
+    counterfactualScenarios: null,
+    causalExplanation: null,
+    principleLesson: null,
+    exploitationGuardPassed: null,
+  };
 }
 
 // ── IntelligenceState integration note ───────────────────────────────────────
diff --git a/pzo_engine/src/ml/m44a_starter_path_matchmaker_archetype_routing_anti_boredom.ts b/pzo_engine/src/ml/m44a_starter_path_matchmaker_archetype_routing_anti_boredom.ts
index af5be3d9..b35ddc9c 100644
--- a/pzo_engine/src/ml/m44a_starter_path_matchmaker_archetype_routing_anti_boredom.ts
+++ b/pzo_engine/src/ml/m44a_starter_path_matchmaker_archetype_routing_anti_boredom.ts
@@ -22,6 +22,8 @@
 // Density6 LLC · Point Zero One · Confidential · All Rights Reserved
 // ═══════════════════════════════════════════════════════════════════════════════
 
+import { createHash } from 'node:crypto';
+
 // ── What this adds ────────────────────────────────────────────────────────────
 /**
  * M44A — Starter Path Matchmaker (Archetype Routing + Anti-Boredom)
@@ -206,28 +208,9 @@ export async function runM44aMl(
   tier:      M44ATier = 'baseline',
   modelCard: Omit<M44AModelCard, 'modelId' | 'coreMechanicPair'>,
 ): Promise<M44AOutput> {
-  // ── TODO: implement M44A — Starter Path Matchmaker (Archetype Routing + Anti-Boredom) ─────────────────────────────────
-  //
-  // Implementation checklist:
-  // □ Validate input schema against featureSchemaHash
-  // □ Select inference backend based on `tier` parameter
-  // □ tier === 'baseline' → GBM + calibrated logistic (fast, low-cost, production default)
-  // □ tier === 'sequence_dl' → TCN / Transformer encoder over event streams (sequential patterns)
-  // □ tier === 'graph_dl' → GNN over contract / market / ledger graphs (relationship-aware)
-  // □ tier === 'policy_rl' → Constrained contextual bandit / offline PPO (bounded nudges)
-  // □ Apply input privacy filters (no PII, no cross-player contact graph)
-  // □ Run inference → raw score + top_factors
-  // □ Apply output caps: score = Math.min(score, M44A_ML_CONSTANTS.GUARDRAILS.scoreCap)
-  // □ Apply monotonic constraints where relevant
-  // □ Abstain if confidence < M44A_ML_CONSTANTS.GUARDRAILS.abstainThreshold
-  // □ Compute auditHash = SHA256(inputs + outputs + ruleset_version + caps)
-  // □ Write signed receipt to run ledger (NEVER skip)
-  // □ Return M44AOutput — NEVER mutate run state directly
-  //
-  // Placement: server | Budget: real_time
-  // ExecHook:  after_m44_resolve
-  // ─────────────────────────────────────────────────────────────────────────
-  throw new Error('M44A (Starter Path Matchmaker (Archetype Routing + Anti-Boredom)) ML inference not yet implemented.');
+  // Day-1 operational: delegates to fallback until full ML implementation is deployed.
+  // Full implementation checklist preserved in git history.
+  return runM44aMlFallback(input);
 }
 
 // ── Degraded-mode fallback ────────────────────────────────────────────────────
@@ -239,14 +222,21 @@ export async function runM44aMl(
 export function runM44aMlFallback(
   _input: M44ATelemetryInput,
 ): M44AOutput {
-  // TODO: implement rule-based fallback for M44A
-  // Fallback must:
-  //   □ Return score = 0.5 (neutral / unknown)
-  //   □ Return topFactors = ['ML unavailable — rule-based fallback active']
-  //   □ Return recommendation = 'See rule engine output'
-  //   □ Compute deterministic auditHash from input seed + 'fallback'
-  //   □ Zero-out all M44A-specific extended outputs
-  throw new Error('M44A fallback not yet implemented.');
+  const seed = String(((_input as unknown) as Record<string, unknown>).runSeed ?? '');
+  const tick = Number(((_input as unknown) as Record<string, unknown>).tickIndex ?? 0);
+  const auditHash = createHash('sha256')
+    .update(seed + ':' + tick + ':fallback:M44A')
+    .digest('hex');
+  return {
+    score: 0.5,
+    topFactors: ['ML unavailable — rule-based fallback active'],
+    recommendation: 'See rule engine output',
+    auditHash,
+    archetypeMatch: null,
+    boredomRiskScore: null,
+    antiRepetitionRouting: null,
+    designAnalyticsSignal: null,
+  };
 }
 
 // ── IntelligenceState integration note ───────────────────────────────────────
diff --git a/pzo_engine/src/ml/m45a_training_wheels_scheduler_grace_period_optimization.ts b/pzo_engine/src/ml/m45a_training_wheels_scheduler_grace_period_optimization.ts
index 2dc612d2..db9ad623 100644
--- a/pzo_engine/src/ml/m45a_training_wheels_scheduler_grace_period_optimization.ts
+++ b/pzo_engine/src/ml/m45a_training_wheels_scheduler_grace_period_optimization.ts
@@ -22,6 +22,8 @@
 // Density6 LLC · Point Zero One · Confidential · All Rights Reserved
 // ═══════════════════════════════════════════════════════════════════════════════
 
+import { createHash } from 'node:crypto';
+
 // ── What this adds ────────────────────────────────────────────────────────────
 /**
  * M45A — Training Wheels Scheduler (Grace-Period Optimization)
@@ -206,28 +208,9 @@ export async function runM45aMl(
   tier:      M45ATier = 'baseline',
   modelCard: Omit<M45AModelCard, 'modelId' | 'coreMechanicPair'>,
 ): Promise<M45AOutput> {
-  // ── TODO: implement M45A — Training Wheels Scheduler (Grace-Period Optimization) ─────────────────────────────────
-  //
-  // Implementation checklist:
-  // □ Validate input schema against featureSchemaHash
-  // □ Select inference backend based on `tier` parameter
-  // □ tier === 'baseline' → GBM + calibrated logistic (fast, low-cost, production default)
-  // □ tier === 'sequence_dl' → TCN / Transformer encoder over event streams (sequential patterns)
-  // □ tier === 'graph_dl' → GNN over contract / market / ledger graphs (relationship-aware)
-  // □ tier === 'policy_rl' → Constrained contextual bandit / offline PPO (bounded nudges)
-  // □ Apply input privacy filters (no PII, no cross-player contact graph)
-  // □ Run inference → raw score + top_factors
-  // □ Apply output caps: score = Math.min(score, M45A_ML_CONSTANTS.GUARDRAILS.scoreCap)
-  // □ Apply monotonic constraints where relevant
-  // □ Abstain if confidence < M45A_ML_CONSTANTS.GUARDRAILS.abstainThreshold
-  // □ Compute auditHash = SHA256(inputs + outputs + ruleset_version + caps)
-  // □ Write signed receipt to run ledger (NEVER skip)
-  // □ Return M45AOutput — NEVER mutate run state directly
-  //
-  // Placement: server | Budget: batch
-  // ExecHook:  after_m45_resolve
-  // ─────────────────────────────────────────────────────────────────────────
-  throw new Error('M45A (Training Wheels Scheduler (Grace-Period Optimization)) ML inference not yet implemented.');
+  // Day-1 operational: delegates to fallback until full ML implementation is deployed.
+  // Full implementation checklist preserved in git history.
+  return runM45aMlFallback(input);
 }
 
 // ── Degraded-mode fallback ────────────────────────────────────────────────────
@@ -239,14 +222,21 @@ export async function runM45aMl(
 export function runM45aMlFallback(
   _input: M45ATelemetryInput,
 ): M45AOutput {
-  // TODO: implement rule-based fallback for M45A
-  // Fallback must:
-  //   □ Return score = 0.5 (neutral / unknown)
-  //   □ Return topFactors = ['ML unavailable — rule-based fallback active']
-  //   □ Return recommendation = 'See rule engine output'
-  //   □ Compute deterministic auditHash from input seed + 'fallback'
-  //   □ Zero-out all M45A-specific extended outputs
-  throw new Error('M45A fallback not yet implemented.');
+  const seed = String(((_input as unknown) as Record<string, unknown>).runSeed ?? '');
+  const tick = Number(((_input as unknown) as Record<string, unknown>).tickIndex ?? 0);
+  const auditHash = createHash('sha256')
+    .update(seed + ':' + tick + ':fallback:M45A')
+    .digest('hex');
+  return {
+    score: 0.5,
+    topFactors: ['ML unavailable — rule-based fallback active'],
+    recommendation: 'See rule engine output',
+    auditHash,
+    optimalGraceDuration: null,
+    removalReadinessScore: null,
+    abuseFlag: null,
+    protectionStrengthDelta: null,
+  };
 }
 
 // ── IntelligenceState integration note ───────────────────────────────────────
diff --git a/pzo_engine/src/ml/m46a_ledger_anomaly_detector_event_stream_forensics.ts b/pzo_engine/src/ml/m46a_ledger_anomaly_detector_event_stream_forensics.ts
index 71f0d424..40cf96e3 100644
--- a/pzo_engine/src/ml/m46a_ledger_anomaly_detector_event_stream_forensics.ts
+++ b/pzo_engine/src/ml/m46a_ledger_anomaly_detector_event_stream_forensics.ts
@@ -22,6 +22,8 @@
 // Density6 LLC · Point Zero One · Confidential · All Rights Reserved
 // ═══════════════════════════════════════════════════════════════════════════════
 
+import { createHash } from 'node:crypto';
+
 // ── What this adds ────────────────────────────────────────────────────────────
 /**
  * M46A — Ledger Anomaly Detector (Event Stream Forensics)
@@ -206,28 +208,9 @@ export async function runM46aMl(
   tier:      M46ATier = 'baseline',
   modelCard: Omit<M46AModelCard, 'modelId' | 'coreMechanicPair'>,
 ): Promise<M46AOutput> {
-  // ── TODO: implement M46A — Ledger Anomaly Detector (Event Stream Forensics) ─────────────────────────────────
-  //
-  // Implementation checklist:
-  // □ Validate input schema against featureSchemaHash
-  // □ Select inference backend based on `tier` parameter
-  // □ tier === 'baseline' → GBM + calibrated logistic (fast, low-cost, production default)
-  // □ tier === 'sequence_dl' → TCN / Transformer encoder over event streams (sequential patterns)
-  // □ tier === 'graph_dl' → GNN over contract / market / ledger graphs (relationship-aware)
-  // □ tier === 'policy_rl' → Constrained contextual bandit / offline PPO (bounded nudges)
-  // □ Apply input privacy filters (no PII, no cross-player contact graph)
-  // □ Run inference → raw score + top_factors
-  // □ Apply output caps: score = Math.min(score, M46A_ML_CONSTANTS.GUARDRAILS.scoreCap)
-  // □ Apply monotonic constraints where relevant
-  // □ Abstain if confidence < M46A_ML_CONSTANTS.GUARDRAILS.abstainThreshold
-  // □ Compute auditHash = SHA256(inputs + outputs + ruleset_version + caps)
-  // □ Write signed receipt to run ledger (NEVER skip)
-  // □ Return M46AOutput — NEVER mutate run state directly
-  //
-  // Placement: server | Budget: real_time
-  // ExecHook:  after_m46_resolve
-  // ─────────────────────────────────────────────────────────────────────────
-  throw new Error('M46A (Ledger Anomaly Detector (Event Stream Forensics)) ML inference not yet implemented.');
+  // Day-1 operational: delegates to fallback until full ML implementation is deployed.
+  // Full implementation checklist preserved in git history.
+  return runM46aMlFallback(input);
 }
 
 // ── Degraded-mode fallback ────────────────────────────────────────────────────
@@ -239,14 +222,21 @@ export async function runM46aMl(
 export function runM46aMlFallback(
   _input: M46ATelemetryInput,
 ): M46AOutput {
-  // TODO: implement rule-based fallback for M46A
-  // Fallback must:
-  //   □ Return score = 0.5 (neutral / unknown)
-  //   □ Return topFactors = ['ML unavailable — rule-based fallback active']
-  //   □ Return recommendation = 'See rule engine output'
-  //   □ Compute deterministic auditHash from input seed + 'fallback'
-  //   □ Zero-out all M46A-specific extended outputs
-  throw new Error('M46A fallback not yet implemented.');
+  const seed = String(((_input as unknown) as Record<string, unknown>).runSeed ?? '');
+  const tick = Number(((_input as unknown) as Record<string, unknown>).tickIndex ?? 0);
+  const auditHash = createHash('sha256')
+    .update(seed + ':' + tick + ':fallback:M46A')
+    .digest('hex');
+  return {
+    score: 0.5,
+    topFactors: ['ML unavailable — rule-based fallback active'],
+    recommendation: 'See rule engine output',
+    auditHash,
+    anomalyScore: null,
+    manipulationFlag: null,
+    clockAbuseFlag: null,
+    forensicEvidenceChain: null,
+  };
 }
 
 // ── IntelligenceState integration note ───────────────────────────────────────
diff --git a/pzo_engine/src/ml/m47a_signature_freshness_monitor_client_action_integrity.ts b/pzo_engine/src/ml/m47a_signature_freshness_monitor_client_action_integrity.ts
index 790f30fd..366204dc 100644
--- a/pzo_engine/src/ml/m47a_signature_freshness_monitor_client_action_integrity.ts
+++ b/pzo_engine/src/ml/m47a_signature_freshness_monitor_client_action_integrity.ts
@@ -22,6 +22,8 @@
 // Density6 LLC · Point Zero One · Confidential · All Rights Reserved
 // ═══════════════════════════════════════════════════════════════════════════════
 
+import { createHash } from 'node:crypto';
+
 // ── What this adds ────────────────────────────────────────────────────────────
 /**
  * M47A — Signature & Freshness Monitor (Client Action Integrity)
@@ -206,28 +208,9 @@ export async function runM47aMl(
   tier:      M47ATier = 'baseline',
   modelCard: Omit<M47AModelCard, 'modelId' | 'coreMechanicPair'>,
 ): Promise<M47AOutput> {
-  // ── TODO: implement M47A — Signature & Freshness Monitor (Client Action Integrity) ─────────────────────────────────
-  //
-  // Implementation checklist:
-  // □ Validate input schema against featureSchemaHash
-  // □ Select inference backend based on `tier` parameter
-  // □ tier === 'baseline' → GBM + calibrated logistic (fast, low-cost, production default)
-  // □ tier === 'sequence_dl' → TCN / Transformer encoder over event streams (sequential patterns)
-  // □ tier === 'graph_dl' → GNN over contract / market / ledger graphs (relationship-aware)
-  // □ tier === 'policy_rl' → Constrained contextual bandit / offline PPO (bounded nudges)
-  // □ Apply input privacy filters (no PII, no cross-player contact graph)
-  // □ Run inference → raw score + top_factors
-  // □ Apply output caps: score = Math.min(score, M47A_ML_CONSTANTS.GUARDRAILS.scoreCap)
-  // □ Apply monotonic constraints where relevant
-  // □ Abstain if confidence < M47A_ML_CONSTANTS.GUARDRAILS.abstainThreshold
-  // □ Compute auditHash = SHA256(inputs + outputs + ruleset_version + caps)
-  // □ Write signed receipt to run ledger (NEVER skip)
-  // □ Return M47AOutput — NEVER mutate run state directly
-  //
-  // Placement: server | Budget: real_time
-  // ExecHook:  after_m47_resolve
-  // ─────────────────────────────────────────────────────────────────────────
-  throw new Error('M47A (Signature & Freshness Monitor (Client Action Integrity)) ML inference not yet implemented.');
+  // Day-1 operational: delegates to fallback until full ML implementation is deployed.
+  // Full implementation checklist preserved in git history.
+  return runM47aMlFallback(input);
 }
 
 // ── Degraded-mode fallback ────────────────────────────────────────────────────
@@ -239,14 +222,21 @@ export async function runM47aMl(
 export function runM47aMlFallback(
   _input: M47ATelemetryInput,
 ): M47AOutput {
-  // TODO: implement rule-based fallback for M47A
-  // Fallback must:
-  //   □ Return score = 0.5 (neutral / unknown)
-  //   □ Return topFactors = ['ML unavailable — rule-based fallback active']
-  //   □ Return recommendation = 'See rule engine output'
-  //   □ Compute deterministic auditHash from input seed + 'fallback'
-  //   □ Zero-out all M47A-specific extended outputs
-  throw new Error('M47A fallback not yet implemented.');
+  const seed = String(((_input as unknown) as Record<string, unknown>).runSeed ?? '');
+  const tick = Number(((_input as unknown) as Record<string, unknown>).tickIndex ?? 0);
+  const auditHash = createHash('sha256')
+    .update(seed + ':' + tick + ':fallback:M47A')
+    .digest('hex');
+  return {
+    score: 0.5,
+    topFactors: ['ML unavailable — rule-based fallback active'],
+    recommendation: 'See rule engine output',
+    auditHash,
+    signatureFreshnessScore: null,
+    replayAttackFlag: null,
+    forgeryProbability: null,
+    deviceClusterAnomaly: null,
+  };
 }
 
 // ── IntelligenceState integration note ───────────────────────────────────────
diff --git a/pzo_engine/src/ml/m48a_replay_consistency_model_deterministic_validator_assist.ts b/pzo_engine/src/ml/m48a_replay_consistency_model_deterministic_validator_assist.ts
index 71b68612..fd190557 100644
--- a/pzo_engine/src/ml/m48a_replay_consistency_model_deterministic_validator_assist.ts
+++ b/pzo_engine/src/ml/m48a_replay_consistency_model_deterministic_validator_assist.ts
@@ -22,6 +22,8 @@
 // Density6 LLC · Point Zero One · Confidential · All Rights Reserved
 // ═══════════════════════════════════════════════════════════════════════════════
 
+import { createHash } from 'node:crypto';
+
 // ── What this adds ────────────────────────────────────────────────────────────
 /**
  * M48A — Replay Consistency Model (Deterministic Validator Assist)
@@ -206,28 +208,9 @@ export async function runM48aMl(
   tier:      M48ATier = 'baseline',
   modelCard: Omit<M48AModelCard, 'modelId' | 'coreMechanicPair'>,
 ): Promise<M48AOutput> {
-  // ── TODO: implement M48A — Replay Consistency Model (Deterministic Validator Assist) ─────────────────────────────────
-  //
-  // Implementation checklist:
-  // □ Validate input schema against featureSchemaHash
-  // □ Select inference backend based on `tier` parameter
-  // □ tier === 'baseline' → GBM + calibrated logistic (fast, low-cost, production default)
-  // □ tier === 'sequence_dl' → TCN / Transformer encoder over event streams (sequential patterns)
-  // □ tier === 'graph_dl' → GNN over contract / market / ledger graphs (relationship-aware)
-  // □ tier === 'policy_rl' → Constrained contextual bandit / offline PPO (bounded nudges)
-  // □ Apply input privacy filters (no PII, no cross-player contact graph)
-  // □ Run inference → raw score + top_factors
-  // □ Apply output caps: score = Math.min(score, M48A_ML_CONSTANTS.GUARDRAILS.scoreCap)
-  // □ Apply monotonic constraints where relevant
-  // □ Abstain if confidence < M48A_ML_CONSTANTS.GUARDRAILS.abstainThreshold
-  // □ Compute auditHash = SHA256(inputs + outputs + ruleset_version + caps)
-  // □ Write signed receipt to run ledger (NEVER skip)
-  // □ Return M48AOutput — NEVER mutate run state directly
-  //
-  // Placement: server | Budget: real_time
-  // ExecHook:  after_m48_resolve
-  // ─────────────────────────────────────────────────────────────────────────
-  throw new Error('M48A (Replay Consistency Model (Deterministic Validator Assist)) ML inference not yet implemented.');
+  // Day-1 operational: delegates to fallback until full ML implementation is deployed.
+  // Full implementation checklist preserved in git history.
+  return runM48aMlFallback(input);
 }
 
 // ── Degraded-mode fallback ────────────────────────────────────────────────────
@@ -239,14 +222,21 @@ export async function runM48aMl(
 export function runM48aMlFallback(
   _input: M48ATelemetryInput,
 ): M48AOutput {
-  // TODO: implement rule-based fallback for M48A
-  // Fallback must:
-  //   □ Return score = 0.5 (neutral / unknown)
-  //   □ Return topFactors = ['ML unavailable — rule-based fallback active']
-  //   □ Return recommendation = 'See rule engine output'
-  //   □ Compute deterministic auditHash from input seed + 'fallback'
-  //   □ Zero-out all M48A-specific extended outputs
-  throw new Error('M48A fallback not yet implemented.');
+  const seed = String(((_input as unknown) as Record<string, unknown>).runSeed ?? '');
+  const tick = Number(((_input as unknown) as Record<string, unknown>).tickIndex ?? 0);
+  const auditHash = createHash('sha256')
+    .update(seed + ':' + tick + ':fallback:M48A')
+    .digest('hex');
+  return {
+    score: 0.5,
+    topFactors: ['ML unavailable — rule-based fallback active'],
+    recommendation: 'See rule engine output',
+    auditHash,
+    consistencyPreScore: null,
+    verificationPriority: null,
+    cleanRunFilterPassed: null,
+    backlogReductionDelta: null,
+  };
 }
 
 // ── IntelligenceState integration note ───────────────────────────────────────
diff --git a/pzo_engine/src/ml/m49a_exploit_response_policy_auto_containment_optimizer.ts b/pzo_engine/src/ml/m49a_exploit_response_policy_auto_containment_optimizer.ts
index 84a61dd7..04915618 100644
--- a/pzo_engine/src/ml/m49a_exploit_response_policy_auto_containment_optimizer.ts
+++ b/pzo_engine/src/ml/m49a_exploit_response_policy_auto_containment_optimizer.ts
@@ -22,6 +22,8 @@
 // Density6 LLC · Point Zero One · Confidential · All Rights Reserved
 // ═══════════════════════════════════════════════════════════════════════════════
 
+import { createHash } from 'node:crypto';
+
 // ── What this adds ────────────────────────────────────────────────────────────
 /**
  * M49A — Exploit Response Policy (Auto-Containment Optimizer)
@@ -206,28 +208,9 @@ export async function runM49aMl(
   tier:      M49ATier = 'baseline',
   modelCard: Omit<M49AModelCard, 'modelId' | 'coreMechanicPair'>,
 ): Promise<M49AOutput> {
-  // ── TODO: implement M49A — Exploit Response Policy (Auto-Containment Optimizer) ─────────────────────────────────
-  //
-  // Implementation checklist:
-  // □ Validate input schema against featureSchemaHash
-  // □ Select inference backend based on `tier` parameter
-  // □ tier === 'baseline' → GBM + calibrated logistic (fast, low-cost, production default)
-  // □ tier === 'sequence_dl' → TCN / Transformer encoder over event streams (sequential patterns)
-  // □ tier === 'graph_dl' → GNN over contract / market / ledger graphs (relationship-aware)
-  // □ tier === 'policy_rl' → Constrained contextual bandit / offline PPO (bounded nudges)
-  // □ Apply input privacy filters (no PII, no cross-player contact graph)
-  // □ Run inference → raw score + top_factors
-  // □ Apply output caps: score = Math.min(score, M49A_ML_CONSTANTS.GUARDRAILS.scoreCap)
-  // □ Apply monotonic constraints where relevant
-  // □ Abstain if confidence < M49A_ML_CONSTANTS.GUARDRAILS.abstainThreshold
-  // □ Compute auditHash = SHA256(inputs + outputs + ruleset_version + caps)
-  // □ Write signed receipt to run ledger (NEVER skip)
-  // □ Return M49AOutput — NEVER mutate run state directly
-  //
-  // Placement: server | Budget: real_time
-  // ExecHook:  after_m49_resolve
-  // ─────────────────────────────────────────────────────────────────────────
-  throw new Error('M49A (Exploit Response Policy (Auto-Containment Optimizer)) ML inference not yet implemented.');
+  // Day-1 operational: delegates to fallback until full ML implementation is deployed.
+  // Full implementation checklist preserved in git history.
+  return runM49aMlFallback(input);
 }
 
 // ── Degraded-mode fallback ────────────────────────────────────────────────────
@@ -239,14 +222,21 @@ export async function runM49aMl(
 export function runM49aMlFallback(
   _input: M49ATelemetryInput,
 ): M49AOutput {
-  // TODO: implement rule-based fallback for M49A
-  // Fallback must:
-  //   □ Return score = 0.5 (neutral / unknown)
-  //   □ Return topFactors = ['ML unavailable — rule-based fallback active']
-  //   □ Return recommendation = 'See rule engine output'
-  //   □ Compute deterministic auditHash from input seed + 'fallback'
-  //   □ Zero-out all M49A-specific extended outputs
-  throw new Error('M49A fallback not yet implemented.');
+  const seed = String(((_input as unknown) as Record<string, unknown>).runSeed ?? '');
+  const tick = Number(((_input as unknown) as Record<string, unknown>).tickIndex ?? 0);
+  const auditHash = createHash('sha256')
+    .update(seed + ':' + tick + ':fallback:M49A')
+    .digest('hex');
+  return {
+    score: 0.5,
+    topFactors: ['ML unavailable — rule-based fallback active'],
+    recommendation: 'See rule engine output',
+    auditHash,
+    responseSeverity: null,
+    falsePositiveRisk: null,
+    taxonomyUpdateSignal: null,
+    containmentPolicy: null,
+  };
 }
 
 // ── IntelligenceState integration note ───────────────────────────────────────
diff --git a/pzo_engine/src/ml/m50a_proof_card_verifier_receipt_hash_trust_layer.ts b/pzo_engine/src/ml/m50a_proof_card_verifier_receipt_hash_trust_layer.ts
index 46a950cf..c0246770 100644
--- a/pzo_engine/src/ml/m50a_proof_card_verifier_receipt_hash_trust_layer.ts
+++ b/pzo_engine/src/ml/m50a_proof_card_verifier_receipt_hash_trust_layer.ts
@@ -22,6 +22,8 @@
 // Density6 LLC · Point Zero One · Confidential · All Rights Reserved
 // ═══════════════════════════════════════════════════════════════════════════════
 
+import { createHash } from 'node:crypto';
+
 // ── What this adds ────────────────────────────────────────────────────────────
 /**
  * M50A — Proof Card Verifier (Receipt Hash Trust Layer)
@@ -206,28 +208,9 @@ export async function runM50aMl(
   tier:      M50ATier = 'baseline',
   modelCard: Omit<M50AModelCard, 'modelId' | 'coreMechanicPair'>,
 ): Promise<M50AOutput> {
-  // ── TODO: implement M50A — Proof Card Verifier (Receipt Hash Trust Layer) ─────────────────────────────────
-  //
-  // Implementation checklist:
-  // □ Validate input schema against featureSchemaHash
-  // □ Select inference backend based on `tier` parameter
-  // □ tier === 'baseline' → GBM + calibrated logistic (fast, low-cost, production default)
-  // □ tier === 'sequence_dl' → TCN / Transformer encoder over event streams (sequential patterns)
-  // □ tier === 'graph_dl' → GNN over contract / market / ledger graphs (relationship-aware)
-  // □ tier === 'policy_rl' → Constrained contextual bandit / offline PPO (bounded nudges)
-  // □ Apply input privacy filters (no PII, no cross-player contact graph)
-  // □ Run inference → raw score + top_factors
-  // □ Apply output caps: score = Math.min(score, M50A_ML_CONSTANTS.GUARDRAILS.scoreCap)
-  // □ Apply monotonic constraints where relevant
-  // □ Abstain if confidence < M50A_ML_CONSTANTS.GUARDRAILS.abstainThreshold
-  // □ Compute auditHash = SHA256(inputs + outputs + ruleset_version + caps)
-  // □ Write signed receipt to run ledger (NEVER skip)
-  // □ Return M50AOutput — NEVER mutate run state directly
-  //
-  // Placement: server | Budget: real_time
-  // ExecHook:  after_m50_resolve
-  // ─────────────────────────────────────────────────────────────────────────
-  throw new Error('M50A (Proof Card Verifier (Receipt Hash Trust Layer)) ML inference not yet implemented.');
+  // Day-1 operational: delegates to fallback until full ML implementation is deployed.
+  // Full implementation checklist preserved in git history.
+  return runM50aMlFallback(input);
 }
 
 // ── Degraded-mode fallback ────────────────────────────────────────────────────
@@ -239,14 +222,21 @@ export async function runM50aMl(
 export function runM50aMlFallback(
   _input: M50ATelemetryInput,
 ): M50AOutput {
-  // TODO: implement rule-based fallback for M50A
-  // Fallback must:
-  //   □ Return score = 0.5 (neutral / unknown)
-  //   □ Return topFactors = ['ML unavailable — rule-based fallback active']
-  //   □ Return recommendation = 'See rule engine output'
-  //   □ Compute deterministic auditHash from input seed + 'fallback'
-  //   □ Zero-out all M50A-specific extended outputs
-  throw new Error('M50A fallback not yet implemented.');
+  const seed = String(((_input as unknown) as Record<string, unknown>).runSeed ?? '');
+  const tick = Number(((_input as unknown) as Record<string, unknown>).tickIndex ?? 0);
+  const auditHash = createHash('sha256')
+    .update(seed + ':' + tick + ':fallback:M50A')
+    .digest('hex');
+  return {
+    score: 0.5,
+    topFactors: ['ML unavailable — rule-based fallback active'],
+    recommendation: 'See rule engine output',
+    auditHash,
+    authenticityScore: null,
+    hashCollisionFlag: null,
+    truncatedProofFlag: null,
+    trustTier: null,
+  };
 }
 
 // ── IntelligenceState integration note ───────────────────────────────────────
diff --git a/pzo_engine/src/ml/m51a_syndicate_coalition_scoring_multi_party_deal_stability.ts b/pzo_engine/src/ml/m51a_syndicate_coalition_scoring_multi_party_deal_stability.ts
index b90762c1..a1d72177 100644
--- a/pzo_engine/src/ml/m51a_syndicate_coalition_scoring_multi_party_deal_stability.ts
+++ b/pzo_engine/src/ml/m51a_syndicate_coalition_scoring_multi_party_deal_stability.ts
@@ -22,6 +22,8 @@
 // Density6 LLC · Point Zero One · Confidential · All Rights Reserved
 // ═══════════════════════════════════════════════════════════════════════════════
 
+import { createHash } from 'node:crypto';
+
 // ── What this adds ────────────────────────────────────────────────────────────
 /**
  * M51A — Syndicate Coalition Scoring (Multi-Party Deal Stability)
@@ -206,28 +208,9 @@ export async function runM51aMl(
   tier:      M51ATier = 'baseline',
   modelCard: Omit<M51AModelCard, 'modelId' | 'coreMechanicPair'>,
 ): Promise<M51AOutput> {
-  // ── TODO: implement M51A — Syndicate Coalition Scoring (Multi-Party Deal Stability) ─────────────────────────────────
-  //
-  // Implementation checklist:
-  // □ Validate input schema against featureSchemaHash
-  // □ Select inference backend based on `tier` parameter
-  // □ tier === 'baseline' → GBM + calibrated logistic (fast, low-cost, production default)
-  // □ tier === 'sequence_dl' → TCN / Transformer encoder over event streams (sequential patterns)
-  // □ tier === 'graph_dl' → GNN over contract / market / ledger graphs (relationship-aware)
-  // □ tier === 'policy_rl' → Constrained contextual bandit / offline PPO (bounded nudges)
-  // □ Apply input privacy filters (no PII, no cross-player contact graph)
-  // □ Run inference → raw score + top_factors
-  // □ Apply output caps: score = Math.min(score, M51A_ML_CONSTANTS.GUARDRAILS.scoreCap)
-  // □ Apply monotonic constraints where relevant
-  // □ Abstain if confidence < M51A_ML_CONSTANTS.GUARDRAILS.abstainThreshold
-  // □ Compute auditHash = SHA256(inputs + outputs + ruleset_version + caps)
-  // □ Write signed receipt to run ledger (NEVER skip)
-  // □ Return M51AOutput — NEVER mutate run state directly
-  //
-  // Placement: server | Budget: real_time
-  // ExecHook:  after_m51_resolve
-  // ─────────────────────────────────────────────────────────────────────────
-  throw new Error('M51A (Syndicate Coalition Scoring (Multi-Party Deal Stability)) ML inference not yet implemented.');
+  // Day-1 operational: delegates to fallback until full ML implementation is deployed.
+  // Full implementation checklist preserved in git history.
+  return runM51aMlFallback(input);
 }
 
 // ── Degraded-mode fallback ────────────────────────────────────────────────────
@@ -239,14 +222,21 @@ export async function runM51aMl(
 export function runM51aMlFallback(
   _input: M51ATelemetryInput,
 ): M51AOutput {
-  // TODO: implement rule-based fallback for M51A
-  // Fallback must:
-  //   □ Return score = 0.5 (neutral / unknown)
-  //   □ Return topFactors = ['ML unavailable — rule-based fallback active']
-  //   □ Return recommendation = 'See rule engine output'
-  //   □ Compute deterministic auditHash from input seed + 'fallback'
-  //   □ Zero-out all M51A-specific extended outputs
-  throw new Error('M51A fallback not yet implemented.');
+  const seed = String(((_input as unknown) as Record<string, unknown>).runSeed ?? '');
+  const tick = Number(((_input as unknown) as Record<string, unknown>).tickIndex ?? 0);
+  const auditHash = createHash('sha256')
+    .update(seed + ':' + tick + ':fallback:M51A')
+    .digest('hex');
+  return {
+    score: 0.5,
+    topFactors: ['ML unavailable — rule-based fallback active'],
+    recommendation: 'See rule engine output',
+    auditHash,
+    coalitionStabilityScore: null,
+    collapseProbability: null,
+    restructureRecommendation: null,
+    dealHealthReceipt: null,
+  };
 }
 
 // ── IntelligenceState integration note ───────────────────────────────────────
diff --git a/pzo_engine/src/ml/m52a_escrow_milestone_success_model_gaming_resistant_release.ts b/pzo_engine/src/ml/m52a_escrow_milestone_success_model_gaming_resistant_release.ts
index 0157d862..65241746 100644
--- a/pzo_engine/src/ml/m52a_escrow_milestone_success_model_gaming_resistant_release.ts
+++ b/pzo_engine/src/ml/m52a_escrow_milestone_success_model_gaming_resistant_release.ts
@@ -22,6 +22,8 @@
 // Density6 LLC · Point Zero One · Confidential · All Rights Reserved
 // ═══════════════════════════════════════════════════════════════════════════════
 
+import { createHash } from 'node:crypto';
+
 // ── What this adds ────────────────────────────────────────────────────────────
 /**
  * M52A — Escrow Milestone Success Model (Gaming-Resistant Release Gates)
@@ -206,28 +208,9 @@ export async function runM52aMl(
   tier:      M52ATier = 'baseline',
   modelCard: Omit<M52AModelCard, 'modelId' | 'coreMechanicPair'>,
 ): Promise<M52AOutput> {
-  // ── TODO: implement M52A — Escrow Milestone Success Model (Gaming-Resistant Release Gates) ─────────────────────────────────
-  //
-  // Implementation checklist:
-  // □ Validate input schema against featureSchemaHash
-  // □ Select inference backend based on `tier` parameter
-  // □ tier === 'baseline' → GBM + calibrated logistic (fast, low-cost, production default)
-  // □ tier === 'sequence_dl' → TCN / Transformer encoder over event streams (sequential patterns)
-  // □ tier === 'graph_dl' → GNN over contract / market / ledger graphs (relationship-aware)
-  // □ tier === 'policy_rl' → Constrained contextual bandit / offline PPO (bounded nudges)
-  // □ Apply input privacy filters (no PII, no cross-player contact graph)
-  // □ Run inference → raw score + top_factors
-  // □ Apply output caps: score = Math.min(score, M52A_ML_CONSTANTS.GUARDRAILS.scoreCap)
-  // □ Apply monotonic constraints where relevant
-  // □ Abstain if confidence < M52A_ML_CONSTANTS.GUARDRAILS.abstainThreshold
-  // □ Compute auditHash = SHA256(inputs + outputs + ruleset_version + caps)
-  // □ Write signed receipt to run ledger (NEVER skip)
-  // □ Return M52AOutput — NEVER mutate run state directly
-  //
-  // Placement: server | Budget: real_time
-  // ExecHook:  after_m52_resolve
-  // ─────────────────────────────────────────────────────────────────────────
-  throw new Error('M52A (Escrow Milestone Success Model (Gaming-Resistant Release Gates)) ML inference not yet implemented.');
+  // Day-1 operational: delegates to fallback until full ML implementation is deployed.
+  // Full implementation checklist preserved in git history.
+  return runM52aMlFallback(input);
 }
 
 // ── Degraded-mode fallback ────────────────────────────────────────────────────
@@ -239,14 +222,21 @@ export async function runM52aMl(
 export function runM52aMlFallback(
   _input: M52ATelemetryInput,
 ): M52AOutput {
-  // TODO: implement rule-based fallback for M52A
-  // Fallback must:
-  //   □ Return score = 0.5 (neutral / unknown)
-  //   □ Return topFactors = ['ML unavailable — rule-based fallback active']
-  //   □ Return recommendation = 'See rule engine output'
-  //   □ Compute deterministic auditHash from input seed + 'fallback'
-  //   □ Zero-out all M52A-specific extended outputs
-  throw new Error('M52A fallback not yet implemented.');
+  const seed = String(((_input as unknown) as Record<string, unknown>).runSeed ?? '');
+  const tick = Number(((_input as unknown) as Record<string, unknown>).tickIndex ?? 0);
+  const auditHash = createHash('sha256')
+    .update(seed + ':' + tick + ':fallback:M52A')
+    .digest('hex');
+  return {
+    score: 0.5,
+    topFactors: ['ML unavailable — rule-based fallback active'],
+    recommendation: 'See rule engine output',
+    auditHash,
+    milestoneAuthenticityScore: null,
+    gamingPatternFlag: null,
+    releaseGateRecommendation: null,
+    arbitrationEvidence: null,
+  };
 }
 
 // ── IntelligenceState integration note ───────────────────────────────────────
diff --git a/pzo_engine/src/ml/m53a_reliability_collusion_monitor_reputation_stake.ts b/pzo_engine/src/ml/m53a_reliability_collusion_monitor_reputation_stake.ts
index 6eaf28c4..2797f523 100644
--- a/pzo_engine/src/ml/m53a_reliability_collusion_monitor_reputation_stake.ts
+++ b/pzo_engine/src/ml/m53a_reliability_collusion_monitor_reputation_stake.ts
@@ -22,6 +22,8 @@
 // Density6 LLC · Point Zero One · Confidential · All Rights Reserved
 // ═══════════════════════════════════════════════════════════════════════════════
 
+import { createHash } from 'node:crypto';
+
 // ── What this adds ────────────────────────────────────────────────────────────
 /**
  * M53A — Reliability & Collusion Monitor (Reputation Stake)
@@ -206,28 +208,9 @@ export async function runM53aMl(
   tier:      M53ATier = 'baseline',
   modelCard: Omit<M53AModelCard, 'modelId' | 'coreMechanicPair'>,
 ): Promise<M53AOutput> {
-  // ── TODO: implement M53A — Reliability & Collusion Monitor (Reputation Stake) ─────────────────────────────────
-  //
-  // Implementation checklist:
-  // □ Validate input schema against featureSchemaHash
-  // □ Select inference backend based on `tier` parameter
-  // □ tier === 'baseline' → GBM + calibrated logistic (fast, low-cost, production default)
-  // □ tier === 'sequence_dl' → TCN / Transformer encoder over event streams (sequential patterns)
-  // □ tier === 'graph_dl' → GNN over contract / market / ledger graphs (relationship-aware)
-  // □ tier === 'policy_rl' → Constrained contextual bandit / offline PPO (bounded nudges)
-  // □ Apply input privacy filters (no PII, no cross-player contact graph)
-  // □ Run inference → raw score + top_factors
-  // □ Apply output caps: score = Math.min(score, M53A_ML_CONSTANTS.GUARDRAILS.scoreCap)
-  // □ Apply monotonic constraints where relevant
-  // □ Abstain if confidence < M53A_ML_CONSTANTS.GUARDRAILS.abstainThreshold
-  // □ Compute auditHash = SHA256(inputs + outputs + ruleset_version + caps)
-  // □ Write signed receipt to run ledger (NEVER skip)
-  // □ Return M53AOutput — NEVER mutate run state directly
-  //
-  // Placement: server | Budget: batch
-  // ExecHook:  after_m53_resolve
-  // ─────────────────────────────────────────────────────────────────────────
-  throw new Error('M53A (Reliability & Collusion Monitor (Reputation Stake)) ML inference not yet implemented.');
+  // Day-1 operational: delegates to fallback until full ML implementation is deployed.
+  // Full implementation checklist preserved in git history.
+  return runM53aMlFallback(input);
 }
 
 // ── Degraded-mode fallback ────────────────────────────────────────────────────
@@ -239,14 +222,21 @@ export async function runM53aMl(
 export function runM53aMlFallback(
   _input: M53ATelemetryInput,
 ): M53AOutput {
-  // TODO: implement rule-based fallback for M53A
-  // Fallback must:
-  //   □ Return score = 0.5 (neutral / unknown)
-  //   □ Return topFactors = ['ML unavailable — rule-based fallback active']
-  //   □ Return recommendation = 'See rule engine output'
-  //   □ Compute deterministic auditHash from input seed + 'fallback'
-  //   □ Zero-out all M53A-specific extended outputs
-  throw new Error('M53A fallback not yet implemented.');
+  const seed = String(((_input as unknown) as Record<string, unknown>).runSeed ?? '');
+  const tick = Number(((_input as unknown) as Record<string, unknown>).tickIndex ?? 0);
+  const auditHash = createHash('sha256')
+    .update(seed + ':' + tick + ':fallback:M53A')
+    .digest('hex');
+  return {
+    score: 0.5,
+    topFactors: ['ML unavailable — rule-based fallback active'],
+    recommendation: 'See rule engine output',
+    auditHash,
+    reliabilityTrajectory: null,
+    collusionRingProbability: null,
+    reputationAdjustment: null,
+    evidenceChain: null,
+  };
 }
 
 // ── IntelligenceState integration note ───────────────────────────────────────
diff --git a/pzo_engine/src/ml/m54a_restructure_acceptance_forecaster_distress_term_triage.ts b/pzo_engine/src/ml/m54a_restructure_acceptance_forecaster_distress_term_triage.ts
index 6bb7e16a..e7937bb1 100644
--- a/pzo_engine/src/ml/m54a_restructure_acceptance_forecaster_distress_term_triage.ts
+++ b/pzo_engine/src/ml/m54a_restructure_acceptance_forecaster_distress_term_triage.ts
@@ -22,6 +22,8 @@
 // Density6 LLC · Point Zero One · Confidential · All Rights Reserved
 // ═══════════════════════════════════════════════════════════════════════════════
 
+import { createHash } from 'node:crypto';
+
 // ── What this adds ────────────────────────────────────────────────────────────
 /**
  * M54A — Restructure Acceptance Forecaster (Distress Term Triage)
@@ -206,28 +208,9 @@ export async function runM54aMl(
   tier:      M54ATier = 'baseline',
   modelCard: Omit<M54AModelCard, 'modelId' | 'coreMechanicPair'>,
 ): Promise<M54AOutput> {
-  // ── TODO: implement M54A — Restructure Acceptance Forecaster (Distress Term Triage) ─────────────────────────────────
-  //
-  // Implementation checklist:
-  // □ Validate input schema against featureSchemaHash
-  // □ Select inference backend based on `tier` parameter
-  // □ tier === 'baseline' → GBM + calibrated logistic (fast, low-cost, production default)
-  // □ tier === 'sequence_dl' → TCN / Transformer encoder over event streams (sequential patterns)
-  // □ tier === 'graph_dl' → GNN over contract / market / ledger graphs (relationship-aware)
-  // □ tier === 'policy_rl' → Constrained contextual bandit / offline PPO (bounded nudges)
-  // □ Apply input privacy filters (no PII, no cross-player contact graph)
-  // □ Run inference → raw score + top_factors
-  // □ Apply output caps: score = Math.min(score, M54A_ML_CONSTANTS.GUARDRAILS.scoreCap)
-  // □ Apply monotonic constraints where relevant
-  // □ Abstain if confidence < M54A_ML_CONSTANTS.GUARDRAILS.abstainThreshold
-  // □ Compute auditHash = SHA256(inputs + outputs + ruleset_version + caps)
-  // □ Write signed receipt to run ledger (NEVER skip)
-  // □ Return M54AOutput — NEVER mutate run state directly
-  //
-  // Placement: server | Budget: real_time
-  // ExecHook:  after_m54_resolve
-  // ─────────────────────────────────────────────────────────────────────────
-  throw new Error('M54A (Restructure Acceptance Forecaster (Distress Term Triage)) ML inference not yet implemented.');
+  // Day-1 operational: delegates to fallback until full ML implementation is deployed.
+  // Full implementation checklist preserved in git history.
+  return runM54aMlFallback(input);
 }
 
 // ── Degraded-mode fallback ────────────────────────────────────────────────────
@@ -239,14 +222,21 @@ export async function runM54aMl(
 export function runM54aMlFallback(
   _input: M54ATelemetryInput,
 ): M54AOutput {
-  // TODO: implement rule-based fallback for M54A
-  // Fallback must:
-  //   □ Return score = 0.5 (neutral / unknown)
-  //   □ Return topFactors = ['ML unavailable — rule-based fallback active']
-  //   □ Return recommendation = 'See rule engine output'
-  //   □ Compute deterministic auditHash from input seed + 'fallback'
-  //   □ Zero-out all M54A-specific extended outputs
-  throw new Error('M54A fallback not yet implemented.');
+  const seed = String(((_input as unknown) as Record<string, unknown>).runSeed ?? '');
+  const tick = Number(((_input as unknown) as Record<string, unknown>).tickIndex ?? 0);
+  const auditHash = createHash('sha256')
+    .update(seed + ':' + tick + ':fallback:M54A')
+    .digest('hex');
+  return {
+    score: 0.5,
+    topFactors: ['ML unavailable — rule-based fallback active'],
+    recommendation: 'See rule engine output',
+    auditHash,
+    acceptanceProbability: null,
+    distressTriage: null,
+    negotiationWindowRecommendation: null,
+    cooperativeOutcomeScore: null,
+  };
 }
 
 // ── IntelligenceState integration note ───────────────────────────────────────
diff --git a/pzo_engine/src/ml/m55a_arbitration_triage_verdict_summarizer_replay_evidence.ts b/pzo_engine/src/ml/m55a_arbitration_triage_verdict_summarizer_replay_evidence.ts
index 436709a0..fa878784 100644
--- a/pzo_engine/src/ml/m55a_arbitration_triage_verdict_summarizer_replay_evidence.ts
+++ b/pzo_engine/src/ml/m55a_arbitration_triage_verdict_summarizer_replay_evidence.ts
@@ -22,6 +22,8 @@
 // Density6 LLC · Point Zero One · Confidential · All Rights Reserved
 // ═══════════════════════════════════════════════════════════════════════════════
 
+import { createHash } from 'node:crypto';
+
 // ── What this adds ────────────────────────────────────────────────────────────
 /**
  * M55A — Arbitration Triage + Verdict Summarizer (Replay Evidence)
@@ -206,28 +208,9 @@ export async function runM55aMl(
   tier:      M55ATier = 'baseline',
   modelCard: Omit<M55AModelCard, 'modelId' | 'coreMechanicPair'>,
 ): Promise<M55AOutput> {
-  // ── TODO: implement M55A — Arbitration Triage + Verdict Summarizer (Replay Evidence) ─────────────────────────────────
-  //
-  // Implementation checklist:
-  // □ Validate input schema against featureSchemaHash
-  // □ Select inference backend based on `tier` parameter
-  // □ tier === 'baseline' → GBM + calibrated logistic (fast, low-cost, production default)
-  // □ tier === 'sequence_dl' → TCN / Transformer encoder over event streams (sequential patterns)
-  // □ tier === 'graph_dl' → GNN over contract / market / ledger graphs (relationship-aware)
-  // □ tier === 'policy_rl' → Constrained contextual bandit / offline PPO (bounded nudges)
-  // □ Apply input privacy filters (no PII, no cross-player contact graph)
-  // □ Run inference → raw score + top_factors
-  // □ Apply output caps: score = Math.min(score, M55A_ML_CONSTANTS.GUARDRAILS.scoreCap)
-  // □ Apply monotonic constraints where relevant
-  // □ Abstain if confidence < M55A_ML_CONSTANTS.GUARDRAILS.abstainThreshold
-  // □ Compute auditHash = SHA256(inputs + outputs + ruleset_version + caps)
-  // □ Write signed receipt to run ledger (NEVER skip)
-  // □ Return M55AOutput — NEVER mutate run state directly
-  //
-  // Placement: server | Budget: batch
-  // ExecHook:  after_m55_resolve
-  // ─────────────────────────────────────────────────────────────────────────
-  throw new Error('M55A (Arbitration Triage + Verdict Summarizer (Replay Evidence)) ML inference not yet implemented.');
+  // Day-1 operational: delegates to fallback until full ML implementation is deployed.
+  // Full implementation checklist preserved in git history.
+  return runM55aMlFallback(input);
 }
 
 // ── Degraded-mode fallback ────────────────────────────────────────────────────
@@ -239,14 +222,21 @@ export async function runM55aMl(
 export function runM55aMlFallback(
   _input: M55ATelemetryInput,
 ): M55AOutput {
-  // TODO: implement rule-based fallback for M55A
-  // Fallback must:
-  //   □ Return score = 0.5 (neutral / unknown)
-  //   □ Return topFactors = ['ML unavailable — rule-based fallback active']
-  //   □ Return recommendation = 'See rule engine output'
-  //   □ Compute deterministic auditHash from input seed + 'fallback'
-  //   □ Zero-out all M55A-specific extended outputs
-  throw new Error('M55A fallback not yet implemented.');
+  const seed = String(((_input as unknown) as Record<string, unknown>).runSeed ?? '');
+  const tick = Number(((_input as unknown) as Record<string, unknown>).tickIndex ?? 0);
+  const auditHash = createHash('sha256')
+    .update(seed + ':' + tick + ':fallback:M55A')
+    .digest('hex');
+  return {
+    score: 0.5,
+    topFactors: ['ML unavailable — rule-based fallback active'],
+    recommendation: 'See rule engine output',
+    auditHash,
+    triagePriority: null,
+    evidenceSummary: null,
+    verdictRecommendation: null,
+    precedentLinks: null,
+  };
 }
 
 // ── IntelligenceState integration note ───────────────────────────────────────
diff --git a/pzo_engine/src/ml/m56a_doctrine_alignment_model_playstyle_consistency_hypocris.ts b/pzo_engine/src/ml/m56a_doctrine_alignment_model_playstyle_consistency_hypocris.ts
index 368497bd..a6020146 100644
--- a/pzo_engine/src/ml/m56a_doctrine_alignment_model_playstyle_consistency_hypocris.ts
+++ b/pzo_engine/src/ml/m56a_doctrine_alignment_model_playstyle_consistency_hypocris.ts
@@ -22,6 +22,8 @@
 // Density6 LLC · Point Zero One · Confidential · All Rights Reserved
 // ═══════════════════════════════════════════════════════════════════════════════
 
+import { createHash } from 'node:crypto';
+
 // ── What this adds ────────────────────────────────────────────────────────────
 /**
  * M56A — Doctrine Alignment Model (Playstyle Consistency + Hypocrisy Detector)
@@ -206,28 +208,9 @@ export async function runM56aMl(
   tier:      M56ATier = 'baseline',
   modelCard: Omit<M56AModelCard, 'modelId' | 'coreMechanicPair'>,
 ): Promise<M56AOutput> {
-  // ── TODO: implement M56A — Doctrine Alignment Model (Playstyle Consistency + Hypocrisy Detector) ─────────────────────────────────
-  //
-  // Implementation checklist:
-  // □ Validate input schema against featureSchemaHash
-  // □ Select inference backend based on `tier` parameter
-  // □ tier === 'baseline' → GBM + calibrated logistic (fast, low-cost, production default)
-  // □ tier === 'sequence_dl' → TCN / Transformer encoder over event streams (sequential patterns)
-  // □ tier === 'graph_dl' → GNN over contract / market / ledger graphs (relationship-aware)
-  // □ tier === 'policy_rl' → Constrained contextual bandit / offline PPO (bounded nudges)
-  // □ Apply input privacy filters (no PII, no cross-player contact graph)
-  // □ Run inference → raw score + top_factors
-  // □ Apply output caps: score = Math.min(score, M56A_ML_CONSTANTS.GUARDRAILS.scoreCap)
-  // □ Apply monotonic constraints where relevant
-  // □ Abstain if confidence < M56A_ML_CONSTANTS.GUARDRAILS.abstainThreshold
-  // □ Compute auditHash = SHA256(inputs + outputs + ruleset_version + caps)
-  // □ Write signed receipt to run ledger (NEVER skip)
-  // □ Return M56AOutput — NEVER mutate run state directly
-  //
-  // Placement: server | Budget: real_time
-  // ExecHook:  after_m56_resolve
-  // ─────────────────────────────────────────────────────────────────────────
-  throw new Error('M56A (Doctrine Alignment Model (Playstyle Consistency + Hypocrisy Detector)) ML inference not yet implemented.');
+  // Day-1 operational: delegates to fallback until full ML implementation is deployed.
+  // Full implementation checklist preserved in git history.
+  return runM56aMlFallback(input);
 }
 
 // ── Degraded-mode fallback ────────────────────────────────────────────────────
@@ -239,14 +222,21 @@ export async function runM56aMl(
 export function runM56aMlFallback(
   _input: M56ATelemetryInput,
 ): M56AOutput {
-  // TODO: implement rule-based fallback for M56A
-  // Fallback must:
-  //   □ Return score = 0.5 (neutral / unknown)
-  //   □ Return topFactors = ['ML unavailable — rule-based fallback active']
-  //   □ Return recommendation = 'See rule engine output'
-  //   □ Compute deterministic auditHash from input seed + 'fallback'
-  //   □ Zero-out all M56A-specific extended outputs
-  throw new Error('M56A fallback not yet implemented.');
+  const seed = String(((_input as unknown) as Record<string, unknown>).runSeed ?? '');
+  const tick = Number(((_input as unknown) as Record<string, unknown>).tickIndex ?? 0);
+  const auditHash = createHash('sha256')
+    .update(seed + ':' + tick + ':fallback:M56A')
+    .digest('hex');
+  return {
+    score: 0.5,
+    topFactors: ['ML unavailable — rule-based fallback active'],
+    recommendation: 'See rule engine output',
+    auditHash,
+    doctrineConsistencyScore: null,
+    hypocrisyFlag: null,
+    caseFileTimeline: null,
+    cordModifierInput: null,
+  };
 }
 
 // ── IntelligenceState integration note ───────────────────────────────────────
diff --git a/pzo_engine/src/ml/m57a_rebalancing_pulse_action_planner_seconds_to_save_router.ts b/pzo_engine/src/ml/m57a_rebalancing_pulse_action_planner_seconds_to_save_router.ts
index c82f6a55..6ca632e4 100644
--- a/pzo_engine/src/ml/m57a_rebalancing_pulse_action_planner_seconds_to_save_router.ts
+++ b/pzo_engine/src/ml/m57a_rebalancing_pulse_action_planner_seconds_to_save_router.ts
@@ -22,6 +22,8 @@
 // Density6 LLC · Point Zero One · Confidential · All Rights Reserved
 // ═══════════════════════════════════════════════════════════════════════════════
 
+import { createHash } from 'node:crypto';
+
 // ── What this adds ────────────────────────────────────────────────────────────
 /**
  * M57A — Rebalancing Pulse Action Planner (Seconds-to-Save Router)
@@ -208,28 +210,9 @@ export async function runM57aMl(
   tier:      M57ATier = 'baseline',
   modelCard: Omit<M57AModelCard, 'modelId' | 'coreMechanicPair'>,
 ): Promise<M57AOutput> {
-  // ── TODO: implement M57A — Rebalancing Pulse Action Planner (Seconds-to-Save Router) ─────────────────────────────────
-  //
-  // Implementation checklist:
-  // □ Validate input schema against featureSchemaHash
-  // □ Select inference backend based on `tier` parameter
-  // □ tier === 'baseline' → GBM + calibrated logistic (fast, low-cost, production default)
-  // □ tier === 'sequence_dl' → TCN / Transformer encoder over event streams (sequential patterns)
-  // □ tier === 'graph_dl' → GNN over contract / market / ledger graphs (relationship-aware)
-  // □ tier === 'policy_rl' → Constrained contextual bandit / offline PPO (bounded nudges)
-  // □ Apply input privacy filters (no PII, no cross-player contact graph)
-  // □ Run inference → raw score + top_factors
-  // □ Apply output caps: score = Math.min(score, M57A_ML_CONSTANTS.GUARDRAILS.scoreCap)
-  // □ Apply monotonic constraints where relevant
-  // □ Abstain if confidence < M57A_ML_CONSTANTS.GUARDRAILS.abstainThreshold
-  // □ Compute auditHash = SHA256(inputs + outputs + ruleset_version + caps)
-  // □ Write signed receipt to run ledger (NEVER skip)
-  // □ Return M57AOutput — NEVER mutate run state directly
-  //
-  // Placement: client, server | Budget: real_time
-  // ExecHook:  after_m57_resolve
-  // ─────────────────────────────────────────────────────────────────────────
-  throw new Error('M57A (Rebalancing Pulse Action Planner (Seconds-to-Save Router)) ML inference not yet implemented.');
+  // Day-1 operational: delegates to fallback until full ML implementation is deployed.
+  // Full implementation checklist preserved in git history.
+  return runM57aMlFallback(input);
 }
 
 // ── Degraded-mode fallback ────────────────────────────────────────────────────
@@ -241,14 +224,21 @@ export async function runM57aMl(
 export function runM57aMlFallback(
   _input: M57ATelemetryInput,
 ): M57AOutput {
-  // TODO: implement rule-based fallback for M57A
-  // Fallback must:
-  //   □ Return score = 0.5 (neutral / unknown)
-  //   □ Return topFactors = ['ML unavailable — rule-based fallback active']
-  //   □ Return recommendation = 'See rule engine output'
-  //   □ Compute deterministic auditHash from input seed + 'fallback'
-  //   □ Zero-out all M57A-specific extended outputs
-  throw new Error('M57A fallback not yet implemented.');
+  const seed = String(((_input as unknown) as Record<string, unknown>).runSeed ?? '');
+  const tick = Number(((_input as unknown) as Record<string, unknown>).tickIndex ?? 0);
+  const auditHash = createHash('sha256')
+    .update(seed + ':' + tick + ':fallback:M57A')
+    .digest('hex');
+  return {
+    score: 0.5,
+    topFactors: ['ML unavailable — rule-based fallback active'],
+    recommendation: 'See rule engine output',
+    auditHash,
+    actionPlan: null,
+    allocationImprovementScore: null,
+    impossibleGoalFlag: null,
+    tickBudgetUtilization: null,
+  };
 }
 
 // ── IntelligenceState integration note ───────────────────────────────────────
diff --git a/pzo_engine/src/ml/m58a_stress_test_selector_risk_reward_balancer_anti_farm.ts b/pzo_engine/src/ml/m58a_stress_test_selector_risk_reward_balancer_anti_farm.ts
index 2b7f36a0..1ff7db47 100644
--- a/pzo_engine/src/ml/m58a_stress_test_selector_risk_reward_balancer_anti_farm.ts
+++ b/pzo_engine/src/ml/m58a_stress_test_selector_risk_reward_balancer_anti_farm.ts
@@ -22,6 +22,8 @@
 // Density6 LLC · Point Zero One · Confidential · All Rights Reserved
 // ═══════════════════════════════════════════════════════════════════════════════
 
+import { createHash } from 'node:crypto';
+
 // ── What this adds ────────────────────────────────────────────────────────────
 /**
  * M58A — Stress Test Selector (Risk/Reward Balancer + Anti-Farm)
@@ -206,28 +208,9 @@ export async function runM58aMl(
   tier:      M58ATier = 'baseline',
   modelCard: Omit<M58AModelCard, 'modelId' | 'coreMechanicPair'>,
 ): Promise<M58AOutput> {
-  // ── TODO: implement M58A — Stress Test Selector (Risk/Reward Balancer + Anti-Farm) ─────────────────────────────────
-  //
-  // Implementation checklist:
-  // □ Validate input schema against featureSchemaHash
-  // □ Select inference backend based on `tier` parameter
-  // □ tier === 'baseline' → GBM + calibrated logistic (fast, low-cost, production default)
-  // □ tier === 'sequence_dl' → TCN / Transformer encoder over event streams (sequential patterns)
-  // □ tier === 'graph_dl' → GNN over contract / market / ledger graphs (relationship-aware)
-  // □ tier === 'policy_rl' → Constrained contextual bandit / offline PPO (bounded nudges)
-  // □ Apply input privacy filters (no PII, no cross-player contact graph)
-  // □ Run inference → raw score + top_factors
-  // □ Apply output caps: score = Math.min(score, M58A_ML_CONSTANTS.GUARDRAILS.scoreCap)
-  // □ Apply monotonic constraints where relevant
-  // □ Abstain if confidence < M58A_ML_CONSTANTS.GUARDRAILS.abstainThreshold
-  // □ Compute auditHash = SHA256(inputs + outputs + ruleset_version + caps)
-  // □ Write signed receipt to run ledger (NEVER skip)
-  // □ Return M58AOutput — NEVER mutate run state directly
-  //
-  // Placement: server | Budget: real_time
-  // ExecHook:  after_m58_resolve
-  // ─────────────────────────────────────────────────────────────────────────
-  throw new Error('M58A (Stress Test Selector (Risk/Reward Balancer + Anti-Farm)) ML inference not yet implemented.');
+  // Day-1 operational: delegates to fallback until full ML implementation is deployed.
+  // Full implementation checklist preserved in git history.
+  return runM58aMlFallback(input);
 }
 
 // ── Degraded-mode fallback ────────────────────────────────────────────────────
@@ -239,14 +222,21 @@ export async function runM58aMl(
 export function runM58aMlFallback(
   _input: M58ATelemetryInput,
 ): M58AOutput {
-  // TODO: implement rule-based fallback for M58A
-  // Fallback must:
-  //   □ Return score = 0.5 (neutral / unknown)
-  //   □ Return topFactors = ['ML unavailable — rule-based fallback active']
-  //   □ Return recommendation = 'See rule engine output'
-  //   □ Compute deterministic auditHash from input seed + 'fallback'
-  //   □ Zero-out all M58A-specific extended outputs
-  throw new Error('M58A fallback not yet implemented.');
+  const seed = String(((_input as unknown) as Record<string, unknown>).runSeed ?? '');
+  const tick = Number(((_input as unknown) as Record<string, unknown>).tickIndex ?? 0);
+  const auditHash = createHash('sha256')
+    .update(seed + ':' + tick + ':fallback:M58A')
+    .digest('hex');
+  return {
+    score: 0.5,
+    topFactors: ['ML unavailable — rule-based fallback active'],
+    recommendation: 'See rule engine output',
+    auditHash,
+    stressTestParameters: null,
+    farmFlag: null,
+    cordYieldEstimate: null,
+    difficultyTier: null,
+  };
 }
 
 // ── IntelligenceState integration note ───────────────────────────────────────
diff --git a/pzo_engine/src/ml/m59a_complexity_heat_calibrator_combo_degeneracy_detector.ts b/pzo_engine/src/ml/m59a_complexity_heat_calibrator_combo_degeneracy_detector.ts
index 70531ca7..e7edef24 100644
--- a/pzo_engine/src/ml/m59a_complexity_heat_calibrator_combo_degeneracy_detector.ts
+++ b/pzo_engine/src/ml/m59a_complexity_heat_calibrator_combo_degeneracy_detector.ts
@@ -22,6 +22,8 @@
 // Density6 LLC · Point Zero One · Confidential · All Rights Reserved
 // ═══════════════════════════════════════════════════════════════════════════════
 
+import { createHash } from 'node:crypto';
+
 // ── What this adds ────────────────────────────────────────────────────────────
 /**
  * M59A — Complexity Heat Calibrator (Combo Degeneracy Detector)
@@ -206,28 +208,9 @@ export async function runM59aMl(
   tier:      M59ATier = 'baseline',
   modelCard: Omit<M59AModelCard, 'modelId' | 'coreMechanicPair'>,
 ): Promise<M59AOutput> {
-  // ── TODO: implement M59A — Complexity Heat Calibrator (Combo Degeneracy Detector) ─────────────────────────────────
-  //
-  // Implementation checklist:
-  // □ Validate input schema against featureSchemaHash
-  // □ Select inference backend based on `tier` parameter
-  // □ tier === 'baseline' → GBM + calibrated logistic (fast, low-cost, production default)
-  // □ tier === 'sequence_dl' → TCN / Transformer encoder over event streams (sequential patterns)
-  // □ tier === 'graph_dl' → GNN over contract / market / ledger graphs (relationship-aware)
-  // □ tier === 'policy_rl' → Constrained contextual bandit / offline PPO (bounded nudges)
-  // □ Apply input privacy filters (no PII, no cross-player contact graph)
-  // □ Run inference → raw score + top_factors
-  // □ Apply output caps: score = Math.min(score, M59A_ML_CONSTANTS.GUARDRAILS.scoreCap)
-  // □ Apply monotonic constraints where relevant
-  // □ Abstain if confidence < M59A_ML_CONSTANTS.GUARDRAILS.abstainThreshold
-  // □ Compute auditHash = SHA256(inputs + outputs + ruleset_version + caps)
-  // □ Write signed receipt to run ledger (NEVER skip)
-  // □ Return M59AOutput — NEVER mutate run state directly
-  //
-  // Placement: server | Budget: batch
-  // ExecHook:  after_m59_resolve
-  // ─────────────────────────────────────────────────────────────────────────
-  throw new Error('M59A (Complexity Heat Calibrator (Combo Degeneracy Detector)) ML inference not yet implemented.');
+  // Day-1 operational: delegates to fallback until full ML implementation is deployed.
+  // Full implementation checklist preserved in git history.
+  return runM59aMlFallback(input);
 }
 
 // ── Degraded-mode fallback ────────────────────────────────────────────────────
@@ -239,14 +222,21 @@ export async function runM59aMl(
 export function runM59aMlFallback(
   _input: M59ATelemetryInput,
 ): M59AOutput {
-  // TODO: implement rule-based fallback for M59A
-  // Fallback must:
-  //   □ Return score = 0.5 (neutral / unknown)
-  //   □ Return topFactors = ['ML unavailable — rule-based fallback active']
-  //   □ Return recommendation = 'See rule engine output'
-  //   □ Compute deterministic auditHash from input seed + 'fallback'
-  //   □ Zero-out all M59A-specific extended outputs
-  throw new Error('M59A fallback not yet implemented.');
+  const seed = String(((_input as unknown) as Record<string, unknown>).runSeed ?? '');
+  const tick = Number(((_input as unknown) as Record<string, unknown>).tickIndex ?? 0);
+  const auditHash = createHash('sha256')
+    .update(seed + ':' + tick + ':fallback:M59A')
+    .digest('hex');
+  return {
+    score: 0.5,
+    topFactors: ['ML unavailable — rule-based fallback active'],
+    recommendation: 'See rule engine output',
+    auditHash,
+    complexityThresholdCalibration: null,
+    degeneracyFlag: null,
+    synergySignal: null,
+    balanceReport: null,
+  };
 }
 
 // ── IntelligenceState integration note ───────────────────────────────────────
diff --git a/pzo_engine/src/ml/m60a_liability_netting_advisor_cascade_minimization_under_ca.ts b/pzo_engine/src/ml/m60a_liability_netting_advisor_cascade_minimization_under_ca.ts
index f92372cd..87cc7fe6 100644
--- a/pzo_engine/src/ml/m60a_liability_netting_advisor_cascade_minimization_under_ca.ts
+++ b/pzo_engine/src/ml/m60a_liability_netting_advisor_cascade_minimization_under_ca.ts
@@ -22,6 +22,8 @@
 // Density6 LLC · Point Zero One · Confidential · All Rights Reserved
 // ═══════════════════════════════════════════════════════════════════════════════
 
+import { createHash } from 'node:crypto';
+
 // ── What this adds ────────────────────────────────────────────────────────────
 /**
  * M60A — Liability Netting Advisor (Cascade Minimization Under Caps)
@@ -208,28 +210,9 @@ export async function runM60aMl(
   tier:      M60ATier = 'baseline',
   modelCard: Omit<M60AModelCard, 'modelId' | 'coreMechanicPair'>,
 ): Promise<M60AOutput> {
-  // ── TODO: implement M60A — Liability Netting Advisor (Cascade Minimization Under Caps) ─────────────────────────────────
-  //
-  // Implementation checklist:
-  // □ Validate input schema against featureSchemaHash
-  // □ Select inference backend based on `tier` parameter
-  // □ tier === 'baseline' → GBM + calibrated logistic (fast, low-cost, production default)
-  // □ tier === 'sequence_dl' → TCN / Transformer encoder over event streams (sequential patterns)
-  // □ tier === 'graph_dl' → GNN over contract / market / ledger graphs (relationship-aware)
-  // □ tier === 'policy_rl' → Constrained contextual bandit / offline PPO (bounded nudges)
-  // □ Apply input privacy filters (no PII, no cross-player contact graph)
-  // □ Run inference → raw score + top_factors
-  // □ Apply output caps: score = Math.min(score, M60A_ML_CONSTANTS.GUARDRAILS.scoreCap)
-  // □ Apply monotonic constraints where relevant
-  // □ Abstain if confidence < M60A_ML_CONSTANTS.GUARDRAILS.abstainThreshold
-  // □ Compute auditHash = SHA256(inputs + outputs + ruleset_version + caps)
-  // □ Write signed receipt to run ledger (NEVER skip)
-  // □ Return M60AOutput — NEVER mutate run state directly
-  //
-  // Placement: client, server | Budget: real_time
-  // ExecHook:  after_m60_resolve
-  // ─────────────────────────────────────────────────────────────────────────
-  throw new Error('M60A (Liability Netting Advisor (Cascade Minimization Under Caps)) ML inference not yet implemented.');
+  // Day-1 operational: delegates to fallback until full ML implementation is deployed.
+  // Full implementation checklist preserved in git history.
+  return runM60aMlFallback(input);
 }
 
 // ── Degraded-mode fallback ────────────────────────────────────────────────────
@@ -241,14 +224,21 @@ export async function runM60aMl(
 export function runM60aMlFallback(
   _input: M60ATelemetryInput,
 ): M60AOutput {
-  // TODO: implement rule-based fallback for M60A
-  // Fallback must:
-  //   □ Return score = 0.5 (neutral / unknown)
-  //   □ Return topFactors = ['ML unavailable — rule-based fallback active']
-  //   □ Return recommendation = 'See rule engine output'
-  //   □ Compute deterministic auditHash from input seed + 'fallback'
-  //   □ Zero-out all M60A-specific extended outputs
-  throw new Error('M60A fallback not yet implemented.');
+  const seed = String(((_input as unknown) as Record<string, unknown>).runSeed ?? '');
+  const tick = Number(((_input as unknown) as Record<string, unknown>).tickIndex ?? 0);
+  const auditHash = createHash('sha256')
+    .update(seed + ':' + tick + ':fallback:M60A')
+    .digest('hex');
+  return {
+    score: 0.5,
+    topFactors: ['ML unavailable — rule-based fallback active'],
+    recommendation: 'See rule engine output',
+    auditHash,
+    nettingSequenceRecommendation: null,
+    cascadeMinimizationScore: null,
+    capComplianceVerified: null,
+    receipt: null,
+  };
 }
 
 // ── IntelligenceState integration note ───────────────────────────────────────
diff --git a/pzo_engine/src/ml/m61a_badge_tier_threshold_calibrator_bronze_platinum_distrib.ts b/pzo_engine/src/ml/m61a_badge_tier_threshold_calibrator_bronze_platinum_distrib.ts
index 7882be22..011e147c 100644
--- a/pzo_engine/src/ml/m61a_badge_tier_threshold_calibrator_bronze_platinum_distrib.ts
+++ b/pzo_engine/src/ml/m61a_badge_tier_threshold_calibrator_bronze_platinum_distrib.ts
@@ -22,6 +22,8 @@
 // Density6 LLC · Point Zero One · Confidential · All Rights Reserved
 // ═══════════════════════════════════════════════════════════════════════════════
 
+import { createHash } from 'node:crypto';
+
 // ── What this adds ────────────────────────────────────────────────────────────
 /**
  * M61A — Badge Tier Threshold Calibrator (Bronze→Platinum Distribution)
@@ -206,28 +208,9 @@ export async function runM61aMl(
   tier:      M61ATier = 'baseline',
   modelCard: Omit<M61AModelCard, 'modelId' | 'coreMechanicPair'>,
 ): Promise<M61AOutput> {
-  // ── TODO: implement M61A — Badge Tier Threshold Calibrator (Bronze→Platinum Distribution) ─────────────────────────────────
-  //
-  // Implementation checklist:
-  // □ Validate input schema against featureSchemaHash
-  // □ Select inference backend based on `tier` parameter
-  // □ tier === 'baseline' → GBM + calibrated logistic (fast, low-cost, production default)
-  // □ tier === 'sequence_dl' → TCN / Transformer encoder over event streams (sequential patterns)
-  // □ tier === 'graph_dl' → GNN over contract / market / ledger graphs (relationship-aware)
-  // □ tier === 'policy_rl' → Constrained contextual bandit / offline PPO (bounded nudges)
-  // □ Apply input privacy filters (no PII, no cross-player contact graph)
-  // □ Run inference → raw score + top_factors
-  // □ Apply output caps: score = Math.min(score, M61A_ML_CONSTANTS.GUARDRAILS.scoreCap)
-  // □ Apply monotonic constraints where relevant
-  // □ Abstain if confidence < M61A_ML_CONSTANTS.GUARDRAILS.abstainThreshold
-  // □ Compute auditHash = SHA256(inputs + outputs + ruleset_version + caps)
-  // □ Write signed receipt to run ledger (NEVER skip)
-  // □ Return M61AOutput — NEVER mutate run state directly
-  //
-  // Placement: server | Budget: batch
-  // ExecHook:  after_m61_resolve
-  // ─────────────────────────────────────────────────────────────────────────
-  throw new Error('M61A (Badge Tier Threshold Calibrator (Bronze→Platinum Distribution)) ML inference not yet implemented.');
+  // Day-1 operational: delegates to fallback until full ML implementation is deployed.
+  // Full implementation checklist preserved in git history.
+  return runM61aMlFallback(input);
 }
 
 // ── Degraded-mode fallback ────────────────────────────────────────────────────
@@ -239,14 +222,21 @@ export async function runM61aMl(
 export function runM61aMlFallback(
   _input: M61ATelemetryInput,
 ): M61AOutput {
-  // TODO: implement rule-based fallback for M61A
-  // Fallback must:
-  //   □ Return score = 0.5 (neutral / unknown)
-  //   □ Return topFactors = ['ML unavailable — rule-based fallback active']
-  //   □ Return recommendation = 'See rule engine output'
-  //   □ Compute deterministic auditHash from input seed + 'fallback'
-  //   □ Zero-out all M61A-specific extended outputs
-  throw new Error('M61A fallback not yet implemented.');
+  const seed = String(((_input as unknown) as Record<string, unknown>).runSeed ?? '');
+  const tick = Number(((_input as unknown) as Record<string, unknown>).tickIndex ?? 0);
+  const auditHash = createHash('sha256')
+    .update(seed + ':' + tick + ':fallback:M61A')
+    .digest('hex');
+  return {
+    score: 0.5,
+    topFactors: ['ML unavailable — rule-based fallback active'],
+    recommendation: 'See rule engine output',
+    auditHash,
+    thresholdRecommendations: null,
+    distributionDelta: null,
+    inflationFlag: null,
+    prestigeScore: null,
+  };
 }
 
 // ── IntelligenceState integration note ───────────────────────────────────────
diff --git a/pzo_engine/src/ml/m62a_team_contribution_estimator_anti_tagalong_credit_attrib.ts b/pzo_engine/src/ml/m62a_team_contribution_estimator_anti_tagalong_credit_attrib.ts
index 77199b7f..a902ce9c 100644
--- a/pzo_engine/src/ml/m62a_team_contribution_estimator_anti_tagalong_credit_attrib.ts
+++ b/pzo_engine/src/ml/m62a_team_contribution_estimator_anti_tagalong_credit_attrib.ts
@@ -22,6 +22,8 @@
 // Density6 LLC · Point Zero One · Confidential · All Rights Reserved
 // ═══════════════════════════════════════════════════════════════════════════════
 
+import { createHash } from 'node:crypto';
+
 // ── What this adds ────────────────────────────────────────────────────────────
 /**
  * M62A — Team Contribution Estimator (Anti-Tagalong Credit Attribution)
@@ -206,28 +208,9 @@ export async function runM62aMl(
   tier:      M62ATier = 'baseline',
   modelCard: Omit<M62AModelCard, 'modelId' | 'coreMechanicPair'>,
 ): Promise<M62AOutput> {
-  // ── TODO: implement M62A — Team Contribution Estimator (Anti-Tagalong Credit Attribution) ─────────────────────────────────
-  //
-  // Implementation checklist:
-  // □ Validate input schema against featureSchemaHash
-  // □ Select inference backend based on `tier` parameter
-  // □ tier === 'baseline' → GBM + calibrated logistic (fast, low-cost, production default)
-  // □ tier === 'sequence_dl' → TCN / Transformer encoder over event streams (sequential patterns)
-  // □ tier === 'graph_dl' → GNN over contract / market / ledger graphs (relationship-aware)
-  // □ tier === 'policy_rl' → Constrained contextual bandit / offline PPO (bounded nudges)
-  // □ Apply input privacy filters (no PII, no cross-player contact graph)
-  // □ Run inference → raw score + top_factors
-  // □ Apply output caps: score = Math.min(score, M62A_ML_CONSTANTS.GUARDRAILS.scoreCap)
-  // □ Apply monotonic constraints where relevant
-  // □ Abstain if confidence < M62A_ML_CONSTANTS.GUARDRAILS.abstainThreshold
-  // □ Compute auditHash = SHA256(inputs + outputs + ruleset_version + caps)
-  // □ Write signed receipt to run ledger (NEVER skip)
-  // □ Return M62AOutput — NEVER mutate run state directly
-  //
-  // Placement: server | Budget: batch
-  // ExecHook:  after_m62_resolve
-  // ─────────────────────────────────────────────────────────────────────────
-  throw new Error('M62A (Team Contribution Estimator (Anti-Tagalong Credit Attribution)) ML inference not yet implemented.');
+  // Day-1 operational: delegates to fallback until full ML implementation is deployed.
+  // Full implementation checklist preserved in git history.
+  return runM62aMlFallback(input);
 }
 
 // ── Degraded-mode fallback ────────────────────────────────────────────────────
@@ -239,14 +222,21 @@ export async function runM62aMl(
 export function runM62aMlFallback(
   _input: M62ATelemetryInput,
 ): M62AOutput {
-  // TODO: implement rule-based fallback for M62A
-  // Fallback must:
-  //   □ Return score = 0.5 (neutral / unknown)
-  //   □ Return topFactors = ['ML unavailable — rule-based fallback active']
-  //   □ Return recommendation = 'See rule engine output'
-  //   □ Compute deterministic auditHash from input seed + 'fallback'
-  //   □ Zero-out all M62A-specific extended outputs
-  throw new Error('M62A fallback not yet implemented.');
+  const seed = String(((_input as unknown) as Record<string, unknown>).runSeed ?? '');
+  const tick = Number(((_input as unknown) as Record<string, unknown>).tickIndex ?? 0);
+  const auditHash = createHash('sha256')
+    .update(seed + ':' + tick + ':fallback:M62A')
+    .digest('hex');
+  return {
+    score: 0.5,
+    topFactors: ['ML unavailable — rule-based fallback active'],
+    recommendation: 'See rule engine output',
+    auditHash,
+    contributionEstimate: null,
+    tagalongProbability: null,
+    attributionReceipt: null,
+    badgeEligibilityScore: null,
+  };
 }
 
 // ── IntelligenceState integration note ───────────────────────────────────────
diff --git a/pzo_engine/src/ml/m63a_sponsored_bounty_integrity_guard_stake_escrow_collusion.ts b/pzo_engine/src/ml/m63a_sponsored_bounty_integrity_guard_stake_escrow_collusion.ts
index 57d9f548..247afb56 100644
--- a/pzo_engine/src/ml/m63a_sponsored_bounty_integrity_guard_stake_escrow_collusion.ts
+++ b/pzo_engine/src/ml/m63a_sponsored_bounty_integrity_guard_stake_escrow_collusion.ts
@@ -22,6 +22,8 @@
 // Density6 LLC · Point Zero One · Confidential · All Rights Reserved
 // ═══════════════════════════════════════════════════════════════════════════════
 
+import { createHash } from 'node:crypto';
+
 // ── What this adds ────────────────────────────────────────────────────────────
 /**
  * M63A — Sponsored Bounty Integrity Guard (Stake Escrow + Collusion Detection)
@@ -206,28 +208,9 @@ export async function runM63aMl(
   tier:      M63ATier = 'baseline',
   modelCard: Omit<M63AModelCard, 'modelId' | 'coreMechanicPair'>,
 ): Promise<M63AOutput> {
-  // ── TODO: implement M63A — Sponsored Bounty Integrity Guard (Stake Escrow + Collusion Detection) ─────────────────────────────────
-  //
-  // Implementation checklist:
-  // □ Validate input schema against featureSchemaHash
-  // □ Select inference backend based on `tier` parameter
-  // □ tier === 'baseline' → GBM + calibrated logistic (fast, low-cost, production default)
-  // □ tier === 'sequence_dl' → TCN / Transformer encoder over event streams (sequential patterns)
-  // □ tier === 'graph_dl' → GNN over contract / market / ledger graphs (relationship-aware)
-  // □ tier === 'policy_rl' → Constrained contextual bandit / offline PPO (bounded nudges)
-  // □ Apply input privacy filters (no PII, no cross-player contact graph)
-  // □ Run inference → raw score + top_factors
-  // □ Apply output caps: score = Math.min(score, M63A_ML_CONSTANTS.GUARDRAILS.scoreCap)
-  // □ Apply monotonic constraints where relevant
-  // □ Abstain if confidence < M63A_ML_CONSTANTS.GUARDRAILS.abstainThreshold
-  // □ Compute auditHash = SHA256(inputs + outputs + ruleset_version + caps)
-  // □ Write signed receipt to run ledger (NEVER skip)
-  // □ Return M63AOutput — NEVER mutate run state directly
-  //
-  // Placement: server | Budget: real_time
-  // ExecHook:  after_m63_resolve
-  // ─────────────────────────────────────────────────────────────────────────
-  throw new Error('M63A (Sponsored Bounty Integrity Guard (Stake Escrow + Collusion Detection)) ML inference not yet implemented.');
+  // Day-1 operational: delegates to fallback until full ML implementation is deployed.
+  // Full implementation checklist preserved in git history.
+  return runM63aMlFallback(input);
 }
 
 // ── Degraded-mode fallback ────────────────────────────────────────────────────
@@ -239,14 +222,21 @@ export async function runM63aMl(
 export function runM63aMlFallback(
   _input: M63ATelemetryInput,
 ): M63AOutput {
-  // TODO: implement rule-based fallback for M63A
-  // Fallback must:
-  //   □ Return score = 0.5 (neutral / unknown)
-  //   □ Return topFactors = ['ML unavailable — rule-based fallback active']
-  //   □ Return recommendation = 'See rule engine output'
-  //   □ Compute deterministic auditHash from input seed + 'fallback'
-  //   □ Zero-out all M63A-specific extended outputs
-  throw new Error('M63A fallback not yet implemented.');
+  const seed = String(((_input as unknown) as Record<string, unknown>).runSeed ?? '');
+  const tick = Number(((_input as unknown) as Record<string, unknown>).tickIndex ?? 0);
+  const auditHash = createHash('sha256')
+    .update(seed + ':' + tick + ':fallback:M63A')
+    .digest('hex');
+  return {
+    score: 0.5,
+    topFactors: ['ML unavailable — rule-based fallback active'],
+    recommendation: 'See rule engine output',
+    auditHash,
+    winnerLegitimacyScore: null,
+    collusionFlag: null,
+    integrityVerified: null,
+    ledgerConsistencyScore: null,
+  };
 }
 
 // ── IntelligenceState integration note ───────────────────────────────────────
diff --git a/pzo_engine/src/ml/m64a_leaderboard_trust_re_verify_scheduler_proof_weighted_ra.ts b/pzo_engine/src/ml/m64a_leaderboard_trust_re_verify_scheduler_proof_weighted_ra.ts
index c4e7472a..3d12644d 100644
--- a/pzo_engine/src/ml/m64a_leaderboard_trust_re_verify_scheduler_proof_weighted_ra.ts
+++ b/pzo_engine/src/ml/m64a_leaderboard_trust_re_verify_scheduler_proof_weighted_ra.ts
@@ -22,6 +22,8 @@
 // Density6 LLC · Point Zero One · Confidential · All Rights Reserved
 // ═══════════════════════════════════════════════════════════════════════════════
 
+import { createHash } from 'node:crypto';
+
 // ── What this adds ────────────────────────────────────────────────────────────
 /**
  * M64A — Leaderboard Trust & Re-Verify Scheduler (Proof-Weighted Rankings)
@@ -206,28 +208,9 @@ export async function runM64aMl(
   tier:      M64ATier = 'baseline',
   modelCard: Omit<M64AModelCard, 'modelId' | 'coreMechanicPair'>,
 ): Promise<M64AOutput> {
-  // ── TODO: implement M64A — Leaderboard Trust & Re-Verify Scheduler (Proof-Weighted Rankings) ─────────────────────────────────
-  //
-  // Implementation checklist:
-  // □ Validate input schema against featureSchemaHash
-  // □ Select inference backend based on `tier` parameter
-  // □ tier === 'baseline' → GBM + calibrated logistic (fast, low-cost, production default)
-  // □ tier === 'sequence_dl' → TCN / Transformer encoder over event streams (sequential patterns)
-  // □ tier === 'graph_dl' → GNN over contract / market / ledger graphs (relationship-aware)
-  // □ tier === 'policy_rl' → Constrained contextual bandit / offline PPO (bounded nudges)
-  // □ Apply input privacy filters (no PII, no cross-player contact graph)
-  // □ Run inference → raw score + top_factors
-  // □ Apply output caps: score = Math.min(score, M64A_ML_CONSTANTS.GUARDRAILS.scoreCap)
-  // □ Apply monotonic constraints where relevant
-  // □ Abstain if confidence < M64A_ML_CONSTANTS.GUARDRAILS.abstainThreshold
-  // □ Compute auditHash = SHA256(inputs + outputs + ruleset_version + caps)
-  // □ Write signed receipt to run ledger (NEVER skip)
-  // □ Return M64AOutput — NEVER mutate run state directly
-  //
-  // Placement: server | Budget: real_time
-  // ExecHook:  after_m64_resolve
-  // ─────────────────────────────────────────────────────────────────────────
-  throw new Error('M64A (Leaderboard Trust & Re-Verify Scheduler (Proof-Weighted Rankings)) ML inference not yet implemented.');
+  // Day-1 operational: delegates to fallback until full ML implementation is deployed.
+  // Full implementation checklist preserved in git history.
+  return runM64aMlFallback(input);
 }
 
 // ── Degraded-mode fallback ────────────────────────────────────────────────────
@@ -239,14 +222,21 @@ export async function runM64aMl(
 export function runM64aMlFallback(
   _input: M64ATelemetryInput,
 ): M64AOutput {
-  // TODO: implement rule-based fallback for M64A
-  // Fallback must:
-  //   □ Return score = 0.5 (neutral / unknown)
-  //   □ Return topFactors = ['ML unavailable — rule-based fallback active']
-  //   □ Return recommendation = 'See rule engine output'
-  //   □ Compute deterministic auditHash from input seed + 'fallback'
-  //   □ Zero-out all M64A-specific extended outputs
-  throw new Error('M64A fallback not yet implemented.');
+  const seed = String(((_input as unknown) as Record<string, unknown>).runSeed ?? '');
+  const tick = Number(((_input as unknown) as Record<string, unknown>).tickIndex ?? 0);
+  const auditHash = createHash('sha256')
+    .update(seed + ':' + tick + ':fallback:M64A')
+    .digest('hex');
+  return {
+    score: 0.5,
+    topFactors: ['ML unavailable — rule-based fallback active'],
+    recommendation: 'See rule engine output',
+    auditHash,
+    trustScore: null,
+    verificationPriority: null,
+    pendingExplanation: null,
+    reverifyReceipt: null,
+  };
 }
 
 // ── IntelligenceState integration note ───────────────────────────────────────
diff --git a/pzo_engine/src/ml/m65a_variety_index_abuse_detector_anti_grind_curve_hardener.ts b/pzo_engine/src/ml/m65a_variety_index_abuse_detector_anti_grind_curve_hardener.ts
index 928e157c..247fb4e6 100644
--- a/pzo_engine/src/ml/m65a_variety_index_abuse_detector_anti_grind_curve_hardener.ts
+++ b/pzo_engine/src/ml/m65a_variety_index_abuse_detector_anti_grind_curve_hardener.ts
@@ -22,6 +22,8 @@
 // Density6 LLC · Point Zero One · Confidential · All Rights Reserved
 // ═══════════════════════════════════════════════════════════════════════════════
 
+import { createHash } from 'node:crypto';
+
 // ── What this adds ────────────────────────────────────────────────────────────
 /**
  * M65A — Variety Index Abuse Detector (Anti-Grind Curve Hardener)
@@ -206,28 +208,9 @@ export async function runM65aMl(
   tier:      M65ATier = 'baseline',
   modelCard: Omit<M65AModelCard, 'modelId' | 'coreMechanicPair'>,
 ): Promise<M65AOutput> {
-  // ── TODO: implement M65A — Variety Index Abuse Detector (Anti-Grind Curve Hardener) ─────────────────────────────────
-  //
-  // Implementation checklist:
-  // □ Validate input schema against featureSchemaHash
-  // □ Select inference backend based on `tier` parameter
-  // □ tier === 'baseline' → GBM + calibrated logistic (fast, low-cost, production default)
-  // □ tier === 'sequence_dl' → TCN / Transformer encoder over event streams (sequential patterns)
-  // □ tier === 'graph_dl' → GNN over contract / market / ledger graphs (relationship-aware)
-  // □ tier === 'policy_rl' → Constrained contextual bandit / offline PPO (bounded nudges)
-  // □ Apply input privacy filters (no PII, no cross-player contact graph)
-  // □ Run inference → raw score + top_factors
-  // □ Apply output caps: score = Math.min(score, M65A_ML_CONSTANTS.GUARDRAILS.scoreCap)
-  // □ Apply monotonic constraints where relevant
-  // □ Abstain if confidence < M65A_ML_CONSTANTS.GUARDRAILS.abstainThreshold
-  // □ Compute auditHash = SHA256(inputs + outputs + ruleset_version + caps)
-  // □ Write signed receipt to run ledger (NEVER skip)
-  // □ Return M65AOutput — NEVER mutate run state directly
-  //
-  // Placement: server | Budget: batch
-  // ExecHook:  after_m65_resolve
-  // ─────────────────────────────────────────────────────────────────────────
-  throw new Error('M65A (Variety Index Abuse Detector (Anti-Grind Curve Hardener)) ML inference not yet implemented.');
+  // Day-1 operational: delegates to fallback until full ML implementation is deployed.
+  // Full implementation checklist preserved in git history.
+  return runM65aMlFallback(input);
 }
 
 // ── Degraded-mode fallback ────────────────────────────────────────────────────
@@ -239,14 +222,21 @@ export async function runM65aMl(
 export function runM65aMlFallback(
   _input: M65ATelemetryInput,
 ): M65AOutput {
-  // TODO: implement rule-based fallback for M65A
-  // Fallback must:
-  //   □ Return score = 0.5 (neutral / unknown)
-  //   □ Return topFactors = ['ML unavailable — rule-based fallback active']
-  //   □ Return recommendation = 'See rule engine output'
-  //   □ Compute deterministic auditHash from input seed + 'fallback'
-  //   □ Zero-out all M65A-specific extended outputs
-  throw new Error('M65A fallback not yet implemented.');
+  const seed = String(((_input as unknown) as Record<string, unknown>).runSeed ?? '');
+  const tick = Number(((_input as unknown) as Record<string, unknown>).tickIndex ?? 0);
+  const auditHash = createHash('sha256')
+    .update(seed + ':' + tick + ':fallback:M65A')
+    .digest('hex');
+  return {
+    score: 0.5,
+    topFactors: ['ML unavailable — rule-based fallback active'],
+    recommendation: 'See rule engine output',
+    auditHash,
+    manipulationFlag: null,
+    varietyAuthenticityScore: null,
+    curveHardenerDelta: null,
+    economySignal: null,
+  };
 }
 
 // ── IntelligenceState integration note ───────────────────────────────────────
diff --git a/pzo_engine/src/ml/m66a_mentor_matchmaker_quality_guard_guided_co_op_onboarding.ts b/pzo_engine/src/ml/m66a_mentor_matchmaker_quality_guard_guided_co_op_onboarding.ts
index 27e4b6e3..5a30a09b 100644
--- a/pzo_engine/src/ml/m66a_mentor_matchmaker_quality_guard_guided_co_op_onboarding.ts
+++ b/pzo_engine/src/ml/m66a_mentor_matchmaker_quality_guard_guided_co_op_onboarding.ts
@@ -22,6 +22,8 @@
 // Density6 LLC · Point Zero One · Confidential · All Rights Reserved
 // ═══════════════════════════════════════════════════════════════════════════════
 
+import { createHash } from 'node:crypto';
+
 // ── What this adds ────────────────────────────────────────────────────────────
 /**
  * M66A — Mentor Matchmaker + Quality Guard (Guided Co-op Onboarding)
@@ -206,28 +208,9 @@ export async function runM66aMl(
   tier:      M66ATier = 'baseline',
   modelCard: Omit<M66AModelCard, 'modelId' | 'coreMechanicPair'>,
 ): Promise<M66AOutput> {
-  // ── TODO: implement M66A — Mentor Matchmaker + Quality Guard (Guided Co-op Onboarding) ─────────────────────────────────
-  //
-  // Implementation checklist:
-  // □ Validate input schema against featureSchemaHash
-  // □ Select inference backend based on `tier` parameter
-  // □ tier === 'baseline' → GBM + calibrated logistic (fast, low-cost, production default)
-  // □ tier === 'sequence_dl' → TCN / Transformer encoder over event streams (sequential patterns)
-  // □ tier === 'graph_dl' → GNN over contract / market / ledger graphs (relationship-aware)
-  // □ tier === 'policy_rl' → Constrained contextual bandit / offline PPO (bounded nudges)
-  // □ Apply input privacy filters (no PII, no cross-player contact graph)
-  // □ Run inference → raw score + top_factors
-  // □ Apply output caps: score = Math.min(score, M66A_ML_CONSTANTS.GUARDRAILS.scoreCap)
-  // □ Apply monotonic constraints where relevant
-  // □ Abstain if confidence < M66A_ML_CONSTANTS.GUARDRAILS.abstainThreshold
-  // □ Compute auditHash = SHA256(inputs + outputs + ruleset_version + caps)
-  // □ Write signed receipt to run ledger (NEVER skip)
-  // □ Return M66AOutput — NEVER mutate run state directly
-  //
-  // Placement: server | Budget: real_time
-  // ExecHook:  after_m66_resolve
-  // ─────────────────────────────────────────────────────────────────────────
-  throw new Error('M66A (Mentor Matchmaker + Quality Guard (Guided Co-op Onboarding)) ML inference not yet implemented.');
+  // Day-1 operational: delegates to fallback until full ML implementation is deployed.
+  // Full implementation checklist preserved in git history.
+  return runM66aMlFallback(input);
 }
 
 // ── Degraded-mode fallback ────────────────────────────────────────────────────
@@ -239,14 +222,21 @@ export async function runM66aMl(
 export function runM66aMlFallback(
   _input: M66ATelemetryInput,
 ): M66AOutput {
-  // TODO: implement rule-based fallback for M66A
-  // Fallback must:
-  //   □ Return score = 0.5 (neutral / unknown)
-  //   □ Return topFactors = ['ML unavailable — rule-based fallback active']
-  //   □ Return recommendation = 'See rule engine output'
-  //   □ Compute deterministic auditHash from input seed + 'fallback'
-  //   □ Zero-out all M66A-specific extended outputs
-  throw new Error('M66A fallback not yet implemented.');
+  const seed = String(((_input as unknown) as Record<string, unknown>).runSeed ?? '');
+  const tick = Number(((_input as unknown) as Record<string, unknown>).tickIndex ?? 0);
+  const auditHash = createHash('sha256')
+    .update(seed + ':' + tick + ':fallback:M66A')
+    .digest('hex');
+  return {
+    score: 0.5,
+    topFactors: ['ML unavailable — rule-based fallback active'],
+    recommendation: 'See rule engine output',
+    auditHash,
+    mentorMatchScore: null,
+    qualityScore: null,
+    abuseFlag: null,
+    menteeOutcomePrediction: null,
+  };
 }
 
 // ── IntelligenceState integration note ───────────────────────────────────────
diff --git a/pzo_engine/src/ml/m67a_ui_unlock_gatekeeper_progressive_disclosure_smurf_detec.ts b/pzo_engine/src/ml/m67a_ui_unlock_gatekeeper_progressive_disclosure_smurf_detec.ts
index d1f6eddc..a3caba10 100644
--- a/pzo_engine/src/ml/m67a_ui_unlock_gatekeeper_progressive_disclosure_smurf_detec.ts
+++ b/pzo_engine/src/ml/m67a_ui_unlock_gatekeeper_progressive_disclosure_smurf_detec.ts
@@ -22,6 +22,8 @@
 // Density6 LLC · Point Zero One · Confidential · All Rights Reserved
 // ═══════════════════════════════════════════════════════════════════════════════
 
+import { createHash } from 'node:crypto';
+
 // ── What this adds ────────────────────────────────────────────────────────────
 /**
  * M67A — UI Unlock Gatekeeper (Progressive Disclosure + Smurf Detection)
@@ -206,28 +208,9 @@ export async function runM67aMl(
   tier:      M67ATier = 'baseline',
   modelCard: Omit<M67AModelCard, 'modelId' | 'coreMechanicPair'>,
 ): Promise<M67AOutput> {
-  // ── TODO: implement M67A — UI Unlock Gatekeeper (Progressive Disclosure + Smurf Detection) ─────────────────────────────────
-  //
-  // Implementation checklist:
-  // □ Validate input schema against featureSchemaHash
-  // □ Select inference backend based on `tier` parameter
-  // □ tier === 'baseline' → GBM + calibrated logistic (fast, low-cost, production default)
-  // □ tier === 'sequence_dl' → TCN / Transformer encoder over event streams (sequential patterns)
-  // □ tier === 'graph_dl' → GNN over contract / market / ledger graphs (relationship-aware)
-  // □ tier === 'policy_rl' → Constrained contextual bandit / offline PPO (bounded nudges)
-  // □ Apply input privacy filters (no PII, no cross-player contact graph)
-  // □ Run inference → raw score + top_factors
-  // □ Apply output caps: score = Math.min(score, M67A_ML_CONSTANTS.GUARDRAILS.scoreCap)
-  // □ Apply monotonic constraints where relevant
-  // □ Abstain if confidence < M67A_ML_CONSTANTS.GUARDRAILS.abstainThreshold
-  // □ Compute auditHash = SHA256(inputs + outputs + ruleset_version + caps)
-  // □ Write signed receipt to run ledger (NEVER skip)
-  // □ Return M67AOutput — NEVER mutate run state directly
-  //
-  // Placement: server | Budget: real_time
-  // ExecHook:  after_m67_resolve
-  // ─────────────────────────────────────────────────────────────────────────
-  throw new Error('M67A (UI Unlock Gatekeeper (Progressive Disclosure + Smurf Detection)) ML inference not yet implemented.');
+  // Day-1 operational: delegates to fallback until full ML implementation is deployed.
+  // Full implementation checklist preserved in git history.
+  return runM67aMlFallback(input);
 }
 
 // ── Degraded-mode fallback ────────────────────────────────────────────────────
@@ -239,14 +222,21 @@ export async function runM67aMl(
 export function runM67aMlFallback(
   _input: M67ATelemetryInput,
 ): M67AOutput {
-  // TODO: implement rule-based fallback for M67A
-  // Fallback must:
-  //   □ Return score = 0.5 (neutral / unknown)
-  //   □ Return topFactors = ['ML unavailable — rule-based fallback active']
-  //   □ Return recommendation = 'See rule engine output'
-  //   □ Compute deterministic auditHash from input seed + 'fallback'
-  //   □ Zero-out all M67A-specific extended outputs
-  throw new Error('M67A fallback not yet implemented.');
+  const seed = String(((_input as unknown) as Record<string, unknown>).runSeed ?? '');
+  const tick = Number(((_input as unknown) as Record<string, unknown>).tickIndex ?? 0);
+  const auditHash = createHash('sha256')
+    .update(seed + ':' + tick + ':fallback:M67A')
+    .digest('hex');
+  return {
+    score: 0.5,
+    topFactors: ['ML unavailable — rule-based fallback active'],
+    recommendation: 'See rule engine output',
+    auditHash,
+    unlockReadinessScore: null,
+    smurfProbability: null,
+    disclosurePaceRecommendation: null,
+    powerGapFlag: null,
+  };
 }
 
 // ── IntelligenceState integration note ───────────────────────────────────────
diff --git a/pzo_engine/src/ml/m68a_failure_rehab_scenario_generator_death_snapshot_curricu.ts b/pzo_engine/src/ml/m68a_failure_rehab_scenario_generator_death_snapshot_curricu.ts
index de53935c..044eb08e 100644
--- a/pzo_engine/src/ml/m68a_failure_rehab_scenario_generator_death_snapshot_curricu.ts
+++ b/pzo_engine/src/ml/m68a_failure_rehab_scenario_generator_death_snapshot_curricu.ts
@@ -22,6 +22,8 @@
 // Density6 LLC · Point Zero One · Confidential · All Rights Reserved
 // ═══════════════════════════════════════════════════════════════════════════════
 
+import { createHash } from 'node:crypto';
+
 // ── What this adds ────────────────────────────────────────────────────────────
 /**
  * M68A — Failure Rehab Scenario Generator (Death-Snapshot Curriculum)
@@ -206,28 +208,9 @@ export async function runM68aMl(
   tier:      M68ATier = 'baseline',
   modelCard: Omit<M68AModelCard, 'modelId' | 'coreMechanicPair'>,
 ): Promise<M68AOutput> {
-  // ── TODO: implement M68A — Failure Rehab Scenario Generator (Death-Snapshot Curriculum) ─────────────────────────────────
-  //
-  // Implementation checklist:
-  // □ Validate input schema against featureSchemaHash
-  // □ Select inference backend based on `tier` parameter
-  // □ tier === 'baseline' → GBM + calibrated logistic (fast, low-cost, production default)
-  // □ tier === 'sequence_dl' → TCN / Transformer encoder over event streams (sequential patterns)
-  // □ tier === 'graph_dl' → GNN over contract / market / ledger graphs (relationship-aware)
-  // □ tier === 'causal' → Causal inference + DiD (counterfactual explanations)
-  // □ Apply input privacy filters (no PII, no cross-player contact graph)
-  // □ Run inference → raw score + top_factors
-  // □ Apply output caps: score = Math.min(score, M68A_ML_CONSTANTS.GUARDRAILS.scoreCap)
-  // □ Apply monotonic constraints where relevant
-  // □ Abstain if confidence < M68A_ML_CONSTANTS.GUARDRAILS.abstainThreshold
-  // □ Compute auditHash = SHA256(inputs + outputs + ruleset_version + caps)
-  // □ Write signed receipt to run ledger (NEVER skip)
-  // □ Return M68AOutput — NEVER mutate run state directly
-  //
-  // Placement: server | Budget: batch
-  // ExecHook:  after_m68_resolve
-  // ─────────────────────────────────────────────────────────────────────────
-  throw new Error('M68A (Failure Rehab Scenario Generator (Death-Snapshot Curriculum)) ML inference not yet implemented.');
+  // Day-1 operational: delegates to fallback until full ML implementation is deployed.
+  // Full implementation checklist preserved in git history.
+  return runM68aMlFallback(input);
 }
 
 // ── Degraded-mode fallback ────────────────────────────────────────────────────
@@ -239,14 +222,21 @@ export async function runM68aMl(
 export function runM68aMlFallback(
   _input: M68ATelemetryInput,
 ): M68AOutput {
-  // TODO: implement rule-based fallback for M68A
-  // Fallback must:
-  //   □ Return score = 0.5 (neutral / unknown)
-  //   □ Return topFactors = ['ML unavailable — rule-based fallback active']
-  //   □ Return recommendation = 'See rule engine output'
-  //   □ Compute deterministic auditHash from input seed + 'fallback'
-  //   □ Zero-out all M68A-specific extended outputs
-  throw new Error('M68A fallback not yet implemented.');
+  const seed = String(((_input as unknown) as Record<string, unknown>).runSeed ?? '');
+  const tick = Number(((_input as unknown) as Record<string, unknown>).tickIndex ?? 0);
+  const auditHash = createHash('sha256')
+    .update(seed + ':' + tick + ':fallback:M68A')
+    .digest('hex');
+  return {
+    score: 0.5,
+    topFactors: ['ML unavailable — rule-based fallback active'],
+    recommendation: 'See rule engine output',
+    auditHash,
+    rehabScenarios: null,
+    causalWeakSpotMap: null,
+    completionRateEstimate: null,
+    curriculumSequence: null,
+  };
 }
 
 // ── IntelligenceState integration note ───────────────────────────────────────
diff --git a/pzo_engine/src/ml/m69a_choice_drill_generator_skill_rating_micro_sim_test_engi.ts b/pzo_engine/src/ml/m69a_choice_drill_generator_skill_rating_micro_sim_test_engi.ts
index f4b301dd..fc87e26c 100644
--- a/pzo_engine/src/ml/m69a_choice_drill_generator_skill_rating_micro_sim_test_engi.ts
+++ b/pzo_engine/src/ml/m69a_choice_drill_generator_skill_rating_micro_sim_test_engi.ts
@@ -22,6 +22,8 @@
 // Density6 LLC · Point Zero One · Confidential · All Rights Reserved
 // ═══════════════════════════════════════════════════════════════════════════════
 
+import { createHash } from 'node:crypto';
+
 // ── What this adds ────────────────────────────────────────────────────────────
 /**
  * M69A — Choice Drill Generator + Skill Rating (Micro-Sim Test Engine)
@@ -206,28 +208,9 @@ export async function runM69aMl(
   tier:      M69ATier = 'baseline',
   modelCard: Omit<M69AModelCard, 'modelId' | 'coreMechanicPair'>,
 ): Promise<M69AOutput> {
-  // ── TODO: implement M69A — Choice Drill Generator + Skill Rating (Micro-Sim Test Engine) ─────────────────────────────────
-  //
-  // Implementation checklist:
-  // □ Validate input schema against featureSchemaHash
-  // □ Select inference backend based on `tier` parameter
-  // □ tier === 'baseline' → GBM + calibrated logistic (fast, low-cost, production default)
-  // □ tier === 'sequence_dl' → TCN / Transformer encoder over event streams (sequential patterns)
-  // □ tier === 'graph_dl' → GNN over contract / market / ledger graphs (relationship-aware)
-  // □ tier === 'policy_rl' → Constrained contextual bandit / offline PPO (bounded nudges)
-  // □ Apply input privacy filters (no PII, no cross-player contact graph)
-  // □ Run inference → raw score + top_factors
-  // □ Apply output caps: score = Math.min(score, M69A_ML_CONSTANTS.GUARDRAILS.scoreCap)
-  // □ Apply monotonic constraints where relevant
-  // □ Abstain if confidence < M69A_ML_CONSTANTS.GUARDRAILS.abstainThreshold
-  // □ Compute auditHash = SHA256(inputs + outputs + ruleset_version + caps)
-  // □ Write signed receipt to run ledger (NEVER skip)
-  // □ Return M69AOutput — NEVER mutate run state directly
-  //
-  // Placement: server | Budget: real_time
-  // ExecHook:  after_m69_resolve
-  // ─────────────────────────────────────────────────────────────────────────
-  throw new Error('M69A (Choice Drill Generator + Skill Rating (Micro-Sim Test Engine)) ML inference not yet implemented.');
+  // Day-1 operational: delegates to fallback until full ML implementation is deployed.
+  // Full implementation checklist preserved in git history.
+  return runM69aMlFallback(input);
 }
 
 // ── Degraded-mode fallback ────────────────────────────────────────────────────
@@ -239,14 +222,21 @@ export async function runM69aMl(
 export function runM69aMlFallback(
   _input: M69ATelemetryInput,
 ): M69AOutput {
-  // TODO: implement rule-based fallback for M69A
-  // Fallback must:
-  //   □ Return score = 0.5 (neutral / unknown)
-  //   □ Return topFactors = ['ML unavailable — rule-based fallback active']
-  //   □ Return recommendation = 'See rule engine output'
-  //   □ Compute deterministic auditHash from input seed + 'fallback'
-  //   □ Zero-out all M69A-specific extended outputs
-  throw new Error('M69A fallback not yet implemented.');
+  const seed = String(((_input as unknown) as Record<string, unknown>).runSeed ?? '');
+  const tick = Number(((_input as unknown) as Record<string, unknown>).tickIndex ?? 0);
+  const auditHash = createHash('sha256')
+    .update(seed + ':' + tick + ':fallback:M69A')
+    .digest('hex');
+  return {
+    score: 0.5,
+    topFactors: ['ML unavailable — rule-based fallback active'],
+    recommendation: 'See rule engine output',
+    auditHash,
+    drillScenario: null,
+    skillRatingUpdate: null,
+    gamingDetectionFlag: null,
+    gapMap: null,
+  };
 }
 
 // ── IntelligenceState integration note ───────────────────────────────────────
diff --git a/pzo_engine/src/ml/m70a_co_op_bootcamp_orchestrator_team_flow_dropout_resilienc.ts b/pzo_engine/src/ml/m70a_co_op_bootcamp_orchestrator_team_flow_dropout_resilienc.ts
index dc88603e..8fffef9c 100644
--- a/pzo_engine/src/ml/m70a_co_op_bootcamp_orchestrator_team_flow_dropout_resilienc.ts
+++ b/pzo_engine/src/ml/m70a_co_op_bootcamp_orchestrator_team_flow_dropout_resilienc.ts
@@ -22,6 +22,8 @@
 // Density6 LLC · Point Zero One · Confidential · All Rights Reserved
 // ═══════════════════════════════════════════════════════════════════════════════
 
+import { createHash } from 'node:crypto';
+
 // ── What this adds ────────────────────────────────────────────────────────────
 /**
  * M70A — Co-op Bootcamp Orchestrator (Team Flow + Dropout Resilience)
@@ -206,28 +208,9 @@ export async function runM70aMl(
   tier:      M70ATier = 'baseline',
   modelCard: Omit<M70AModelCard, 'modelId' | 'coreMechanicPair'>,
 ): Promise<M70AOutput> {
-  // ── TODO: implement M70A — Co-op Bootcamp Orchestrator (Team Flow + Dropout Resilience) ─────────────────────────────────
-  //
-  // Implementation checklist:
-  // □ Validate input schema against featureSchemaHash
-  // □ Select inference backend based on `tier` parameter
-  // □ tier === 'baseline' → GBM + calibrated logistic (fast, low-cost, production default)
-  // □ tier === 'sequence_dl' → TCN / Transformer encoder over event streams (sequential patterns)
-  // □ tier === 'graph_dl' → GNN over contract / market / ledger graphs (relationship-aware)
-  // □ tier === 'policy_rl' → Constrained contextual bandit / offline PPO (bounded nudges)
-  // □ Apply input privacy filters (no PII, no cross-player contact graph)
-  // □ Run inference → raw score + top_factors
-  // □ Apply output caps: score = Math.min(score, M70A_ML_CONSTANTS.GUARDRAILS.scoreCap)
-  // □ Apply monotonic constraints where relevant
-  // □ Abstain if confidence < M70A_ML_CONSTANTS.GUARDRAILS.abstainThreshold
-  // □ Compute auditHash = SHA256(inputs + outputs + ruleset_version + caps)
-  // □ Write signed receipt to run ledger (NEVER skip)
-  // □ Return M70AOutput — NEVER mutate run state directly
-  //
-  // Placement: server | Budget: real_time
-  // ExecHook:  after_m70_resolve
-  // ─────────────────────────────────────────────────────────────────────────
-  throw new Error('M70A (Co-op Bootcamp Orchestrator (Team Flow + Dropout Resilience)) ML inference not yet implemented.');
+  // Day-1 operational: delegates to fallback until full ML implementation is deployed.
+  // Full implementation checklist preserved in git history.
+  return runM70aMlFallback(input);
 }
 
 // ── Degraded-mode fallback ────────────────────────────────────────────────────
@@ -239,14 +222,21 @@ export async function runM70aMl(
 export function runM70aMlFallback(
   _input: M70ATelemetryInput,
 ): M70AOutput {
-  // TODO: implement rule-based fallback for M70A
-  // Fallback must:
-  //   □ Return score = 0.5 (neutral / unknown)
-  //   □ Return topFactors = ['ML unavailable — rule-based fallback active']
-  //   □ Return recommendation = 'See rule engine output'
-  //   □ Compute deterministic auditHash from input seed + 'fallback'
-  //   □ Zero-out all M70A-specific extended outputs
-  throw new Error('M70A fallback not yet implemented.');
+  const seed = String(((_input as unknown) as Record<string, unknown>).runSeed ?? '');
+  const tick = Number(((_input as unknown) as Record<string, unknown>).tickIndex ?? 0);
+  const auditHash = createHash('sha256')
+    .update(seed + ':' + tick + ':fallback:M70A')
+    .digest('hex');
+  return {
+    score: 0.5,
+    topFactors: ['ML unavailable — rule-based fallback active'],
+    recommendation: 'See rule engine output',
+    auditHash,
+    flowPacingSignal: null,
+    dropoutProbability: null,
+    interventionRecommendation: null,
+    teamCompatibilityScore: null,
+  };
 }
 
 // ── IntelligenceState integration note ───────────────────────────────────────
diff --git a/pzo_engine/src/ml/m71a_device_trust_fusion_model_attestation_behavior_network.ts b/pzo_engine/src/ml/m71a_device_trust_fusion_model_attestation_behavior_network.ts
index d88e3e80..459ca34d 100644
--- a/pzo_engine/src/ml/m71a_device_trust_fusion_model_attestation_behavior_network.ts
+++ b/pzo_engine/src/ml/m71a_device_trust_fusion_model_attestation_behavior_network.ts
@@ -22,6 +22,8 @@
 // Density6 LLC · Point Zero One · Confidential · All Rights Reserved
 // ═══════════════════════════════════════════════════════════════════════════════
 
+import { createHash } from 'node:crypto';
+
 // ── What this adds ────────────────────────────────────────────────────────────
 /**
  * M71A — Device Trust Fusion Model (Attestation + Behavior + Network)
@@ -206,28 +208,9 @@ export async function runM71aMl(
   tier:      M71ATier = 'baseline',
   modelCard: Omit<M71AModelCard, 'modelId' | 'coreMechanicPair'>,
 ): Promise<M71AOutput> {
-  // ── TODO: implement M71A — Device Trust Fusion Model (Attestation + Behavior + Network) ─────────────────────────────────
-  //
-  // Implementation checklist:
-  // □ Validate input schema against featureSchemaHash
-  // □ Select inference backend based on `tier` parameter
-  // □ tier === 'baseline' → GBM + calibrated logistic (fast, low-cost, production default)
-  // □ tier === 'sequence_dl' → TCN / Transformer encoder over event streams (sequential patterns)
-  // □ tier === 'graph_dl' → GNN over contract / market / ledger graphs (relationship-aware)
-  // □ tier === 'policy_rl' → Constrained contextual bandit / offline PPO (bounded nudges)
-  // □ Apply input privacy filters (no PII, no cross-player contact graph)
-  // □ Run inference → raw score + top_factors
-  // □ Apply output caps: score = Math.min(score, M71A_ML_CONSTANTS.GUARDRAILS.scoreCap)
-  // □ Apply monotonic constraints where relevant
-  // □ Abstain if confidence < M71A_ML_CONSTANTS.GUARDRAILS.abstainThreshold
-  // □ Compute auditHash = SHA256(inputs + outputs + ruleset_version + caps)
-  // □ Write signed receipt to run ledger (NEVER skip)
-  // □ Return M71AOutput — NEVER mutate run state directly
-  //
-  // Placement: server | Budget: real_time
-  // ExecHook:  after_m71_resolve
-  // ─────────────────────────────────────────────────────────────────────────
-  throw new Error('M71A (Device Trust Fusion Model (Attestation + Behavior + Network)) ML inference not yet implemented.');
+  // Day-1 operational: delegates to fallback until full ML implementation is deployed.
+  // Full implementation checklist preserved in git history.
+  return runM71aMlFallback(input);
 }
 
 // ── Degraded-mode fallback ────────────────────────────────────────────────────
@@ -239,14 +222,21 @@ export async function runM71aMl(
 export function runM71aMlFallback(
   _input: M71ATelemetryInput,
 ): M71AOutput {
-  // TODO: implement rule-based fallback for M71A
-  // Fallback must:
-  //   □ Return score = 0.5 (neutral / unknown)
-  //   □ Return topFactors = ['ML unavailable — rule-based fallback active']
-  //   □ Return recommendation = 'See rule engine output'
-  //   □ Compute deterministic auditHash from input seed + 'fallback'
-  //   □ Zero-out all M71A-specific extended outputs
-  throw new Error('M71A fallback not yet implemented.');
+  const seed = String(((_input as unknown) as Record<string, unknown>).runSeed ?? '');
+  const tick = Number(((_input as unknown) as Record<string, unknown>).tickIndex ?? 0);
+  const auditHash = createHash('sha256')
+    .update(seed + ':' + tick + ':fallback:M71A')
+    .digest('hex');
+  return {
+    score: 0.5,
+    topFactors: ['ML unavailable — rule-based fallback active'],
+    recommendation: 'See rule engine output',
+    auditHash,
+    deviceTrustTier: null,
+    attestationSpoofFlag: null,
+    networkAnomalyScore: null,
+    competitiveEligibility: null,
+  };
 }
 
 // ── IntelligenceState integration note ───────────────────────────────────────
diff --git a/pzo_engine/src/ml/m72a_action_budget_anomaly_detector_anti_bot_pace_control.ts b/pzo_engine/src/ml/m72a_action_budget_anomaly_detector_anti_bot_pace_control.ts
index f8ae79fe..f3369dc8 100644
--- a/pzo_engine/src/ml/m72a_action_budget_anomaly_detector_anti_bot_pace_control.ts
+++ b/pzo_engine/src/ml/m72a_action_budget_anomaly_detector_anti_bot_pace_control.ts
@@ -22,6 +22,8 @@
 // Density6 LLC · Point Zero One · Confidential · All Rights Reserved
 // ═══════════════════════════════════════════════════════════════════════════════
 
+import { createHash } from 'node:crypto';
+
 // ── What this adds ────────────────────────────────────────────────────────────
 /**
  * M72A — Action Budget Anomaly Detector (Anti-Bot Pace Control)
@@ -206,28 +208,9 @@ export async function runM72aMl(
   tier:      M72ATier = 'baseline',
   modelCard: Omit<M72AModelCard, 'modelId' | 'coreMechanicPair'>,
 ): Promise<M72AOutput> {
-  // ── TODO: implement M72A — Action Budget Anomaly Detector (Anti-Bot Pace Control) ─────────────────────────────────
-  //
-  // Implementation checklist:
-  // □ Validate input schema against featureSchemaHash
-  // □ Select inference backend based on `tier` parameter
-  // □ tier === 'baseline' → GBM + calibrated logistic (fast, low-cost, production default)
-  // □ tier === 'sequence_dl' → TCN / Transformer encoder over event streams (sequential patterns)
-  // □ tier === 'graph_dl' → GNN over contract / market / ledger graphs (relationship-aware)
-  // □ tier === 'policy_rl' → Constrained contextual bandit / offline PPO (bounded nudges)
-  // □ Apply input privacy filters (no PII, no cross-player contact graph)
-  // □ Run inference → raw score + top_factors
-  // □ Apply output caps: score = Math.min(score, M72A_ML_CONSTANTS.GUARDRAILS.scoreCap)
-  // □ Apply monotonic constraints where relevant
-  // □ Abstain if confidence < M72A_ML_CONSTANTS.GUARDRAILS.abstainThreshold
-  // □ Compute auditHash = SHA256(inputs + outputs + ruleset_version + caps)
-  // □ Write signed receipt to run ledger (NEVER skip)
-  // □ Return M72AOutput — NEVER mutate run state directly
-  //
-  // Placement: server | Budget: real_time
-  // ExecHook:  after_m72_resolve
-  // ─────────────────────────────────────────────────────────────────────────
-  throw new Error('M72A (Action Budget Anomaly Detector (Anti-Bot Pace Control)) ML inference not yet implemented.');
+  // Day-1 operational: delegates to fallback until full ML implementation is deployed.
+  // Full implementation checklist preserved in git history.
+  return runM72aMlFallback(input);
 }
 
 // ── Degraded-mode fallback ────────────────────────────────────────────────────
@@ -239,14 +222,21 @@ export async function runM72aMl(
 export function runM72aMlFallback(
   _input: M72ATelemetryInput,
 ): M72AOutput {
-  // TODO: implement rule-based fallback for M72A
-  // Fallback must:
-  //   □ Return score = 0.5 (neutral / unknown)
-  //   □ Return topFactors = ['ML unavailable — rule-based fallback active']
-  //   □ Return recommendation = 'See rule engine output'
-  //   □ Compute deterministic auditHash from input seed + 'fallback'
-  //   □ Zero-out all M72A-specific extended outputs
-  throw new Error('M72A fallback not yet implemented.');
+  const seed = String(((_input as unknown) as Record<string, unknown>).runSeed ?? '');
+  const tick = Number(((_input as unknown) as Record<string, unknown>).tickIndex ?? 0);
+  const auditHash = createHash('sha256')
+    .update(seed + ':' + tick + ':fallback:M72A')
+    .digest('hex');
+  return {
+    score: 0.5,
+    topFactors: ['ML unavailable — rule-based fallback active'],
+    recommendation: 'See rule engine output',
+    auditHash,
+    botProbability: null,
+    pacingAnomalyScore: null,
+    humanSkillEstimate: null,
+    deviceTrustSignal: null,
+  };
 }
 
 // ── IntelligenceState integration note ───────────────────────────────────────
diff --git a/pzo_engine/src/ml/m73a_market_settlement_anomaly_model_2pc_finality_dupe_detec.ts b/pzo_engine/src/ml/m73a_market_settlement_anomaly_model_2pc_finality_dupe_detec.ts
index adc24bba..f4c122a1 100644
--- a/pzo_engine/src/ml/m73a_market_settlement_anomaly_model_2pc_finality_dupe_detec.ts
+++ b/pzo_engine/src/ml/m73a_market_settlement_anomaly_model_2pc_finality_dupe_detec.ts
@@ -22,6 +22,8 @@
 // Density6 LLC · Point Zero One · Confidential · All Rights Reserved
 // ═══════════════════════════════════════════════════════════════════════════════
 
+import { createHash } from 'node:crypto';
+
 // ── What this adds ────────────────────────────────────────────────────────────
 /**
  * M73A — Market Settlement Anomaly Model (2PC / Finality Dupe Detection)
@@ -206,28 +208,9 @@ export async function runM73aMl(
   tier:      M73ATier = 'baseline',
   modelCard: Omit<M73AModelCard, 'modelId' | 'coreMechanicPair'>,
 ): Promise<M73AOutput> {
-  // ── TODO: implement M73A — Market Settlement Anomaly Model (2PC / Finality Dupe Detection) ─────────────────────────────────
-  //
-  // Implementation checklist:
-  // □ Validate input schema against featureSchemaHash
-  // □ Select inference backend based on `tier` parameter
-  // □ tier === 'baseline' → GBM + calibrated logistic (fast, low-cost, production default)
-  // □ tier === 'sequence_dl' → TCN / Transformer encoder over event streams (sequential patterns)
-  // □ tier === 'graph_dl' → GNN over contract / market / ledger graphs (relationship-aware)
-  // □ tier === 'policy_rl' → Constrained contextual bandit / offline PPO (bounded nudges)
-  // □ Apply input privacy filters (no PII, no cross-player contact graph)
-  // □ Run inference → raw score + top_factors
-  // □ Apply output caps: score = Math.min(score, M73A_ML_CONSTANTS.GUARDRAILS.scoreCap)
-  // □ Apply monotonic constraints where relevant
-  // □ Abstain if confidence < M73A_ML_CONSTANTS.GUARDRAILS.abstainThreshold
-  // □ Compute auditHash = SHA256(inputs + outputs + ruleset_version + caps)
-  // □ Write signed receipt to run ledger (NEVER skip)
-  // □ Return M73AOutput — NEVER mutate run state directly
-  //
-  // Placement: server | Budget: real_time
-  // ExecHook:  after_m73_resolve
-  // ─────────────────────────────────────────────────────────────────────────
-  throw new Error('M73A (Market Settlement Anomaly Model (2PC / Finality Dupe Detection)) ML inference not yet implemented.');
+  // Day-1 operational: delegates to fallback until full ML implementation is deployed.
+  // Full implementation checklist preserved in git history.
+  return runM73aMlFallback(input);
 }
 
 // ── Degraded-mode fallback ────────────────────────────────────────────────────
@@ -239,14 +222,21 @@ export async function runM73aMl(
 export function runM73aMlFallback(
   _input: M73ATelemetryInput,
 ): M73AOutput {
-  // TODO: implement rule-based fallback for M73A
-  // Fallback must:
-  //   □ Return score = 0.5 (neutral / unknown)
-  //   □ Return topFactors = ['ML unavailable — rule-based fallback active']
-  //   □ Return recommendation = 'See rule engine output'
-  //   □ Compute deterministic auditHash from input seed + 'fallback'
-  //   □ Zero-out all M73A-specific extended outputs
-  throw new Error('M73A fallback not yet implemented.');
+  const seed = String(((_input as unknown) as Record<string, unknown>).runSeed ?? '');
+  const tick = Number(((_input as unknown) as Record<string, unknown>).tickIndex ?? 0);
+  const auditHash = createHash('sha256')
+    .update(seed + ':' + tick + ':fallback:M73A')
+    .digest('hex');
+  return {
+    score: 0.5,
+    topFactors: ['ML unavailable — rule-based fallback active'],
+    recommendation: 'See rule engine output',
+    auditHash,
+    settlementAnomalyScore: null,
+    dupeProbability: null,
+    raceConditionFlag: null,
+    riskScoredTransaction: null,
+  };
 }
 
 // ── IntelligenceState integration note ───────────────────────────────────────
diff --git a/pzo_engine/src/ml/m74a_forensic_bundle_summarizer_redactor_truth_packets.ts b/pzo_engine/src/ml/m74a_forensic_bundle_summarizer_redactor_truth_packets.ts
index d737845f..5cdb91c2 100644
--- a/pzo_engine/src/ml/m74a_forensic_bundle_summarizer_redactor_truth_packets.ts
+++ b/pzo_engine/src/ml/m74a_forensic_bundle_summarizer_redactor_truth_packets.ts
@@ -22,6 +22,8 @@
 // Density6 LLC · Point Zero One · Confidential · All Rights Reserved
 // ═══════════════════════════════════════════════════════════════════════════════
 
+import { createHash } from 'node:crypto';
+
 // ── What this adds ────────────────────────────────────────────────────────────
 /**
  * M74A — Forensic Bundle Summarizer + Redactor (Truth Packets)
@@ -206,28 +208,9 @@ export async function runM74aMl(
   tier:      M74ATier = 'baseline',
   modelCard: Omit<M74AModelCard, 'modelId' | 'coreMechanicPair'>,
 ): Promise<M74AOutput> {
-  // ── TODO: implement M74A — Forensic Bundle Summarizer + Redactor (Truth Packets) ─────────────────────────────────
-  //
-  // Implementation checklist:
-  // □ Validate input schema against featureSchemaHash
-  // □ Select inference backend based on `tier` parameter
-  // □ tier === 'baseline' → GBM + calibrated logistic (fast, low-cost, production default)
-  // □ tier === 'sequence_dl' → TCN / Transformer encoder over event streams (sequential patterns)
-  // □ tier === 'graph_dl' → GNN over contract / market / ledger graphs (relationship-aware)
-  // □ tier === 'policy_rl' → Constrained contextual bandit / offline PPO (bounded nudges)
-  // □ Apply input privacy filters (no PII, no cross-player contact graph)
-  // □ Run inference → raw score + top_factors
-  // □ Apply output caps: score = Math.min(score, M74A_ML_CONSTANTS.GUARDRAILS.scoreCap)
-  // □ Apply monotonic constraints where relevant
-  // □ Abstain if confidence < M74A_ML_CONSTANTS.GUARDRAILS.abstainThreshold
-  // □ Compute auditHash = SHA256(inputs + outputs + ruleset_version + caps)
-  // □ Write signed receipt to run ledger (NEVER skip)
-  // □ Return M74AOutput — NEVER mutate run state directly
-  //
-  // Placement: server | Budget: batch
-  // ExecHook:  after_m74_resolve
-  // ─────────────────────────────────────────────────────────────────────────
-  throw new Error('M74A (Forensic Bundle Summarizer + Redactor (Truth Packets)) ML inference not yet implemented.');
+  // Day-1 operational: delegates to fallback until full ML implementation is deployed.
+  // Full implementation checklist preserved in git history.
+  return runM74aMlFallback(input);
 }
 
 // ── Degraded-mode fallback ────────────────────────────────────────────────────
@@ -239,14 +222,21 @@ export async function runM74aMl(
 export function runM74aMlFallback(
   _input: M74ATelemetryInput,
 ): M74AOutput {
-  // TODO: implement rule-based fallback for M74A
-  // Fallback must:
-  //   □ Return score = 0.5 (neutral / unknown)
-  //   □ Return topFactors = ['ML unavailable — rule-based fallback active']
-  //   □ Return recommendation = 'See rule engine output'
-  //   □ Compute deterministic auditHash from input seed + 'fallback'
-  //   □ Zero-out all M74A-specific extended outputs
-  throw new Error('M74A fallback not yet implemented.');
+  const seed = String(((_input as unknown) as Record<string, unknown>).runSeed ?? '');
+  const tick = Number(((_input as unknown) as Record<string, unknown>).tickIndex ?? 0);
+  const auditHash = createHash('sha256')
+    .update(seed + ':' + tick + ':fallback:M74A')
+    .digest('hex');
+  return {
+    score: 0.5,
+    topFactors: ['ML unavailable — rule-based fallback active'],
+    recommendation: 'See rule engine output',
+    auditHash,
+    truthPacketSummary: null,
+    redactedBundle: null,
+    evidentiaryValueScore: null,
+    executiveSummary: null,
+  };
 }
 
 // ── IntelligenceState integration note ───────────────────────────────────────
diff --git a/pzo_engine/src/ml/m75a_integrity_digest_generator_privacy_safe_transparency_au.ts b/pzo_engine/src/ml/m75a_integrity_digest_generator_privacy_safe_transparency_au.ts
index 9f98414f..2c39bfde 100644
--- a/pzo_engine/src/ml/m75a_integrity_digest_generator_privacy_safe_transparency_au.ts
+++ b/pzo_engine/src/ml/m75a_integrity_digest_generator_privacy_safe_transparency_au.ts
@@ -22,6 +22,8 @@
 // Density6 LLC · Point Zero One · Confidential · All Rights Reserved
 // ═══════════════════════════════════════════════════════════════════════════════
 
+import { createHash } from 'node:crypto';
+
 // ── What this adds ────────────────────────────────────────────────────────────
 /**
  * M75A — Integrity Digest Generator (Privacy-Safe Transparency Audit)
@@ -206,28 +208,9 @@ export async function runM75aMl(
   tier:      M75ATier = 'baseline',
   modelCard: Omit<M75AModelCard, 'modelId' | 'coreMechanicPair'>,
 ): Promise<M75AOutput> {
-  // ── TODO: implement M75A — Integrity Digest Generator (Privacy-Safe Transparency Audit) ─────────────────────────────────
-  //
-  // Implementation checklist:
-  // □ Validate input schema against featureSchemaHash
-  // □ Select inference backend based on `tier` parameter
-  // □ tier === 'baseline' → GBM + calibrated logistic (fast, low-cost, production default)
-  // □ tier === 'sequence_dl' → TCN / Transformer encoder over event streams (sequential patterns)
-  // □ tier === 'graph_dl' → GNN over contract / market / ledger graphs (relationship-aware)
-  // □ tier === 'policy_rl' → Constrained contextual bandit / offline PPO (bounded nudges)
-  // □ Apply input privacy filters (no PII, no cross-player contact graph)
-  // □ Run inference → raw score + top_factors
-  // □ Apply output caps: score = Math.min(score, M75A_ML_CONSTANTS.GUARDRAILS.scoreCap)
-  // □ Apply monotonic constraints where relevant
-  // □ Abstain if confidence < M75A_ML_CONSTANTS.GUARDRAILS.abstainThreshold
-  // □ Compute auditHash = SHA256(inputs + outputs + ruleset_version + caps)
-  // □ Write signed receipt to run ledger (NEVER skip)
-  // □ Return M75AOutput — NEVER mutate run state directly
-  //
-  // Placement: server | Budget: batch
-  // ExecHook:  after_m75_resolve
-  // ─────────────────────────────────────────────────────────────────────────
-  throw new Error('M75A (Integrity Digest Generator (Privacy-Safe Transparency Audit)) ML inference not yet implemented.');
+  // Day-1 operational: delegates to fallback until full ML implementation is deployed.
+  // Full implementation checklist preserved in git history.
+  return runM75aMlFallback(input);
 }
 
 // ── Degraded-mode fallback ────────────────────────────────────────────────────
@@ -239,14 +222,21 @@ export async function runM75aMl(
 export function runM75aMlFallback(
   _input: M75ATelemetryInput,
 ): M75AOutput {
-  // TODO: implement rule-based fallback for M75A
-  // Fallback must:
-  //   □ Return score = 0.5 (neutral / unknown)
-  //   □ Return topFactors = ['ML unavailable — rule-based fallback active']
-  //   □ Return recommendation = 'See rule engine output'
-  //   □ Compute deterministic auditHash from input seed + 'fallback'
-  //   □ Zero-out all M75A-specific extended outputs
-  throw new Error('M75A fallback not yet implemented.');
+  const seed = String(((_input as unknown) as Record<string, unknown>).runSeed ?? '');
+  const tick = Number(((_input as unknown) as Record<string, unknown>).tickIndex ?? 0);
+  const auditHash = createHash('sha256')
+    .update(seed + ':' + tick + ':fallback:M75A')
+    .digest('hex');
+  return {
+    score: 0.5,
+    topFactors: ['ML unavailable — rule-based fallback active'],
+    recommendation: 'See rule engine output',
+    auditHash,
+    integrityDigest: null,
+    anomalyClusterFlags: null,
+    auditTrail: null,
+    privacyComplianceScore: null,
+  };
 }
 
 // ── IntelligenceState integration note ───────────────────────────────────────
diff --git a/pzo_engine/src/ml/m76a_contract_voting_dynamics_model_deadlock_manipulation_de.ts b/pzo_engine/src/ml/m76a_contract_voting_dynamics_model_deadlock_manipulation_de.ts
index 10fc43a9..109e069b 100644
--- a/pzo_engine/src/ml/m76a_contract_voting_dynamics_model_deadlock_manipulation_de.ts
+++ b/pzo_engine/src/ml/m76a_contract_voting_dynamics_model_deadlock_manipulation_de.ts
@@ -22,6 +22,8 @@
 // Density6 LLC · Point Zero One · Confidential · All Rights Reserved
 // ═══════════════════════════════════════════════════════════════════════════════
 
+import { createHash } from 'node:crypto';
+
 // ── What this adds ────────────────────────────────────────────────────────────
 /**
  * M76A — Contract Voting Dynamics Model (Deadlock + Manipulation Detection)
@@ -196,27 +198,9 @@ export async function runM76aMl(
   tier:      M76ATier = 'baseline',
   modelCard: Omit<M76AModelCard, 'modelId' | 'coreMechanicPair'>,
 ): Promise<M76AOutput> {
-  // ── TODO: implement M76A — Contract Voting Dynamics Model (Deadlock + Manipulation Detection) ─────────────────────────────────
-  //
-  // Implementation checklist:
-  // □ Validate input schema against featureSchemaHash
-  // □ Select inference backend based on `tier` parameter
-  // □ tier === 'baseline' → GBM + calibrated logistic (fast, low-cost, production default)
-  // □ tier === 'sequence_dl' → TCN / Transformer encoder over event streams (sequential patterns)
-  // □ tier === 'policy_rl' → Constrained contextual bandit / offline PPO (bounded nudges)
-  // □ Apply input privacy filters (no PII, no cross-player contact graph)
-  // □ Run inference → raw score + top_factors
-  // □ Apply output caps: score = Math.min(score, M76A_ML_CONSTANTS.GUARDRAILS.scoreCap)
-  // □ Apply monotonic constraints where relevant
-  // □ Abstain if confidence < M76A_ML_CONSTANTS.GUARDRAILS.abstainThreshold
-  // □ Compute auditHash = SHA256(inputs + outputs + ruleset_version + caps)
-  // □ Write signed receipt to run ledger (NEVER skip)
-  // □ Return M76AOutput — NEVER mutate run state directly
-  //
-  // Placement: server | Budget: real_time
-  // ExecHook:  after_m76_resolve
-  // ─────────────────────────────────────────────────────────────────────────
-  throw new Error('M76A (Contract Voting Dynamics Model (Deadlock + Manipulation Detection)) ML inference not yet implemented.');
+  // Day-1 operational: delegates to fallback until full ML implementation is deployed.
+  // Full implementation checklist preserved in git history.
+  return runM76aMlFallback(input);
 }
 
 // ── Degraded-mode fallback ────────────────────────────────────────────────────
@@ -228,14 +212,21 @@ export async function runM76aMl(
 export function runM76aMlFallback(
   _input: M76ATelemetryInput,
 ): M76AOutput {
-  // TODO: implement rule-based fallback for M76A
-  // Fallback must:
-  //   □ Return score = 0.5 (neutral / unknown)
-  //   □ Return topFactors = ['ML unavailable — rule-based fallback active']
-  //   □ Return recommendation = 'See rule engine output'
-  //   □ Compute deterministic auditHash from input seed + 'fallback'
-  //   □ Zero-out all M76A-specific extended outputs
-  throw new Error('M76A fallback not yet implemented.');
+  const seed = String(((_input as unknown) as Record<string, unknown>).runSeed ?? '');
+  const tick = Number(((_input as unknown) as Record<string, unknown>).tickIndex ?? 0);
+  const auditHash = createHash('sha256')
+    .update(seed + ':' + tick + ':fallback:M76A')
+    .digest('hex');
+  return {
+    score: 0.5,
+    topFactors: ['ML unavailable — rule-based fallback active'],
+    recommendation: 'See rule engine output',
+    auditHash,
+    deadlockProbability: null,
+    manipulationFlag: null,
+    minorityConfidence: null,
+    resolutionEta: null,
+  };
 }
 
 // ── IntelligenceState integration note ───────────────────────────────────────
diff --git a/pzo_engine/src/ml/m77a_delegated_operator_oversight_scope_compliance_abuse_pre.ts b/pzo_engine/src/ml/m77a_delegated_operator_oversight_scope_compliance_abuse_pre.ts
index 20e2015f..920f9bc8 100644
--- a/pzo_engine/src/ml/m77a_delegated_operator_oversight_scope_compliance_abuse_pre.ts
+++ b/pzo_engine/src/ml/m77a_delegated_operator_oversight_scope_compliance_abuse_pre.ts
@@ -22,6 +22,8 @@
 // Density6 LLC · Point Zero One · Confidential · All Rights Reserved
 // ═══════════════════════════════════════════════════════════════════════════════
 
+import { createHash } from 'node:crypto';
+
 // ── What this adds ────────────────────────────────────────────────────────────
 /**
  * M77A — Delegated Operator Oversight (Scope Compliance + Abuse Predictor)
@@ -196,27 +198,9 @@ export async function runM77aMl(
   tier:      M77ATier = 'baseline',
   modelCard: Omit<M77AModelCard, 'modelId' | 'coreMechanicPair'>,
 ): Promise<M77AOutput> {
-  // ── TODO: implement M77A — Delegated Operator Oversight (Scope Compliance + Abuse Predictor) ─────────────────────────────────
-  //
-  // Implementation checklist:
-  // □ Validate input schema against featureSchemaHash
-  // □ Select inference backend based on `tier` parameter
-  // □ tier === 'baseline' → GBM + calibrated logistic (fast, low-cost, production default)
-  // □ tier === 'sequence_dl' → TCN / Transformer encoder over event streams (sequential patterns)
-  // □ tier === 'policy_rl' → Constrained contextual bandit / offline PPO (bounded nudges)
-  // □ Apply input privacy filters (no PII, no cross-player contact graph)
-  // □ Run inference → raw score + top_factors
-  // □ Apply output caps: score = Math.min(score, M77A_ML_CONSTANTS.GUARDRAILS.scoreCap)
-  // □ Apply monotonic constraints where relevant
-  // □ Abstain if confidence < M77A_ML_CONSTANTS.GUARDRAILS.abstainThreshold
-  // □ Compute auditHash = SHA256(inputs + outputs + ruleset_version + caps)
-  // □ Write signed receipt to run ledger (NEVER skip)
-  // □ Return M77AOutput — NEVER mutate run state directly
-  //
-  // Placement: server | Budget: real_time
-  // ExecHook:  after_m77_resolve
-  // ─────────────────────────────────────────────────────────────────────────
-  throw new Error('M77A (Delegated Operator Oversight (Scope Compliance + Abuse Predictor)) ML inference not yet implemented.');
+  // Day-1 operational: delegates to fallback until full ML implementation is deployed.
+  // Full implementation checklist preserved in git history.
+  return runM77aMlFallback(input);
 }
 
 // ── Degraded-mode fallback ────────────────────────────────────────────────────
@@ -228,14 +212,21 @@ export async function runM77aMl(
 export function runM77aMlFallback(
   _input: M77ATelemetryInput,
 ): M77AOutput {
-  // TODO: implement rule-based fallback for M77A
-  // Fallback must:
-  //   □ Return score = 0.5 (neutral / unknown)
-  //   □ Return topFactors = ['ML unavailable — rule-based fallback active']
-  //   □ Return recommendation = 'See rule engine output'
-  //   □ Compute deterministic auditHash from input seed + 'fallback'
-  //   □ Zero-out all M77A-specific extended outputs
-  throw new Error('M77A fallback not yet implemented.');
+  const seed = String(((_input as unknown) as Record<string, unknown>).runSeed ?? '');
+  const tick = Number(((_input as unknown) as Record<string, unknown>).tickIndex ?? 0);
+  const auditHash = createHash('sha256')
+    .update(seed + ':' + tick + ':fallback:M77A')
+    .digest('hex');
+  return {
+    score: 0.5,
+    topFactors: ['ML unavailable — rule-based fallback active'],
+    recommendation: 'See rule engine output',
+    auditHash,
+    scopeComplianceScore: null,
+    abusePrediction: null,
+    delegationReceipt: null,
+    interventionRecommendation: null,
+  };
 }
 
 // ── IntelligenceState integration note ───────────────────────────────────────
diff --git a/pzo_engine/src/ml/m78a_collateral_call_stress_forecaster_top_up_likelihood_def.ts b/pzo_engine/src/ml/m78a_collateral_call_stress_forecaster_top_up_likelihood_def.ts
index 101cd641..7b7520be 100644
--- a/pzo_engine/src/ml/m78a_collateral_call_stress_forecaster_top_up_likelihood_def.ts
+++ b/pzo_engine/src/ml/m78a_collateral_call_stress_forecaster_top_up_likelihood_def.ts
@@ -22,6 +22,8 @@
 // Density6 LLC · Point Zero One · Confidential · All Rights Reserved
 // ═══════════════════════════════════════════════════════════════════════════════
 
+import { createHash } from 'node:crypto';
+
 // ── What this adds ────────────────────────────────────────────────────────────
 /**
  * M78A — Collateral Call Stress Forecaster (Top-Up Likelihood + Default Path)
@@ -196,27 +198,9 @@ export async function runM78aMl(
   tier:      M78ATier = 'baseline',
   modelCard: Omit<M78AModelCard, 'modelId' | 'coreMechanicPair'>,
 ): Promise<M78AOutput> {
-  // ── TODO: implement M78A — Collateral Call Stress Forecaster (Top-Up Likelihood + Default Path) ─────────────────────────────────
-  //
-  // Implementation checklist:
-  // □ Validate input schema against featureSchemaHash
-  // □ Select inference backend based on `tier` parameter
-  // □ tier === 'baseline' → GBM + calibrated logistic (fast, low-cost, production default)
-  // □ tier === 'sequence_dl' → TCN / Transformer encoder over event streams (sequential patterns)
-  // □ tier === 'policy_rl' → Constrained contextual bandit / offline PPO (bounded nudges)
-  // □ Apply input privacy filters (no PII, no cross-player contact graph)
-  // □ Run inference → raw score + top_factors
-  // □ Apply output caps: score = Math.min(score, M78A_ML_CONSTANTS.GUARDRAILS.scoreCap)
-  // □ Apply monotonic constraints where relevant
-  // □ Abstain if confidence < M78A_ML_CONSTANTS.GUARDRAILS.abstainThreshold
-  // □ Compute auditHash = SHA256(inputs + outputs + ruleset_version + caps)
-  // □ Write signed receipt to run ledger (NEVER skip)
-  // □ Return M78AOutput — NEVER mutate run state directly
-  //
-  // Placement: server | Budget: real_time
-  // ExecHook:  after_m78_resolve
-  // ─────────────────────────────────────────────────────────────────────────
-  throw new Error('M78A (Collateral Call Stress Forecaster (Top-Up Likelihood + Default Path)) ML inference not yet implemented.');
+  // Day-1 operational: delegates to fallback until full ML implementation is deployed.
+  // Full implementation checklist preserved in git history.
+  return runM78aMlFallback(input);
 }
 
 // ── Degraded-mode fallback ────────────────────────────────────────────────────
@@ -228,14 +212,21 @@ export async function runM78aMl(
 export function runM78aMlFallback(
   _input: M78ATelemetryInput,
 ): M78AOutput {
-  // TODO: implement rule-based fallback for M78A
-  // Fallback must:
-  //   □ Return score = 0.5 (neutral / unknown)
-  //   □ Return topFactors = ['ML unavailable — rule-based fallback active']
-  //   □ Return recommendation = 'See rule engine output'
-  //   □ Compute deterministic auditHash from input seed + 'fallback'
-  //   □ Zero-out all M78A-specific extended outputs
-  throw new Error('M78A fallback not yet implemented.');
+  const seed = String(((_input as unknown) as Record<string, unknown>).runSeed ?? '');
+  const tick = Number(((_input as unknown) as Record<string, unknown>).tickIndex ?? 0);
+  const auditHash = createHash('sha256')
+    .update(seed + ':' + tick + ':fallback:M78A')
+    .digest('hex');
+  return {
+    score: 0.5,
+    topFactors: ['ML unavailable — rule-based fallback active'],
+    recommendation: 'See rule engine output',
+    auditHash,
+    topupProbability: null,
+    defaultPathScore: null,
+    marginCallTiming: null,
+    stressScenarioResults: null,
+  };
 }
 
 // ── IntelligenceState integration note ───────────────────────────────────────
diff --git a/pzo_engine/src/ml/m79a_shared_objective_bond_success_model_coordination_sabota.ts b/pzo_engine/src/ml/m79a_shared_objective_bond_success_model_coordination_sabota.ts
index 50e73668..85e97c7e 100644
--- a/pzo_engine/src/ml/m79a_shared_objective_bond_success_model_coordination_sabota.ts
+++ b/pzo_engine/src/ml/m79a_shared_objective_bond_success_model_coordination_sabota.ts
@@ -22,6 +22,8 @@
 // Density6 LLC · Point Zero One · Confidential · All Rights Reserved
 // ═══════════════════════════════════════════════════════════════════════════════
 
+import { createHash } from 'node:crypto';
+
 // ── What this adds ────────────────────────────────────────────────────────────
 /**
  * M79A — Shared Objective Bond Success Model (Coordination + Sabotage Resistance)
@@ -196,27 +198,9 @@ export async function runM79aMl(
   tier:      M79ATier = 'baseline',
   modelCard: Omit<M79AModelCard, 'modelId' | 'coreMechanicPair'>,
 ): Promise<M79AOutput> {
-  // ── TODO: implement M79A — Shared Objective Bond Success Model (Coordination + Sabotage Resistance) ─────────────────────────────────
-  //
-  // Implementation checklist:
-  // □ Validate input schema against featureSchemaHash
-  // □ Select inference backend based on `tier` parameter
-  // □ tier === 'baseline' → GBM + calibrated logistic (fast, low-cost, production default)
-  // □ tier === 'sequence_dl' → TCN / Transformer encoder over event streams (sequential patterns)
-  // □ tier === 'policy_rl' → Constrained contextual bandit / offline PPO (bounded nudges)
-  // □ Apply input privacy filters (no PII, no cross-player contact graph)
-  // □ Run inference → raw score + top_factors
-  // □ Apply output caps: score = Math.min(score, M79A_ML_CONSTANTS.GUARDRAILS.scoreCap)
-  // □ Apply monotonic constraints where relevant
-  // □ Abstain if confidence < M79A_ML_CONSTANTS.GUARDRAILS.abstainThreshold
-  // □ Compute auditHash = SHA256(inputs + outputs + ruleset_version + caps)
-  // □ Write signed receipt to run ledger (NEVER skip)
-  // □ Return M79AOutput — NEVER mutate run state directly
-  //
-  // Placement: server | Budget: real_time
-  // ExecHook:  after_m79_resolve
-  // ─────────────────────────────────────────────────────────────────────────
-  throw new Error('M79A (Shared Objective Bond Success Model (Coordination + Sabotage Resistance)) ML inference not yet implemented.');
+  // Day-1 operational: delegates to fallback until full ML implementation is deployed.
+  // Full implementation checklist preserved in git history.
+  return runM79aMlFallback(input);
 }
 
 // ── Degraded-mode fallback ────────────────────────────────────────────────────
@@ -228,14 +212,21 @@ export async function runM79aMl(
 export function runM79aMlFallback(
   _input: M79ATelemetryInput,
 ): M79AOutput {
-  // TODO: implement rule-based fallback for M79A
-  // Fallback must:
-  //   □ Return score = 0.5 (neutral / unknown)
-  //   □ Return topFactors = ['ML unavailable — rule-based fallback active']
-  //   □ Return recommendation = 'See rule engine output'
-  //   □ Compute deterministic auditHash from input seed + 'fallback'
-  //   □ Zero-out all M79A-specific extended outputs
-  throw new Error('M79A fallback not yet implemented.');
+  const seed = String(((_input as unknown) as Record<string, unknown>).runSeed ?? '');
+  const tick = Number(((_input as unknown) as Record<string, unknown>).tickIndex ?? 0);
+  const auditHash = createHash('sha256')
+    .update(seed + ':' + tick + ':fallback:M79A')
+    .digest('hex');
+  return {
+    score: 0.5,
+    topFactors: ['ML unavailable — rule-based fallback active'],
+    recommendation: 'See rule engine output',
+    auditHash,
+    bondSuccessProbability: null,
+    coordinationFailureFlag: null,
+    sabotageFlag: null,
+    interventionWindow: null,
+  };
 }
 
 // ── IntelligenceState integration note ───────────────────────────────────────
diff --git a/pzo_engine/src/ml/m80a_contract_receipt_card_generator_proof_compression_reada.ts b/pzo_engine/src/ml/m80a_contract_receipt_card_generator_proof_compression_reada.ts
index c5cd01fa..2832717d 100644
--- a/pzo_engine/src/ml/m80a_contract_receipt_card_generator_proof_compression_reada.ts
+++ b/pzo_engine/src/ml/m80a_contract_receipt_card_generator_proof_compression_reada.ts
@@ -22,6 +22,8 @@
 // Density6 LLC · Point Zero One · Confidential · All Rights Reserved
 // ═══════════════════════════════════════════════════════════════════════════════
 
+import { createHash } from 'node:crypto';
+
 // ── What this adds ────────────────────────────────────────────────────────────
 /**
  * M80A — Contract Receipt Card Generator (Proof Compression + Readability)
@@ -186,26 +188,9 @@ export async function runM80aMl(
   tier:      M80ATier = 'baseline',
   modelCard: Omit<M80AModelCard, 'modelId' | 'coreMechanicPair'>,
 ): Promise<M80AOutput> {
-  // ── TODO: implement M80A — Contract Receipt Card Generator (Proof Compression + Readability) ─────────────────────────────────
-  //
-  // Implementation checklist:
-  // □ Validate input schema against featureSchemaHash
-  // □ Select inference backend based on `tier` parameter
-  // □ tier === 'baseline' → GBM + calibrated logistic (fast, low-cost, production default)
-  // □ tier === 'sequence_dl' → TCN / Transformer encoder over event streams (sequential patterns)
-  // □ Apply input privacy filters (no PII, no cross-player contact graph)
-  // □ Run inference → raw score + top_factors
-  // □ Apply output caps: score = Math.min(score, M80A_ML_CONSTANTS.GUARDRAILS.scoreCap)
-  // □ Apply monotonic constraints where relevant
-  // □ Abstain if confidence < M80A_ML_CONSTANTS.GUARDRAILS.abstainThreshold
-  // □ Compute auditHash = SHA256(inputs + outputs + ruleset_version + caps)
-  // □ Write signed receipt to run ledger (NEVER skip)
-  // □ Return M80AOutput — NEVER mutate run state directly
-  //
-  // Placement: server | Budget: batch
-  // ExecHook:  after_m80_resolve
-  // ─────────────────────────────────────────────────────────────────────────
-  throw new Error('M80A (Contract Receipt Card Generator (Proof Compression + Readability)) ML inference not yet implemented.');
+  // Day-1 operational: delegates to fallback until full ML implementation is deployed.
+  // Full implementation checklist preserved in git history.
+  return runM80aMlFallback(input);
 }
 
 // ── Degraded-mode fallback ────────────────────────────────────────────────────
@@ -217,14 +202,21 @@ export async function runM80aMl(
 export function runM80aMlFallback(
   _input: M80ATelemetryInput,
 ): M80AOutput {
-  // TODO: implement rule-based fallback for M80A
-  // Fallback must:
-  //   □ Return score = 0.5 (neutral / unknown)
-  //   □ Return topFactors = ['ML unavailable — rule-based fallback active']
-  //   □ Return recommendation = 'See rule engine output'
-  //   □ Compute deterministic auditHash from input seed + 'fallback'
-  //   □ Zero-out all M80A-specific extended outputs
-  throw new Error('M80A fallback not yet implemented.');
+  const seed = String(((_input as unknown) as Record<string, unknown>).runSeed ?? '');
+  const tick = Number(((_input as unknown) as Record<string, unknown>).tickIndex ?? 0);
+  const auditHash = createHash('sha256')
+    .update(seed + ':' + tick + ':fallback:M80A')
+    .digest('hex');
+  return {
+    score: 0.5,
+    topFactors: ['ML unavailable — rule-based fallback active'],
+    recommendation: 'See rule engine output',
+    auditHash,
+    receiptCard: null,
+    compressedSummary: null,
+    keyDecisions: null,
+    proofHashBound: null,
+  };
 }
 
 // ── IntelligenceState integration note ───────────────────────────────────────
diff --git a/pzo_engine/src/ml/m81a_synergy_tree_branch_recommender_identity_routing_non_de.ts b/pzo_engine/src/ml/m81a_synergy_tree_branch_recommender_identity_routing_non_de.ts
index a75592bf..6b243ee0 100644
--- a/pzo_engine/src/ml/m81a_synergy_tree_branch_recommender_identity_routing_non_de.ts
+++ b/pzo_engine/src/ml/m81a_synergy_tree_branch_recommender_identity_routing_non_de.ts
@@ -22,6 +22,8 @@
 // Density6 LLC · Point Zero One · Confidential · All Rights Reserved
 // ═══════════════════════════════════════════════════════════════════════════════
 
+import { createHash } from 'node:crypto';
+
 // ── What this adds ────────────────────────────────────────────────────────────
 /**
  * M81A — Synergy Tree Branch Recommender (Identity Routing + Non-Degenerate Paths)
@@ -198,27 +200,9 @@ export async function runM81aMl(
   tier:      M81ATier = 'baseline',
   modelCard: Omit<M81AModelCard, 'modelId' | 'coreMechanicPair'>,
 ): Promise<M81AOutput> {
-  // ── TODO: implement M81A — Synergy Tree Branch Recommender (Identity Routing + Non-Degenerate Paths) ─────────────────────────────────
-  //
-  // Implementation checklist:
-  // □ Validate input schema against featureSchemaHash
-  // □ Select inference backend based on `tier` parameter
-  // □ tier === 'baseline' → GBM + calibrated logistic (fast, low-cost, production default)
-  // □ tier === 'sequence_dl' → TCN / Transformer encoder over event streams (sequential patterns)
-  // □ tier === 'policy_rl' → Constrained contextual bandit / offline PPO (bounded nudges)
-  // □ Apply input privacy filters (no PII, no cross-player contact graph)
-  // □ Run inference → raw score + top_factors
-  // □ Apply output caps: score = Math.min(score, M81A_ML_CONSTANTS.GUARDRAILS.scoreCap)
-  // □ Apply monotonic constraints where relevant
-  // □ Abstain if confidence < M81A_ML_CONSTANTS.GUARDRAILS.abstainThreshold
-  // □ Compute auditHash = SHA256(inputs + outputs + ruleset_version + caps)
-  // □ Write signed receipt to run ledger (NEVER skip)
-  // □ Return M81AOutput — NEVER mutate run state directly
-  //
-  // Placement: client, server | Budget: real_time
-  // ExecHook:  after_m81_resolve
-  // ─────────────────────────────────────────────────────────────────────────
-  throw new Error('M81A (Synergy Tree Branch Recommender (Identity Routing + Non-Degenerate Paths)) ML inference not yet implemented.');
+  // Day-1 operational: delegates to fallback until full ML implementation is deployed.
+  // Full implementation checklist preserved in git history.
+  return runM81aMlFallback(input);
 }
 
 // ── Degraded-mode fallback ────────────────────────────────────────────────────
@@ -230,14 +214,21 @@ export async function runM81aMl(
 export function runM81aMlFallback(
   _input: M81ATelemetryInput,
 ): M81AOutput {
-  // TODO: implement rule-based fallback for M81A
-  // Fallback must:
-  //   □ Return score = 0.5 (neutral / unknown)
-  //   □ Return topFactors = ['ML unavailable — rule-based fallback active']
-  //   □ Return recommendation = 'See rule engine output'
-  //   □ Compute deterministic auditHash from input seed + 'fallback'
-  //   □ Zero-out all M81A-specific extended outputs
-  throw new Error('M81A fallback not yet implemented.');
+  const seed = String(((_input as unknown) as Record<string, unknown>).runSeed ?? '');
+  const tick = Number(((_input as unknown) as Record<string, unknown>).tickIndex ?? 0);
+  const auditHash = createHash('sha256')
+    .update(seed + ':' + tick + ':fallback:M81A')
+    .digest('hex');
+  return {
+    score: 0.5,
+    topFactors: ['ML unavailable — rule-based fallback active'],
+    recommendation: 'See rule engine output',
+    auditHash,
+    branchRecommendation: null,
+    degeneratePathFlag: null,
+    noveltyScore: null,
+    identityMatch: null,
+  };
 }
 
 // ── IntelligenceState integration note ───────────────────────────────────────
diff --git a/pzo_engine/src/ml/m82a_timing_chain_recognizer_sequence_dl_window_coach.ts b/pzo_engine/src/ml/m82a_timing_chain_recognizer_sequence_dl_window_coach.ts
index 613c7314..2cf0000c 100644
--- a/pzo_engine/src/ml/m82a_timing_chain_recognizer_sequence_dl_window_coach.ts
+++ b/pzo_engine/src/ml/m82a_timing_chain_recognizer_sequence_dl_window_coach.ts
@@ -22,6 +22,8 @@
 // Density6 LLC · Point Zero One · Confidential · All Rights Reserved
 // ═══════════════════════════════════════════════════════════════════════════════
 
+import { createHash } from 'node:crypto';
+
 // ── What this adds ────────────────────────────────────────────────────────────
 /**
  * M82A — Timing Chain Recognizer (Sequence DL + Window Coach)
@@ -198,27 +200,9 @@ export async function runM82aMl(
   tier:      M82ATier = 'baseline',
   modelCard: Omit<M82AModelCard, 'modelId' | 'coreMechanicPair'>,
 ): Promise<M82AOutput> {
-  // ── TODO: implement M82A — Timing Chain Recognizer (Sequence DL + Window Coach) ─────────────────────────────────
-  //
-  // Implementation checklist:
-  // □ Validate input schema against featureSchemaHash
-  // □ Select inference backend based on `tier` parameter
-  // □ tier === 'baseline' → GBM + calibrated logistic (fast, low-cost, production default)
-  // □ tier === 'sequence_dl' → TCN / Transformer encoder over event streams (sequential patterns)
-  // □ tier === 'policy_rl' → Constrained contextual bandit / offline PPO (bounded nudges)
-  // □ Apply input privacy filters (no PII, no cross-player contact graph)
-  // □ Run inference → raw score + top_factors
-  // □ Apply output caps: score = Math.min(score, M82A_ML_CONSTANTS.GUARDRAILS.scoreCap)
-  // □ Apply monotonic constraints where relevant
-  // □ Abstain if confidence < M82A_ML_CONSTANTS.GUARDRAILS.abstainThreshold
-  // □ Compute auditHash = SHA256(inputs + outputs + ruleset_version + caps)
-  // □ Write signed receipt to run ledger (NEVER skip)
-  // □ Return M82AOutput — NEVER mutate run state directly
-  //
-  // Placement: client, server | Budget: real_time
-  // ExecHook:  after_m82_resolve
-  // ─────────────────────────────────────────────────────────────────────────
-  throw new Error('M82A (Timing Chain Recognizer (Sequence DL + Window Coach)) ML inference not yet implemented.');
+  // Day-1 operational: delegates to fallback until full ML implementation is deployed.
+  // Full implementation checklist preserved in git history.
+  return runM82aMlFallback(input);
 }
 
 // ── Degraded-mode fallback ────────────────────────────────────────────────────
@@ -230,14 +214,21 @@ export async function runM82aMl(
 export function runM82aMlFallback(
   _input: M82ATelemetryInput,
 ): M82AOutput {
-  // TODO: implement rule-based fallback for M82A
-  // Fallback must:
-  //   □ Return score = 0.5 (neutral / unknown)
-  //   □ Return topFactors = ['ML unavailable — rule-based fallback active']
-  //   □ Return recommendation = 'See rule engine output'
-  //   □ Compute deterministic auditHash from input seed + 'fallback'
-  //   □ Zero-out all M82A-specific extended outputs
-  throw new Error('M82A fallback not yet implemented.');
+  const seed = String(((_input as unknown) as Record<string, unknown>).runSeed ?? '');
+  const tick = Number(((_input as unknown) as Record<string, unknown>).tickIndex ?? 0);
+  const auditHash = createHash('sha256')
+    .update(seed + ':' + tick + ':fallback:M82A')
+    .digest('hex');
+  return {
+    score: 0.5,
+    topFactors: ['ML unavailable — rule-based fallback active'],
+    recommendation: 'See rule engine output',
+    auditHash,
+    chainRecognitionSignal: null,
+    windowCoachingHint: null,
+    abuseFlag: null,
+    executionScore: null,
+  };
 }
 
 // ── IntelligenceState integration note ───────────────────────────────────────
diff --git a/pzo_engine/src/ml/m83a_risk_parity_dial_optimizer_constraint_aware_exposure_re.ts b/pzo_engine/src/ml/m83a_risk_parity_dial_optimizer_constraint_aware_exposure_re.ts
index 7fdcf74b..0aae6c6d 100644
--- a/pzo_engine/src/ml/m83a_risk_parity_dial_optimizer_constraint_aware_exposure_re.ts
+++ b/pzo_engine/src/ml/m83a_risk_parity_dial_optimizer_constraint_aware_exposure_re.ts
@@ -22,6 +22,8 @@
 // Density6 LLC · Point Zero One · Confidential · All Rights Reserved
 // ═══════════════════════════════════════════════════════════════════════════════
 
+import { createHash } from 'node:crypto';
+
 // ── What this adds ────────────────────────────────────────────────────────────
 /**
  * M83A — Risk Parity Dial Optimizer (Constraint-Aware Exposure Rebalancer)
@@ -198,27 +200,9 @@ export async function runM83aMl(
   tier:      M83ATier = 'baseline',
   modelCard: Omit<M83AModelCard, 'modelId' | 'coreMechanicPair'>,
 ): Promise<M83AOutput> {
-  // ── TODO: implement M83A — Risk Parity Dial Optimizer (Constraint-Aware Exposure Rebalancer) ─────────────────────────────────
-  //
-  // Implementation checklist:
-  // □ Validate input schema against featureSchemaHash
-  // □ Select inference backend based on `tier` parameter
-  // □ tier === 'baseline' → GBM + calibrated logistic (fast, low-cost, production default)
-  // □ tier === 'sequence_dl' → TCN / Transformer encoder over event streams (sequential patterns)
-  // □ tier === 'policy_rl' → Constrained contextual bandit / offline PPO (bounded nudges)
-  // □ Apply input privacy filters (no PII, no cross-player contact graph)
-  // □ Run inference → raw score + top_factors
-  // □ Apply output caps: score = Math.min(score, M83A_ML_CONSTANTS.GUARDRAILS.scoreCap)
-  // □ Apply monotonic constraints where relevant
-  // □ Abstain if confidence < M83A_ML_CONSTANTS.GUARDRAILS.abstainThreshold
-  // □ Compute auditHash = SHA256(inputs + outputs + ruleset_version + caps)
-  // □ Write signed receipt to run ledger (NEVER skip)
-  // □ Return M83AOutput — NEVER mutate run state directly
-  //
-  // Placement: client, server | Budget: real_time
-  // ExecHook:  after_m83_resolve
-  // ─────────────────────────────────────────────────────────────────────────
-  throw new Error('M83A (Risk Parity Dial Optimizer (Constraint-Aware Exposure Rebalancer)) ML inference not yet implemented.');
+  // Day-1 operational: delegates to fallback until full ML implementation is deployed.
+  // Full implementation checklist preserved in git history.
+  return runM83aMlFallback(input);
 }
 
 // ── Degraded-mode fallback ────────────────────────────────────────────────────
@@ -230,14 +214,21 @@ export async function runM83aMl(
 export function runM83aMlFallback(
   _input: M83ATelemetryInput,
 ): M83AOutput {
-  // TODO: implement rule-based fallback for M83A
-  // Fallback must:
-  //   □ Return score = 0.5 (neutral / unknown)
-  //   □ Return topFactors = ['ML unavailable — rule-based fallback active']
-  //   □ Return recommendation = 'See rule engine output'
-  //   □ Compute deterministic auditHash from input seed + 'fallback'
-  //   □ Zero-out all M83A-specific extended outputs
-  throw new Error('M83A fallback not yet implemented.');
+  const seed = String(((_input as unknown) as Record<string, unknown>).runSeed ?? '');
+  const tick = Number(((_input as unknown) as Record<string, unknown>).tickIndex ?? 0);
+  const auditHash = createHash('sha256')
+    .update(seed + ':' + tick + ':fallback:M83A')
+    .digest('hex');
+  return {
+    score: 0.5,
+    topFactors: ['ML unavailable — rule-based fallback active'],
+    recommendation: 'See rule engine output',
+    auditHash,
+    dialOptimization: null,
+    rebalanceSuggestions: null,
+    instabilityFlag: null,
+    constraintCompliance: null,
+  };
 }
 
 // ── IntelligenceState integration note ───────────────────────────────────────
diff --git a/pzo_engine/src/ml/m84a_catalyst_pairing_compatibility_model_bridge_safety_dege.ts b/pzo_engine/src/ml/m84a_catalyst_pairing_compatibility_model_bridge_safety_dege.ts
index a27c4ab4..6c4e9492 100644
--- a/pzo_engine/src/ml/m84a_catalyst_pairing_compatibility_model_bridge_safety_dege.ts
+++ b/pzo_engine/src/ml/m84a_catalyst_pairing_compatibility_model_bridge_safety_dege.ts
@@ -22,6 +22,8 @@
 // Density6 LLC · Point Zero One · Confidential · All Rights Reserved
 // ═══════════════════════════════════════════════════════════════════════════════
 
+import { createHash } from 'node:crypto';
+
 // ── What this adds ────────────────────────────────────────────────────────────
 /**
  * M84A — Catalyst Pairing Compatibility Model (Bridge Safety + Degeneracy Watch)
@@ -196,27 +198,9 @@ export async function runM84aMl(
   tier:      M84ATier = 'baseline',
   modelCard: Omit<M84AModelCard, 'modelId' | 'coreMechanicPair'>,
 ): Promise<M84AOutput> {
-  // ── TODO: implement M84A — Catalyst Pairing Compatibility Model (Bridge Safety + Degeneracy Watch) ─────────────────────────────────
-  //
-  // Implementation checklist:
-  // □ Validate input schema against featureSchemaHash
-  // □ Select inference backend based on `tier` parameter
-  // □ tier === 'baseline' → GBM + calibrated logistic (fast, low-cost, production default)
-  // □ tier === 'sequence_dl' → TCN / Transformer encoder over event streams (sequential patterns)
-  // □ tier === 'policy_rl' → Constrained contextual bandit / offline PPO (bounded nudges)
-  // □ Apply input privacy filters (no PII, no cross-player contact graph)
-  // □ Run inference → raw score + top_factors
-  // □ Apply output caps: score = Math.min(score, M84A_ML_CONSTANTS.GUARDRAILS.scoreCap)
-  // □ Apply monotonic constraints where relevant
-  // □ Abstain if confidence < M84A_ML_CONSTANTS.GUARDRAILS.abstainThreshold
-  // □ Compute auditHash = SHA256(inputs + outputs + ruleset_version + caps)
-  // □ Write signed receipt to run ledger (NEVER skip)
-  // □ Return M84AOutput — NEVER mutate run state directly
-  //
-  // Placement: server | Budget: real_time
-  // ExecHook:  after_m84_resolve
-  // ─────────────────────────────────────────────────────────────────────────
-  throw new Error('M84A (Catalyst Pairing Compatibility Model (Bridge Safety + Degeneracy Watch)) ML inference not yet implemented.');
+  // Day-1 operational: delegates to fallback until full ML implementation is deployed.
+  // Full implementation checklist preserved in git history.
+  return runM84aMlFallback(input);
 }
 
 // ── Degraded-mode fallback ────────────────────────────────────────────────────
@@ -228,14 +212,21 @@ export async function runM84aMl(
 export function runM84aMlFallback(
   _input: M84ATelemetryInput,
 ): M84AOutput {
-  // TODO: implement rule-based fallback for M84A
-  // Fallback must:
-  //   □ Return score = 0.5 (neutral / unknown)
-  //   □ Return topFactors = ['ML unavailable — rule-based fallback active']
-  //   □ Return recommendation = 'See rule engine output'
-  //   □ Compute deterministic auditHash from input seed + 'fallback'
-  //   □ Zero-out all M84A-specific extended outputs
-  throw new Error('M84A fallback not yet implemented.');
+  const seed = String(((_input as unknown) as Record<string, unknown>).runSeed ?? '');
+  const tick = Number(((_input as unknown) as Record<string, unknown>).tickIndex ?? 0);
+  const auditHash = createHash('sha256')
+    .update(seed + ':' + tick + ':fallback:M84A')
+    .digest('hex');
+  return {
+    score: 0.5,
+    topFactors: ['ML unavailable — rule-based fallback active'],
+    recommendation: 'See rule engine output',
+    auditHash,
+    compatibilityScore: null,
+    degeneracyFlag: null,
+    designReviewReceipt: null,
+    safeBridgeRecommendation: null,
+  };
 }
 
 // ── IntelligenceState integration note ───────────────────────────────────────
diff --git a/pzo_engine/src/ml/m85a_mutation_draft_planner_rewrite_selection_collapse_risk.ts b/pzo_engine/src/ml/m85a_mutation_draft_planner_rewrite_selection_collapse_risk.ts
index e38f9301..093a575a 100644
--- a/pzo_engine/src/ml/m85a_mutation_draft_planner_rewrite_selection_collapse_risk.ts
+++ b/pzo_engine/src/ml/m85a_mutation_draft_planner_rewrite_selection_collapse_risk.ts
@@ -22,6 +22,8 @@
 // Density6 LLC · Point Zero One · Confidential · All Rights Reserved
 // ═══════════════════════════════════════════════════════════════════════════════
 
+import { createHash } from 'node:crypto';
+
 // ── What this adds ────────────────────────────────────────────────────────────
 /**
  * M85A — Mutation Draft Planner (Rewrite Selection + Collapse Risk Scorer)
@@ -198,27 +200,9 @@ export async function runM85aMl(
   tier:      M85ATier = 'baseline',
   modelCard: Omit<M85AModelCard, 'modelId' | 'coreMechanicPair'>,
 ): Promise<M85AOutput> {
-  // ── TODO: implement M85A — Mutation Draft Planner (Rewrite Selection + Collapse Risk Scorer) ─────────────────────────────────
-  //
-  // Implementation checklist:
-  // □ Validate input schema against featureSchemaHash
-  // □ Select inference backend based on `tier` parameter
-  // □ tier === 'baseline' → GBM + calibrated logistic (fast, low-cost, production default)
-  // □ tier === 'sequence_dl' → TCN / Transformer encoder over event streams (sequential patterns)
-  // □ tier === 'policy_rl' → Constrained contextual bandit / offline PPO (bounded nudges)
-  // □ Apply input privacy filters (no PII, no cross-player contact graph)
-  // □ Run inference → raw score + top_factors
-  // □ Apply output caps: score = Math.min(score, M85A_ML_CONSTANTS.GUARDRAILS.scoreCap)
-  // □ Apply monotonic constraints where relevant
-  // □ Abstain if confidence < M85A_ML_CONSTANTS.GUARDRAILS.abstainThreshold
-  // □ Compute auditHash = SHA256(inputs + outputs + ruleset_version + caps)
-  // □ Write signed receipt to run ledger (NEVER skip)
-  // □ Return M85AOutput — NEVER mutate run state directly
-  //
-  // Placement: client, server | Budget: real_time
-  // ExecHook:  after_m85_resolve
-  // ─────────────────────────────────────────────────────────────────────────
-  throw new Error('M85A (Mutation Draft Planner (Rewrite Selection + Collapse Risk Scorer)) ML inference not yet implemented.');
+  // Day-1 operational: delegates to fallback until full ML implementation is deployed.
+  // Full implementation checklist preserved in git history.
+  return runM85aMlFallback(input);
 }
 
 // ── Degraded-mode fallback ────────────────────────────────────────────────────
@@ -230,14 +214,21 @@ export async function runM85aMl(
 export function runM85aMlFallback(
   _input: M85ATelemetryInput,
 ): M85AOutput {
-  // TODO: implement rule-based fallback for M85A
-  // Fallback must:
-  //   □ Return score = 0.5 (neutral / unknown)
-  //   □ Return topFactors = ['ML unavailable — rule-based fallback active']
-  //   □ Return recommendation = 'See rule engine output'
-  //   □ Compute deterministic auditHash from input seed + 'fallback'
-  //   □ Zero-out all M85A-specific extended outputs
-  throw new Error('M85A fallback not yet implemented.');
+  const seed = String(((_input as unknown) as Record<string, unknown>).runSeed ?? '');
+  const tick = Number(((_input as unknown) as Record<string, unknown>).tickIndex ?? 0);
+  const auditHash = createHash('sha256')
+    .update(seed + ':' + tick + ':fallback:M85A')
+    .digest('hex');
+  return {
+    score: 0.5,
+    topFactors: ['ML unavailable — rule-based fallback active'],
+    recommendation: 'See rule engine output',
+    auditHash,
+    draftPlan: null,
+    collapseRiskScores: null,
+    cordUpsideEstimate: null,
+    fragilityFlag: null,
+  };
 }
 
 // ── IntelligenceState integration note ───────────────────────────────────────
diff --git a/pzo_engine/src/ml/m86a_micro_proof_moment_detector_fine_grain_highlight_miner.ts b/pzo_engine/src/ml/m86a_micro_proof_moment_detector_fine_grain_highlight_miner.ts
index 87b307e9..293b8665 100644
--- a/pzo_engine/src/ml/m86a_micro_proof_moment_detector_fine_grain_highlight_miner.ts
+++ b/pzo_engine/src/ml/m86a_micro_proof_moment_detector_fine_grain_highlight_miner.ts
@@ -22,6 +22,8 @@
 // Density6 LLC · Point Zero One · Confidential · All Rights Reserved
 // ═══════════════════════════════════════════════════════════════════════════════
 
+import { createHash } from 'node:crypto';
+
 // ── What this adds ────────────────────────────────────────────────────────────
 /**
  * M86A — Micro-Proof Moment Detector (Fine-Grain Highlight Miner)
@@ -198,27 +200,9 @@ export async function runM86aMl(
   tier:      M86ATier = 'baseline',
   modelCard: Omit<M86AModelCard, 'modelId' | 'coreMechanicPair'>,
 ): Promise<M86AOutput> {
-  // ── TODO: implement M86A — Micro-Proof Moment Detector (Fine-Grain Highlight Miner) ─────────────────────────────────
-  //
-  // Implementation checklist:
-  // □ Validate input schema against featureSchemaHash
-  // □ Select inference backend based on `tier` parameter
-  // □ tier === 'baseline' → GBM + calibrated logistic (fast, low-cost, production default)
-  // □ tier === 'sequence_dl' → TCN / Transformer encoder over event streams (sequential patterns)
-  // □ tier === 'policy_rl' → Constrained contextual bandit / offline PPO (bounded nudges)
-  // □ Apply input privacy filters (no PII, no cross-player contact graph)
-  // □ Run inference → raw score + top_factors
-  // □ Apply output caps: score = Math.min(score, M86A_ML_CONSTANTS.GUARDRAILS.scoreCap)
-  // □ Apply monotonic constraints where relevant
-  // □ Abstain if confidence < M86A_ML_CONSTANTS.GUARDRAILS.abstainThreshold
-  // □ Compute auditHash = SHA256(inputs + outputs + ruleset_version + caps)
-  // □ Write signed receipt to run ledger (NEVER skip)
-  // □ Return M86AOutput — NEVER mutate run state directly
-  //
-  // Placement: client, server | Budget: real_time
-  // ExecHook:  after_m86_resolve
-  // ─────────────────────────────────────────────────────────────────────────
-  throw new Error('M86A (Micro-Proof Moment Detector (Fine-Grain Highlight Miner)) ML inference not yet implemented.');
+  // Day-1 operational: delegates to fallback until full ML implementation is deployed.
+  // Full implementation checklist preserved in git history.
+  return runM86aMlFallback(input);
 }
 
 // ── Degraded-mode fallback ────────────────────────────────────────────────────
@@ -230,14 +214,21 @@ export async function runM86aMl(
 export function runM86aMlFallback(
   _input: M86ATelemetryInput,
 ): M86AOutput {
-  // TODO: implement rule-based fallback for M86A
-  // Fallback must:
-  //   □ Return score = 0.5 (neutral / unknown)
-  //   □ Return topFactors = ['ML unavailable — rule-based fallback active']
-  //   □ Return recommendation = 'See rule engine output'
-  //   □ Compute deterministic auditHash from input seed + 'fallback'
-  //   □ Zero-out all M86A-specific extended outputs
-  throw new Error('M86A fallback not yet implemented.');
+  const seed = String(((_input as unknown) as Record<string, unknown>).runSeed ?? '');
+  const tick = Number(((_input as unknown) as Record<string, unknown>).tickIndex ?? 0);
+  const auditHash = createHash('sha256')
+    .update(seed + ':' + tick + ':fallback:M86A')
+    .digest('hex');
+  return {
+    score: 0.5,
+    topFactors: ['ML unavailable — rule-based fallback active'],
+    recommendation: 'See rule engine output',
+    auditHash,
+    microProofCandidates: null,
+    exceptionalityScore: null,
+    stampRecommendation: null,
+    clipBoundary: null,
+  };
 }
 
 // ── IntelligenceState integration note ───────────────────────────────────────
diff --git a/pzo_engine/src/ml/m87a_season_relic_mint_governor_scarcity_anti_farm_control.ts b/pzo_engine/src/ml/m87a_season_relic_mint_governor_scarcity_anti_farm_control.ts
index bdb5d485..cb5481cb 100644
--- a/pzo_engine/src/ml/m87a_season_relic_mint_governor_scarcity_anti_farm_control.ts
+++ b/pzo_engine/src/ml/m87a_season_relic_mint_governor_scarcity_anti_farm_control.ts
@@ -22,6 +22,8 @@
 // Density6 LLC · Point Zero One · Confidential · All Rights Reserved
 // ═══════════════════════════════════════════════════════════════════════════════
 
+import { createHash } from 'node:crypto';
+
 // ── What this adds ────────────────────────────────────────────────────────────
 /**
  * M87A — Season Relic Mint Governor (Scarcity + Anti-Farm Control)
@@ -196,27 +198,9 @@ export async function runM87aMl(
   tier:      M87ATier = 'baseline',
   modelCard: Omit<M87AModelCard, 'modelId' | 'coreMechanicPair'>,
 ): Promise<M87AOutput> {
-  // ── TODO: implement M87A — Season Relic Mint Governor (Scarcity + Anti-Farm Control) ─────────────────────────────────
-  //
-  // Implementation checklist:
-  // □ Validate input schema against featureSchemaHash
-  // □ Select inference backend based on `tier` parameter
-  // □ tier === 'baseline' → GBM + calibrated logistic (fast, low-cost, production default)
-  // □ tier === 'sequence_dl' → TCN / Transformer encoder over event streams (sequential patterns)
-  // □ tier === 'policy_rl' → Constrained contextual bandit / offline PPO (bounded nudges)
-  // □ Apply input privacy filters (no PII, no cross-player contact graph)
-  // □ Run inference → raw score + top_factors
-  // □ Apply output caps: score = Math.min(score, M87A_ML_CONSTANTS.GUARDRAILS.scoreCap)
-  // □ Apply monotonic constraints where relevant
-  // □ Abstain if confidence < M87A_ML_CONSTANTS.GUARDRAILS.abstainThreshold
-  // □ Compute auditHash = SHA256(inputs + outputs + ruleset_version + caps)
-  // □ Write signed receipt to run ledger (NEVER skip)
-  // □ Return M87AOutput — NEVER mutate run state directly
-  //
-  // Placement: server | Budget: batch
-  // ExecHook:  after_m87_resolve
-  // ─────────────────────────────────────────────────────────────────────────
-  throw new Error('M87A (Season Relic Mint Governor (Scarcity + Anti-Farm Control)) ML inference not yet implemented.');
+  // Day-1 operational: delegates to fallback until full ML implementation is deployed.
+  // Full implementation checklist preserved in git history.
+  return runM87aMlFallback(input);
 }
 
 // ── Degraded-mode fallback ────────────────────────────────────────────────────
@@ -228,14 +212,21 @@ export async function runM87aMl(
 export function runM87aMlFallback(
   _input: M87ATelemetryInput,
 ): M87AOutput {
-  // TODO: implement rule-based fallback for M87A
-  // Fallback must:
-  //   □ Return score = 0.5 (neutral / unknown)
-  //   □ Return topFactors = ['ML unavailable — rule-based fallback active']
-  //   □ Return recommendation = 'See rule engine output'
-  //   □ Compute deterministic auditHash from input seed + 'fallback'
-  //   □ Zero-out all M87A-specific extended outputs
-  throw new Error('M87A fallback not yet implemented.');
+  const seed = String(((_input as unknown) as Record<string, unknown>).runSeed ?? '');
+  const tick = Number(((_input as unknown) as Record<string, unknown>).tickIndex ?? 0);
+  const auditHash = createHash('sha256')
+    .update(seed + ':' + tick + ':fallback:M87A')
+    .digest('hex');
+  return {
+    score: 0.5,
+    topFactors: ['ML unavailable — rule-based fallback active'],
+    recommendation: 'See rule engine output',
+    auditHash,
+    mintApproval: null,
+    farmFlag: null,
+    scarcityScore: null,
+    prestigeProjection: null,
+  };
 }
 
 // ── IntelligenceState integration note ───────────────────────────────────────
diff --git a/pzo_engine/src/ml/m88a_team_title_attribution_model_contribution_eligibility_v.ts b/pzo_engine/src/ml/m88a_team_title_attribution_model_contribution_eligibility_v.ts
index 88933ae1..b76b9cf5 100644
--- a/pzo_engine/src/ml/m88a_team_title_attribution_model_contribution_eligibility_v.ts
+++ b/pzo_engine/src/ml/m88a_team_title_attribution_model_contribution_eligibility_v.ts
@@ -22,6 +22,8 @@
 // Density6 LLC · Point Zero One · Confidential · All Rights Reserved
 // ═══════════════════════════════════════════════════════════════════════════════
 
+import { createHash } from 'node:crypto';
+
 // ── What this adds ────────────────────────────────────────────────────────────
 /**
  * M88A — Team Title Attribution Model (Contribution + Eligibility Verification)
@@ -196,27 +198,9 @@ export async function runM88aMl(
   tier:      M88ATier = 'baseline',
   modelCard: Omit<M88AModelCard, 'modelId' | 'coreMechanicPair'>,
 ): Promise<M88AOutput> {
-  // ── TODO: implement M88A — Team Title Attribution Model (Contribution + Eligibility Verification) ─────────────────────────────────
-  //
-  // Implementation checklist:
-  // □ Validate input schema against featureSchemaHash
-  // □ Select inference backend based on `tier` parameter
-  // □ tier === 'baseline' → GBM + calibrated logistic (fast, low-cost, production default)
-  // □ tier === 'sequence_dl' → TCN / Transformer encoder over event streams (sequential patterns)
-  // □ tier === 'policy_rl' → Constrained contextual bandit / offline PPO (bounded nudges)
-  // □ Apply input privacy filters (no PII, no cross-player contact graph)
-  // □ Run inference → raw score + top_factors
-  // □ Apply output caps: score = Math.min(score, M88A_ML_CONSTANTS.GUARDRAILS.scoreCap)
-  // □ Apply monotonic constraints where relevant
-  // □ Abstain if confidence < M88A_ML_CONSTANTS.GUARDRAILS.abstainThreshold
-  // □ Compute auditHash = SHA256(inputs + outputs + ruleset_version + caps)
-  // □ Write signed receipt to run ledger (NEVER skip)
-  // □ Return M88AOutput — NEVER mutate run state directly
-  //
-  // Placement: server | Budget: batch
-  // ExecHook:  after_m88_resolve
-  // ─────────────────────────────────────────────────────────────────────────
-  throw new Error('M88A (Team Title Attribution Model (Contribution + Eligibility Verification)) ML inference not yet implemented.');
+  // Day-1 operational: delegates to fallback until full ML implementation is deployed.
+  // Full implementation checklist preserved in git history.
+  return runM88aMlFallback(input);
 }
 
 // ── Degraded-mode fallback ────────────────────────────────────────────────────
@@ -228,14 +212,21 @@ export async function runM88aMl(
 export function runM88aMlFallback(
   _input: M88ATelemetryInput,
 ): M88AOutput {
-  // TODO: implement rule-based fallback for M88A
-  // Fallback must:
-  //   □ Return score = 0.5 (neutral / unknown)
-  //   □ Return topFactors = ['ML unavailable — rule-based fallback active']
-  //   □ Return recommendation = 'See rule engine output'
-  //   □ Compute deterministic auditHash from input seed + 'fallback'
-  //   □ Zero-out all M88A-specific extended outputs
-  throw new Error('M88A fallback not yet implemented.');
+  const seed = String(((_input as unknown) as Record<string, unknown>).runSeed ?? '');
+  const tick = Number(((_input as unknown) as Record<string, unknown>).tickIndex ?? 0);
+  const auditHash = createHash('sha256')
+    .update(seed + ':' + tick + ':fallback:M88A')
+    .digest('hex');
+  return {
+    score: 0.5,
+    topFactors: ['ML unavailable — rule-based fallback active'],
+    recommendation: 'See rule engine output',
+    auditHash,
+    eligibilityScore: null,
+    tagalongFlag: null,
+    contributionReceipt: null,
+    titleGrantRecommendation: null,
+  };
 }
 
 // ── IntelligenceState integration note ───────────────────────────────────────
diff --git a/pzo_engine/src/ml/m89a_trust_multiplier_fairness_auditor_integrity_incentives.ts b/pzo_engine/src/ml/m89a_trust_multiplier_fairness_auditor_integrity_incentives.ts
index a2375679..73b2beb5 100644
--- a/pzo_engine/src/ml/m89a_trust_multiplier_fairness_auditor_integrity_incentives.ts
+++ b/pzo_engine/src/ml/m89a_trust_multiplier_fairness_auditor_integrity_incentives.ts
@@ -22,6 +22,8 @@
 // Density6 LLC · Point Zero One · Confidential · All Rights Reserved
 // ═══════════════════════════════════════════════════════════════════════════════
 
+import { createHash } from 'node:crypto';
+
 // ── What this adds ────────────────────────────────────────────────────────────
 /**
  * M89A — Trust Multiplier Fairness Auditor (Integrity Incentives)
@@ -196,27 +198,9 @@ export async function runM89aMl(
   tier:      M89ATier = 'baseline',
   modelCard: Omit<M89AModelCard, 'modelId' | 'coreMechanicPair'>,
 ): Promise<M89AOutput> {
-  // ── TODO: implement M89A — Trust Multiplier Fairness Auditor (Integrity Incentives) ─────────────────────────────────
-  //
-  // Implementation checklist:
-  // □ Validate input schema against featureSchemaHash
-  // □ Select inference backend based on `tier` parameter
-  // □ tier === 'baseline' → GBM + calibrated logistic (fast, low-cost, production default)
-  // □ tier === 'sequence_dl' → TCN / Transformer encoder over event streams (sequential patterns)
-  // □ tier === 'policy_rl' → Constrained contextual bandit / offline PPO (bounded nudges)
-  // □ Apply input privacy filters (no PII, no cross-player contact graph)
-  // □ Run inference → raw score + top_factors
-  // □ Apply output caps: score = Math.min(score, M89A_ML_CONSTANTS.GUARDRAILS.scoreCap)
-  // □ Apply monotonic constraints where relevant
-  // □ Abstain if confidence < M89A_ML_CONSTANTS.GUARDRAILS.abstainThreshold
-  // □ Compute auditHash = SHA256(inputs + outputs + ruleset_version + caps)
-  // □ Write signed receipt to run ledger (NEVER skip)
-  // □ Return M89AOutput — NEVER mutate run state directly
-  //
-  // Placement: server | Budget: batch
-  // ExecHook:  after_m89_resolve
-  // ─────────────────────────────────────────────────────────────────────────
-  throw new Error('M89A (Trust Multiplier Fairness Auditor (Integrity Incentives)) ML inference not yet implemented.');
+  // Day-1 operational: delegates to fallback until full ML implementation is deployed.
+  // Full implementation checklist preserved in git history.
+  return runM89aMlFallback(input);
 }
 
 // ── Degraded-mode fallback ────────────────────────────────────────────────────
@@ -228,14 +212,21 @@ export async function runM89aMl(
 export function runM89aMlFallback(
   _input: M89ATelemetryInput,
 ): M89AOutput {
-  // TODO: implement rule-based fallback for M89A
-  // Fallback must:
-  //   □ Return score = 0.5 (neutral / unknown)
-  //   □ Return topFactors = ['ML unavailable — rule-based fallback active']
-  //   □ Return recommendation = 'See rule engine output'
-  //   □ Compute deterministic auditHash from input seed + 'fallback'
-  //   □ Zero-out all M89A-specific extended outputs
-  throw new Error('M89A fallback not yet implemented.');
+  const seed = String(((_input as unknown) as Record<string, unknown>).runSeed ?? '');
+  const tick = Number(((_input as unknown) as Record<string, unknown>).tickIndex ?? 0);
+  const auditHash = createHash('sha256')
+    .update(seed + ':' + tick + ':fallback:M89A')
+    .digest('hex');
+  return {
+    score: 0.5,
+    topFactors: ['ML unavailable — rule-based fallback active'],
+    recommendation: 'See rule engine output',
+    auditHash,
+    fairnessAuditScore: null,
+    gamingFlag: null,
+    multiplierCorrection: null,
+    auditReceipt: null,
+  };
 }
 
 // ── IntelligenceState integration note ───────────────────────────────────────
diff --git a/pzo_engine/src/ml/m90a_salvage_reroll_economy_stabilizer_loop_detection_sink_a.ts b/pzo_engine/src/ml/m90a_salvage_reroll_economy_stabilizer_loop_detection_sink_a.ts
index 7c798299..8082eee0 100644
--- a/pzo_engine/src/ml/m90a_salvage_reroll_economy_stabilizer_loop_detection_sink_a.ts
+++ b/pzo_engine/src/ml/m90a_salvage_reroll_economy_stabilizer_loop_detection_sink_a.ts
@@ -22,6 +22,8 @@
 // Density6 LLC · Point Zero One · Confidential · All Rights Reserved
 // ═══════════════════════════════════════════════════════════════════════════════
 
+import { createHash } from 'node:crypto';
+
 // ── What this adds ────────────────────────────────────────────────────────────
 /**
  * M90A — Salvage & Reroll Economy Stabilizer (Loop Detection + Sink Adequacy)
@@ -196,27 +198,9 @@ export async function runM90aMl(
   tier:      M90ATier = 'baseline',
   modelCard: Omit<M90AModelCard, 'modelId' | 'coreMechanicPair'>,
 ): Promise<M90AOutput> {
-  // ── TODO: implement M90A — Salvage & Reroll Economy Stabilizer (Loop Detection + Sink Adequacy) ─────────────────────────────────
-  //
-  // Implementation checklist:
-  // □ Validate input schema against featureSchemaHash
-  // □ Select inference backend based on `tier` parameter
-  // □ tier === 'baseline' → GBM + calibrated logistic (fast, low-cost, production default)
-  // □ tier === 'sequence_dl' → TCN / Transformer encoder over event streams (sequential patterns)
-  // □ tier === 'policy_rl' → Constrained contextual bandit / offline PPO (bounded nudges)
-  // □ Apply input privacy filters (no PII, no cross-player contact graph)
-  // □ Run inference → raw score + top_factors
-  // □ Apply output caps: score = Math.min(score, M90A_ML_CONSTANTS.GUARDRAILS.scoreCap)
-  // □ Apply monotonic constraints where relevant
-  // □ Abstain if confidence < M90A_ML_CONSTANTS.GUARDRAILS.abstainThreshold
-  // □ Compute auditHash = SHA256(inputs + outputs + ruleset_version + caps)
-  // □ Write signed receipt to run ledger (NEVER skip)
-  // □ Return M90AOutput — NEVER mutate run state directly
-  //
-  // Placement: server | Budget: batch
-  // ExecHook:  after_m90_resolve
-  // ─────────────────────────────────────────────────────────────────────────
-  throw new Error('M90A (Salvage & Reroll Economy Stabilizer (Loop Detection + Sink Adequacy)) ML inference not yet implemented.');
+  // Day-1 operational: delegates to fallback until full ML implementation is deployed.
+  // Full implementation checklist preserved in git history.
+  return runM90aMlFallback(input);
 }
 
 // ── Degraded-mode fallback ────────────────────────────────────────────────────
@@ -228,14 +212,21 @@ export async function runM90aMl(
 export function runM90aMlFallback(
   _input: M90ATelemetryInput,
 ): M90AOutput {
-  // TODO: implement rule-based fallback for M90A
-  // Fallback must:
-  //   □ Return score = 0.5 (neutral / unknown)
-  //   □ Return topFactors = ['ML unavailable — rule-based fallback active']
-  //   □ Return recommendation = 'See rule engine output'
-  //   □ Compute deterministic auditHash from input seed + 'fallback'
-  //   □ Zero-out all M90A-specific extended outputs
-  throw new Error('M90A fallback not yet implemented.');
+  const seed = String(((_input as unknown) as Record<string, unknown>).runSeed ?? '');
+  const tick = Number(((_input as unknown) as Record<string, unknown>).tickIndex ?? 0);
+  const auditHash = createHash('sha256')
+    .update(seed + ':' + tick + ':fallback:M90A')
+    .digest('hex');
+  return {
+    score: 0.5,
+    topFactors: ['ML unavailable — rule-based fallback active'],
+    recommendation: 'See rule engine output',
+    auditHash,
+    loopDetectionFlag: null,
+    sinkAdequacyScore: null,
+    economyDelta: null,
+    stabilityReport: null,
+  };
 }
 
 // ── IntelligenceState integration note ───────────────────────────────────────
diff --git a/pzo_engine/src/ml/m91a_first_table_safety_model_social_onboarding_abuse_preven.ts b/pzo_engine/src/ml/m91a_first_table_safety_model_social_onboarding_abuse_preven.ts
index f5488598..452ccb3b 100644
--- a/pzo_engine/src/ml/m91a_first_table_safety_model_social_onboarding_abuse_preven.ts
+++ b/pzo_engine/src/ml/m91a_first_table_safety_model_social_onboarding_abuse_preven.ts
@@ -22,6 +22,8 @@
 // Density6 LLC · Point Zero One · Confidential · All Rights Reserved
 // ═══════════════════════════════════════════════════════════════════════════════
 
+import { createHash } from 'node:crypto';
+
 // ── What this adds ────────────────────────────────────────────────────────────
 /**
  * M91A — First-Table Safety Model (Social Onboarding Abuse Preventer)
@@ -196,27 +198,9 @@ export async function runM91aMl(
   tier:      M91ATier = 'baseline',
   modelCard: Omit<M91AModelCard, 'modelId' | 'coreMechanicPair'>,
 ): Promise<M91AOutput> {
-  // ── TODO: implement M91A — First-Table Safety Model (Social Onboarding Abuse Preventer) ─────────────────────────────────
-  //
-  // Implementation checklist:
-  // □ Validate input schema against featureSchemaHash
-  // □ Select inference backend based on `tier` parameter
-  // □ tier === 'baseline' → GBM + calibrated logistic (fast, low-cost, production default)
-  // □ tier === 'sequence_dl' → TCN / Transformer encoder over event streams (sequential patterns)
-  // □ tier === 'policy_rl' → Constrained contextual bandit / offline PPO (bounded nudges)
-  // □ Apply input privacy filters (no PII, no cross-player contact graph)
-  // □ Run inference → raw score + top_factors
-  // □ Apply output caps: score = Math.min(score, M91A_ML_CONSTANTS.GUARDRAILS.scoreCap)
-  // □ Apply monotonic constraints where relevant
-  // □ Abstain if confidence < M91A_ML_CONSTANTS.GUARDRAILS.abstainThreshold
-  // □ Compute auditHash = SHA256(inputs + outputs + ruleset_version + caps)
-  // □ Write signed receipt to run ledger (NEVER skip)
-  // □ Return M91AOutput — NEVER mutate run state directly
-  //
-  // Placement: server | Budget: real_time
-  // ExecHook:  after_m91_resolve
-  // ─────────────────────────────────────────────────────────────────────────
-  throw new Error('M91A (First-Table Safety Model (Social Onboarding Abuse Preventer)) ML inference not yet implemented.');
+  // Day-1 operational: delegates to fallback until full ML implementation is deployed.
+  // Full implementation checklist preserved in git history.
+  return runM91aMlFallback(input);
 }
 
 // ── Degraded-mode fallback ────────────────────────────────────────────────────
@@ -228,14 +212,21 @@ export async function runM91aMl(
 export function runM91aMlFallback(
   _input: M91ATelemetryInput,
 ): M91AOutput {
-  // TODO: implement rule-based fallback for M91A
-  // Fallback must:
-  //   □ Return score = 0.5 (neutral / unknown)
-  //   □ Return topFactors = ['ML unavailable — rule-based fallback active']
-  //   □ Return recommendation = 'See rule engine output'
-  //   □ Compute deterministic auditHash from input seed + 'fallback'
-  //   □ Zero-out all M91A-specific extended outputs
-  throw new Error('M91A fallback not yet implemented.');
+  const seed = String(((_input as unknown) as Record<string, unknown>).runSeed ?? '');
+  const tick = Number(((_input as unknown) as Record<string, unknown>).tickIndex ?? 0);
+  const auditHash = createHash('sha256')
+    .update(seed + ':' + tick + ':fallback:M91A')
+    .digest('hex');
+  return {
+    score: 0.5,
+    topFactors: ['ML unavailable — rule-based fallback active'],
+    recommendation: 'See rule engine output',
+    auditHash,
+    abuseRiskScore: null,
+    inviterFarmFlag: null,
+    safeSessionCertification: null,
+    protectionRecommendation: null,
+  };
 }
 
 // ── IntelligenceState integration note ───────────────────────────────────────
diff --git a/pzo_engine/src/ml/m92a_ghost_mentor_curation_engine_best_replays_overlay_quali.ts b/pzo_engine/src/ml/m92a_ghost_mentor_curation_engine_best_replays_overlay_quali.ts
index f69bd33b..952cd6b0 100644
--- a/pzo_engine/src/ml/m92a_ghost_mentor_curation_engine_best_replays_overlay_quali.ts
+++ b/pzo_engine/src/ml/m92a_ghost_mentor_curation_engine_best_replays_overlay_quali.ts
@@ -22,6 +22,8 @@
 // Density6 LLC · Point Zero One · Confidential · All Rights Reserved
 // ═══════════════════════════════════════════════════════════════════════════════
 
+import { createHash } from 'node:crypto';
+
 // ── What this adds ────────────────────────────────────────────────────────────
 /**
  * M92A — Ghost Mentor Curation Engine (Best Replays + Overlay Quality)
@@ -196,27 +198,9 @@ export async function runM92aMl(
   tier:      M92ATier = 'baseline',
   modelCard: Omit<M92AModelCard, 'modelId' | 'coreMechanicPair'>,
 ): Promise<M92AOutput> {
-  // ── TODO: implement M92A — Ghost Mentor Curation Engine (Best Replays + Overlay Quality) ─────────────────────────────────
-  //
-  // Implementation checklist:
-  // □ Validate input schema against featureSchemaHash
-  // □ Select inference backend based on `tier` parameter
-  // □ tier === 'baseline' → GBM + calibrated logistic (fast, low-cost, production default)
-  // □ tier === 'sequence_dl' → TCN / Transformer encoder over event streams (sequential patterns)
-  // □ tier === 'policy_rl' → Constrained contextual bandit / offline PPO (bounded nudges)
-  // □ Apply input privacy filters (no PII, no cross-player contact graph)
-  // □ Run inference → raw score + top_factors
-  // □ Apply output caps: score = Math.min(score, M92A_ML_CONSTANTS.GUARDRAILS.scoreCap)
-  // □ Apply monotonic constraints where relevant
-  // □ Abstain if confidence < M92A_ML_CONSTANTS.GUARDRAILS.abstainThreshold
-  // □ Compute auditHash = SHA256(inputs + outputs + ruleset_version + caps)
-  // □ Write signed receipt to run ledger (NEVER skip)
-  // □ Return M92AOutput — NEVER mutate run state directly
-  //
-  // Placement: server | Budget: batch
-  // ExecHook:  after_m92_resolve
-  // ─────────────────────────────────────────────────────────────────────────
-  throw new Error('M92A (Ghost Mentor Curation Engine (Best Replays + Overlay Quality)) ML inference not yet implemented.');
+  // Day-1 operational: delegates to fallback until full ML implementation is deployed.
+  // Full implementation checklist preserved in git history.
+  return runM92aMlFallback(input);
 }
 
 // ── Degraded-mode fallback ────────────────────────────────────────────────────
@@ -228,14 +212,21 @@ export async function runM92aMl(
 export function runM92aMlFallback(
   _input: M92ATelemetryInput,
 ): M92AOutput {
-  // TODO: implement rule-based fallback for M92A
-  // Fallback must:
-  //   □ Return score = 0.5 (neutral / unknown)
-  //   □ Return topFactors = ['ML unavailable — rule-based fallback active']
-  //   □ Return recommendation = 'See rule engine output'
-  //   □ Compute deterministic auditHash from input seed + 'fallback'
-  //   □ Zero-out all M92A-specific extended outputs
-  throw new Error('M92A fallback not yet implemented.');
+  const seed = String(((_input as unknown) as Record<string, unknown>).runSeed ?? '');
+  const tick = Number(((_input as unknown) as Record<string, unknown>).tickIndex ?? 0);
+  const auditHash = createHash('sha256')
+    .update(seed + ':' + tick + ':fallback:M92A')
+    .digest('hex');
+  return {
+    score: 0.5,
+    topFactors: ['ML unavailable — rule-based fallback active'],
+    recommendation: 'See rule engine output',
+    auditHash,
+    curatedReplaySet: null,
+    annotationQualityScore: null,
+    exploitFilterPassed: null,
+    skillCalibratedOverlay: null,
+  };
 }
 
 // ── IntelligenceState integration note ───────────────────────────────────────
diff --git a/pzo_engine/src/ml/m93a_loadout_lab_constraint_explainer_preview_accuracy_cord.ts b/pzo_engine/src/ml/m93a_loadout_lab_constraint_explainer_preview_accuracy_cord.ts
index 623f3fd7..2e187c4d 100644
--- a/pzo_engine/src/ml/m93a_loadout_lab_constraint_explainer_preview_accuracy_cord.ts
+++ b/pzo_engine/src/ml/m93a_loadout_lab_constraint_explainer_preview_accuracy_cord.ts
@@ -22,6 +22,8 @@
 // Density6 LLC · Point Zero One · Confidential · All Rights Reserved
 // ═══════════════════════════════════════════════════════════════════════════════
 
+import { createHash } from 'node:crypto';
+
 // ── What this adds ────────────────────────────────────────────────────────────
 /**
  * M93A — Loadout Lab Constraint Explainer (Preview Accuracy + CORD Estimator)
@@ -198,27 +200,9 @@ export async function runM93aMl(
   tier:      M93ATier = 'baseline',
   modelCard: Omit<M93AModelCard, 'modelId' | 'coreMechanicPair'>,
 ): Promise<M93AOutput> {
-  // ── TODO: implement M93A — Loadout Lab Constraint Explainer (Preview Accuracy + CORD Estimator) ─────────────────────────────────
-  //
-  // Implementation checklist:
-  // □ Validate input schema against featureSchemaHash
-  // □ Select inference backend based on `tier` parameter
-  // □ tier === 'baseline' → GBM + calibrated logistic (fast, low-cost, production default)
-  // □ tier === 'sequence_dl' → TCN / Transformer encoder over event streams (sequential patterns)
-  // □ tier === 'policy_rl' → Constrained contextual bandit / offline PPO (bounded nudges)
-  // □ Apply input privacy filters (no PII, no cross-player contact graph)
-  // □ Run inference → raw score + top_factors
-  // □ Apply output caps: score = Math.min(score, M93A_ML_CONSTANTS.GUARDRAILS.scoreCap)
-  // □ Apply monotonic constraints where relevant
-  // □ Abstain if confidence < M93A_ML_CONSTANTS.GUARDRAILS.abstainThreshold
-  // □ Compute auditHash = SHA256(inputs + outputs + ruleset_version + caps)
-  // □ Write signed receipt to run ledger (NEVER skip)
-  // □ Return M93AOutput — NEVER mutate run state directly
-  //
-  // Placement: client, server | Budget: real_time
-  // ExecHook:  after_m93_resolve
-  // ─────────────────────────────────────────────────────────────────────────
-  throw new Error('M93A (Loadout Lab Constraint Explainer (Preview Accuracy + CORD Estimator)) ML inference not yet implemented.');
+  // Day-1 operational: delegates to fallback until full ML implementation is deployed.
+  // Full implementation checklist preserved in git history.
+  return runM93aMlFallback(input);
 }
 
 // ── Degraded-mode fallback ────────────────────────────────────────────────────
@@ -230,14 +214,21 @@ export async function runM93aMl(
 export function runM93aMlFallback(
   _input: M93ATelemetryInput,
 ): M93AOutput {
-  // TODO: implement rule-based fallback for M93A
-  // Fallback must:
-  //   □ Return score = 0.5 (neutral / unknown)
-  //   □ Return topFactors = ['ML unavailable — rule-based fallback active']
-  //   □ Return recommendation = 'See rule engine output'
-  //   □ Compute deterministic auditHash from input seed + 'fallback'
-  //   □ Zero-out all M93A-specific extended outputs
-  throw new Error('M93A fallback not yet implemented.');
+  const seed = String(((_input as unknown) as Record<string, unknown>).runSeed ?? '');
+  const tick = Number(((_input as unknown) as Record<string, unknown>).tickIndex ?? 0);
+  const auditHash = createHash('sha256')
+    .update(seed + ':' + tick + ':fallback:M93A')
+    .digest('hex');
+  return {
+    score: 0.5,
+    topFactors: ['ML unavailable — rule-based fallback active'],
+    recommendation: 'See rule engine output',
+    auditHash,
+    constraintExplanation: null,
+    cordPremiumEstimate: null,
+    degeneracyFlag: null,
+    misconfigurationAlert: null,
+  };
 }
 
 // ── IntelligenceState integration note ───────────────────────────────────────
diff --git a/pzo_engine/src/ml/m94a_glossary_ping_relevance_model_speak_only_when_needed.ts b/pzo_engine/src/ml/m94a_glossary_ping_relevance_model_speak_only_when_needed.ts
index 2ff981fd..c190ada0 100644
--- a/pzo_engine/src/ml/m94a_glossary_ping_relevance_model_speak_only_when_needed.ts
+++ b/pzo_engine/src/ml/m94a_glossary_ping_relevance_model_speak_only_when_needed.ts
@@ -22,6 +22,8 @@
 // Density6 LLC · Point Zero One · Confidential · All Rights Reserved
 // ═══════════════════════════════════════════════════════════════════════════════
 
+import { createHash } from 'node:crypto';
+
 // ── What this adds ────────────────────────────────────────────────────────────
 /**
  * M94A — Glossary Ping Relevance Model (Speak-Only-When-Needed)
@@ -196,27 +198,9 @@ export async function runM94aMl(
   tier:      M94ATier = 'baseline',
   modelCard: Omit<M94AModelCard, 'modelId' | 'coreMechanicPair'>,
 ): Promise<M94AOutput> {
-  // ── TODO: implement M94A — Glossary Ping Relevance Model (Speak-Only-When-Needed) ─────────────────────────────────
-  //
-  // Implementation checklist:
-  // □ Validate input schema against featureSchemaHash
-  // □ Select inference backend based on `tier` parameter
-  // □ tier === 'baseline' → GBM + calibrated logistic (fast, low-cost, production default)
-  // □ tier === 'sequence_dl' → TCN / Transformer encoder over event streams (sequential patterns)
-  // □ tier === 'policy_rl' → Constrained contextual bandit / offline PPO (bounded nudges)
-  // □ Apply input privacy filters (no PII, no cross-player contact graph)
-  // □ Run inference → raw score + top_factors
-  // □ Apply output caps: score = Math.min(score, M94A_ML_CONSTANTS.GUARDRAILS.scoreCap)
-  // □ Apply monotonic constraints where relevant
-  // □ Abstain if confidence < M94A_ML_CONSTANTS.GUARDRAILS.abstainThreshold
-  // □ Compute auditHash = SHA256(inputs + outputs + ruleset_version + caps)
-  // □ Write signed receipt to run ledger (NEVER skip)
-  // □ Return M94AOutput — NEVER mutate run state directly
-  //
-  // Placement: client | Budget: real_time
-  // ExecHook:  after_m94_resolve
-  // ─────────────────────────────────────────────────────────────────────────
-  throw new Error('M94A (Glossary Ping Relevance Model (Speak-Only-When-Needed)) ML inference not yet implemented.');
+  // Day-1 operational: delegates to fallback until full ML implementation is deployed.
+  // Full implementation checklist preserved in git history.
+  return runM94aMlFallback(input);
 }
 
 // ── Degraded-mode fallback ────────────────────────────────────────────────────
@@ -228,14 +212,21 @@ export async function runM94aMl(
 export function runM94aMlFallback(
   _input: M94ATelemetryInput,
 ): M94AOutput {
-  // TODO: implement rule-based fallback for M94A
-  // Fallback must:
-  //   □ Return score = 0.5 (neutral / unknown)
-  //   □ Return topFactors = ['ML unavailable — rule-based fallback active']
-  //   □ Return recommendation = 'See rule engine output'
-  //   □ Compute deterministic auditHash from input seed + 'fallback'
-  //   □ Zero-out all M94A-specific extended outputs
-  throw new Error('M94A fallback not yet implemented.');
+  const seed = String(((_input as unknown) as Record<string, unknown>).runSeed ?? '');
+  const tick = Number(((_input as unknown) as Record<string, unknown>).tickIndex ?? 0);
+  const auditHash = createHash('sha256')
+    .update(seed + ':' + tick + ':fallback:M94A')
+    .digest('hex');
+  return {
+    score: 0.5,
+    topFactors: ['ML unavailable — rule-based fallback active'],
+    recommendation: 'See rule engine output',
+    auditHash,
+    relevanceScore: null,
+    familiarTermFlag: null,
+    pingRecommendation: null,
+    vocabularyModelUpdate: null,
+  };
 }
 
 // ── IntelligenceState integration note ───────────────────────────────────────
diff --git a/pzo_engine/src/ml/m95a_wipe_clinic_causal_explainer_minimal_counterfactual_gen.ts b/pzo_engine/src/ml/m95a_wipe_clinic_causal_explainer_minimal_counterfactual_gen.ts
index ac8ffe4e..cfd0d072 100644
--- a/pzo_engine/src/ml/m95a_wipe_clinic_causal_explainer_minimal_counterfactual_gen.ts
+++ b/pzo_engine/src/ml/m95a_wipe_clinic_causal_explainer_minimal_counterfactual_gen.ts
@@ -22,6 +22,8 @@
 // Density6 LLC · Point Zero One · Confidential · All Rights Reserved
 // ═══════════════════════════════════════════════════════════════════════════════
 
+import { createHash } from 'node:crypto';
+
 // ── What this adds ────────────────────────────────────────────────────────────
 /**
  * M95A — Wipe Clinic Causal Explainer (Minimal Counterfactual Generator)
@@ -196,27 +198,9 @@ export async function runM95aMl(
   tier:      M95ATier = 'baseline',
   modelCard: Omit<M95AModelCard, 'modelId' | 'coreMechanicPair'>,
 ): Promise<M95AOutput> {
-  // ── TODO: implement M95A — Wipe Clinic Causal Explainer (Minimal Counterfactual Generator) ─────────────────────────────────
-  //
-  // Implementation checklist:
-  // □ Validate input schema against featureSchemaHash
-  // □ Select inference backend based on `tier` parameter
-  // □ tier === 'baseline' → GBM + calibrated logistic (fast, low-cost, production default)
-  // □ tier === 'sequence_dl' → TCN / Transformer encoder over event streams (sequential patterns)
-  // □ tier === 'causal' → Causal inference + DiD (counterfactual explanations)
-  // □ Apply input privacy filters (no PII, no cross-player contact graph)
-  // □ Run inference → raw score + top_factors
-  // □ Apply output caps: score = Math.min(score, M95A_ML_CONSTANTS.GUARDRAILS.scoreCap)
-  // □ Apply monotonic constraints where relevant
-  // □ Abstain if confidence < M95A_ML_CONSTANTS.GUARDRAILS.abstainThreshold
-  // □ Compute auditHash = SHA256(inputs + outputs + ruleset_version + caps)
-  // □ Write signed receipt to run ledger (NEVER skip)
-  // □ Return M95AOutput — NEVER mutate run state directly
-  //
-  // Placement: server | Budget: batch
-  // ExecHook:  after_m95_resolve
-  // ─────────────────────────────────────────────────────────────────────────
-  throw new Error('M95A (Wipe Clinic Causal Explainer (Minimal Counterfactual Generator)) ML inference not yet implemented.');
+  // Day-1 operational: delegates to fallback until full ML implementation is deployed.
+  // Full implementation checklist preserved in git history.
+  return runM95aMlFallback(input);
 }
 
 // ── Degraded-mode fallback ────────────────────────────────────────────────────
@@ -228,14 +212,21 @@ export async function runM95aMl(
 export function runM95aMlFallback(
   _input: M95ATelemetryInput,
 ): M95AOutput {
-  // TODO: implement rule-based fallback for M95A
-  // Fallback must:
-  //   □ Return score = 0.5 (neutral / unknown)
-  //   □ Return topFactors = ['ML unavailable — rule-based fallback active']
-  //   □ Return recommendation = 'See rule engine output'
-  //   □ Compute deterministic auditHash from input seed + 'fallback'
-  //   □ Zero-out all M95A-specific extended outputs
-  throw new Error('M95A fallback not yet implemented.');
+  const seed = String(((_input as unknown) as Record<string, unknown>).runSeed ?? '');
+  const tick = Number(((_input as unknown) as Record<string, unknown>).tickIndex ?? 0);
+  const auditHash = createHash('sha256')
+    .update(seed + ':' + tick + ':fallback:M95A')
+    .digest('hex');
+  return {
+    score: 0.5,
+    topFactors: ['ML unavailable — rule-based fallback active'],
+    recommendation: 'See rule engine output',
+    auditHash,
+    minimalCounterfactual: null,
+    leveragePivot: null,
+    survivalProbabilityDelta: null,
+    principleRecoveryPath: null,
+  };
 }
 
 // ── IntelligenceState integration note ───────────────────────────────────────
diff --git a/pzo_engine/src/ml/m96a_time_drift_lag_exploit_classifier_server_time_authority.ts b/pzo_engine/src/ml/m96a_time_drift_lag_exploit_classifier_server_time_authority.ts
index 1bb67c36..cb8a4c18 100644
--- a/pzo_engine/src/ml/m96a_time_drift_lag_exploit_classifier_server_time_authority.ts
+++ b/pzo_engine/src/ml/m96a_time_drift_lag_exploit_classifier_server_time_authority.ts
@@ -22,6 +22,8 @@
 // Density6 LLC · Point Zero One · Confidential · All Rights Reserved
 // ═══════════════════════════════════════════════════════════════════════════════
 
+import { createHash } from 'node:crypto';
+
 // ── What this adds ────────────────────────────────────────────────────────────
 /**
  * M96A — Time Drift & Lag Exploit Classifier (Server-Time Authority Guard)
@@ -196,27 +198,9 @@ export async function runM96aMl(
   tier:      M96ATier = 'baseline',
   modelCard: Omit<M96AModelCard, 'modelId' | 'coreMechanicPair'>,
 ): Promise<M96AOutput> {
-  // ── TODO: implement M96A — Time Drift & Lag Exploit Classifier (Server-Time Authority Guard) ─────────────────────────────────
-  //
-  // Implementation checklist:
-  // □ Validate input schema against featureSchemaHash
-  // □ Select inference backend based on `tier` parameter
-  // □ tier === 'baseline' → GBM + calibrated logistic (fast, low-cost, production default)
-  // □ tier === 'sequence_dl' → TCN / Transformer encoder over event streams (sequential patterns)
-  // □ tier === 'policy_rl' → Constrained contextual bandit / offline PPO (bounded nudges)
-  // □ Apply input privacy filters (no PII, no cross-player contact graph)
-  // □ Run inference → raw score + top_factors
-  // □ Apply output caps: score = Math.min(score, M96A_ML_CONSTANTS.GUARDRAILS.scoreCap)
-  // □ Apply monotonic constraints where relevant
-  // □ Abstain if confidence < M96A_ML_CONSTANTS.GUARDRAILS.abstainThreshold
-  // □ Compute auditHash = SHA256(inputs + outputs + ruleset_version + caps)
-  // □ Write signed receipt to run ledger (NEVER skip)
-  // □ Return M96AOutput — NEVER mutate run state directly
-  //
-  // Placement: server | Budget: real_time
-  // ExecHook:  after_m96_resolve
-  // ─────────────────────────────────────────────────────────────────────────
-  throw new Error('M96A (Time Drift & Lag Exploit Classifier (Server-Time Authority Guard)) ML inference not yet implemented.');
+  // Day-1 operational: delegates to fallback until full ML implementation is deployed.
+  // Full implementation checklist preserved in git history.
+  return runM96aMlFallback(input);
 }
 
 // ── Degraded-mode fallback ────────────────────────────────────────────────────
@@ -228,14 +212,21 @@ export async function runM96aMl(
 export function runM96aMlFallback(
   _input: M96ATelemetryInput,
 ): M96AOutput {
-  // TODO: implement rule-based fallback for M96A
-  // Fallback must:
-  //   □ Return score = 0.5 (neutral / unknown)
-  //   □ Return topFactors = ['ML unavailable — rule-based fallback active']
-  //   □ Return recommendation = 'See rule engine output'
-  //   □ Compute deterministic auditHash from input seed + 'fallback'
-  //   □ Zero-out all M96A-specific extended outputs
-  throw new Error('M96A fallback not yet implemented.');
+  const seed = String(((_input as unknown) as Record<string, unknown>).runSeed ?? '');
+  const tick = Number(((_input as unknown) as Record<string, unknown>).tickIndex ?? 0);
+  const auditHash = createHash('sha256')
+    .update(seed + ':' + tick + ':fallback:M96A')
+    .digest('hex');
+  return {
+    score: 0.5,
+    topFactors: ['ML unavailable — rule-based fallback active'],
+    recommendation: 'See rule engine output',
+    auditHash,
+    driftClassification: null,
+    exploitProbability: null,
+    lagLegitimacyScore: null,
+    deviceTrustSignal: null,
+  };
 }
 
 // ── IntelligenceState integration note ───────────────────────────────────────
diff --git a/pzo_engine/src/ml/m97a_seed_commit_reveal_verifier_rng_integrity_bias_monitor.ts b/pzo_engine/src/ml/m97a_seed_commit_reveal_verifier_rng_integrity_bias_monitor.ts
index 1bfb6045..5ebe2241 100644
--- a/pzo_engine/src/ml/m97a_seed_commit_reveal_verifier_rng_integrity_bias_monitor.ts
+++ b/pzo_engine/src/ml/m97a_seed_commit_reveal_verifier_rng_integrity_bias_monitor.ts
@@ -22,6 +22,8 @@
 // Density6 LLC · Point Zero One · Confidential · All Rights Reserved
 // ═══════════════════════════════════════════════════════════════════════════════
 
+import { createHash } from 'node:crypto';
+
 // ── What this adds ────────────────────────────────────────────────────────────
 /**
  * M97A — Seed Commit/Reveal Verifier (RNG Integrity + Bias Monitor)
@@ -196,27 +198,9 @@ export async function runM97aMl(
   tier:      M97ATier = 'baseline',
   modelCard: Omit<M97AModelCard, 'modelId' | 'coreMechanicPair'>,
 ): Promise<M97AOutput> {
-  // ── TODO: implement M97A — Seed Commit/Reveal Verifier (RNG Integrity + Bias Monitor) ─────────────────────────────────
-  //
-  // Implementation checklist:
-  // □ Validate input schema against featureSchemaHash
-  // □ Select inference backend based on `tier` parameter
-  // □ tier === 'baseline' → GBM + calibrated logistic (fast, low-cost, production default)
-  // □ tier === 'sequence_dl' → TCN / Transformer encoder over event streams (sequential patterns)
-  // □ tier === 'policy_rl' → Constrained contextual bandit / offline PPO (bounded nudges)
-  // □ Apply input privacy filters (no PII, no cross-player contact graph)
-  // □ Run inference → raw score + top_factors
-  // □ Apply output caps: score = Math.min(score, M97A_ML_CONSTANTS.GUARDRAILS.scoreCap)
-  // □ Apply monotonic constraints where relevant
-  // □ Abstain if confidence < M97A_ML_CONSTANTS.GUARDRAILS.abstainThreshold
-  // □ Compute auditHash = SHA256(inputs + outputs + ruleset_version + caps)
-  // □ Write signed receipt to run ledger (NEVER skip)
-  // □ Return M97AOutput — NEVER mutate run state directly
-  //
-  // Placement: server | Budget: real_time
-  // ExecHook:  after_m97_resolve
-  // ─────────────────────────────────────────────────────────────────────────
-  throw new Error('M97A (Seed Commit/Reveal Verifier (RNG Integrity + Bias Monitor)) ML inference not yet implemented.');
+  // Day-1 operational: delegates to fallback until full ML implementation is deployed.
+  // Full implementation checklist preserved in git history.
+  return runM97aMlFallback(input);
 }
 
 // ── Degraded-mode fallback ────────────────────────────────────────────────────
@@ -228,14 +212,21 @@ export async function runM97aMl(
 export function runM97aMlFallback(
   _input: M97ATelemetryInput,
 ): M97AOutput {
-  // TODO: implement rule-based fallback for M97A
-  // Fallback must:
-  //   □ Return score = 0.5 (neutral / unknown)
-  //   □ Return topFactors = ['ML unavailable — rule-based fallback active']
-  //   □ Return recommendation = 'See rule engine output'
-  //   □ Compute deterministic auditHash from input seed + 'fallback'
-  //   □ Zero-out all M97A-specific extended outputs
-  throw new Error('M97A fallback not yet implemented.');
+  const seed = String(((_input as unknown) as Record<string, unknown>).runSeed ?? '');
+  const tick = Number(((_input as unknown) as Record<string, unknown>).tickIndex ?? 0);
+  const auditHash = createHash('sha256')
+    .update(seed + ':' + tick + ':fallback:M97A')
+    .digest('hex');
+  return {
+    score: 0.5,
+    topFactors: ['ML unavailable — rule-based fallback active'],
+    recommendation: 'See rule engine output',
+    auditHash,
+    rngIntegrityScore: null,
+    biasDetectionFlag: null,
+    predeterminationProbability: null,
+    forensicSignal: null,
+  };
 }
 
 // ── IntelligenceState integration note ───────────────────────────────────────
diff --git a/pzo_engine/src/ml/m98a_quarantine_compute_scheduler_fast_path_clean_runs_deep.ts b/pzo_engine/src/ml/m98a_quarantine_compute_scheduler_fast_path_clean_runs_deep.ts
index 89f5c1c0..5f9bb573 100644
--- a/pzo_engine/src/ml/m98a_quarantine_compute_scheduler_fast_path_clean_runs_deep.ts
+++ b/pzo_engine/src/ml/m98a_quarantine_compute_scheduler_fast_path_clean_runs_deep.ts
@@ -22,6 +22,8 @@
 // Density6 LLC · Point Zero One · Confidential · All Rights Reserved
 // ═══════════════════════════════════════════════════════════════════════════════
 
+import { createHash } from 'node:crypto';
+
 // ── What this adds ────────────────────────────────────────────────────────────
 /**
  * M98A — Quarantine Compute Scheduler (Fast-Path Clean Runs, Deep-Path Suspicious)
@@ -196,27 +198,9 @@ export async function runM98aMl(
   tier:      M98ATier = 'baseline',
   modelCard: Omit<M98AModelCard, 'modelId' | 'coreMechanicPair'>,
 ): Promise<M98AOutput> {
-  // ── TODO: implement M98A — Quarantine Compute Scheduler (Fast-Path Clean Runs, Deep-Path Suspicious) ─────────────────────────────────
-  //
-  // Implementation checklist:
-  // □ Validate input schema against featureSchemaHash
-  // □ Select inference backend based on `tier` parameter
-  // □ tier === 'baseline' → GBM + calibrated logistic (fast, low-cost, production default)
-  // □ tier === 'sequence_dl' → TCN / Transformer encoder over event streams (sequential patterns)
-  // □ tier === 'policy_rl' → Constrained contextual bandit / offline PPO (bounded nudges)
-  // □ Apply input privacy filters (no PII, no cross-player contact graph)
-  // □ Run inference → raw score + top_factors
-  // □ Apply output caps: score = Math.min(score, M98A_ML_CONSTANTS.GUARDRAILS.scoreCap)
-  // □ Apply monotonic constraints where relevant
-  // □ Abstain if confidence < M98A_ML_CONSTANTS.GUARDRAILS.abstainThreshold
-  // □ Compute auditHash = SHA256(inputs + outputs + ruleset_version + caps)
-  // □ Write signed receipt to run ledger (NEVER skip)
-  // □ Return M98AOutput — NEVER mutate run state directly
-  //
-  // Placement: server | Budget: real_time
-  // ExecHook:  after_m98_resolve
-  // ─────────────────────────────────────────────────────────────────────────
-  throw new Error('M98A (Quarantine Compute Scheduler (Fast-Path Clean Runs, Deep-Path Suspicious)) ML inference not yet implemented.');
+  // Day-1 operational: delegates to fallback until full ML implementation is deployed.
+  // Full implementation checklist preserved in git history.
+  return runM98aMlFallback(input);
 }
 
 // ── Degraded-mode fallback ────────────────────────────────────────────────────
@@ -228,14 +212,21 @@ export async function runM98aMl(
 export function runM98aMlFallback(
   _input: M98ATelemetryInput,
 ): M98AOutput {
-  // TODO: implement rule-based fallback for M98A
-  // Fallback must:
-  //   □ Return score = 0.5 (neutral / unknown)
-  //   □ Return topFactors = ['ML unavailable — rule-based fallback active']
-  //   □ Return recommendation = 'See rule engine output'
-  //   □ Compute deterministic auditHash from input seed + 'fallback'
-  //   □ Zero-out all M98A-specific extended outputs
-  throw new Error('M98A fallback not yet implemented.');
+  const seed = String(((_input as unknown) as Record<string, unknown>).runSeed ?? '');
+  const tick = Number(((_input as unknown) as Record<string, unknown>).tickIndex ?? 0);
+  const auditHash = createHash('sha256')
+    .update(seed + ':' + tick + ':fallback:M98A')
+    .digest('hex');
+  return {
+    score: 0.5,
+    topFactors: ['ML unavailable — rule-based fallback active'],
+    recommendation: 'See rule engine output',
+    auditHash,
+    computeSchedule: null,
+    fastPathFlag: null,
+    deepCheckPriority: null,
+    queueReweightingSignal: null,
+  };
 }
 
 // ── IntelligenceState integration note ───────────────────────────────────────
diff --git a/pzo_engine/src/ml/m99a_integrity_challenge_placement_optimizer_non_disruptive.ts b/pzo_engine/src/ml/m99a_integrity_challenge_placement_optimizer_non_disruptive.ts
index 0d20b9c3..dc81f221 100644
--- a/pzo_engine/src/ml/m99a_integrity_challenge_placement_optimizer_non_disruptive.ts
+++ b/pzo_engine/src/ml/m99a_integrity_challenge_placement_optimizer_non_disruptive.ts
@@ -22,6 +22,8 @@
 // Density6 LLC · Point Zero One · Confidential · All Rights Reserved
 // ═══════════════════════════════════════════════════════════════════════════════
 
+import { createHash } from 'node:crypto';
+
 // ── What this adds ────────────────────────────────────────────────────────────
 /**
  * M99A — Integrity Challenge Placement Optimizer (Non-Disruptive + High-Signal)
@@ -196,27 +198,9 @@ export async function runM99aMl(
   tier:      M99ATier = 'baseline',
   modelCard: Omit<M99AModelCard, 'modelId' | 'coreMechanicPair'>,
 ): Promise<M99AOutput> {
-  // ── TODO: implement M99A — Integrity Challenge Placement Optimizer (Non-Disruptive + High-Signal) ─────────────────────────────────
-  //
-  // Implementation checklist:
-  // □ Validate input schema against featureSchemaHash
-  // □ Select inference backend based on `tier` parameter
-  // □ tier === 'baseline' → GBM + calibrated logistic (fast, low-cost, production default)
-  // □ tier === 'sequence_dl' → TCN / Transformer encoder over event streams (sequential patterns)
-  // □ tier === 'policy_rl' → Constrained contextual bandit / offline PPO (bounded nudges)
-  // □ Apply input privacy filters (no PII, no cross-player contact graph)
-  // □ Run inference → raw score + top_factors
-  // □ Apply output caps: score = Math.min(score, M99A_ML_CONSTANTS.GUARDRAILS.scoreCap)
-  // □ Apply monotonic constraints where relevant
-  // □ Abstain if confidence < M99A_ML_CONSTANTS.GUARDRAILS.abstainThreshold
-  // □ Compute auditHash = SHA256(inputs + outputs + ruleset_version + caps)
-  // □ Write signed receipt to run ledger (NEVER skip)
-  // □ Return M99AOutput — NEVER mutate run state directly
-  //
-  // Placement: server | Budget: real_time
-  // ExecHook:  after_m99_resolve
-  // ─────────────────────────────────────────────────────────────────────────
-  throw new Error('M99A (Integrity Challenge Placement Optimizer (Non-Disruptive + High-Signal)) ML inference not yet implemented.');
+  // Day-1 operational: delegates to fallback until full ML implementation is deployed.
+  // Full implementation checklist preserved in git history.
+  return runM99aMlFallback(input);
 }
 
 // ── Degraded-mode fallback ────────────────────────────────────────────────────
@@ -228,14 +212,21 @@ export async function runM99aMl(
 export function runM99aMlFallback(
   _input: M99ATelemetryInput,
 ): M99AOutput {
-  // TODO: implement rule-based fallback for M99A
-  // Fallback must:
-  //   □ Return score = 0.5 (neutral / unknown)
-  //   □ Return topFactors = ['ML unavailable — rule-based fallback active']
-  //   □ Return recommendation = 'See rule engine output'
-  //   □ Compute deterministic auditHash from input seed + 'fallback'
-  //   □ Zero-out all M99A-specific extended outputs
-  throw new Error('M99A fallback not yet implemented.');
+  const seed = String(((_input as unknown) as Record<string, unknown>).runSeed ?? '');
+  const tick = Number(((_input as unknown) as Record<string, unknown>).tickIndex ?? 0);
+  const auditHash = createHash('sha256')
+    .update(seed + ':' + tick + ':fallback:M99A')
+    .digest('hex');
+  return {
+    score: 0.5,
+    topFactors: ['ML unavailable — rule-based fallback active'],
+    recommendation: 'See rule engine output',
+    auditHash,
+    challengePlacement: null,
+    signalYieldEstimate: null,
+    disruptionScore: null,
+    difficultyTier: null,
+  };
 }
 
 // ── IntelligenceState integration note ───────────────────────────────────────
