```bash
npm install --save @testing-library/react @testing-library/jest-dom ts-jest
```

If you're using yarn, use these commands instead:

```bash
yarn add @testing-library/react @testing-library/jest-dom ts-jest
```
