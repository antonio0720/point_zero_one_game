1. Renders the Biometrics1 component correctly.
2. Tests that `onAuthSuccess` callback is called when biometric authentication is successful.
3. Tests that `onAuthError` callback is called when biometric authentication fails.
