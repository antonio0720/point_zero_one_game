import { ClipStudio8 } from '../src/clip-studio-8';

describe('ClipStudio8', () => {
it('should initialize correctly', () => {
const clipStudio = new ClipStudio8();
expect(clipStudio).toBeInstanceOf(ClipStudio8);
});

// Add more test cases as needed
});
