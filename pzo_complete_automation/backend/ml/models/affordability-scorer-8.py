pickle.dump(affordability_scorer, file)
```

The above code trains a Random Forest Regressor model and calculates the Mean Absolute Error (MAE), Mean Squared Error (MSE), and R2 Score for performance evaluation. The trained model is then saved to a pickle file.
