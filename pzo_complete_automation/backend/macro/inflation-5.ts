const basePrice = 100; // The initial price of the item
const years = 10; // Number of years to calculate inflation for
const currentPrice = calculatePrice(basePrice, years);
console.log(`The price after ${years} years is ${currentPrice}`);
```
