- Expected Loss: AL \* A \* (RL - 1)
- Specific Risk: G \* AV² \* VL² \* BV

Where:

- `AL` is the asset's current value
- `A` is the Alpha factor, typically between 0.3 and 0.5
- `RL` is the liability's rating (1 for a perfect credit, 0 for default)
- `AV²` is the square of the asset's volatility
- `VL²` is the square of the liability's volatility
- `BV` is the Beta factor, typically between 0.08 and 0.16
