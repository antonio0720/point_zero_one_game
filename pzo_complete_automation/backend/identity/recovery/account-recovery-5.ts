1. You have a TypeORM configuration set up with a `User` entity that has an email and recoveryCode properties.
2. You have imported environment variables using the `dotenv` package (not shown in the code above).
3. The `config.jwtSecret` is your JWT secret key.
