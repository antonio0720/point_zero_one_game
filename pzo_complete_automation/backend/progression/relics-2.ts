1. `/api/relic/:id` - Retrieves a relic object given its ID
2. `/api/progress/:id` - Updates progress for the specified relic and unlocks achievements if progress is sufficient. The achievement details are stored in a JSON file named 'relics2.json'.
