-- Migration 015: Create cohort_assignments
            -- Generated by pzo_curriculum_gateway_generator.py
            -- Date: 2026-03-07T14:32:44Z

            BEGIN;

            CREATE TABLE IF NOT EXISTS cohort_assignments (
                id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    institution_id UUID NOT NULL REFERENCES institutions(id) ON DELETE CASCADE,
    cohort_id UUID NOT NULL REFERENCES cohorts(id) ON DELETE CASCADE,
    pack_version_id UUID NOT NULL REFERENCES pack_versions(id),
    assigned_by UUID NOT NULL,
    status VARCHAR(50) NOT NULL DEFAULT 'active',
    starts_at TIMESTAMPTZ,
    ends_at TIMESTAMPTZ,
    created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
    updated_at TIMESTAMPTZ NOT NULL DEFAULT NOW()
            );

            CREATE INDEX IF NOT EXISTS idx_cohort_assignments_institution_id ON cohort_assignments(institution_id);
CREATE INDEX IF NOT EXISTS idx_cohort_assignments_cohort_id ON cohort_assignments(cohort_id);
CREATE INDEX IF NOT EXISTS idx_cohort_assignments_pack_version_id ON cohort_assignments(pack_version_id);
CREATE INDEX IF NOT EXISTS idx_cohort_assignments_status ON cohort_assignments(status);
CREATE INDEX IF NOT EXISTS idx_cohort_assignments_created_at ON cohort_assignments(created_at);

            COMMIT;

            -- ROLLBACK SQL:
            -- DROP TABLE IF EXISTS cohort_assignments CASCADE;
