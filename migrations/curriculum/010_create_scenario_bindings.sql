-- Migration 010: Create scenario_bindings
            -- Generated by pzo_curriculum_gateway_generator.py
            -- Date: 2026-03-07T14:32:44Z

            BEGIN;

            CREATE TABLE IF NOT EXISTS scenario_bindings (
                id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    pack_version_id UUID NOT NULL REFERENCES pack_versions(id) ON DELETE CASCADE,
    scenario_index INTEGER NOT NULL,
    mode VARCHAR(50) NOT NULL,
    scenario_seed VARCHAR(64) NOT NULL,
    objective_tags JSONB NOT NULL DEFAULT '[]',
    allowed_card_overlays JSONB NOT NULL DEFAULT '[]',
    benchmark_eligible BOOLEAN NOT NULL DEFAULT TRUE,
    facilitator_guide_id UUID,
    episode_title VARCHAR(255),
    episode_description TEXT,
    created_at TIMESTAMPTZ NOT NULL DEFAULT NOW()
            );

            CREATE INDEX IF NOT EXISTS idx_scenario_bindings_pack_version_id ON scenario_bindings(pack_version_id);
CREATE INDEX IF NOT EXISTS idx_scenario_bindings_created_at ON scenario_bindings(created_at);

            COMMIT;

            -- ROLLBACK SQL:
            -- DROP TABLE IF EXISTS scenario_bindings CASCADE;
