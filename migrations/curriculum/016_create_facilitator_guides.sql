-- Migration 016: Create facilitator_guides
            -- Generated by pzo_curriculum_gateway_generator.py
            -- Date: 2026-03-07T14:32:44Z

            BEGIN;

            CREATE TABLE IF NOT EXISTS facilitator_guides (
                id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    pack_version_id UUID NOT NULL REFERENCES pack_versions(id) ON DELETE CASCADE,
    scenario_binding_id UUID NOT NULL REFERENCES scenario_bindings(id) ON DELETE CASCADE,
    objective TEXT NOT NULL,
    watch_fors JSONB NOT NULL DEFAULT '[]',
    debrief_questions JSONB NOT NULL DEFAULT '[]',
    next_run_recommendation TEXT,
    reflection_prompts JSONB NOT NULL DEFAULT '[]',
    facilitator_notes TEXT,
    printable BOOLEAN NOT NULL DEFAULT TRUE,
    created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
    updated_at TIMESTAMPTZ NOT NULL DEFAULT NOW()
            );

            CREATE INDEX IF NOT EXISTS idx_facilitator_guides_pack_version_id ON facilitator_guides(pack_version_id);
CREATE INDEX IF NOT EXISTS idx_facilitator_guides_scenario_binding_id ON facilitator_guides(scenario_binding_id);
CREATE INDEX IF NOT EXISTS idx_facilitator_guides_created_at ON facilitator_guides(created_at);

            COMMIT;

            -- ROLLBACK SQL:
            -- DROP TABLE IF EXISTS facilitator_guides CASCADE;
