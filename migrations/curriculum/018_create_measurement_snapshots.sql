-- Migration 018: Create measurement_snapshots
            -- Generated by pzo_curriculum_gateway_generator.py
            -- Date: 2026-03-07T14:32:44Z

            BEGIN;

            CREATE TABLE IF NOT EXISTS measurement_snapshots (
                id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    institution_id UUID NOT NULL REFERENCES institutions(id) ON DELETE CASCADE,
    cohort_id UUID NOT NULL REFERENCES cohorts(id) ON DELETE CASCADE,
    learner_id UUID REFERENCES learners(id),
    pack_version_id UUID NOT NULL REFERENCES pack_versions(id),
    snapshot_type VARCHAR(50) NOT NULL,
    survival_rate NUMERIC(5,4),
    failure_modes JSONB NOT NULL DEFAULT '{}',
    improvement_delta NUMERIC(8,4),
    risk_literacy_composite NUMERIC(8,4),
    reserve_preservation NUMERIC(5,4),
    debt_discipline NUMERIC(5,4),
    recovery_quality NUMERIC(5,4),
    overextension_penalty NUMERIC(5,4),
    shield_maintenance NUMERIC(5,4),
    pressure_response NUMERIC(5,4),
    decision_timing NUMERIC(5,4),
    repeat_improvement_trend NUMERIC(5,4),
    attempts_count INTEGER NOT NULL DEFAULT 0,
    computed_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
    created_at TIMESTAMPTZ NOT NULL DEFAULT NOW()
            );

            CREATE INDEX IF NOT EXISTS idx_measurement_snapshots_institution_id ON measurement_snapshots(institution_id);
CREATE INDEX IF NOT EXISTS idx_measurement_snapshots_cohort_id ON measurement_snapshots(cohort_id);
CREATE INDEX IF NOT EXISTS idx_measurement_snapshots_learner_id ON measurement_snapshots(learner_id);
CREATE INDEX IF NOT EXISTS idx_measurement_snapshots_pack_version_id ON measurement_snapshots(pack_version_id);
CREATE INDEX IF NOT EXISTS idx_measurement_snapshots_created_at ON measurement_snapshots(created_at);

            COMMIT;

            -- ROLLBACK SQL:
            -- DROP TABLE IF EXISTS measurement_snapshots CASCADE;
