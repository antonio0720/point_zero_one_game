-- Migration 024: Create entitlement_receipts
            -- Generated by pzo_curriculum_gateway_generator.py
            -- Date: 2026-03-07T14:32:44Z

            BEGIN;

            CREATE TABLE IF NOT EXISTS entitlement_receipts (
                id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    entitlement_id UUID NOT NULL REFERENCES org_entitlements(id),
    event_type VARCHAR(50) NOT NULL,
    stripe_event_id VARCHAR(255),
    receipt_hash VARCHAR(64) NOT NULL,
    payload JSONB NOT NULL,
    created_at TIMESTAMPTZ NOT NULL DEFAULT NOW()
            );

            CREATE INDEX IF NOT EXISTS idx_entitlement_receipts_entitlement_id ON entitlement_receipts(entitlement_id);
CREATE INDEX IF NOT EXISTS idx_entitlement_receipts_created_at ON entitlement_receipts(created_at);

            COMMIT;

            -- ROLLBACK SQL:
            -- DROP TABLE IF EXISTS entitlement_receipts CASCADE;
