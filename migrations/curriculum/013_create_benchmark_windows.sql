-- Migration 013: Create benchmark_windows
            -- Generated by pzo_curriculum_gateway_generator.py
            -- Date: 2026-03-07T14:32:44Z

            BEGIN;

            CREATE TABLE IF NOT EXISTS benchmark_windows (
                id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    institution_id UUID NOT NULL REFERENCES institutions(id) ON DELETE CASCADE,
    cohort_id UUID NOT NULL REFERENCES cohorts(id) ON DELETE CASCADE,
    benchmark_definition_id UUID NOT NULL REFERENCES benchmark_definitions(id),
    status VARCHAR(50) NOT NULL DEFAULT 'scheduled',
    opens_at TIMESTAMPTZ NOT NULL,
    closes_at TIMESTAMPTZ NOT NULL,
    attempt_policy JSONB NOT NULL DEFAULT '{}',
    created_by UUID NOT NULL,
    created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
    updated_at TIMESTAMPTZ NOT NULL DEFAULT NOW()
            );

            CREATE INDEX IF NOT EXISTS idx_benchmark_windows_institution_id ON benchmark_windows(institution_id);
CREATE INDEX IF NOT EXISTS idx_benchmark_windows_cohort_id ON benchmark_windows(cohort_id);
CREATE INDEX IF NOT EXISTS idx_benchmark_windows_benchmark_definition_id ON benchmark_windows(benchmark_definition_id);
CREATE INDEX IF NOT EXISTS idx_benchmark_windows_status ON benchmark_windows(status);
CREATE INDEX IF NOT EXISTS idx_benchmark_windows_created_at ON benchmark_windows(created_at);

            COMMIT;

            -- ROLLBACK SQL:
            -- DROP TABLE IF EXISTS benchmark_windows CASCADE;
