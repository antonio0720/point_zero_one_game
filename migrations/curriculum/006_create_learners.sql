-- Migration 006: Create learners
            -- Generated by pzo_curriculum_gateway_generator.py
            -- Date: 2026-03-07T14:32:44Z

            BEGIN;

            CREATE TABLE IF NOT EXISTS learners (
                id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    institution_id UUID NOT NULL REFERENCES institutions(id) ON DELETE CASCADE,
    cohort_id UUID NOT NULL REFERENCES cohorts(id) ON DELETE CASCADE,
    user_id UUID,
    external_id VARCHAR(255),
    display_name VARCHAR(255),
    email VARCHAR(255),
    enrollment_status VARCHAR(50) NOT NULL DEFAULT 'active',
    consent_status VARCHAR(50) NOT NULL DEFAULT 'pending',
    consent_given_at TIMESTAMPTZ,
    enrolled_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
    withdrawn_at TIMESTAMPTZ,
    created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
    updated_at TIMESTAMPTZ NOT NULL DEFAULT NOW()
            );

            CREATE INDEX IF NOT EXISTS idx_learners_institution_id ON learners(institution_id);
CREATE INDEX IF NOT EXISTS idx_learners_cohort_id ON learners(cohort_id);
CREATE INDEX IF NOT EXISTS idx_learners_created_at ON learners(created_at);

            COMMIT;

            -- ROLLBACK SQL:
            -- DROP TABLE IF EXISTS learners CASCADE;
