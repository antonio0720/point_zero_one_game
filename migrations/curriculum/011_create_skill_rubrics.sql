-- Migration 011: Create skill_rubrics
            -- Generated by pzo_curriculum_gateway_generator.py
            -- Date: 2026-03-07T14:32:44Z

            BEGIN;

            CREATE TABLE IF NOT EXISTS skill_rubrics (
                id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    pack_version_id UUID NOT NULL REFERENCES pack_versions(id) ON DELETE CASCADE,
    metric_key VARCHAR(100) NOT NULL,
    metric_label VARCHAR(255) NOT NULL,
    weight NUMERIC(5,3) NOT NULL DEFAULT 1.0,
    scoring_formula TEXT NOT NULL,
    description TEXT,
    created_at TIMESTAMPTZ NOT NULL DEFAULT NOW()
            );

            CREATE INDEX IF NOT EXISTS idx_skill_rubrics_pack_version_id ON skill_rubrics(pack_version_id);
CREATE INDEX IF NOT EXISTS idx_skill_rubrics_created_at ON skill_rubrics(created_at);

            COMMIT;

            -- ROLLBACK SQL:
            -- DROP TABLE IF EXISTS skill_rubrics CASCADE;
