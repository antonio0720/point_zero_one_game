-- Migration 004: Create facilitators
            -- Generated by pzo_curriculum_gateway_generator.py
            -- Date: 2026-03-07T14:32:44Z

            BEGIN;

            CREATE TABLE IF NOT EXISTS facilitators (
                id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    institution_id UUID NOT NULL REFERENCES institutions(id) ON DELETE CASCADE,
    user_id UUID NOT NULL,
    display_name VARCHAR(255) NOT NULL,
    email VARCHAR(255) NOT NULL,
    certification_status VARCHAR(50) NOT NULL DEFAULT 'uncertified',
    certified_at TIMESTAMPTZ,
    certification_expires_at TIMESTAMPTZ,
    badge_image_url TEXT,
    badge_verification_slug VARCHAR(255) UNIQUE,
    training_completed_at TIMESTAMPTZ,
    activity_last_at TIMESTAMPTZ,
    created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
    updated_at TIMESTAMPTZ NOT NULL DEFAULT NOW()
            );

            CREATE INDEX IF NOT EXISTS idx_facilitators_institution_id ON facilitators(institution_id);
CREATE INDEX IF NOT EXISTS idx_facilitators_created_at ON facilitators(created_at);

            COMMIT;

            -- ROLLBACK SQL:
            -- DROP TABLE IF EXISTS facilitators CASCADE;
