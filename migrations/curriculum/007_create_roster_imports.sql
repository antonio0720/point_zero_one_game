-- Migration 007: Create roster_imports
            -- Generated by pzo_curriculum_gateway_generator.py
            -- Date: 2026-03-07T14:32:44Z

            BEGIN;

            CREATE TABLE IF NOT EXISTS roster_imports (
                id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    institution_id UUID NOT NULL REFERENCES institutions(id) ON DELETE CASCADE,
    cohort_id UUID NOT NULL REFERENCES cohorts(id) ON DELETE CASCADE,
    file_name VARCHAR(255) NOT NULL,
    file_size_bytes INTEGER NOT NULL,
    file_hash VARCHAR(64) NOT NULL,
    status VARCHAR(50) NOT NULL DEFAULT 'pending',
    total_rows INTEGER,
    valid_rows INTEGER,
    invalid_rows INTEGER,
    error_details JSONB,
    imported_by UUID NOT NULL,
    started_at TIMESTAMPTZ,
    completed_at TIMESTAMPTZ,
    created_at TIMESTAMPTZ NOT NULL DEFAULT NOW()
            );

            CREATE INDEX IF NOT EXISTS idx_roster_imports_institution_id ON roster_imports(institution_id);
CREATE INDEX IF NOT EXISTS idx_roster_imports_cohort_id ON roster_imports(cohort_id);
CREATE INDEX IF NOT EXISTS idx_roster_imports_status ON roster_imports(status);
CREATE INDEX IF NOT EXISTS idx_roster_imports_created_at ON roster_imports(created_at);

            COMMIT;

            -- ROLLBACK SQL:
            -- DROP TABLE IF EXISTS roster_imports CASCADE;
