-- Migration 017: Create debrief_prompt_bank
            -- Generated by pzo_curriculum_gateway_generator.py
            -- Date: 2026-03-07T14:32:44Z

            BEGIN;

            CREATE TABLE IF NOT EXISTS debrief_prompt_bank (
                id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    prompt_text TEXT NOT NULL,
    tags JSONB NOT NULL DEFAULT '[]',
    severity VARCHAR(50) NOT NULL DEFAULT 'standard',
    audience VARCHAR(50) NOT NULL DEFAULT 'learner',
    failure_mode VARCHAR(100),
    pack_id UUID REFERENCES curriculum_packs(id),
    created_at TIMESTAMPTZ NOT NULL DEFAULT NOW()
            );

            CREATE INDEX IF NOT EXISTS idx_debrief_prompt_bank_pack_id ON debrief_prompt_bank(pack_id);
CREATE INDEX IF NOT EXISTS idx_debrief_prompt_bank_created_at ON debrief_prompt_bank(created_at);

            COMMIT;

            -- ROLLBACK SQL:
            -- DROP TABLE IF EXISTS debrief_prompt_bank CASCADE;
