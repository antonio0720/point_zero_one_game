-- Migration 014: Create benchmark_attempts
            -- Generated by pzo_curriculum_gateway_generator.py
            -- Date: 2026-03-07T14:32:44Z

            BEGIN;

            CREATE TABLE IF NOT EXISTS benchmark_attempts (
                id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    benchmark_window_id UUID NOT NULL REFERENCES benchmark_windows(id),
    learner_id UUID NOT NULL REFERENCES learners(id),
    run_id UUID NOT NULL,
    attempt_number INTEGER NOT NULL,
    status VARCHAR(50) NOT NULL DEFAULT 'in_progress',
    cord_score NUMERIC(10,4),
    proof_hash VARCHAR(64),
    proof_verified BOOLEAN NOT NULL DEFAULT FALSE,
    integrity_status VARCHAR(50) NOT NULL DEFAULT 'pending',
    metric_snapshot JSONB,
    seed_used VARCHAR(64) NOT NULL,
    pack_version_id UUID NOT NULL REFERENCES pack_versions(id),
    ruleset_version VARCHAR(50) NOT NULL,
    engine_version VARCHAR(50) NOT NULL,
    started_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
    completed_at TIMESTAMPTZ,
    created_at TIMESTAMPTZ NOT NULL DEFAULT NOW()
            );

            CREATE INDEX IF NOT EXISTS idx_benchmark_attempts_benchmark_window_id ON benchmark_attempts(benchmark_window_id);
CREATE INDEX IF NOT EXISTS idx_benchmark_attempts_learner_id ON benchmark_attempts(learner_id);
CREATE INDEX IF NOT EXISTS idx_benchmark_attempts_status ON benchmark_attempts(status);
CREATE INDEX IF NOT EXISTS idx_benchmark_attempts_pack_version_id ON benchmark_attempts(pack_version_id);
CREATE INDEX IF NOT EXISTS idx_benchmark_attempts_created_at ON benchmark_attempts(created_at);

            COMMIT;

            -- ROLLBACK SQL:
            -- DROP TABLE IF EXISTS benchmark_attempts CASCADE;
