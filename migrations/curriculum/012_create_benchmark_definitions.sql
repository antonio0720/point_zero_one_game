-- Migration 012: Create benchmark_definitions
            -- Generated by pzo_curriculum_gateway_generator.py
            -- Date: 2026-03-07T14:32:44Z

            BEGIN;

            CREATE TABLE IF NOT EXISTS benchmark_definitions (
                id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    benchmark_code VARCHAR(100) NOT NULL UNIQUE,
    pack_version_id UUID NOT NULL REFERENCES pack_versions(id) ON DELETE CASCADE,
    scenario_set JSONB NOT NULL,
    seed_set JSONB NOT NULL,
    attempt_limit INTEGER NOT NULL DEFAULT 3,
    scoring_formula_version VARCHAR(50) NOT NULL,
    comparability_policy JSONB NOT NULL,
    proof_required BOOLEAN NOT NULL DEFAULT TRUE,
    min_client_build VARCHAR(50),
    status VARCHAR(50) NOT NULL DEFAULT 'active',
    created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
    updated_at TIMESTAMPTZ NOT NULL DEFAULT NOW()
            );

            CREATE INDEX IF NOT EXISTS idx_benchmark_definitions_pack_version_id ON benchmark_definitions(pack_version_id);
CREATE INDEX IF NOT EXISTS idx_benchmark_definitions_status ON benchmark_definitions(status);
CREATE INDEX IF NOT EXISTS idx_benchmark_definitions_created_at ON benchmark_definitions(created_at);

            COMMIT;

            -- ROLLBACK SQL:
            -- DROP TABLE IF EXISTS benchmark_definitions CASCADE;
