-- Migration 002: Create institution_branding
            -- Generated by pzo_curriculum_gateway_generator.py
            -- Date: 2026-03-07T14:32:44Z

            BEGIN;

            CREATE TABLE IF NOT EXISTS institution_branding (
                id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    institution_id UUID NOT NULL REFERENCES institutions(id) ON DELETE CASCADE,
    logo_light_url TEXT,
    logo_dark_url TEXT,
    square_mark_url TEXT,
    og_image_url TEXT,
    report_header_logo_url TEXT,
    favicon_url TEXT,
    primary_color VARCHAR(7) NOT NULL DEFAULT '#1A1A2E',
    secondary_color VARCHAR(7) NOT NULL DEFAULT '#16213E',
    accent_color VARCHAR(7) NOT NULL DEFAULT '#E94560',
    cohort_certificate_template TEXT,
    proof_co_branding_enabled BOOLEAN NOT NULL DEFAULT FALSE,
    report_footer_disclosure TEXT,
    created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
    updated_at TIMESTAMPTZ NOT NULL DEFAULT NOW()
            );

            CREATE INDEX IF NOT EXISTS idx_institution_branding_institution_id ON institution_branding(institution_id);
CREATE INDEX IF NOT EXISTS idx_institution_branding_created_at ON institution_branding(created_at);

            COMMIT;

            -- ROLLBACK SQL:
            -- DROP TABLE IF EXISTS institution_branding CASCADE;
