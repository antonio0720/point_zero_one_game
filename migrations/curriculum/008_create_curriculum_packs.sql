-- Migration 008: Create curriculum_packs
            -- Generated by pzo_curriculum_gateway_generator.py
            -- Date: 2026-03-07T14:32:44Z

            BEGIN;

            CREATE TABLE IF NOT EXISTS curriculum_packs (
                id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    slug VARCHAR(255) NOT NULL UNIQUE,
    title VARCHAR(255) NOT NULL,
    description TEXT,
    competency_domain VARCHAR(100) NOT NULL,
    audience_type VARCHAR(100) NOT NULL DEFAULT 'general',
    status VARCHAR(50) NOT NULL DEFAULT 'draft',
    current_version_id UUID,
    created_by UUID NOT NULL,
    created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
    updated_at TIMESTAMPTZ NOT NULL DEFAULT NOW()
            );

            CREATE INDEX IF NOT EXISTS idx_curriculum_packs_slug ON curriculum_packs(slug);
CREATE INDEX IF NOT EXISTS idx_curriculum_packs_status ON curriculum_packs(status);
CREATE INDEX IF NOT EXISTS idx_curriculum_packs_created_at ON curriculum_packs(created_at);

            COMMIT;

            -- ROLLBACK SQL:
            -- DROP TABLE IF EXISTS curriculum_packs CASCADE;
