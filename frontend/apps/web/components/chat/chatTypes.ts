/** App-shell compatibility types re-exporting canonical package chat contracts. */
export type {
  ActiveRivalry,
  AlliancePanelState,
  ChatChannel,
  ChatPanelView,
  DealRoomState,
  GameEventType,
  LearningProfileSnapshot,
  MarketMoveAlertPayload,
  MessageKind,
  OmnipresentChatContext,
  PackageChatMessage as ChatMessage,
  PackageChatRoom as Room,
  PackageChatRoomMember as RoomMember,
  RivalryPhase,
  RoomType,
} from '@pzo/engine';
