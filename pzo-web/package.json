// pzo-web/package.json - Ensure all dependencies are up-to-date and compatible with strict TypeScript execution (adjust versions as necessary).
{
  "name": "@pzo/point-zero-one", // Adjust the name to match your project's package identifier, if different.
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "react-scripts test --env=jsdom" // Adjust the script to use Vitest or Jest as needed, ensuring it's compatible with your setup and dependencies (e.g., using `vitest` command).
  },
  "dependencies": {},
  "devDependencies": {
    "@testing-library/jest-dom": "^5.16.1", // Ensure all necessary testing libraries are installed, adjust versions as needed for compatibility with strict TypeScript execution and your project's setup (e.g., `vitest` or `@typescript-eslint`).
    "ts-jest": "^27.0.5", // Use the latest version of ts-jest compatible with Vitest/Jest, adjust as needed for compatibility checks against strict TypeScript execution and your project's setup (e.g., `vitest` or `@typescript-eslint`).
    "tsconfig-paths": "^3.10.0", // Optional: Use tsconfig-paths to handle aliases in a way that works with both Jest/Vitest and TypeScript, adjust as needed for compatibility checks against strict TypeScript execution (e.g., `vitest` or `@typescript-eslint`).
    "tsc": "^4.0.3", // Optional: Use the TypeScript compiler to ensure all code is transpiled correctly before running tests in a non-TypeScript environment, adjust as needed for compatibility checks against strict TypeScript execution (e.g., `vitest` or `@typescript-eslint`).
  },
  "peerDependencies": {
    "@babel/runtime": "^7.15.0", // Optional: Specify peer dependencies required by your project, adjust versions as needed for compatibility checks against strict TypeScript execution (e.g., `vitest` or `@typescript-eslint`).
  },
  "author": "",
  "license": ""
}
