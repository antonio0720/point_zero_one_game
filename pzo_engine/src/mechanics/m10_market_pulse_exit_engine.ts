// ═══════════════════════════════════════════════════════════════════════════════
// POINT ZERO ONE — src/mechanics/m10_market_pulse_exit_engine.ts
// AUTO-GENERATED by scripts/build_mechanics.py — DO NOT EDIT MANUALLY
// Regenerate: python3 scripts/build_mechanics.py --force
//
// Mechanic : M10 — Market Pulse Exit Engine
// Family   : run_core
// Layer    : card_handler
// Priority : 1  Batch: 1
// ML Pair  : m10a
// Deps     : M05, M09
//
// Density6 LLC · Point Zero One · Confidential · All Rights Reserved
// ═══════════════════════════════════════════════════════════════════════════════

// import type { M05Types } from './m05_...';
// import type { M09Types } from './m09_...';

// ── Input contract ────────────────────────────────────────────────────────────
export interface M10Input {
  state_assets: unknown;  // state.assets
  state_macroregime: unknown;  // state.macroRegime
  exitcard: unknown;  // exitCard
  state_tick: unknown;  // state.tick
}

// ── Output contract ───────────────────────────────────────────────────────────
export interface M10Output {
  exitresult: unknown;  // exitResult
  saleproceeds: unknown;  // saleProceeds
  capitalgain: unknown;  // capitalGain
}

// ── Telemetry events ──────────────────────────────────────────────────────────
export type M10Event = 'ASSET_EXITED' | 'EXIT_TIMING_SCORED' | 'MARKET_PULSE_READ' | 'CAPITAL_GAIN_REALIZED';

export interface M10TelemetryPayload {
  event: M10Event;
  mechanic_id: 'M10';
  tick: number;
  runId: string;
  payload: Record<string, unknown>;
}

// ── Design constants ──────────────────────────────────────────────────────────
export const M10_CONSTANTS = {
  MECHANIC_ID:  'M10',
  TITLE:        'Market Pulse Exit Engine',
  FAMILY:       'run_core',
  LAYER:        'card_handler',
  PRIORITY:      1,
  BATCH:         1,
  ML_PAIR:      'm10a',
  EXEC_HOOK:    'marketPulseExit',
  DESIGN_LAWS: [
    'deterministic-by-seed',
    'server-verified-via-ledger',
    'bounded-chaos',
    'no-pay-to-win',
  ],
} as const;

// ── Exec hook ─────────────────────────────────────────────────────────────────
/**
 * marketPulseExit
 *
 * Called by the EngineOrchestrator (or appropriate engine) on each
 * relevant tick/event. All state mutations MUST be returned in the
 * output — never applied in place. All telemetry MUST be emitted
 * via the EventBus, never fired directly.
 *
 * @param input  - Typed input snapshot (see M10Input)
 * @param emit   - Telemetry emitter — call for every meaningful state change
 * @returns      - Typed output (see M10Output)
 */
export function marketPulseExit(
  input: M10Input,
  emit: (payload: M10TelemetryPayload) => void,
): M10Output {
  // ── TODO: implement M10 — Market Pulse Exit Engine ───────────────────────────────
  //
  // Implementation checklist:
  // □ handle: state.assets
  // □ handle: state.macroRegime
  // □ handle: exitCard
  // □ handle: state.tick
  //
  // Emit at minimum: ASSET_EXITED
  //
  // Required telemetry shape:
  //   emit({ event: '...', mechanic_id: 'M10', tick: <tick>, runId: <runId>, payload: {} });
  //
  // ─────────────────────────────────────────────────────────────────────────
  throw new Error('M10 (marketPulseExit) is not yet implemented.');
}

// ── ML companion hook ─────────────────────────────────────────────────────────
/**
 * ML companion: m10a
 * Async — fires after exec_hook, reads output, returns advisory signals only.
 * NEVER mutates state. Results feed Case File, Intel bars, and CORD scoring.
 */
export type M10MLInput = M10Output & { runId: string; tick: number };
export interface M10MLOutput {
  score: number;           // 0–1
  topFactors: string[];    // max 5 plain-English factors
  recommendation: string;  // single sentence
  auditHash: string;       // SHA256(inputs + outputs + rulesVersion)
}

export async function marketPulseExitMLCompanion(
  _input: M10MLInput,
): Promise<M10MLOutput> {
  // TODO: call ML inference endpoint for m10a
  throw new Error('M10 ML companion (m10a) is not yet implemented.');
}
