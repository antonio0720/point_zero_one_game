// AUTO-GENERATED by scripts/build_mechanics.py — DO NOT EDIT exec_hook body manually
// Density6 LLC · Point Zero One · Confidential
// pzo_engine/src/mechanics/m08_shield_cancel_system.ts
//
// Mechanic : M08 — Shield / Cancel System
// Family   : run_core   Layer: card_handler   Priority: 1   Batch: 1
// ML Pair  : m08a
// Deps     : M04
//
// Design Laws:
//   ✦ Deterministic-by-seed  ✦ Server-verified via ledger
//   ✦ Bounded chaos          ✦ No pay-to-win

import { clamp, computeHash, seededShuffle, seededIndex,
         buildMacroSchedule, buildChaosWindows,
         buildWeightedPool, OPPORTUNITY_POOL, DEFAULT_CARD, DEFAULT_CARD_IDS,
         computeDecayRate, EXIT_PULSE_MULTIPLIERS,
         MACRO_EVENTS_PER_RUN, CHAOS_WINDOWS_PER_RUN, RUN_TOTAL_TICKS,
         PRESSURE_WEIGHTS, PHASE_WEIGHTS, REGIME_WEIGHTS,
         REGIME_MULTIPLIERS } from './mechanicsUtils';
import type {
  RunPhase, TickTier, MacroRegime, PressureTier, SolvencyStatus,
  Asset, IPAItem, GameCard, GameEvent, ShieldLayer, Debt, Buff,
  Liability, SetBonus, AssetMod, IncomeItem, MacroEvent, ChaosWindow,
  AuctionResult, PurchaseResult, ShieldResult, ExitResult, TickResult,
  DeckComposition, TierProgress, WipeEvent, RegimeShiftEvent,
  PhaseTransitionEvent, TimerExpiredEvent, StreakEvent, FubarEvent,
  LedgerEntry, ProofCard, CompletedRun, SeasonState, RunState,
  MomentEvent, ClipBoundary, MechanicTelemetryPayload, MechanicEmitter,
} from './types';


// ── Input / Output contracts ──────────────────────────────────────────────

export interface M08Input {
  incomingEvent?: GameEvent;
  stateShieldLayers?: ShieldLayer[];
  cardPlayed?: GameCard;
}

export interface M08Output {
  shieldResult: ShieldResult;
  damageAbsorbed: number;
  shieldLayerUpdated: ShieldLayer[];
}

// ── Telemetry ─────────────────────────────────────────────────────────────

export type M08Event = 'SHIELD_ACTIVATED' | 'SHIELD_PIERCED' | 'SHIELD_DEPLETED' | 'SHIELD_REGEN_APPLIED';

export interface M08TelemetryPayload extends MechanicTelemetryPayload {
  event: M08Event;
  mechanic_id: 'M08';
}

// ── Design bounds (never mutate at runtime) ────────────────────────────────

export const M08_BOUNDS = {
  TRIGGER_THRESHOLD:   3,
  MULTIPLIER:          1.5,
  MAX_AMOUNT:          50_000,
  MIN_CASH_DELTA:      -20_000,
  MAX_CASH_DELTA:       20_000,
  MIN_CASHFLOW_DELTA:  -10_000,
  MAX_CASHFLOW_DELTA:   10_000,
  TIER_ESCAPE_TARGET:   3_000,
  REGIME_SHIFT_THRESHOLD: 500,
  BASE_DECAY_RATE:     0.02,
  BLEED_CASH_THRESHOLD: 1_000,
  FIRST_REFUSAL_TICKS: 6,
  PULSE_CYCLE:         12,
  MAX_PROCEEDS:        999_999,
  EFFECT_MULTIPLIER:   1.0,
  MIN_EFFECT:          0,
  MAX_EFFECT:          100_000,
} as const;

// ── Exec hook ─────────────────────────────────────────────────────────────

/**
 * shieldCancelSystem
 *
 * Called by MechanicsRouter inside the EngineOrchestrator tick or card handler.
 * All state mutations MUST be returned in the output — never applied in place.
 * All telemetry MUST be emitted via the emit callback.
 *
 * @param input  Typed input snapshot
 * @param emit   Telemetry emitter — call for every meaningful state change
 * @returns      Typed output (all fields populated, no throws)
 */
export function shieldCancelSystem(
  input: M08Input,
  emit: MechanicEmitter,
): M08Output {
  const incomingEvent = input.incomingEvent as GameEvent;
  const shieldLayers = (input.stateShieldLayers as ShieldLayer[]) ?? [];
  const cardPlayed = input.cardPlayed as GameCard | undefined;
  const incomingDamage: number = (incomingEvent as any)?.damage ?? 0;
  const shieldBonus: number = (cardPlayed as any)?.shieldValue ?? 0;
  const totalShield: number = shieldLayers.reduce((s, l) => s + l.strength, 0) + shieldBonus;
  const pierced = incomingDamage > totalShield;
  const absorbed = Math.min(incomingDamage, totalShield);
  const depleted = totalShield <= incomingDamage;
  const updatedLayers: ShieldLayer[] = shieldLayers
    .map(l => ({ ...l, strength: Math.max(0, l.strength - absorbed / Math.max(shieldLayers.length, 1)) }))
    .filter(l => l.strength > 0);
  const shieldResult: ShieldResult = { absorbed, pierced, depleted, remainingShield: Math.max(0, totalShield - absorbed) };
    emit({ event: 'SHIELD_ACTIVATED', mechanic_id: 'M08', tick: 0, runId: '', payload: { absorbed, pierced, depleted } });
  if (pierced) {   emit({ event: 'SHIELD_PIERCED', mechanic_id: 'M08', tick: '0', runId: '', payload: { absorbed, incomingDamage } }); }
  if (depleted) {   emit({ event: 'SHIELD_DEPLETED', mechanic_id: 'M08', tick: '0', runId: '', payload: { totalShield } }); }
    return {{
    shieldResult: shieldResult,
    damageAbsorbed: absorbed,
    shieldLayerUpdated: updatedLayers,
  }};
}

// ── ML companion hook ─────────────────────────────────────────────────────

export interface M08MLInput {
  shieldResult?: ShieldResult, damageAbsorbed?: number, shieldLayerUpdated?: ShieldLayer[];
  runId: string;
  tick:  number;
}

export interface M08MLOutput {
  score:          number;         // 0–1
  topFactors:     string[];       // max 5 plain-English factors
  recommendation: string;         // single sentence
  auditHash:      string;         // SHA256(inputs+outputs+rulesVersion)
  confidenceDecay: number;        // 0–1, how fast this signal should decay
}

/**
 * shieldCancelSystemMLCompanion
 * Async advisory — fires AFTER exec_hook, reads output, returns signals only.
 * NEVER mutates state. Results feed Case File, Intel bars, and CORD scoring.
 */
export async function shieldCancelSystemMLCompanion(
  input: M08MLInput,
): Promise<M08MLOutput> {
  // Advisory signal — bounded [0,1], no state mutation
  const score = Math.min(0.99, Math.max(0.01, Object.keys(input).length * 0.05));
  return {
    score,
    topFactors:     ['M08 signal computed', 'advisory only'],
    recommendation: 'Monitor M08 output and adjust strategy accordingly.',
    auditHash:      computeHash(JSON.stringify(input) + ':ml:M08'),
    confidenceDecay: 0.05,
  };
}
