// ═══════════════════════════════════════════════════════════════════════════════
// POINT ZERO ONE — src/mechanics/m13_systemic_friction_modules_so.ts
// AUTO-GENERATED by scripts/build_mechanics.py — DO NOT EDIT MANUALLY
// Regenerate: python3 scripts/build_mechanics.py --force
//
// Mechanic : M13 — Systemic Friction Modules (SO)
// Family   : chaos_engine
// Layer    : tick_engine
// Priority : 1  Batch: 1
// ML Pair  : m13a
// Deps     : M12
//
// Density6 LLC · Point Zero One · Confidential · All Rights Reserved
// ═══════════════════════════════════════════════════════════════════════════════

// import type { M12Types } from './m12_...';

// ── Input contract ────────────────────────────────────────────────────────────
export interface M13Input {
  fatetick: unknown;  // fateTick
  state_cash: unknown;  // state.cash
  state_tick: unknown;  // state.tick
  runseed: unknown;  // RunSeed
}

// ── Output contract ───────────────────────────────────────────────────────────
export interface M13Output {
  frictionexpenseevent: unknown;  // frictionExpenseEvent
  cashdrained: unknown;  // cashDrained
  frictionclass: unknown;  // frictionClass
}

// ── Telemetry events ──────────────────────────────────────────────────────────
export type M13Event = 'SO_FRICTION_APPLIED' | 'OBSTACLE_EXPENSE_CHARGED' | 'FRICTION_CLASS_RESOLVED';

export interface M13TelemetryPayload {
  event: M13Event;
  mechanic_id: 'M13';
  tick: number;
  runId: string;
  payload: Record<string, unknown>;
}

// ── Design constants ──────────────────────────────────────────────────────────
export const M13_CONSTANTS = {
  MECHANIC_ID:  'M13',
  TITLE:        'Systemic Friction Modules (SO)',
  FAMILY:       'chaos_engine',
  LAYER:        'tick_engine',
  PRIORITY:      1,
  BATCH:         1,
  ML_PAIR:      'm13a',
  EXEC_HOOK:    'systemicFrictionModule',
  DESIGN_LAWS: [
    'deterministic-by-seed',
    'server-verified-via-ledger',
    'bounded-chaos',
    'no-pay-to-win',
  ],
} as const;

// ── Exec hook ─────────────────────────────────────────────────────────────────
/**
 * systemicFrictionModule
 *
 * Called by the EngineOrchestrator (or appropriate engine) on each
 * relevant tick/event. All state mutations MUST be returned in the
 * output — never applied in place. All telemetry MUST be emitted
 * via the EventBus, never fired directly.
 *
 * @param input  - Typed input snapshot (see M13Input)
 * @param emit   - Telemetry emitter — call for every meaningful state change
 * @returns      - Typed output (see M13Output)
 */
export function systemicFrictionModule(
  input: M13Input,
  emit: (payload: M13TelemetryPayload) => void,
): M13Output {
  // ── TODO: implement M13 — Systemic Friction Modules (SO) ───────────────────────────────
  //
  // Implementation checklist:
  // □ handle: fateTick
  // □ handle: state.cash
  // □ handle: state.tick
  // □ handle: RunSeed
  //
  // Emit at minimum: SO_FRICTION_APPLIED
  //
  // Required telemetry shape:
  //   emit({ event: '...', mechanic_id: 'M13', tick: <tick>, runId: <runId>, payload: {} });
  //
  // ─────────────────────────────────────────────────────────────────────────
  throw new Error('M13 (systemicFrictionModule) is not yet implemented.');
}

// ── ML companion hook ─────────────────────────────────────────────────────────
/**
 * ML companion: m13a
 * Async — fires after exec_hook, reads output, returns advisory signals only.
 * NEVER mutates state. Results feed Case File, Intel bars, and CORD scoring.
 */
export type M13MLInput = M13Output & { runId: string; tick: number };
export interface M13MLOutput {
  score: number;           // 0–1
  topFactors: string[];    // max 5 plain-English factors
  recommendation: string;  // single sentence
  auditHash: string;       // SHA256(inputs + outputs + rulesVersion)
}

export async function systemicFrictionModuleMLCompanion(
  _input: M13MLInput,
): Promise<M13MLOutput> {
  // TODO: call ML inference endpoint for m13a
  throw new Error('M13 ML companion (m13a) is not yet implemented.');
}
