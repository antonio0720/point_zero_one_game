// ═══════════════════════════════════════════════════════════════════════════════
// POINT ZERO ONE — src/mechanics/m91_first_table_invite:_safe_social_onboardi.ts
// AUTO-GENERATED by scripts/build_mechanics.py — DO NOT EDIT MANUALLY
// Regenerate: python3 scripts/build_mechanics.py --force
//
// Mechanic : M91 — First Table Invite: Safe Social Onboarding Run
// Family   : onboarding_expert
// Layer    : backend_service
// Priority : 2  Batch: 2
// ML Pair  : m91a
// Deps     : M41, M15
//
// Density6 LLC · Point Zero One · Confidential · All Rights Reserved
// ═══════════════════════════════════════════════════════════════════════════════

// import type { M41Types } from './m41_...';
// import type { M15Types } from './m15_...';

// ── Input contract ────────────────────────────────────────────────────────────
export interface M91Input {
  inviterid: unknown;  // inviterId
  newplayerid: unknown;  // newPlayerId
  saferunconfig: unknown;  // safeRunConfig
}

// ── Output contract ───────────────────────────────────────────────────────────
export interface M91Output {
  saferunstarted: unknown;  // safeRunStarted
  guidedsocialmoment: unknown;  // guidedSocialMoment
  invitebonus: unknown;  // inviteBonus
}

// ── Telemetry events ──────────────────────────────────────────────────────────
export type M91Event = 'FIRST_TABLE_INVITE_SENT' | 'SAFE_RUN_STARTED' | 'SOCIAL_MOMENT_CAPTURED';

export interface M91TelemetryPayload {
  event: M91Event;
  mechanic_id: 'M91';
  tick: number;
  runId: string;
  payload: Record<string, unknown>;
}

// ── Design constants ──────────────────────────────────────────────────────────
export const M91_CONSTANTS = {
  MECHANIC_ID:  'M91',
  TITLE:        'First Table Invite: Safe Social Onboarding Run',
  FAMILY:       'onboarding_expert',
  LAYER:        'backend_service',
  PRIORITY:      2,
  BATCH:         2,
  ML_PAIR:      'm91a',
  EXEC_HOOK:    'firstTableInviteSafeRun',
  DESIGN_LAWS: [
    'deterministic-by-seed',
    'server-verified-via-ledger',
    'bounded-chaos',
    'no-pay-to-win',
  ],
} as const;

// ── Exec hook ─────────────────────────────────────────────────────────────────
/**
 * firstTableInviteSafeRun
 *
 * Called by the EngineOrchestrator (or appropriate engine) on each
 * relevant tick/event. All state mutations MUST be returned in the
 * output — never applied in place. All telemetry MUST be emitted
 * via the EventBus, never fired directly.
 *
 * @param input  - Typed input snapshot (see M91Input)
 * @param emit   - Telemetry emitter — call for every meaningful state change
 * @returns      - Typed output (see M91Output)
 */
export function firstTableInviteSafeRun(
  input: M91Input,
  emit: (payload: M91TelemetryPayload) => void,
): M91Output {
  // ── TODO: implement M91 — First Table Invite: Safe Social Onboarding Run ───────────────────────────────
  //
  // Implementation checklist:
  // □ handle: inviterId
  // □ handle: newPlayerId
  // □ handle: safeRunConfig
  //
  // Emit at minimum: FIRST_TABLE_INVITE_SENT
  //
  // Required telemetry shape:
  //   emit({ event: '...', mechanic_id: 'M91', tick: <tick>, runId: <runId>, payload: {} });
  //
  // ─────────────────────────────────────────────────────────────────────────
  throw new Error('M91 (firstTableInviteSafeRun) is not yet implemented.');
}

// ── ML companion hook ─────────────────────────────────────────────────────────
/**
 * ML companion: m91a
 * Async — fires after exec_hook, reads output, returns advisory signals only.
 * NEVER mutates state. Results feed Case File, Intel bars, and CORD scoring.
 */
export type M91MLInput = M91Output & { runId: string; tick: number };
export interface M91MLOutput {
  score: number;           // 0–1
  topFactors: string[];    // max 5 plain-English factors
  recommendation: string;  // single sentence
  auditHash: string;       // SHA256(inputs + outputs + rulesVersion)
}

export async function firstTableInviteSafeRunMLCompanion(
  _input: M91MLInput,
): Promise<M91MLOutput> {
  // TODO: call ML inference endpoint for m91a
  throw new Error('M91 ML companion (m91a) is not yet implemented.');
}
