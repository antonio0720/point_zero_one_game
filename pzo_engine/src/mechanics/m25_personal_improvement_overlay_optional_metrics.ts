// ═══════════════════════════════════════════════════════════════════════════════
// POINT ZERO ONE — src/mechanics/m25_personal_improvement_overlay_optional_metrics.ts
// AUTO-GENERATED by scripts/build_mechanics.py — DO NOT EDIT MANUALLY
// Regenerate: python3 scripts/build_mechanics.py --force
//
// Mechanic : M25 — Personal Improvement Overlay (Optional Metrics)
// Family   : moment_engine
// Layer    : ui_component
// Priority : 2  Batch: 1
// ML Pair  : m25a
// Deps     : M22
//
// Density6 LLC · Point Zero One · Confidential · All Rights Reserved
// ═══════════════════════════════════════════════════════════════════════════════

// import type { M22Types } from './m22_...';

// ── Input contract ────────────────────────────────────────────────────────────
export interface M25Input {
  runhistory: unknown;  // runHistory
  currentrunstate: unknown;  // currentRunState
  overlayenabled: unknown;  // overlayEnabled
}

// ── Output contract ───────────────────────────────────────────────────────────
export interface M25Output {
  improvementdeltas: unknown;  // improvementDeltas
  comparisonoverlay: unknown;  // comparisonOverlay
  performanceannotations: unknown;  // performanceAnnotations
}

// ── Telemetry events ──────────────────────────────────────────────────────────
export type M25Event = 'OVERLAY_VIEWED' | 'IMPROVEMENT_DELTA_COMPUTED' | 'PERSONAL_BEST_SET';

export interface M25TelemetryPayload {
  event: M25Event;
  mechanic_id: 'M25';
  tick: number;
  runId: string;
  payload: Record<string, unknown>;
}

// ── Design constants ──────────────────────────────────────────────────────────
export const M25_CONSTANTS = {
  MECHANIC_ID:  'M25',
  TITLE:        'Personal Improvement Overlay (Optional Metrics)',
  FAMILY:       'moment_engine',
  LAYER:        'ui_component',
  PRIORITY:      2,
  BATCH:         1,
  ML_PAIR:      'm25a',
  EXEC_HOOK:    'personalImprovementOverlay',
  DESIGN_LAWS: [
    'deterministic-by-seed',
    'server-verified-via-ledger',
    'bounded-chaos',
    'no-pay-to-win',
  ],
} as const;

// ── Exec hook ─────────────────────────────────────────────────────────────────
/**
 * personalImprovementOverlay
 *
 * Called by the EngineOrchestrator (or appropriate engine) on each
 * relevant tick/event. All state mutations MUST be returned in the
 * output — never applied in place. All telemetry MUST be emitted
 * via the EventBus, never fired directly.
 *
 * @param input  - Typed input snapshot (see M25Input)
 * @param emit   - Telemetry emitter — call for every meaningful state change
 * @returns      - Typed output (see M25Output)
 */
export function personalImprovementOverlay(
  input: M25Input,
  emit: (payload: M25TelemetryPayload) => void,
): M25Output {
  // ── TODO: implement M25 — Personal Improvement Overlay (Optional Metrics) ───────────────────────────────
  //
  // Implementation checklist:
  // □ handle: runHistory
  // □ handle: currentRunState
  // □ handle: overlayEnabled
  //
  // Emit at minimum: OVERLAY_VIEWED
  //
  // Required telemetry shape:
  //   emit({ event: '...', mechanic_id: 'M25', tick: <tick>, runId: <runId>, payload: {} });
  //
  // ─────────────────────────────────────────────────────────────────────────
  throw new Error('M25 (personalImprovementOverlay) is not yet implemented.');
}

// ── ML companion hook ─────────────────────────────────────────────────────────
/**
 * ML companion: m25a
 * Async — fires after exec_hook, reads output, returns advisory signals only.
 * NEVER mutates state. Results feed Case File, Intel bars, and CORD scoring.
 */
export type M25MLInput = M25Output & { runId: string; tick: number };
export interface M25MLOutput {
  score: number;           // 0–1
  topFactors: string[];    // max 5 plain-English factors
  recommendation: string;  // single sentence
  auditHash: string;       // SHA256(inputs + outputs + rulesVersion)
}

export async function personalImprovementOverlayMLCompanion(
  _input: M25MLInput,
): Promise<M25MLOutput> {
  // TODO: call ML inference endpoint for m25a
  throw new Error('M25 ML companion (m25a) is not yet implemented.');
}
