// ═══════════════════════════════════════════════════════════════════════════════
// POINT ZERO ONE — src/mechanics/m46_event_sourced_ledger:_append_only_run_tr.ts
// AUTO-GENERATED by scripts/build_mechanics.py — DO NOT EDIT MANUALLY
// Regenerate: python3 scripts/build_mechanics.py --force
//
// Mechanic : M46 — Event-Sourced Ledger: Append-Only Run Truth
// Family   : integrity_core
// Layer    : backend_service
// Priority : 1  Batch: 1
// ML Pair  : m46a
// Deps     : M01
//
// Density6 LLC · Point Zero One · Confidential · All Rights Reserved
// ═══════════════════════════════════════════════════════════════════════════════

// import type { M01Types } from './m01_...';

// ── Input contract ────────────────────────────────────────────────────────────
export interface M46Input {
  gameaction: unknown;  // gameAction
  state_tick: unknown;  // state.tick
  state_runid: unknown;  // state.runId
}

// ── Output contract ───────────────────────────────────────────────────────────
export interface M46Output {
  ledgerentry: unknown;  // ledgerEntry
  ledgerhash: unknown;  // ledgerHash
  appendconfirmed: unknown;  // appendConfirmed
}

// ── Telemetry events ──────────────────────────────────────────────────────────
export type M46Event = 'LEDGER_APPEND' | 'LEDGER_SEALED' | 'LEDGER_TAMPER_DETECTED';

export interface M46TelemetryPayload {
  event: M46Event;
  mechanic_id: 'M46';
  tick: number;
  runId: string;
  payload: Record<string, unknown>;
}

// ── Design constants ──────────────────────────────────────────────────────────
export const M46_CONSTANTS = {
  MECHANIC_ID:  'M46',
  TITLE:        'Event-Sourced Ledger: Append-Only Run Truth',
  FAMILY:       'integrity_core',
  LAYER:        'backend_service',
  PRIORITY:      1,
  BATCH:         1,
  ML_PAIR:      'm46a',
  EXEC_HOOK:    'eventSourcedLedgerAppend',
  DESIGN_LAWS: [
    'deterministic-by-seed',
    'server-verified-via-ledger',
    'bounded-chaos',
    'no-pay-to-win',
  ],
} as const;

// ── Exec hook ─────────────────────────────────────────────────────────────────
/**
 * eventSourcedLedgerAppend
 *
 * Called by the EngineOrchestrator (or appropriate engine) on each
 * relevant tick/event. All state mutations MUST be returned in the
 * output — never applied in place. All telemetry MUST be emitted
 * via the EventBus, never fired directly.
 *
 * @param input  - Typed input snapshot (see M46Input)
 * @param emit   - Telemetry emitter — call for every meaningful state change
 * @returns      - Typed output (see M46Output)
 */
export function eventSourcedLedgerAppend(
  input: M46Input,
  emit: (payload: M46TelemetryPayload) => void,
): M46Output {
  // ── TODO: implement M46 — Event-Sourced Ledger: Append-Only Run Truth ───────────────────────────────
  //
  // Implementation checklist:
  // □ handle: gameAction
  // □ handle: state.tick
  // □ handle: state.runId
  //
  // Emit at minimum: LEDGER_APPEND
  //
  // Required telemetry shape:
  //   emit({ event: '...', mechanic_id: 'M46', tick: <tick>, runId: <runId>, payload: {} });
  //
  // ─────────────────────────────────────────────────────────────────────────
  throw new Error('M46 (eventSourcedLedgerAppend) is not yet implemented.');
}

// ── ML companion hook ─────────────────────────────────────────────────────────
/**
 * ML companion: m46a
 * Async — fires after exec_hook, reads output, returns advisory signals only.
 * NEVER mutates state. Results feed Case File, Intel bars, and CORD scoring.
 */
export type M46MLInput = M46Output & { runId: string; tick: number };
export interface M46MLOutput {
  score: number;           // 0–1
  topFactors: string[];    // max 5 plain-English factors
  recommendation: string;  // single sentence
  auditHash: string;       // SHA256(inputs + outputs + rulesVersion)
}

export async function eventSourcedLedgerAppendMLCompanion(
  _input: M46MLInput,
): Promise<M46MLOutput> {
  // TODO: call ML inference endpoint for m46a
  throw new Error('M46 ML companion (m46a) is not yet implemented.');
}
