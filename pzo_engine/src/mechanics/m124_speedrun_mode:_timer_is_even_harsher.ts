// ═══════════════════════════════════════════════════════════════════════════════
// POINT ZERO ONE — src/mechanics/m124_speedrun_mode:_timer_is_even_harsher.ts
// AUTO-GENERATED by scripts/build_mechanics.py — DO NOT EDIT MANUALLY
// Regenerate: python3 scripts/build_mechanics.py --force
//
// Mechanic : M124 — Speedrun Mode: Timer Is Even Harsher
// Family   : social_advanced
// Layer    : season_runtime
// Priority : 2  Batch: 3
// ML Pair  : m124a
// Deps     : M02, M64
//
// Density6 LLC · Point Zero One · Confidential · All Rights Reserved
// ═══════════════════════════════════════════════════════════════════════════════

// import type { M02Types } from './m02_...';
// import type { M64Types } from './m64_...';

// ── Input contract ────────────────────────────────────────────────────────────
export interface M124Input {
  speedrunconfig: unknown;  // speedrunConfig
  runseed: unknown;  // RunSeed
  timerconfig: unknown;  // timerConfig
}

// ── Output contract ───────────────────────────────────────────────────────────
export interface M124Output {
  speedrunstate: unknown;  // speedrunState
  splittimes: unknown;  // splitTimes
  personalbest: unknown;  // personalBest
}

// ── Telemetry events ──────────────────────────────────────────────────────────
export type M124Event = 'SPEEDRUN_STARTED' | 'SPLIT_RECORDED' | 'PERSONAL_BEST_SET';

export interface M124TelemetryPayload {
  event: M124Event;
  mechanic_id: 'M124';
  tick: number;
  runId: string;
  payload: Record<string, unknown>;
}

// ── Design constants ──────────────────────────────────────────────────────────
export const M124_CONSTANTS = {
  MECHANIC_ID:  'M124',
  TITLE:        'Speedrun Mode: Timer Is Even Harsher',
  FAMILY:       'social_advanced',
  LAYER:        'season_runtime',
  PRIORITY:      2,
  BATCH:         3,
  ML_PAIR:      'm124a',
  EXEC_HOOK:    'speedrunModeEngine',
  DESIGN_LAWS: [
    'deterministic-by-seed',
    'server-verified-via-ledger',
    'bounded-chaos',
    'no-pay-to-win',
  ],
} as const;

// ── Exec hook ─────────────────────────────────────────────────────────────────
/**
 * speedrunModeEngine
 *
 * Called by the EngineOrchestrator (or appropriate engine) on each
 * relevant tick/event. All state mutations MUST be returned in the
 * output — never applied in place. All telemetry MUST be emitted
 * via the EventBus, never fired directly.
 *
 * @param input  - Typed input snapshot (see M124Input)
 * @param emit   - Telemetry emitter — call for every meaningful state change
 * @returns      - Typed output (see M124Output)
 */
export function speedrunModeEngine(
  input: M124Input,
  emit: (payload: M124TelemetryPayload) => void,
): M124Output {
  // ── TODO: implement M124 — Speedrun Mode: Timer Is Even Harsher ───────────────────────────────
  //
  // Implementation checklist:
  // □ handle: speedrunConfig
  // □ handle: RunSeed
  // □ handle: timerConfig
  //
  // Emit at minimum: SPEEDRUN_STARTED
  //
  // Required telemetry shape:
  //   emit({ event: '...', mechanic_id: 'M124', tick: <tick>, runId: <runId>, payload: {} });
  //
  // ─────────────────────────────────────────────────────────────────────────
  throw new Error('M124 (speedrunModeEngine) is not yet implemented.');
}

// ── ML companion hook ─────────────────────────────────────────────────────────
/**
 * ML companion: m124a
 * Async — fires after exec_hook, reads output, returns advisory signals only.
 * NEVER mutates state. Results feed Case File, Intel bars, and CORD scoring.
 */
export type M124MLInput = M124Output & { runId: string; tick: number };
export interface M124MLOutput {
  score: number;           // 0–1
  topFactors: string[];    // max 5 plain-English factors
  recommendation: string;  // single sentence
  auditHash: string;       // SHA256(inputs + outputs + rulesVersion)
}

export async function speedrunModeEngineMLCompanion(
  _input: M124MLInput,
): Promise<M124MLOutput> {
  // TODO: call ML inference endpoint for m124a
  throw new Error('M124 ML companion (m124a) is not yet implemented.');
}
