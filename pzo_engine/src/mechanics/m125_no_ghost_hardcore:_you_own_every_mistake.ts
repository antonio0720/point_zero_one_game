// ═══════════════════════════════════════════════════════════════════════════════
// POINT ZERO ONE — src/mechanics/m125_no_ghost_hardcore:_you_own_every_mistake.ts
// AUTO-GENERATED by scripts/build_mechanics.py — DO NOT EDIT MANUALLY
// Regenerate: python3 scripts/build_mechanics.py --force
//
// Mechanic : M125 — No Ghost Hardcore: You Own Every Mistake
// Family   : social_advanced
// Layer    : season_runtime
// Priority : 2  Batch: 3
// ML Pair  : m125a
// Deps     : M14, M03
//
// Density6 LLC · Point Zero One · Confidential · All Rights Reserved
// ═══════════════════════════════════════════════════════════════════════════════

// import type { M14Types } from './m14_...';
// import type { M03Types } from './m03_...';

// ── Input contract ────────────────────────────────────────────────────────────
export interface M125Input {
  hardcoreflag: unknown;  // hardcoreFlag
  runseed: unknown;  // RunSeed
  hardcoreconfig: unknown;  // hardcoreConfig
}

// ── Output contract ───────────────────────────────────────────────────────────
export interface M125Output {
  hardcorerunstate: unknown;  // hardcoreRunState
  cordceiling: unknown;  // cordCeiling
  ghostdisabled: unknown;  // ghostDisabled
}

// ── Telemetry events ──────────────────────────────────────────────────────────
export type M125Event = 'HARDCORE_STARTED' | 'GHOST_DISABLED' | 'HARDCORE_FAILED' | 'HARDCORE_COMPLETED';

export interface M125TelemetryPayload {
  event: M125Event;
  mechanic_id: 'M125';
  tick: number;
  runId: string;
  payload: Record<string, unknown>;
}

// ── Design constants ──────────────────────────────────────────────────────────
export const M125_CONSTANTS = {
  MECHANIC_ID:  'M125',
  TITLE:        'No Ghost Hardcore: You Own Every Mistake',
  FAMILY:       'social_advanced',
  LAYER:        'season_runtime',
  PRIORITY:      2,
  BATCH:         3,
  ML_PAIR:      'm125a',
  EXEC_HOOK:    'noGhostHardcoreEngine',
  DESIGN_LAWS: [
    'deterministic-by-seed',
    'server-verified-via-ledger',
    'bounded-chaos',
    'no-pay-to-win',
  ],
} as const;

// ── Exec hook ─────────────────────────────────────────────────────────────────
/**
 * noGhostHardcoreEngine
 *
 * Called by the EngineOrchestrator (or appropriate engine) on each
 * relevant tick/event. All state mutations MUST be returned in the
 * output — never applied in place. All telemetry MUST be emitted
 * via the EventBus, never fired directly.
 *
 * @param input  - Typed input snapshot (see M125Input)
 * @param emit   - Telemetry emitter — call for every meaningful state change
 * @returns      - Typed output (see M125Output)
 */
export function noGhostHardcoreEngine(
  input: M125Input,
  emit: (payload: M125TelemetryPayload) => void,
): M125Output {
  // ── TODO: implement M125 — No Ghost Hardcore: You Own Every Mistake ───────────────────────────────
  //
  // Implementation checklist:
  // □ handle: hardcoreFlag
  // □ handle: RunSeed
  // □ handle: hardcoreConfig
  //
  // Emit at minimum: HARDCORE_STARTED
  //
  // Required telemetry shape:
  //   emit({ event: '...', mechanic_id: 'M125', tick: <tick>, runId: <runId>, payload: {} });
  //
  // ─────────────────────────────────────────────────────────────────────────
  throw new Error('M125 (noGhostHardcoreEngine) is not yet implemented.');
}

// ── ML companion hook ─────────────────────────────────────────────────────────
/**
 * ML companion: m125a
 * Async — fires after exec_hook, reads output, returns advisory signals only.
 * NEVER mutates state. Results feed Case File, Intel bars, and CORD scoring.
 */
export type M125MLInput = M125Output & { runId: string; tick: number };
export interface M125MLOutput {
  score: number;           // 0–1
  topFactors: string[];    // max 5 plain-English factors
  recommendation: string;  // single sentence
  auditHash: string;       // SHA256(inputs + outputs + rulesVersion)
}

export async function noGhostHardcoreEngineMLCompanion(
  _input: M125MLInput,
): Promise<M125MLOutput> {
  // TODO: call ML inference endpoint for m125a
  throw new Error('M125 ML companion (m125a) is not yet implemented.');
}
