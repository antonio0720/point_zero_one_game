// ═══════════════════════════════════════════════════════════════════════════════
// POINT ZERO ONE — src/mechanics/m120_consent_gates:_social_chaos_must_be_opt_.ts
// AUTO-GENERATED by scripts/build_mechanics.py — DO NOT EDIT MANUALLY
// Regenerate: python3 scripts/build_mechanics.py --force
//
// Mechanic : M120 — Consent Gates: Social Chaos Must Be Opt-In
// Family   : social_advanced
// Layer    : ui_component
// Priority : 1  Batch: 3
// ML Pair  : m120a
// Deps     : M15
//
// Density6 LLC · Point Zero One · Confidential · All Rights Reserved
// ═══════════════════════════════════════════════════════════════════════════════

// import type { M15Types } from './m15_...';

// ── Input contract ────────────────────────────────────────────────────────────
export interface M120Input {
  socialaction: unknown;  // socialAction
  targetplayerid: unknown;  // targetPlayerId
  consentstatus: unknown;  // consentStatus
}

// ── Output contract ───────────────────────────────────────────────────────────
export interface M120Output {
  consentverified: unknown;  // consentVerified
  actionpermitted: unknown;  // actionPermitted
  consentdenied: unknown;  // consentDenied
}

// ── Telemetry events ──────────────────────────────────────────────────────────
export type M120Event = 'CONSENT_REQUESTED' | 'CONSENT_GRANTED' | 'CONSENT_DENIED';

export interface M120TelemetryPayload {
  event: M120Event;
  mechanic_id: 'M120';
  tick: number;
  runId: string;
  payload: Record<string, unknown>;
}

// ── Design constants ──────────────────────────────────────────────────────────
export const M120_CONSTANTS = {
  MECHANIC_ID:  'M120',
  TITLE:        'Consent Gates: Social Chaos Must Be Opt-In',
  FAMILY:       'social_advanced',
  LAYER:        'ui_component',
  PRIORITY:      1,
  BATCH:         3,
  ML_PAIR:      'm120a',
  EXEC_HOOK:    'consentGateVerifier',
  DESIGN_LAWS: [
    'deterministic-by-seed',
    'server-verified-via-ledger',
    'bounded-chaos',
    'no-pay-to-win',
  ],
} as const;

// ── Exec hook ─────────────────────────────────────────────────────────────────
/**
 * consentGateVerifier
 *
 * Called by the EngineOrchestrator (or appropriate engine) on each
 * relevant tick/event. All state mutations MUST be returned in the
 * output — never applied in place. All telemetry MUST be emitted
 * via the EventBus, never fired directly.
 *
 * @param input  - Typed input snapshot (see M120Input)
 * @param emit   - Telemetry emitter — call for every meaningful state change
 * @returns      - Typed output (see M120Output)
 */
export function consentGateVerifier(
  input: M120Input,
  emit: (payload: M120TelemetryPayload) => void,
): M120Output {
  // ── TODO: implement M120 — Consent Gates: Social Chaos Must Be Opt-In ───────────────────────────────
  //
  // Implementation checklist:
  // □ handle: socialAction
  // □ handle: targetPlayerId
  // □ handle: consentStatus
  //
  // Emit at minimum: CONSENT_REQUESTED
  //
  // Required telemetry shape:
  //   emit({ event: '...', mechanic_id: 'M120', tick: <tick>, runId: <runId>, payload: {} });
  //
  // ─────────────────────────────────────────────────────────────────────────
  throw new Error('M120 (consentGateVerifier) is not yet implemented.');
}

// ── ML companion hook ─────────────────────────────────────────────────────────
/**
 * ML companion: m120a
 * Async — fires after exec_hook, reads output, returns advisory signals only.
 * NEVER mutates state. Results feed Case File, Intel bars, and CORD scoring.
 */
export type M120MLInput = M120Output & { runId: string; tick: number };
export interface M120MLOutput {
  score: number;           // 0–1
  topFactors: string[];    // max 5 plain-English factors
  recommendation: string;  // single sentence
  auditHash: string;       // SHA256(inputs + outputs + rulesVersion)
}

export async function consentGateVerifierMLCompanion(
  _input: M120MLInput,
): Promise<M120MLOutput> {
  // TODO: call ML inference endpoint for m120a
  throw new Error('M120 ML companion (m120a) is not yet implemented.');
}
