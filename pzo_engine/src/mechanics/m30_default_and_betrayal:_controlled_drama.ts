// ═══════════════════════════════════════════════════════════════════════════════
// POINT ZERO ONE — src/mechanics/m30_default_and_betrayal:_controlled_drama.ts
// AUTO-GENERATED by scripts/build_mechanics.py — DO NOT EDIT MANUALLY
// Regenerate: python3 scripts/build_mechanics.py --force
//
// Mechanic : M30 — Default and Betrayal: Controlled Drama
// Family   : coop_contracts
// Layer    : api_endpoint
// Priority : 2  Batch: 1
// ML Pair  : m30a
// Deps     : M26, M27
//
// Density6 LLC · Point Zero One · Confidential · All Rights Reserved
// ═══════════════════════════════════════════════════════════════════════════════

// import type { M26Types } from './m26_...';
// import type { M27Types } from './m27_...';

// ── Input contract ────────────────────────────────────────────────────────────
export interface M30Input {
  contractid: unknown;  // contractId
  defectiontrigger: unknown;  // defectionTrigger
  state_tick: unknown;  // state.tick
  state_trustscore: unknown;  // state.trustScore
}

// ── Output contract ───────────────────────────────────────────────────────────
export interface M30Output {
  defectionevent: unknown;  // defectionEvent
  treasuryextraction: unknown;  // treasuryExtraction
  cordpenalty: unknown;  // cordPenalty
  runsplitevent: unknown;  // runSplitEvent
}

// ── Telemetry events ──────────────────────────────────────────────────────────
export type M30Event = 'DEFECTION_INITIATED' | 'DEFECTION_COUNTDOWN' | 'DEFECTION_EXECUTED' | 'BETRAYAL_RECORDED';

export interface M30TelemetryPayload {
  event: M30Event;
  mechanic_id: 'M30';
  tick: number;
  runId: string;
  payload: Record<string, unknown>;
}

// ── Design constants ──────────────────────────────────────────────────────────
export const M30_CONSTANTS = {
  MECHANIC_ID:  'M30',
  TITLE:        'Default and Betrayal: Controlled Drama',
  FAMILY:       'coop_contracts',
  LAYER:        'api_endpoint',
  PRIORITY:      2,
  BATCH:         1,
  ML_PAIR:      'm30a',
  EXEC_HOOK:    'defaultAndBetrayalHandler',
  DESIGN_LAWS: [
    'deterministic-by-seed',
    'server-verified-via-ledger',
    'bounded-chaos',
    'no-pay-to-win',
  ],
} as const;

// ── Exec hook ─────────────────────────────────────────────────────────────────
/**
 * defaultAndBetrayalHandler
 *
 * Called by the EngineOrchestrator (or appropriate engine) on each
 * relevant tick/event. All state mutations MUST be returned in the
 * output — never applied in place. All telemetry MUST be emitted
 * via the EventBus, never fired directly.
 *
 * @param input  - Typed input snapshot (see M30Input)
 * @param emit   - Telemetry emitter — call for every meaningful state change
 * @returns      - Typed output (see M30Output)
 */
export function defaultAndBetrayalHandler(
  input: M30Input,
  emit: (payload: M30TelemetryPayload) => void,
): M30Output {
  // ── TODO: implement M30 — Default and Betrayal: Controlled Drama ───────────────────────────────
  //
  // Implementation checklist:
  // □ handle: contractId
  // □ handle: defectionTrigger
  // □ handle: state.tick
  // □ handle: state.trustScore
  //
  // Emit at minimum: DEFECTION_INITIATED
  //
  // Required telemetry shape:
  //   emit({ event: '...', mechanic_id: 'M30', tick: <tick>, runId: <runId>, payload: {} });
  //
  // ─────────────────────────────────────────────────────────────────────────
  throw new Error('M30 (defaultAndBetrayalHandler) is not yet implemented.');
}

// ── ML companion hook ─────────────────────────────────────────────────────────
/**
 * ML companion: m30a
 * Async — fires after exec_hook, reads output, returns advisory signals only.
 * NEVER mutates state. Results feed Case File, Intel bars, and CORD scoring.
 */
export type M30MLInput = M30Output & { runId: string; tick: number };
export interface M30MLOutput {
  score: number;           // 0–1
  topFactors: string[];    // max 5 plain-English factors
  recommendation: string;  // single sentence
  auditHash: string;       // SHA256(inputs + outputs + rulesVersion)
}

export async function defaultAndBetrayalHandlerMLCompanion(
  _input: M30MLInput,
): Promise<M30MLOutput> {
  // TODO: call ML inference endpoint for m30a
  throw new Error('M30 ML companion (m30a) is not yet implemented.');
}
