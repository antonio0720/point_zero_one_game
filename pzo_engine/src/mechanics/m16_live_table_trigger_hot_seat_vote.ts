// ═══════════════════════════════════════════════════════════════════════════════
// POINT ZERO ONE — src/mechanics/m16_live_table_trigger_hot_seat_vote.ts
// AUTO-GENERATED by scripts/build_mechanics.py — DO NOT EDIT MANUALLY
// Regenerate: python3 scripts/build_mechanics.py --force
//
// Mechanic : M16 — Live Table Trigger: Hot Seat Vote
// Family   : social_engine
// Layer    : api_endpoint
// Priority : 1  Batch: 1
// ML Pair  : m16a
// Deps     : M15
//
// Density6 LLC · Point Zero One · Confidential · All Rights Reserved
// ═══════════════════════════════════════════════════════════════════════════════

// import type { M15Types } from './m15_...';

// ── Input contract ────────────────────────────────────────────────────────────
export interface M16Input {
  tablevoterequest: unknown;  // tableVoteRequest
  state_tick: unknown;  // state.tick
  participantlist: unknown;  // participantList
}

// ── Output contract ───────────────────────────────────────────────────────────
export interface M16Output {
  voteresult: unknown;  // voteResult
  hotseateffect: unknown;  // hotSeatEffect
  votewindowexpiry: unknown;  // voteWindowExpiry
}

// ── Telemetry events ──────────────────────────────────────────────────────────
export type M16Event = 'HOT_SEAT_VOTE_OPENED' | 'HOT_SEAT_VOTE_RESOLVED' | 'HOT_SEAT_TIMEOUT';

export interface M16TelemetryPayload {
  event: M16Event;
  mechanic_id: 'M16';
  tick: number;
  runId: string;
  payload: Record<string, unknown>;
}

// ── Design constants ──────────────────────────────────────────────────────────
export const M16_CONSTANTS = {
  MECHANIC_ID:  'M16',
  TITLE:        'Live Table Trigger: Hot Seat Vote',
  FAMILY:       'social_engine',
  LAYER:        'api_endpoint',
  PRIORITY:      1,
  BATCH:         1,
  ML_PAIR:      'm16a',
  EXEC_HOOK:    'liveTableHotSeatVote',
  DESIGN_LAWS: [
    'deterministic-by-seed',
    'server-verified-via-ledger',
    'bounded-chaos',
    'no-pay-to-win',
  ],
} as const;

// ── Exec hook ─────────────────────────────────────────────────────────────────
/**
 * liveTableHotSeatVote
 *
 * Called by the EngineOrchestrator (or appropriate engine) on each
 * relevant tick/event. All state mutations MUST be returned in the
 * output — never applied in place. All telemetry MUST be emitted
 * via the EventBus, never fired directly.
 *
 * @param input  - Typed input snapshot (see M16Input)
 * @param emit   - Telemetry emitter — call for every meaningful state change
 * @returns      - Typed output (see M16Output)
 */
export function liveTableHotSeatVote(
  input: M16Input,
  emit: (payload: M16TelemetryPayload) => void,
): M16Output {
  // ── TODO: implement M16 — Live Table Trigger: Hot Seat Vote ───────────────────────────────
  //
  // Implementation checklist:
  // □ handle: tableVoteRequest
  // □ handle: state.tick
  // □ handle: participantList
  //
  // Emit at minimum: HOT_SEAT_VOTE_OPENED
  //
  // Required telemetry shape:
  //   emit({ event: '...', mechanic_id: 'M16', tick: <tick>, runId: <runId>, payload: {} });
  //
  // ─────────────────────────────────────────────────────────────────────────
  throw new Error('M16 (liveTableHotSeatVote) is not yet implemented.');
}

// ── ML companion hook ─────────────────────────────────────────────────────────
/**
 * ML companion: m16a
 * Async — fires after exec_hook, reads output, returns advisory signals only.
 * NEVER mutates state. Results feed Case File, Intel bars, and CORD scoring.
 */
export type M16MLInput = M16Output & { runId: string; tick: number };
export interface M16MLOutput {
  score: number;           // 0–1
  topFactors: string[];    // max 5 plain-English factors
  recommendation: string;  // single sentence
  auditHash: string;       // SHA256(inputs + outputs + rulesVersion)
}

export async function liveTableHotSeatVoteMLCompanion(
  _input: M16MLInput,
): Promise<M16MLOutput> {
  // TODO: call ML inference endpoint for m16a
  throw new Error('M16 ML companion (m16a) is not yet implemented.');
}
