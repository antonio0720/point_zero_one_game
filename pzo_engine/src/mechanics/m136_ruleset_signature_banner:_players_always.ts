// ═══════════════════════════════════════════════════════════════════════════════
// POINT ZERO ONE — src/mechanics/m136_ruleset_signature_banner:_players_always.ts
// AUTO-GENERATED by scripts/build_mechanics.py — DO NOT EDIT MANUALLY
// Regenerate: python3 scripts/build_mechanics.py --force
//
// Mechanic : M136 — Ruleset Signature Banner: Players Always Know What's Live
// Family   : ops
// Layer    : season_runtime
// Priority : 1  Batch: 3
// ML Pair  : m136a
// Deps     : M19
//
// Density6 LLC · Point Zero One · Confidential · All Rights Reserved
// ═══════════════════════════════════════════════════════════════════════════════

// import type { M19Types } from './m19_...';

// ── Input contract ────────────────────────────────────────────────────────────
export interface M136Input {
  activeruleset: unknown;  // activeRuleset
  rulesversion: unknown;  // rulesVersion
  state_runid: unknown;  // state.runId
}

// ── Output contract ───────────────────────────────────────────────────────────
export interface M136Output {
  bannerdisplayed: unknown;  // bannerDisplayed
  rulesetsignature: unknown;  // rulesetSignature
  playeracknowledged: unknown;  // playerAcknowledged
}

// ── Telemetry events ──────────────────────────────────────────────────────────
export type M136Event = 'RULESET_SIGNATURE_SHOWN' | 'RULESET_VERSION_CONFIRMED' | 'PLAYER_ACKNOWLEDGED';

export interface M136TelemetryPayload {
  event: M136Event;
  mechanic_id: 'M136';
  tick: number;
  runId: string;
  payload: Record<string, unknown>;
}

// ── Design constants ──────────────────────────────────────────────────────────
export const M136_CONSTANTS = {
  MECHANIC_ID:  'M136',
  TITLE:        'Ruleset Signature Banner: Players Always Know What's Live',
  FAMILY:       'ops',
  LAYER:        'season_runtime',
  PRIORITY:      1,
  BATCH:         3,
  ML_PAIR:      'm136a',
  EXEC_HOOK:    'rulesetSignatureBanner',
  DESIGN_LAWS: [
    'deterministic-by-seed',
    'server-verified-via-ledger',
    'bounded-chaos',
    'no-pay-to-win',
  ],
} as const;

// ── Exec hook ─────────────────────────────────────────────────────────────────
/**
 * rulesetSignatureBanner
 *
 * Called by the EngineOrchestrator (or appropriate engine) on each
 * relevant tick/event. All state mutations MUST be returned in the
 * output — never applied in place. All telemetry MUST be emitted
 * via the EventBus, never fired directly.
 *
 * @param input  - Typed input snapshot (see M136Input)
 * @param emit   - Telemetry emitter — call for every meaningful state change
 * @returns      - Typed output (see M136Output)
 */
export function rulesetSignatureBanner(
  input: M136Input,
  emit: (payload: M136TelemetryPayload) => void,
): M136Output {
  // ── TODO: implement M136 — Ruleset Signature Banner: Players Always Know What's Live ───────────────────────────────
  //
  // Implementation checklist:
  // □ handle: activeRuleset
  // □ handle: rulesVersion
  // □ handle: state.runId
  //
  // Emit at minimum: RULESET_SIGNATURE_SHOWN
  //
  // Required telemetry shape:
  //   emit({ event: '...', mechanic_id: 'M136', tick: <tick>, runId: <runId>, payload: {} });
  //
  // ─────────────────────────────────────────────────────────────────────────
  throw new Error('M136 (rulesetSignatureBanner) is not yet implemented.');
}

// ── ML companion hook ─────────────────────────────────────────────────────────
/**
 * ML companion: m136a
 * Async — fires after exec_hook, reads output, returns advisory signals only.
 * NEVER mutates state. Results feed Case File, Intel bars, and CORD scoring.
 */
export type M136MLInput = M136Output & { runId: string; tick: number };
export interface M136MLOutput {
  score: number;           // 0–1
  topFactors: string[];    // max 5 plain-English factors
  recommendation: string;  // single sentence
  auditHash: string;       // SHA256(inputs + outputs + rulesVersion)
}

export async function rulesetSignatureBannerMLCompanion(
  _input: M136MLInput,
): Promise<M136MLOutput> {
  // TODO: call ML inference endpoint for m136a
  throw new Error('M136 ML companion (m136a) is not yet implemented.');
}
