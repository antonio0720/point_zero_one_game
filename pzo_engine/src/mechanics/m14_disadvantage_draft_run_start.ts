// ═══════════════════════════════════════════════════════════════════════════════
// POINT ZERO ONE — src/mechanics/m14_disadvantage_draft_run_start.ts
// AUTO-GENERATED by scripts/build_mechanics.py — DO NOT EDIT MANUALLY
// Regenerate: python3 scripts/build_mechanics.py --force
//
// Mechanic : M14 — Disadvantage Draft (Run Start)
// Family   : chaos_engine
// Layer    : card_handler
// Priority : 1  Batch: 1
// ML Pair  : m14a
// Deps     : M01
//
// Density6 LLC · Point Zero One · Confidential · All Rights Reserved
// ═══════════════════════════════════════════════════════════════════════════════

// import type { M01Types } from './m01_...';

// ── Input contract ────────────────────────────────────────────────────────────
export interface M14Input {
  playerselection: unknown;  // playerSelection
  handicapoptions: unknown;  // handicapOptions
  cordpremiumtable: unknown;  // cordPremiumTable
}

// ── Output contract ───────────────────────────────────────────────────────────
export interface M14Output {
  activehandicaps: unknown;  // activeHandicaps
  cordpremiummultiplier: unknown;  // cordPremiumMultiplier
  bleedmodeflag: unknown;  // bleedModeFlag
}

// ── Telemetry events ──────────────────────────────────────────────────────────
export type M14Event = 'HANDICAP_SELECTED' | 'BLEED_MODE_ARMED' | 'CORD_PREMIUM_APPLIED' | 'ADVANTAGE_SELECTED';

export interface M14TelemetryPayload {
  event: M14Event;
  mechanic_id: 'M14';
  tick: number;
  runId: string;
  payload: Record<string, unknown>;
}

// ── Design constants ──────────────────────────────────────────────────────────
export const M14_CONSTANTS = {
  MECHANIC_ID:  'M14',
  TITLE:        'Disadvantage Draft (Run Start)',
  FAMILY:       'chaos_engine',
  LAYER:        'card_handler',
  PRIORITY:      1,
  BATCH:         1,
  ML_PAIR:      'm14a',
  EXEC_HOOK:    'disadvantageDraftResolver',
  DESIGN_LAWS: [
    'deterministic-by-seed',
    'server-verified-via-ledger',
    'bounded-chaos',
    'no-pay-to-win',
  ],
} as const;

// ── Exec hook ─────────────────────────────────────────────────────────────────
/**
 * disadvantageDraftResolver
 *
 * Called by the EngineOrchestrator (or appropriate engine) on each
 * relevant tick/event. All state mutations MUST be returned in the
 * output — never applied in place. All telemetry MUST be emitted
 * via the EventBus, never fired directly.
 *
 * @param input  - Typed input snapshot (see M14Input)
 * @param emit   - Telemetry emitter — call for every meaningful state change
 * @returns      - Typed output (see M14Output)
 */
export function disadvantageDraftResolver(
  input: M14Input,
  emit: (payload: M14TelemetryPayload) => void,
): M14Output {
  // ── TODO: implement M14 — Disadvantage Draft (Run Start) ───────────────────────────────
  //
  // Implementation checklist:
  // □ handle: playerSelection
  // □ handle: handicapOptions
  // □ handle: cordPremiumTable
  //
  // Emit at minimum: HANDICAP_SELECTED
  //
  // Required telemetry shape:
  //   emit({ event: '...', mechanic_id: 'M14', tick: <tick>, runId: <runId>, payload: {} });
  //
  // ─────────────────────────────────────────────────────────────────────────
  throw new Error('M14 (disadvantageDraftResolver) is not yet implemented.');
}

// ── ML companion hook ─────────────────────────────────────────────────────────
/**
 * ML companion: m14a
 * Async — fires after exec_hook, reads output, returns advisory signals only.
 * NEVER mutates state. Results feed Case File, Intel bars, and CORD scoring.
 */
export type M14MLInput = M14Output & { runId: string; tick: number };
export interface M14MLOutput {
  score: number;           // 0–1
  topFactors: string[];    // max 5 plain-English factors
  recommendation: string;  // single sentence
  auditHash: string;       // SHA256(inputs + outputs + rulesVersion)
}

export async function disadvantageDraftResolverMLCompanion(
  _input: M14MLInput,
): Promise<M14MLOutput> {
  // TODO: call ML inference endpoint for m14a
  throw new Error('M14 ML companion (m14a) is not yet implemented.');
}
