// ═══════════════════════════════════════════════════════════════════════════════
// POINT ZERO ONE — src/mechanics/m114_timing_tax:_fast_choices_get_better_term.ts
// AUTO-GENERATED by scripts/build_mechanics.py — DO NOT EDIT MANUALLY
// Regenerate: python3 scripts/build_mechanics.py --force
//
// Mechanic : M114 — Timing Tax: Fast Choices Get Better Terms
// Family   : portfolio_experimental
// Layer    : card_handler
// Priority : 2  Batch: 3
// ML Pair  : m114a
// Deps     : M02, M09
//
// Density6 LLC · Point Zero One · Confidential · All Rights Reserved
// ═══════════════════════════════════════════════════════════════════════════════

// import type { M02Types } from './m02_...';
// import type { M09Types } from './m09_...';

// ── Input contract ────────────────────────────────────────────────────────────
export interface M114Input {
  decisiontime: unknown;  // decisionTime
  windowduration: unknown;  // windowDuration
  timingtaxtable: unknown;  // timingTaxTable
}

// ── Output contract ───────────────────────────────────────────────────────────
export interface M114Output {
  timingbonus: unknown;  // timingBonus
  termimproved: unknown;  // termImproved
  taxapplied: unknown;  // taxApplied
}

// ── Telemetry events ──────────────────────────────────────────────────────────
export type M114Event = 'TIMING_TAX_APPLIED' | 'TIMING_BONUS_AWARDED' | 'FAST_DECISION_RECORDED';

export interface M114TelemetryPayload {
  event: M114Event;
  mechanic_id: 'M114';
  tick: number;
  runId: string;
  payload: Record<string, unknown>;
}

// ── Design constants ──────────────────────────────────────────────────────────
export const M114_CONSTANTS = {
  MECHANIC_ID:  'M114',
  TITLE:        'Timing Tax: Fast Choices Get Better Terms',
  FAMILY:       'portfolio_experimental',
  LAYER:        'card_handler',
  PRIORITY:      2,
  BATCH:         3,
  ML_PAIR:      'm114a',
  EXEC_HOOK:    'timingTaxResolver',
  DESIGN_LAWS: [
    'deterministic-by-seed',
    'server-verified-via-ledger',
    'bounded-chaos',
    'no-pay-to-win',
  ],
} as const;

// ── Exec hook ─────────────────────────────────────────────────────────────────
/**
 * timingTaxResolver
 *
 * Called by the EngineOrchestrator (or appropriate engine) on each
 * relevant tick/event. All state mutations MUST be returned in the
 * output — never applied in place. All telemetry MUST be emitted
 * via the EventBus, never fired directly.
 *
 * @param input  - Typed input snapshot (see M114Input)
 * @param emit   - Telemetry emitter — call for every meaningful state change
 * @returns      - Typed output (see M114Output)
 */
export function timingTaxResolver(
  input: M114Input,
  emit: (payload: M114TelemetryPayload) => void,
): M114Output {
  // ── TODO: implement M114 — Timing Tax: Fast Choices Get Better Terms ───────────────────────────────
  //
  // Implementation checklist:
  // □ handle: decisionTime
  // □ handle: windowDuration
  // □ handle: timingTaxTable
  //
  // Emit at minimum: TIMING_TAX_APPLIED
  //
  // Required telemetry shape:
  //   emit({ event: '...', mechanic_id: 'M114', tick: <tick>, runId: <runId>, payload: {} });
  //
  // ─────────────────────────────────────────────────────────────────────────
  throw new Error('M114 (timingTaxResolver) is not yet implemented.');
}

// ── ML companion hook ─────────────────────────────────────────────────────────
/**
 * ML companion: m114a
 * Async — fires after exec_hook, reads output, returns advisory signals only.
 * NEVER mutates state. Results feed Case File, Intel bars, and CORD scoring.
 */
export type M114MLInput = M114Output & { runId: string; tick: number };
export interface M114MLOutput {
  score: number;           // 0–1
  topFactors: string[];    // max 5 plain-English factors
  recommendation: string;  // single sentence
  auditHash: string;       // SHA256(inputs + outputs + rulesVersion)
}

export async function timingTaxResolverMLCompanion(
  _input: M114MLInput,
): Promise<M114MLOutput> {
  // TODO: call ML inference endpoint for m114a
  throw new Error('M114 ML companion (m114a) is not yet implemented.');
}
