// ═══════════════════════════════════════════════════════════════════════════════
// POINT ZERO ONE — src/mechanics/m108_partial_fills:_you_don't_get_the_whole_d.ts
// AUTO-GENERATED by scripts/build_mechanics.py — DO NOT EDIT MANUALLY
// Regenerate: python3 scripts/build_mechanics.py --force
//
// Mechanic : M108 — Partial Fills: You Don't Get the Whole Deal
// Family   : portfolio_experimental
// Layer    : card_handler
// Priority : 2  Batch: 3
// ML Pair  : m108a
// Deps     : M09, M104
//
// Density6 LLC · Point Zero One · Confidential · All Rights Reserved
// ═══════════════════════════════════════════════════════════════════════════════

// import type { M09Types } from './m09_...';
// import type { M104Types } from './m104_...';

// ── Input contract ────────────────────────────────────────────────────────────
export interface M108Input {
  dealrequest: unknown;  // dealRequest
  availablecapacity: unknown;  // availableCapacity
  fillrules: unknown;  // fillRules
}

// ── Output contract ───────────────────────────────────────────────────────────
export interface M108Output {
  fillamount: unknown;  // fillAmount
  partialfillresult: unknown;  // partialFillResult
  remainingcapacity: unknown;  // remainingCapacity
}

// ── Telemetry events ──────────────────────────────────────────────────────────
export type M108Event = 'PARTIAL_FILL_EXECUTED' | 'FILL_PROBABILITY_APPLIED' | 'PARTIAL_FILL_MISSED';

export interface M108TelemetryPayload {
  event: M108Event;
  mechanic_id: 'M108';
  tick: number;
  runId: string;
  payload: Record<string, unknown>;
}

// ── Design constants ──────────────────────────────────────────────────────────
export const M108_CONSTANTS = {
  MECHANIC_ID:  'M108',
  TITLE:        'Partial Fills: You Don't Get the Whole Deal',
  FAMILY:       'portfolio_experimental',
  LAYER:        'card_handler',
  PRIORITY:      2,
  BATCH:         3,
  ML_PAIR:      'm108a',
  EXEC_HOOK:    'partialFillResolver',
  DESIGN_LAWS: [
    'deterministic-by-seed',
    'server-verified-via-ledger',
    'bounded-chaos',
    'no-pay-to-win',
  ],
} as const;

// ── Exec hook ─────────────────────────────────────────────────────────────────
/**
 * partialFillResolver
 *
 * Called by the EngineOrchestrator (or appropriate engine) on each
 * relevant tick/event. All state mutations MUST be returned in the
 * output — never applied in place. All telemetry MUST be emitted
 * via the EventBus, never fired directly.
 *
 * @param input  - Typed input snapshot (see M108Input)
 * @param emit   - Telemetry emitter — call for every meaningful state change
 * @returns      - Typed output (see M108Output)
 */
export function partialFillResolver(
  input: M108Input,
  emit: (payload: M108TelemetryPayload) => void,
): M108Output {
  // ── TODO: implement M108 — Partial Fills: You Don't Get the Whole Deal ───────────────────────────────
  //
  // Implementation checklist:
  // □ handle: dealRequest
  // □ handle: availableCapacity
  // □ handle: fillRules
  //
  // Emit at minimum: PARTIAL_FILL_EXECUTED
  //
  // Required telemetry shape:
  //   emit({ event: '...', mechanic_id: 'M108', tick: <tick>, runId: <runId>, payload: {} });
  //
  // ─────────────────────────────────────────────────────────────────────────
  throw new Error('M108 (partialFillResolver) is not yet implemented.');
}

// ── ML companion hook ─────────────────────────────────────────────────────────
/**
 * ML companion: m108a
 * Async — fires after exec_hook, reads output, returns advisory signals only.
 * NEVER mutates state. Results feed Case File, Intel bars, and CORD scoring.
 */
export type M108MLInput = M108Output & { runId: string; tick: number };
export interface M108MLOutput {
  score: number;           // 0–1
  topFactors: string[];    // max 5 plain-English factors
  recommendation: string;  // single sentence
  auditHash: string;       // SHA256(inputs + outputs + rulesVersion)
}

export async function partialFillResolverMLCompanion(
  _input: M108MLInput,
): Promise<M108MLOutput> {
  // TODO: call ML inference endpoint for m108a
  throw new Error('M108 ML companion (m108a) is not yet implemented.');
}
