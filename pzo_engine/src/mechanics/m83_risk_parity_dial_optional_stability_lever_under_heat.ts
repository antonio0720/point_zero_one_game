// AUTO-GENERATED by scripts/build_mechanics.py — DO NOT EDIT exec_hook body manually
// Density6 LLC · Point Zero One · Confidential
// pzo_engine/src/mechanics/m83_risk_parity_dial_optional_stability_lever_under_heat.ts
//
// Mechanic : M83 — Risk Parity Dial: Optional Stability Lever Under Heat
// Family   : portfolio_expert   Layer: card_handler   Priority: 2   Batch: 2
// ML Pair  : m83a
// Deps     : M35, M57
//
// Design Laws:
//   ✦ Deterministic-by-seed  ✦ Server-verified via ledger
//   ✦ Bounded chaos          ✦ No pay-to-win

import { clamp, computeHash, seededShuffle, seededIndex,
         buildMacroSchedule, buildChaosWindows,
         buildWeightedPool, OPPORTUNITY_POOL, DEFAULT_CARD, DEFAULT_CARD_IDS,
         computeDecayRate, EXIT_PULSE_MULTIPLIERS,
         MACRO_EVENTS_PER_RUN, CHAOS_WINDOWS_PER_RUN, RUN_TOTAL_TICKS,
         PRESSURE_WEIGHTS, PHASE_WEIGHTS, REGIME_WEIGHTS,
         REGIME_MULTIPLIERS } from './mechanicsUtils';
import type {
  RunPhase, TickTier, MacroRegime, PressureTier, SolvencyStatus,
  Asset, IPAItem, GameCard, GameEvent, ShieldLayer, Debt, Buff,
  Liability, SetBonus, AssetMod, IncomeItem, MacroEvent, ChaosWindow,
  AuctionResult, PurchaseResult, ShieldResult, ExitResult, TickResult,
  DeckComposition, TierProgress, WipeEvent, RegimeShiftEvent,
  PhaseTransitionEvent, TimerExpiredEvent, StreakEvent, FubarEvent,
  LedgerEntry, ProofCard, CompletedRun, SeasonState, RunState,
  MomentEvent, ClipBoundary, MechanicTelemetryPayload, MechanicEmitter,
} from './types';


// ── Input / Output contracts ──────────────────────────────────────────────

export interface M83Input {
  stateExposureHeat?: number;
  riskParityTarget?: unknown;
  stateAssets?: Asset[];
}

export interface M83Output {
  parityAdjusted: boolean;
  stabilityBonus: number;
  dialSetting: number;
}

// ── Telemetry ─────────────────────────────────────────────────────────────

export type M83Event = 'RISK_PARITY_ADJUSTED' | 'STABILITY_BONUS_APPLIED' | 'DIAL_CHANGED';

export interface M83TelemetryPayload extends MechanicTelemetryPayload {
  event: M83Event;
  mechanic_id: 'M83';
}

// ── Design bounds (never mutate at runtime) ────────────────────────────────

export const M83_BOUNDS = {
  TRIGGER_THRESHOLD:   3,
  MULTIPLIER:          1.5,
  MAX_AMOUNT:          50_000,
  MIN_CASH_DELTA:      -20_000,
  MAX_CASH_DELTA:       20_000,
  MIN_CASHFLOW_DELTA:  -10_000,
  MAX_CASHFLOW_DELTA:   10_000,
  TIER_ESCAPE_TARGET:   3_000,
  REGIME_SHIFT_THRESHOLD: 500,
  BASE_DECAY_RATE:     0.02,
  BLEED_CASH_THRESHOLD: 1_000,
  FIRST_REFUSAL_TICKS: 6,
  PULSE_CYCLE:         12,
  MAX_PROCEEDS:        999_999,
  EFFECT_MULTIPLIER:   1.0,
  MIN_EFFECT:          0,
  MAX_EFFECT:          100_000,
} as const;

// ── Exec hook ─────────────────────────────────────────────────────────────

/**
 * riskParityDialAdjuster
 *
 * Called by MechanicsRouter inside the EngineOrchestrator tick or card handler.
 * All state mutations MUST be returned in the output — never applied in place.
 * All telemetry MUST be emitted via the emit callback.
 *
 * @param input  Typed input snapshot
 * @param emit   Telemetry emitter — call for every meaningful state change
 * @returns      Typed output (all fields populated, no throws)
 */
export function riskParityDialAdjuster(
  input: M83Input,
  emit: MechanicEmitter,
): M83Output {
    const stateExposureHeat = (input.stateExposureHeat as number) ?? 0;
    const riskParityTarget = input.riskParityTarget;
    const stateAssets = (input.stateAssets as Asset[]) ?? [];
    emit({ event: 'RISK_PARITY_ADJUSTED', mechanic_id: 'M83', tick: 0, runId: '', payload: { stateExposureHeat, riskParityTarget } });
    return {{
    parityAdjusted: true,
    stabilityBonus: clamp(stateExposureHeat * M83_BOUNDS.EFFECT_MULTIPLIER, M83_BOUNDS.MIN_EFFECT, M83_BOUNDS.MAX_EFFECT),
    dialSetting: clamp(stateExposureHeat * M83_BOUNDS.EFFECT_MULTIPLIER, M83_BOUNDS.MIN_EFFECT, M83_BOUNDS.MAX_EFFECT),
  }};
}

// ── ML companion hook ─────────────────────────────────────────────────────

export interface M83MLInput {
  parityAdjusted?: boolean, stabilityBonus?: number, dialSetting?: number;
  runId: string;
  tick:  number;
}

export interface M83MLOutput {
  score:          number;         // 0–1
  topFactors:     string[];       // max 5 plain-English factors
  recommendation: string;         // single sentence
  auditHash:      string;         // SHA256(inputs+outputs+rulesVersion)
  confidenceDecay: number;        // 0–1, how fast this signal should decay
}

/**
 * riskParityDialAdjusterMLCompanion
 * Async advisory — fires AFTER exec_hook, reads output, returns signals only.
 * NEVER mutates state. Results feed Case File, Intel bars, and CORD scoring.
 */
export async function riskParityDialAdjusterMLCompanion(
  input: M83MLInput,
): Promise<M83MLOutput> {
  // Advisory signal — bounded [0,1], no state mutation
  const score = Math.min(0.99, Math.max(0.01, Object.keys(input).length * 0.05));
  return {
    score,
    topFactors:     ['M83 signal computed', 'advisory only'],
    recommendation: 'Monitor M83 output and adjust strategy accordingly.',
    auditHash:      computeHash(JSON.stringify(input) + ':ml:M83'),
    confidenceDecay: 0.05,
  };
}
