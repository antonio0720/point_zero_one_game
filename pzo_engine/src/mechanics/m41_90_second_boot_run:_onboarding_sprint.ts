// ═══════════════════════════════════════════════════════════════════════════════
// POINT ZERO ONE — src/mechanics/m41_90_second_boot_run:_onboarding_sprint.ts
// AUTO-GENERATED by scripts/build_mechanics.py — DO NOT EDIT MANUALLY
// Regenerate: python3 scripts/build_mechanics.py --force
//
// Mechanic : M41 — 90-Second Boot Run: Onboarding Sprint
// Family   : onboarding
// Layer    : backend_service
// Priority : 1  Batch: 1
// ML Pair  : m41a
// Deps     : M01, M02
//
// Density6 LLC · Point Zero One · Confidential · All Rights Reserved
// ═══════════════════════════════════════════════════════════════════════════════

// import type { M01Types } from './m01_...';
// import type { M02Types } from './m02_...';

// ── Input contract ────────────────────────────────────────────────────────────
export interface M41Input {
  newplayerflag: unknown;  // newPlayerFlag
  onboardingconfig: unknown;  // onboardingConfig
  runseed: unknown;  // RunSeed
}

// ── Output contract ───────────────────────────────────────────────────────────
export interface M41Output {
  onboardingrunstate: unknown;  // onboardingRunState
  guidedprompts: unknown;  // guidedPrompts
  completionstatus: unknown;  // completionStatus
}

// ── Telemetry events ──────────────────────────────────────────────────────────
export type M41Event = 'ONBOARDING_STARTED' | 'ONBOARDING_COMPLETED' | 'ONBOARDING_SKIPPED';

export interface M41TelemetryPayload {
  event: M41Event;
  mechanic_id: 'M41';
  tick: number;
  runId: string;
  payload: Record<string, unknown>;
}

// ── Design constants ──────────────────────────────────────────────────────────
export const M41_CONSTANTS = {
  MECHANIC_ID:  'M41',
  TITLE:        '90-Second Boot Run: Onboarding Sprint',
  FAMILY:       'onboarding',
  LAYER:        'backend_service',
  PRIORITY:      1,
  BATCH:         1,
  ML_PAIR:      'm41a',
  EXEC_HOOK:    'bootRunOnboardingSprint',
  DESIGN_LAWS: [
    'deterministic-by-seed',
    'server-verified-via-ledger',
    'bounded-chaos',
    'no-pay-to-win',
  ],
} as const;

// ── Exec hook ─────────────────────────────────────────────────────────────────
/**
 * bootRunOnboardingSprint
 *
 * Called by the EngineOrchestrator (or appropriate engine) on each
 * relevant tick/event. All state mutations MUST be returned in the
 * output — never applied in place. All telemetry MUST be emitted
 * via the EventBus, never fired directly.
 *
 * @param input  - Typed input snapshot (see M41Input)
 * @param emit   - Telemetry emitter — call for every meaningful state change
 * @returns      - Typed output (see M41Output)
 */
export function bootRunOnboardingSprint(
  input: M41Input,
  emit: (payload: M41TelemetryPayload) => void,
): M41Output {
  // ── TODO: implement M41 — 90-Second Boot Run: Onboarding Sprint ───────────────────────────────
  //
  // Implementation checklist:
  // □ handle: newPlayerFlag
  // □ handle: onboardingConfig
  // □ handle: RunSeed
  //
  // Emit at minimum: ONBOARDING_STARTED
  //
  // Required telemetry shape:
  //   emit({ event: '...', mechanic_id: 'M41', tick: <tick>, runId: <runId>, payload: {} });
  //
  // ─────────────────────────────────────────────────────────────────────────
  throw new Error('M41 (bootRunOnboardingSprint) is not yet implemented.');
}

// ── ML companion hook ─────────────────────────────────────────────────────────
/**
 * ML companion: m41a
 * Async — fires after exec_hook, reads output, returns advisory signals only.
 * NEVER mutates state. Results feed Case File, Intel bars, and CORD scoring.
 */
export type M41MLInput = M41Output & { runId: string; tick: number };
export interface M41MLOutput {
  score: number;           // 0–1
  topFactors: string[];    // max 5 plain-English factors
  recommendation: string;  // single sentence
  auditHash: string;       // SHA256(inputs + outputs + rulesVersion)
}

export async function bootRunOnboardingSprintMLCompanion(
  _input: M41MLInput,
): Promise<M41MLOutput> {
  // TODO: call ML inference endpoint for m41a
  throw new Error('M41 ML companion (m41a) is not yet implemented.');
}
