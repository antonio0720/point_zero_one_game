// AUTO-GENERATED by scripts/build_mechanics.py — DO NOT EDIT exec_hook body manually
// Density6 LLC · Point Zero One · Confidential
// pzo_engine/src/mechanics/m13_systemic_friction_module.ts
//
// Mechanic : M13 — Systemic Friction Module
// Family   : chaos_engine   Layer: tick_engine   Priority: 1   Batch: 1
// ML Pair  : m13a
// Deps     : M05
//
// Design Laws:
//   ✦ Deterministic-by-seed  ✦ Server-verified via ledger
//   ✦ Bounded chaos          ✦ No pay-to-win

import { clamp, computeHash, seededShuffle, seededIndex,
         buildMacroSchedule, buildChaosWindows,
         buildWeightedPool, OPPORTUNITY_POOL, DEFAULT_CARD, DEFAULT_CARD_IDS,
         computeDecayRate, EXIT_PULSE_MULTIPLIERS,
         MACRO_EVENTS_PER_RUN, CHAOS_WINDOWS_PER_RUN, RUN_TOTAL_TICKS,
         PRESSURE_WEIGHTS, PHASE_WEIGHTS, REGIME_WEIGHTS,
         REGIME_MULTIPLIERS } from './mechanicsUtils';
import type {
  RunPhase, TickTier, MacroRegime, PressureTier, SolvencyStatus,
  Asset, IPAItem, GameCard, GameEvent, ShieldLayer, Debt, Buff,
  Liability, SetBonus, AssetMod, IncomeItem, MacroEvent, ChaosWindow,
  AuctionResult, PurchaseResult, ShieldResult, ExitResult, TickResult,
  DeckComposition, TierProgress, WipeEvent, RegimeShiftEvent,
  PhaseTransitionEvent, TimerExpiredEvent, StreakEvent, FubarEvent,
  LedgerEntry, ProofCard, CompletedRun, SeasonState, RunState,
  MomentEvent, ClipBoundary, MechanicTelemetryPayload, MechanicEmitter,
} from './types';


// ── Input / Output contracts ──────────────────────────────────────────────

export interface M13Input {
  fateTick?: number;
  stateCash?: number;
  stateMacroRegime?: MacroRegime;
}

export interface M13Output {
  frictionExpenseEvent: Record<string, unknown>;
  cashDrained: number;
  frictionClass: string;
}

// ── Telemetry ─────────────────────────────────────────────────────────────

export type M13Event = 'SO_FRICTION_APPLIED' | 'CASH_DRAINED' | 'FRICTION_CLASS_ASSIGNED' | 'SO_DOUBLED';

export interface M13TelemetryPayload extends MechanicTelemetryPayload {
  event: M13Event;
  mechanic_id: 'M13';
}

// ── Design bounds (never mutate at runtime) ────────────────────────────────

export const M13_BOUNDS = {
  BASE_AMOUNT:         1_000,
  TRIGGER_THRESHOLD:   3,
  MULTIPLIER:          1.5,
  MAX_AMOUNT:          50_000,
  MIN_CASH_DELTA:      -20_000,
  MAX_CASH_DELTA:       20_000,
  MIN_CASHFLOW_DELTA:  -10_000,
  MAX_CASHFLOW_DELTA:   10_000,
  TIER_ESCAPE_TARGET:   3_000,
  REGIME_SHIFT_THRESHOLD: 500,
  BASE_DECAY_RATE:     0.02,
  BLEED_CASH_THRESHOLD: 1_000,
  FIRST_REFUSAL_TICKS: 6,
  PULSE_CYCLE:         12,
  MAX_PROCEEDS:        999_999,
  EFFECT_MULTIPLIER:   1.0,
  MIN_EFFECT:          0,
  MAX_EFFECT:          100_000,
} as const;

// ── Exec hook ─────────────────────────────────────────────────────────────

/**
 * systemicFrictionModule
 *
 * Called by MechanicsRouter inside the EngineOrchestrator tick or card handler.
 * All state mutations MUST be returned in the output — never applied in place.
 * All telemetry MUST be emitted via the emit callback.
 *
 * @param input  Typed input snapshot
 * @param emit   Telemetry emitter — call for every meaningful state change
 * @returns      Typed output (all fields populated, no throws)
 */
export function systemicFrictionModule(
  input: M13Input,
  emit: MechanicEmitter,
): M13Output {
  const fateTick = (input.fateTick as number) ?? 0;
  const fateTick = (input.fateTick as number) ?? 0;
  const taxApplied = fateTick >= M13_BOUNDS.TRIGGER_THRESHOLD;
  const excessCount = Math.max(0, fateTick - M13_BOUNDS.TRIGGER_THRESHOLD);
  const taxAmount = taxApplied
    ? clamp(M13_BOUNDS.BASE_AMOUNT * Math.pow(M13_BOUNDS.MULTIPLIER, excessCount), 0, M13_BOUNDS.MAX_AMOUNT)
    : 0;
    emit({ event: 'SO_FRICTION_APPLIED', mechanic_id: 'M13', tick: fateTick, runId: '', payload: { fateTick, taxAmount, taxApplied } });
    emit({ event: 'CASH_DRAINED', mechanic_id: 'M13', tick: fateTick, runId: '', payload: { fateTick } });
    return {{
    frictionExpenseEvent: {},
    cashDrained: taxApplied ? taxAmount : 0,
    frictionClass: '',
  }};
}

// ── ML companion hook ─────────────────────────────────────────────────────

export interface M13MLInput {
  frictionExpenseEvent?: Record<string, unknown>, cashDrained?: number, frictionClass?: string;
  runId: string;
  tick:  number;
}

export interface M13MLOutput {
  score:          number;         // 0–1
  topFactors:     string[];       // max 5 plain-English factors
  recommendation: string;         // single sentence
  auditHash:      string;         // SHA256(inputs+outputs+rulesVersion)
  confidenceDecay: number;        // 0–1, how fast this signal should decay
}

/**
 * systemicFrictionModuleMLCompanion
 * Async advisory — fires AFTER exec_hook, reads output, returns signals only.
 * NEVER mutates state. Results feed Case File, Intel bars, and CORD scoring.
 */
export async function systemicFrictionModuleMLCompanion(
  input: M13MLInput,
): Promise<M13MLOutput> {
  // Advisory signal — bounded [0,1], no state mutation
  const score = Math.min(0.99, Math.max(0.01, Object.keys(input).length * 0.05));
  return {
    score,
    topFactors:     ['M13 signal computed', 'advisory only'],
    recommendation: 'Monitor M13 output and adjust strategy accordingly.',
    auditHash:      computeHash(JSON.stringify(input) + ':ml:M13'),
    confidenceDecay: 0.05,
  };
}
