// ═══════════════════════════════════════════════════════════════════════════════
// POINT ZERO ONE — src/mechanics/m113_order_priority_stack:_you_choose_what_di.ts
// AUTO-GENERATED by scripts/build_mechanics.py — DO NOT EDIT MANUALLY
// Regenerate: python3 scripts/build_mechanics.py --force
//
// Mechanic : M113 — Order Priority Stack: You Choose What Dies First
// Family   : portfolio_experimental
// Layer    : card_handler
// Priority : 2  Batch: 3
// ML Pair  : m113a
// Deps     : M08, M32
//
// Density6 LLC · Point Zero One · Confidential · All Rights Reserved
// ═══════════════════════════════════════════════════════════════════════════════

// import type { M08Types } from './m08_...';
// import type { M32Types } from './m32_...';

// ── Input contract ────────────────────────────────────────────────────────────
export interface M113Input {
  sacrificeorder: unknown;  // sacrificeOrder
  state_assets: unknown;  // state.assets
  incomingdamage: unknown;  // incomingDamage
}

// ── Output contract ───────────────────────────────────────────────────────────
export interface M113Output {
  sacrificeexecuted: unknown;  // sacrificeExecuted
  orderapplied: unknown;  // orderApplied
  survivorupdated: unknown;  // survivorUpdated
}

// ── Telemetry events ──────────────────────────────────────────────────────────
export type M113Event = 'SACRIFICE_ORDER_SET' | 'ASSET_SACRIFICED' | 'ORDER_EXHAUSTED';

export interface M113TelemetryPayload {
  event: M113Event;
  mechanic_id: 'M113';
  tick: number;
  runId: string;
  payload: Record<string, unknown>;
}

// ── Design constants ──────────────────────────────────────────────────────────
export const M113_CONSTANTS = {
  MECHANIC_ID:  'M113',
  TITLE:        'Order Priority Stack: You Choose What Dies First',
  FAMILY:       'portfolio_experimental',
  LAYER:        'card_handler',
  PRIORITY:      2,
  BATCH:         3,
  ML_PAIR:      'm113a',
  EXEC_HOOK:    'orderPriorityStackResolver',
  DESIGN_LAWS: [
    'deterministic-by-seed',
    'server-verified-via-ledger',
    'bounded-chaos',
    'no-pay-to-win',
  ],
} as const;

// ── Exec hook ─────────────────────────────────────────────────────────────────
/**
 * orderPriorityStackResolver
 *
 * Called by the EngineOrchestrator (or appropriate engine) on each
 * relevant tick/event. All state mutations MUST be returned in the
 * output — never applied in place. All telemetry MUST be emitted
 * via the EventBus, never fired directly.
 *
 * @param input  - Typed input snapshot (see M113Input)
 * @param emit   - Telemetry emitter — call for every meaningful state change
 * @returns      - Typed output (see M113Output)
 */
export function orderPriorityStackResolver(
  input: M113Input,
  emit: (payload: M113TelemetryPayload) => void,
): M113Output {
  // ── TODO: implement M113 — Order Priority Stack: You Choose What Dies First ───────────────────────────────
  //
  // Implementation checklist:
  // □ handle: sacrificeOrder
  // □ handle: state.assets
  // □ handle: incomingDamage
  //
  // Emit at minimum: SACRIFICE_ORDER_SET
  //
  // Required telemetry shape:
  //   emit({ event: '...', mechanic_id: 'M113', tick: <tick>, runId: <runId>, payload: {} });
  //
  // ─────────────────────────────────────────────────────────────────────────
  throw new Error('M113 (orderPriorityStackResolver) is not yet implemented.');
}

// ── ML companion hook ─────────────────────────────────────────────────────────
/**
 * ML companion: m113a
 * Async — fires after exec_hook, reads output, returns advisory signals only.
 * NEVER mutates state. Results feed Case File, Intel bars, and CORD scoring.
 */
export type M113MLInput = M113Output & { runId: string; tick: number };
export interface M113MLOutput {
  score: number;           // 0–1
  topFactors: string[];    // max 5 plain-English factors
  recommendation: string;  // single sentence
  auditHash: string;       // SHA256(inputs + outputs + rulesVersion)
}

export async function orderPriorityStackResolverMLCompanion(
  _input: M113MLInput,
): Promise<M113MLOutput> {
  // TODO: call ML inference endpoint for m113a
  throw new Error('M113 ML companion (m113a) is not yet implemented.');
}
