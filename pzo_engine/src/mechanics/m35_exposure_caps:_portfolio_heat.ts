// ═══════════════════════════════════════════════════════════════════════════════
// POINT ZERO ONE — src/mechanics/m35_exposure_caps:_portfolio_heat.ts
// AUTO-GENERATED by scripts/build_mechanics.py — DO NOT EDIT MANUALLY
// Regenerate: python3 scripts/build_mechanics.py --force
//
// Mechanic : M35 — Exposure Caps: Portfolio Heat
// Family   : portfolio_engine
// Layer    : card_handler
// Priority : 1  Batch: 1
// ML Pair  : m35a
// Deps     : M07, M31
//
// Density6 LLC · Point Zero One · Confidential · All Rights Reserved
// ═══════════════════════════════════════════════════════════════════════════════

// import type { M07Types } from './m07_...';
// import type { M31Types } from './m31_...';

// ── Input contract ────────────────────────────────────────────────────────────
export interface M35Input {
  state_assets: unknown;  // state.assets
  exposurethresholds: unknown;  // exposureThresholds
  state_macroregime: unknown;  // state.macroRegime
}

// ── Output contract ───────────────────────────────────────────────────────────
export interface M35Output {
  exposureheat: unknown;  // exposureHeat
  cappedwarning: unknown;  // cappedWarning
  overloadpenalty: unknown;  // overloadPenalty
}

// ── Telemetry events ──────────────────────────────────────────────────────────
export type M35Event = 'EXPOSURE_THRESHOLD_BREACHED' | 'OVERLOAD_WARNING' | 'HEAT_REDUCED';

export interface M35TelemetryPayload {
  event: M35Event;
  mechanic_id: 'M35';
  tick: number;
  runId: string;
  payload: Record<string, unknown>;
}

// ── Design constants ──────────────────────────────────────────────────────────
export const M35_CONSTANTS = {
  MECHANIC_ID:  'M35',
  TITLE:        'Exposure Caps: Portfolio Heat',
  FAMILY:       'portfolio_engine',
  LAYER:        'card_handler',
  PRIORITY:      1,
  BATCH:         1,
  ML_PAIR:      'm35a',
  EXEC_HOOK:    'exposureCapMonitor',
  DESIGN_LAWS: [
    'deterministic-by-seed',
    'server-verified-via-ledger',
    'bounded-chaos',
    'no-pay-to-win',
  ],
} as const;

// ── Exec hook ─────────────────────────────────────────────────────────────────
/**
 * exposureCapMonitor
 *
 * Called by the EngineOrchestrator (or appropriate engine) on each
 * relevant tick/event. All state mutations MUST be returned in the
 * output — never applied in place. All telemetry MUST be emitted
 * via the EventBus, never fired directly.
 *
 * @param input  - Typed input snapshot (see M35Input)
 * @param emit   - Telemetry emitter — call for every meaningful state change
 * @returns      - Typed output (see M35Output)
 */
export function exposureCapMonitor(
  input: M35Input,
  emit: (payload: M35TelemetryPayload) => void,
): M35Output {
  // ── TODO: implement M35 — Exposure Caps: Portfolio Heat ───────────────────────────────
  //
  // Implementation checklist:
  // □ handle: state.assets
  // □ handle: exposureThresholds
  // □ handle: state.macroRegime
  //
  // Emit at minimum: EXPOSURE_THRESHOLD_BREACHED
  //
  // Required telemetry shape:
  //   emit({ event: '...', mechanic_id: 'M35', tick: <tick>, runId: <runId>, payload: {} });
  //
  // ─────────────────────────────────────────────────────────────────────────
  throw new Error('M35 (exposureCapMonitor) is not yet implemented.');
}

// ── ML companion hook ─────────────────────────────────────────────────────────
/**
 * ML companion: m35a
 * Async — fires after exec_hook, reads output, returns advisory signals only.
 * NEVER mutates state. Results feed Case File, Intel bars, and CORD scoring.
 */
export type M35MLInput = M35Output & { runId: string; tick: number };
export interface M35MLOutput {
  score: number;           // 0–1
  topFactors: string[];    // max 5 plain-English factors
  recommendation: string;  // single sentence
  auditHash: string;       // SHA256(inputs + outputs + rulesVersion)
}

export async function exposureCapMonitorMLCompanion(
  _input: M35MLInput,
): Promise<M35MLOutput> {
  // TODO: call ML inference endpoint for m35a
  throw new Error('M35 ML companion (m35a) is not yet implemented.');
}
